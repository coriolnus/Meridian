# TSK-176 Faz A0 — tek host envanteri (A1 Oracle Cloud Always Free Ampere).
# "A1'e komut ssh sarmalı yazılır" kuralının Ansible karşılığı: bağlantı bilgisi TEK yerde
# (CLAUDE.md §1'deki ssh komutuyla aynı host/kullanıcı/anahtar — tek kaynak).
# GRUP ADI HOST ADINDAN FARKLI (Rol-1 hükmü, 2026-09-08): ikisi de `a1` iken ansible her
# koşumda `[WARNING]: Found both group and host with same name: a1` basıyordu — gürültü
# gerçek uyarıları gömer. Grup `meridian`, host `a1`; site.yml `hosts: meridian` der.
[meridian]
a1 ansible_host=130.61.126.87 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/oci-a1.key
