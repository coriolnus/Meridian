# Meridian Tam Denetim — 2026-07-18 (KISMİ: 9/13 ajan tamamlandı, oturum limiti nedeniyle durduruldu)

**Kapsam:** Çekirdek motor (broker/strategy, backtest/score, loop/scheduler) + Öğrenme yığını (reflect/guard/versioning, hermes/sprint, rollback/memory/analytics) + Veri (data/fmp/dataset, alpaca/secrets/constituents, skills/regime/indicators). **KOŞMADI:** api.py sözleşme ajanı, 2 pano ajanı, ops ajanı (obs/health/store). Pano "sabit satır" taraması elle yapıldı (aşağıda).
**Durum:** Bulgular BULUCU ajanlardan; adversarial doğrulama koşmadı — "canlı kanıtlı" işaretli olanlar disk/state üzerinde doğrulanmış, diğerleri mekanizma-düzeyinde.

## DÜZELTME DURUMU (2026-07-18 akşam — 175 pytest yeşil, sunucu düzeltilmiş kodla yeniden başlatıldı)
✅ DÜZELTİLDİ: #41 fetch_end() artık çağrı-başına (donma kökü) · #10 peak_equity persist · #45 boş-veri sabitlenmesi koruması · #12 seans kapanmadan refetch · #13 'current' yolunda heartbeat tazeleme · #17+#31 sürüm numarası asla yeniden kullanılmaz (_next_version) · #11 mükerrer trade dedup (_persist_trade) · #19 hypotheses.jsonl kilidi · #20 status_ts yalnız geçişte · #21 promoted sonuç dondurma · #30 refetch'te WF cache temizliği · #22 sprint zombie reap · #23 HALT sandbox'a kopyalanmaz · #36+#38 PROTECTED/ENGINE/shadow skiller anahtar-kapatılamaz · #26 prompt'ta gerçek rejimler · #27 Claude sertifikasız rejim reddi · #28 arama göstergesi try/finally · #34 0.0-skor karşılaştırması · #2/#8 scale_out stop-önceliği · PANO: "v1/v2/min 30/67" sabitleri canlı alanlara bağlandı (current_version, min_sample, skill_count), sözlük sabit iddiaları kaldırıldı.
✅ 2. TUR (ajansız inline denetim — api.py + pano canlı-veri + ops, daha önce hiç taranmamış yüzeyler): _auth tüm POST/DELETE rotalarında (constant-time) doğrulandı · **BULGU+FIX: Phase-1 uzlaştırma telemetrisi (mirror_drift/position_drift/failed_submissions) hiçbir endpoint'te sunulmuyor ve panoda görünmüyordu** → /api/alpaca `reconcile` alanı + Ayarlar kartında canlı "Ayna uzlaştırma" satırı + brifing'de ⚠ sapma rozeti (heartbeat.mirror_drift) · #32 config.dump_yaml ATOMİK (hot-reload strategy.yaml yarışı) · #29 sprint_run status ATOMİK · store.read_json bozuk dosyada default döner (500 yerine) · app.js kalan `||N` fallback'leri meşru (boş-durum sayaçları/form varsayılanları) · index.html temiz · obs/spend/store mekanik temiz (events.jsonl sınırsız büyüme = düşük, 9.7K).
✅ 3. TUR (2026-07-18 gece — kalan TÜM maddeler, ajansız): #1 açılış markları (backtest+canlı: breaker/de-risk/boyutlama artık D'nin kapanışını göremez) · #3/#4 segment yasası (yarı-açık sınırlar + kapanmış-olma: holdout fiyatı OOS kapısına sızamaz, sınır işlemi iki segmentte sayılamaz) · #5 pencere-bazlı yıllıklaştırma · #6 M2M drawdown kapıya dahil · #7/#46 delisted_markout (kaybolan pozisyonlar notlanır) · #42/#43/#45 load_bars reworku (use_cache=False artık merge+corp-check+boş-fallback; min-tarih koşulu kaldırıldı — 36 cache'in boşuna refetch'i bitti) · #44 warmup telemetrisi · #47 SPY validate · #48 disjoint-seam kontrolü · #49/#52/#53 constituents (NAN hayaleti canlıda temizlendi, tarih parse, soğuk-cache) · #14 engine_orphans alarmı · #15 arm-anı day_pnl=0 (backtest paritesi) · #16 scaled_out adet-alarm muafiyeti · #18 rejim-etiketli backtest_oos · #24 süreçler-arası flock (reflect.submit) · #25 hermes.loop baseline restore+persist · #33 regime.* @regime yasağı (guard+arama) · #35 pullback-screener kayıt (P2+registry+SKILL.md) · #37 rec dedup · #39 P4_EXECUTE+P5_LEARN pipeline_run · #40 rs tek-ticker=50 · #50 mirror'a size_mult · #51 endpoint host doğrulama · #54 partial-fill bacağı. YENİDEN-TABAN: dürüst yasayla OOS 0.1894 / 111 işlem. 175 pytest yeşil; sunucu yeniden başlatıldı, canlı doğrulandı.
⏳ AÇIK (bilinçli ertelenen, düşük): events.jsonl sınırsız büyüme (şu an ~10K; ileride trim) · #14'ün OTOMATİK iyileştirme yarısı (motor yetimi tespit+alarm VAR; Alpaca bracket'ını otomatik iptal etmek gerçek emir politikası gerektirir — operatör 'Tüm pozisyonları kapat' ile manuel; L0'da bilinçli tercih) · #44 warmup verisi kaynak sınırı (36 cache 2021-07'den başlar — veri var edilemez, telemetri uyarır).

## ÖNCELİK SIRASI (işlem planı)
1. KRİTİK/CANLI: #41 FETCH_END donması (sunucu portföyü İLERLEMİYOR) · #10 peak_equity kalıcı değil (de-risk ölü) · #42-45 veri tazeliği ailesi (19 ticker 2 seans bayat) · #49 NAN ticker (diskte canlı)
2. ÖĞRENMEYİ ZEHİRLER: #17+#31 rollback sonrası sürüm numarası yeniden kullanımı · #11 yarım-döngü istisnasında trades.jsonl mükerrer satır · #19 hypotheses.jsonl kayıp-güncelleme yarışı · #30 refetch sonrası bayat walk-forward cache
3. GERÇEKÇİLİK: #1 OPEN(D) kapanış fiyatıyla marklama (same-day look-ahead) · #2/#8 scale_out stop'tan önce (dormant: frac=0) · #3 OOS→holdout sızıntısı · #5 span bazlı yıllıklaştırma şişmesi (rejim dilimlerinde kritik) · #6 max_drawdown yalnız kapanmış işlemlerden
4. OPS: #12/#13 scheduler seans/heartbeat · #22 sprint zombie · #23 HALT sandbox'a kopyalanıyor · #36 PROTECTED skill kapatılabiliyor · #35 pullback-screener kayıt dışı
5. PANO SABİT SATIRLAR (elle tarama): app.js:548 "v2 yayında" sabit · :547 "v1 duruyor" sabit · :553 "min 30" sabit · :637 skill_count||67 · TERMS sözlüğünde sabit "(30)" ve "Şu an L0" · sprint kart etiketleri "v1/v2" sabit → hepsi /api/summary (strategy_version, goal.min_sample, autonomy_level) ve /api/hermes.skill_count'a bağlanmalı.

---
## HAM BULGULAR (9 ajan, 54 bulgu)

===== agent result 1: 7 finding(s) =====
[HIGH    ] #1 OPEN(D) risk decisions are marked at D's CLOSE — same-day look-ahead in breaker, de-risk sizing, and position throttle
   meridian/backtest.py:119
   CLAIM: marks_on(d) (lines 109-110) returns per[t].loc[d, 'close'] — day D's closing price — but it is called during the OPEN(D) phase: eq_now (line 119) feeds peak_equity, derisk_mult, max_positions_at, the daily-loss breaker_tripped test (line 123), the equity passed to fill_entry for position sizing (line 128), and day_start_equity (line 131). Every risk decision executed at D's open therefore uses D's closing prices, vio
   SCEN : Market gaps flat at OPEN(D) then sells off 3% intraday. At the open, eq_now is already marked at D's close, so breaker_tripped fires and derisk_mult shrinks — the backtest blocks/shrinks entries at the morning open using knowledge of the afternoon crash, while the live engine would have filled them 
[HIGH    ] #2 scale_out called before _touch_exit assumes the high came first — banks +2R partials on bars whose low (or even open) hit the stop
   meridian/backtest.py:139
   CLAIM: In INTRADAY(D), broker.scale_out(pos, {'high': bar['high']}, prev_eff) runs unconditionally before broker._touch_exit. scale_out banks frac of the position at entry+scale_out_r*R and ratchets trail_stop to breakeven whenever high >= level, with no reference to open/low. _touch_exit's documented convention is 'stop checked before target (conservative)', but this call ordering resolves the scale-out leg high-first, and
   SCEN : With exit.scale_out_frac=0.5, scale_out_r=2: a bar whose low touched the original stop and whose high reached entry+2R (order unknowable from OHLC) is recorded as 0.5*+2R banked plus the remainder out at breakeven (trail was ratcheted to entry before the stop check) — instead of the conservative ful
[MEDIUM  ] #3 OOS magnitude gate includes P&L realized after oos_end — holdout-window prices leak into the acceptance score
   meridian/backtest.py:252
   CLAIM: segment_score assigns a trade's ENTIRE pnl to a segment based only on ts_open. A trade opened inside [oos_start, oos_end] that exits during the holdout window — or is force-closed by eod_markout at holdout_end (line 232) — contributes P&L earned from holdout-period price action to oos_score, which drives acceptance. The docstring's guarantee that 'the frozen holdout is reported to the human only and never drives acce
   SCEN : Candidate opens a position 2 days before oos_end; it runs 30 bars into the holdout window and exits at +3R on holdout prices (or rides to eod_markout at holdout_end). That +3R lands in oos_detail/oos_score and can flip the magnitude gate from reject to accept — acceptance driven by holdout data. The
[MEDIUM  ] #4 Inclusive-inclusive segment and fold boundaries double-count trades opened on a boundary date
   meridian/backtest.py:306
   CLAIM: segment_score filters lo <= ts_open <= seg_end with both ends inclusive, and walk_forward uses shared boundary dates: is=[is_start, oos_start], oos=[oos_start(+embargo), oos_end], hold=[oos_end, holdout_end]. A trade opened exactly on oos_end is always counted in BOTH oos_detail and holdout_detail (the embargo never shifts the oos_end/holdout boundary); with embargo_days=0 a trade opened on oos_start is counted in bo
   SCEN : A trade with ts_open == oos_end contributes its pnl and its unit of n to both the OOS magnitude gate and the reported holdout — n inflated in both segments, and the same pnl graded twice; a fold-boundary trade counts toward two adjacent folds' avg_r, so one good boundary trade can carry the robustne
[MEDIUM  ] #5 realized_30d and Sharpe annualize off the trade cluster's span, not the segment window — burst and regime-sliced scores inflated
   meridian/score.py:66
   CLAIM: _span_days derives span from the min/max timestamps of the trades themselves, not from the evaluation window. realized_30d = (1+total_return)^(30/span)-1 and trades_per_year = n/(span/365) (sqrt-scaled into sharpe, line 73) both use it. A strategy that trades only during a short burst — or a thin eval_regime slice whose trades cluster in the regime's episodes, the exact population the new Dynamic Regime grading creat
   SCEN : OOS window is 183 days; all 24 graded trades sit in one 20-day burst with +4% total return. span=20 gives realized_30d=(1.04)^1.5-1 ≈ 6.1% vs the honest ≈0.65% per-30d over the actual OOS window — ret_c (weight 0.5) inflated ~9x, and sqrt(trades_per_year) uses 24/(20/365) ≈ 438 trades/yr instead of 
[MEDIUM  ] #6 Gate's max_drawdown is computed from the closed-trade-only equity curve — open-position and intra-trade drawdown invisible
   meridian/score.py:67
   CLAIM: score_detail builds equity_curve from closed trades' pnl_dollars in ts_close order and measures max_drawdown on that step curve. Unrealized mark-to-market dips — positions deep underwater that later recover, or many concurrent open losers — never appear. The replay already produces an honest daily mark-to-market curve (BacktestResult.equity, backtest.py line 225) but the scoring/gating path ignores it, so the dd_c co
   SCEN : Portfolio holds 5 positions through a correlated 20% mark-to-market drawdown that recovers before any exit; each trade closes near breakeven or better. score_detail reports max_drawdown ≈ 0, dd_c = 1.0, and the candidate passes a goal max_drawdown of 10% that the daily equity curve in the same Backt
[LOW     ] #7 Positions in tickers whose data ends before the final bar are silently dropped from grading (eod_markout skipped)
   meridian/backtest.py:231
   CLAIM: The final mark-out loop only closes a position if the ticker has a bar on the last calendar date (if t in per and d in per[t].index). A ticker whose history ends early — plausible for the deliberately-included laggard/delisting names (PARA, VFC, WBA, ENPH...) — leaves its position open forever: the intraday and close loops also `continue` on missing bars, so it never stops out, never time-stops (bars_held frozen), ne
   SCEN : Replay runs through 2026-06 but VFC's bars end 2025-11 with an open VFC position down -0.8R. The position vanishes from grading: trades list, n, avg_r, drawdown, and tail_risk all exclude the loss, and the equity curve carries it at entry-price mark forever — a survivorship-style silent no-op that f

===== agent result 2: 2 finding(s) =====
[HIGH    ] #8 scale_out banks partial profit on bars where the stop was gapped/hit first (look-ahead, inverts stop-before-target conservatism)
   meridian/broker.py:197
   CLAIM: scale_out decides solely on bar['high'] >= level (both call sites, backtest.py:139 and loop.py:135, pass {'high': ...} only and call it BEFORE _touch_exit), so on a bar whose open or low breaches the effective stop but whose high later reaches entry + scale_out_r*R, the broker banks frac of the position at the scale level with profit and only then stops the remainder — while _touch_exit itself documents and implement
   SCEN : exit.scale_out_frac=0.5, scale_out_r=2; position entry 100, stop 95 (R=5), qty 100; bar open 94 (gap through stop), high 111, low 93. Code: banks 50 sh at ~110*(1-slip) ≈ +$500, then _touch_exit sees o<=eff_stop and closes 50 sh at open 94 ≈ -$300; net ≈ +$200 booked. Honest simulation: entire 100 s
[MEDIUM  ] #9 fill_entry has no guard against filling above the profit target — same-bar guaranteed-loss exit booked as 'target_gap'
   meridian/broker.py:123
   CLAIM: The entry gap guard only rejects next_open > trigger*(1+MAX_ENTRY_GAP_PCT=4%) or next_open <= stop; nothing rejects next_open at/above plan['profit_target']. With DISCIPLINE_MIN_RR=2.0 and stop_loss_atr_mult down to 0.8 (bounds.yaml), target - trigger can be ~1.6*ATR, inside the 4% window for any name with ATR ≲ 2.5% of price, so a gap open between target and trigger*1.04 fills the entry above its own target; the sam
   SCEN : Signal close 100, ATR 1.5, stop_mult 1.0 → stop 98.5, rr 2.0 → target 103.0; next open 103.5 (3.5% gap, passes the 4% guard). Entry fills ~103.5*(1+slip); same-bar _touch_exit fires target_gap at raw 103.5 → exit_fill 103.5*(1-slip). Certain round-trip loss of 2*slippage*notional + both commissions,

===== agent result 3: 7 finding(s) =====
[CRITICAL] #10 peak_equity is never persisted, so drawdown de-risk and position throttle are permanently inert
   meridian/loop.py:114
   CLAIM: daily_cycle computes meta["peak_equity"] = max(meta.get("peak_equity", START_EQUITY), eq_now) and feeds it to derisk_mult() and max_positions_at(), but _save_broker (loop.py:38-46) omits peak_equity from the persisted dict, and _load_broker rebuilds meta straight from portfolio.json. Live state/portfolio.json confirms the key is absent. Since every cycle reloads meta from disk, peak_equity always collapses to max(STA
   SCEN : Book runs 100k -> 118k -> 108k (true 8.5% drawdown, past DERISK_FLOOR_DD=8%: derisk_mult should be 0.0 and max_positions_at should be 0 — no new size at all). Next cycle loads meta without peak_equity, computes peak=max(100k,108k)=108k, dd=0 -> size_mult=1.0 and eff_max_open=base_max, so the engine 
[HIGH    ] #11 Mid-cycle exception replays exits and appends duplicate rows to trades.jsonl on every 300s retry
   meridian/loop.py:99
   CLAIM: _persist_trade appends closed trades to trades.jsonl immediately (loop.py:99 open exits, :138 intraday exits), but broker state + last_date dedup are only saved at the very end (_save_broker, loop.py:265). skills.pipeline_run re-raises exceptions (skills.py:269-272), so any unhandled error in P1/P2/P3 or manage_position aborts daily_cycle after trades were appended but before portfolio.json was updated. scheduler._ru
   SCEN : A malformed bar makes strat.scan_entry raise in P2 at 20:05. trades.jsonl already got the day's pending-exit row; portfolio.json still shows the position open with last_date=D-1. Every 300s poll reloads the old state, re-closes the same position at the same open (b.closed row with the same reloaded 
[HIGH    ] #12 Scheduler refetch-once flag is consumed before the session closes, so new bars are fetched a full session late
   meridian/scheduler.py:61
   CLAIM: _last_closed_session (scheduler.py:31-40) claims 'most recent CLOSED session' but never checks the close time: mcal.schedule(end_date=today) includes today's session from midnight. The refetch dedup 'fresh = last_closed != last_refetch_session' therefore fires on the first poll of a trading day (pre-open), marks last_refetch_session=today, and every post-close poll that same evening runs cache-only — the newly closed
   SCEN : Dashboard runs continuously. Friday 00:05 poll: last_closed=Friday, refetch fires, feed only has Thursday's bar, flag set to Friday. Friday 20:05+ polls: fresh=False, cached data lacks Friday's bar, advance_once returns 'current' all evening AND all weekend (Sat/Sun last_closed is still Friday). Fri
[HIGH    ] #13 advance_once's 'current' path never writes a heartbeat, so under the local scheduler /healthz 503s and Hermes reflection is gated off nearly all day
   meridian/scheduler.py:68
   CLAIM: The heartbeat is written only by loop.daily_cycle (and run.py's VM worker, which writes a 'between sessions' heartbeat every tick). scheduler.advance_once returns {'status':'current'} at scheduler.py:67-69 (and 'halted' at :50-52) BEFORE calling daily_cycle, writing only scheduler_status.json — never heartbeat.json. daily_cycle's noop branch (loop.py:86-90) that refreshes the heartbeat is unreachable from the schedul
   SCEN : Steady state: daily_cycle runs once (per finding above, around midnight); for the next ~24h every poll returns 'current' with no heartbeat write. health.stale(900) is true ~98% of the time, so /healthz (api.py:74-79) reports stale/503, the meridian_up gauge (api.py:98) reads 0, and hermes_runtime._r
[MEDIUM  ] #14 Engine-originated Alpaca positions are misclassified as 'external' (never alarmed) because dropped armed plans and internal exits never cancel the mirror bracket
   meridian/loop.py:375
   CLAIM: Armed plans are submitted to Alpaca as GTC bracket stop-entries at close of D, but at open of D+1 the internal fill loop drops them without any Alpaca cancel when halted/breaker/data_bad/size_mult==0 (loop.py:117 gate skips fills, :124 unconditionally clears meta['armed']), when the slot race loses (len(positions)>=eff_max_open), or when fill_entry's gap guards return None. Likewise pending_exits/trail/time exits clo
   SCEN : Breaker trips on day D: internal armed list is cleared, but the two GTC brackets submitted at D-1's close stay live on Alpaca and fill at D's open. Internal book: flat. Alpaca: 2 open positions. reconcile lists both under 'external', position_drift stays false, no ALARM_MIRROR_DRIFT — the mirror sil
[MEDIUM  ] #15 Live gate receives real day_pnl_pct at arm time while the backtest hardcodes 0.0 — live and simulated behavior diverge on breach days
   meridian/loop.py:191
   CLAIM: loop.py:191 passes the actual day_pnl_pct into the portfolio dict consumed by guard.classify_gate, which returns NO_GO when day_pnl_pct <= -max_daily_loss_pct (guard.py:157, :200). backtest.py:211 deliberately passes day_pnl_pct: 0.0 at arm time ('breaker enforced at fill (open), not at arm time'). Same code path, different inputs — violating the module docstring's core invariant that live and simulated behavior cann
   SCEN : On any day whose close-to-close P&L breaches max_daily_loss_pct, the live loop NO_GOes every candidate at the close (nothing armed, nothing sent to Alpaca), while a replay/walk_forward over the identical bars arms those same plans and fills them next open if the next day's breaker allows. Walk-forwa
[LOW     ] #16 Enabling exit.scale_out_frac will fire a false mirror qty-drift alarm every cycle for every scaled-out position
   meridian/loop.py:370
   CLAIM: reconcile alarms when |alpaca_qty - local_qty|/local_qty > 0.25, but broker.scale_out (broker.py:199-210) reduces the internal qty by int(qty*frac) while the Alpaca mirror only ever holds the original full-qty bracket (alpaca.submit_plan/submit_bracket — no scale-out order is ever mirrored). bounds.yaml explicitly lets Hermes set exit.scale_out_frac to 0.25/0.5/0.75, and any value >=0.25 pushes the relative gap to >=
   SCEN : Reflection promotes exit.scale_out_frac=0.5 (a legal single-variable change within bounds.yaml). A position hits 2R, internal qty halves to 50 while Alpaca still holds 100: |100-50|/50 = 100% > 25%, so ALARM_MIRROR_DRIFT fires and broker_reconcile.json sets position_drift=true — repeated every daily

===== agent result 4: 5 finding(s) =====
[CRITICAL] #17 Version numbers are reused after rollback, so the next shipped hypothesis is instantly judged on the rolled-back version's trades
   meridian/rollback.py:79
   CLAIM: check_and_rollback calls versioning.revert_to(parent), which rewrites strategy.yaml from the parent snapshot (version=parent). versioning.bump (versioning.py:20) computes child version as current.version+1 with no scoreboard consult, so the next ship after any rollback reuses the exact version number that was just rolled back. trades.jsonl rows tagged strategy_version==N from the failed run persist, and rollback.py:5
   SCEN : v2 rolls back to v1 after accumulating min_sample losing trades. Reflect later ships a new, different change: bump(v1) -> version 2 again (hypothesis B, status live). On the very next cycle evaluate_outcomes(2) finds >= min_sample trades — ALL from the old failed v2 parameter set, zero from B — comp
[HIGH    ] #18 Global child of a regime-shipped parent uses the parent's regime-sliced backtest_oos as its global baseline — fabricated delta, false rollback/promote
   meridian/rollback.py:130
   CLAIM: For a global ship (ereg is None), evaluate_outcomes and check_and_rollback (rollback.py:70-72, 128-130) fall back to scoreboard v.get("live_score", v.get("backtest_oos")) for par_score. But when the parent itself was a regime ship, reflect.submit (reflect.py:312-314) recorded backtest_oos=cand_oos from an eval_regime-SLICED walk_forward, and its live evaluation only ever writes live_score@{regime}, never plain live_s
   SCEN : v2 ships as atr_mult@bull with bull-slice OOS 0.45 (scoreboard["2"].backtest_oos=0.45, no plain live_score). Days later, within the monthly quota and before v2 accumulates min_sample live trades in the eval window, a global v3 ships (its gate graded incumbent v2 globally at 0.12). v3 reaches min_sam
[HIGH    ] #19 hypotheses.jsonl lost-update race between the scheduler cycle and the reflection thread — a shipped hypothesis row can vanish while strategy.yaml is already bumped
   meridian/memory.py:52
   CLAIM: memory.update_status and writeback_outcome do unlocked full-file read-modify-rewrite of hypotheses.jsonl (memory.py:42-53, 66-87) while memory.record appends (memory.py:35-39). These run on two different threads under two different, non-overlapping locks: the scheduler daemon (scheduler._run_lock) calls rollback.evaluate_outcomes -> writeback_outcome/update_status every cycle once min_sample is reached, and the herme
   SCEN : The scheduler's writeback_outcome reads all rows; concurrently the reflection thread finishes a multi-minute walk-forward, calls versioning.commit (strategy.yaml now vN+1) and appends the new live hypothesis; writeback_outcome then write_jsonl's its stale snapshot, atomically replacing the file WITH
[MEDIUM  ] #20 Neutral-hold status refresh bumps status_ts every cycle, so accepted_this_month counts a months-old ship against every subsequent month's quota
   meridian/rollback.py:147
   CLAIM: evaluate_outcomes, on every neutral cycle (delta within ±threshold) after min_sample, calls memory.update_status(hyp["id"], "live", market_regime=...) which unconditionally sets status_ts=now (memory.py:48). memory.accepted_this_month (memory.py:56-63) counts live/promoted hypotheses whose status_ts falls in the current UTC month, and guard.validate_change (guard.py:119-122) rejects proposals once that count reaches 
   SCEN : A hypothesis ships in May and stays live. Every daily cycle from May onward refreshes its status_ts, so in July accepted_this_month still counts it as a July acceptance — one quota slot is permanently consumed by a change accepted months ago (and the ledger's shipped-date audit trail is destroyed, s
[MEDIUM  ] #21 Promoted hypotheses keep having realized_delta/calibration_hit rewritten every cycle, flip-flopping the autonomy-ladder calibration gate and lessons.md while status stays 'promoted'
   meridian/memory.py:84
   CLAIM: writeback_outcome matches status in ("live","promoted") and recomputes realized_delta and calibration_hit from the current live reading on every cycle, but promotion is a one-way ratchet (rollback.py:149-151 only acts when status=="live" and never demotes). So a promoted hypothesis's recorded outcome keeps mutating with market noise while its promoted status and scoreboard promoted:true are frozen.
   SCEN : A hypothesis is promoted at delta +0.03 (calibration_hit=true). Weeks later the live delta drifts to -0.01 — above -threshold so no rollback — and the nightly writeback flips calibration_hit to false: analytics.autonomy_ladder's '≥3 accepted hypotheses matched prediction' criterion (analytics.py:75,

===== agent result 5: 8 finding(s) =====
[HIGH    ] #22 Completed sprint child becomes a zombie; sprint reads 'active' forever and can never be restarted
   meridian/sprint.py:56
   CLAIM: sprint.status() decides liveness with os.kill(pid, 0), which succeeds for a zombie. The child is spawned with subprocess.Popen in start() and the Popen object is dropped at function return; the API process never wait()s for it, and there is no other Popen call in the codebase to trigger subprocess._cleanup(), so when the sprint child exits it stays a zombie in the API process's table for the life of the server.
   SCEN : Operator runs one sprint to completion (or the child crashes). Child exits -> zombie. Every subsequent sprint.status() call returns active=True (phase says 'done' while 'active' is true — contradictory dashboard display), and sprint.start() at line 106-107 returns 'already_running' forever. The lear
[HIGH    ] #23 Live HALT kill-switch file is copied into the sprint sandbox, silently zeroing all sprint trades
   meridian/sprint.py:43
   CLAIM: SKIP_COPY = {"bars", "sprint", "secrets.json"} does not exclude HALT, and _reset_sandbox_state() does not delete it, so start()'s copy loop (lines 115-121) clones the live kill-switch into sbstate. In the child, loop.daily_cycle checks health.halted() (loop.py line 102) and suppresses every new entry (lines 117, 164).
   SCEN : Operator touches state/HALT to halt live trading (the natural time to run an offline training sprint), then starts a sprint. Phase A walks ~500 eval sessions with halted=True, opens zero positions, ends with n_v1=0 < min_sample, and reports 'v1 ileri baz 0/30 işlem — döngü kapanışı için yetersiz örn
[MEDIUM  ] #24 No cross-process interlock between hermes.loop (tmux) and hermes_runtime — concurrent reflections can clobber strategy.yaml/version state
   meridian/hermes.py:252
   CLAIM: hermes.loop() calls reflect_once() with no lock of any kind, and hermes_runtime's _reflect_lock is a threading.Lock — per-process only. There is no file lock or PID guard anywhere (verified: no flock/lockfile in reflect.py, versioning.py, scheduler.py). versioning.commit() is a plain yaml write with no compare-and-swap.
   SCEN : Operator runs serve.sh (MERIDIAN_AUTOSTART_HERMES defaults to 1, starting hermes_runtime) and also `python -m meridian.hermes --loop` (the deploy/install_hermes.sh tmux command, or for testing). When the horizon fires, both standby loops start multi-minute reflections; both submit() calls read strat
[MEDIUM  ] #25 hermes.loop() re-baselines last_reflect_at to the current ledger length on every start — the 30-day horizon clock resets on restart (livelock)
   meridian/hermes.py:234
   CLAIM: loop() initializes last_reflect_at = _closed_count() and never persists it nor restores it from hermes_status.json. This is exactly the livelock hermes_runtime._restored_baseline() (hermes_runtime.py lines 100-109) was added to fix — but only the in-process path was fixed; the VM/tmux path still re-baselines, discarding all calendar span accrued since the last real reflection.
   SCEN : On the VM with real-time daily bars and REFLECTION_MIN_DAYS=30: the tmux hermes session restarts (reboot, redeploy, install_hermes.sh re-run) every <30 days. Each restart sets the baseline to the current trade count, so _horizon_ok's span_days restarts from zero; the strict-AND >=30-day condition is
[MEDIUM  ] #26 Prompt advertises phantom regime 'risk_off' and omits the two real regimes trend_down/high_vol
   meridian/hermes.py:105
   CLAIM: build_context()'s note_regime_conditional tells Claude the valid regimes are 'trend_up, chop, risk_off', but config.VALID_REGIMES is ('trend_up','trend_down','chop','high_vol') and guard.validate_change (guard.py lines 63-66) rejects any @regime not in that tuple. config.py's own comment (lines 67-68) names 'risk_off' as the phantom-label bug; the prompt was never updated.
   SCEN : Claude follows the prompt and proposes e.g. 'stop_loss_atr_mult@risk_off': the paid Opus call is spent, guard returns rejected_by_guard ('unknown regime risk_off') 100% of the time, a junk hypothesis is written to the ledger, and the reflection falls through to the free search — the LLM axis can nev
[MEDIUM  ] #27 Claude proposal path bypasses the certified target_regime — ships regime knobs the horizon never certified
   meridian/hermes.py:186
   CLAIM: reflect_once(target_regime=...) threads the caller's horizon-certified regime only into the coordinate-descent search (line 198-199). The propose_with_claude() proposal at lines 184-189 is submitted unconditionally: Claude may return any 'variable@regime', and submit() gates it on that regime's slice with no check against the certified regime.
   SCEN : The standby loop certifies live_reg=chop (>=reflection_every trades and >=30 days of span in chop trades). Claude proposes 'exit.time_stop_days@high_vol'; guard accepts (high_vol is valid), the gate grades on the high_vol slice, and if it clears, a high_vol override ships into params_by_regime even 
[LOW     ] #28 SEARCH_PROGRESS stuck at running=True after a search exception — dashboard shows a phantom in-flight search indefinitely
   meridian/hermes.py:205
   CLAIM: reflect_once() sets SEARCH_PROGRESS running=True/phase='incumbent' (and _on_probe sets phase='probing') with no try/finally; the terminal running=False update at line 220 is only reached on success. hermes_runtime._run and reflect_now._bg catch the exception and release _reflect_lock, but never reset SEARCH_PROGRESS.
   SCEN : dataset.load() or a walk_forward inside reflect.search_and_submit raises (e.g. transient data fetch failure). status() then permanently reports search={'running': True, 'phase': 'probing', ...} while 'reflecting' is False — the dashboard shows a live search in progress for hours/days until the next 
[LOW     ] #29 Sprint child writes the live status file non-atomically while the API reads it unguarded — intermittent 500 on /api/hermes and /api/sprint
   meridian/sprint_run.py:27
   CLAIM: _write_live_status truncates and rewrites the live sprint_status.json with open(path,'w') + json.dump (not the atomic tmp+os.replace pattern store.write_json uses for exactly this reason), and store.read_json (used by sprint.status()) calls json.load with no JSONDecodeError handling.
   SCEN : During a sprint the child rewrites the file on every ~8th session; a dashboard poll of /api/sprint — or /api/hermes, which embeds sprint.status() — that reads between truncate and write-completion gets empty/partial JSON, json.load raises JSONDecodeError, and the endpoint returns 500, intermittently

===== agent result 6: 5 finding(s) =====
[HIGH    ] #30 Walk-forward caches survive dataset refetches — gate can compare candidate-on-new-bars vs incumbent-on-old-bars
   meridian/reflect.py:48
   CLAIM: _INC_CACHE and _PROBE_CACHE keys contain (version, params digest, by_regime digest, windows, eval_regime/var) but nothing identifying the bars revision, and neither cache is ever invalidated in production (only tests clear them). scheduler.advance_once() runs dataset.load(use_cache=False) once per newly-closed session in the SAME server process (scheduler.py:62), replacing the in-memory bars with a full network refet
   SCEN : Day 1: reflection walks the incumbent, result cached. Overnight a universe ticker pays a dividend or splits, so the refetched adjusted series differs across the whole 2022–2026 replay window (and the lagging free feed can also backfill sessions near HOLDOUT_END=2026-07-10). Day 2: scheduler refetche
[HIGH    ] #31 Rollback + reship reuses the version number and update_scoreboard merges the new incarnation into the dead version's row
   meridian/versioning.py:49
   CLAIM: update_scoreboard does sb['versions'].setdefault(v, {}).update(fields) — it never resets a row. After rollback.check_and_rollback runs revert_to(parent), strategy.yaml carries the parent's version, so the next ship's bump() reuses the rolled-back version number (acknowledged in reflect.py:350-352 for the probe cache, but the scoreboard and history were not fixed). submit()'s update_scoreboard(candidate...) therefore 
   SCEN : v2 (parent 1) underperforms: rollback writes row '2' = {rolled_back: True, live_score: -0.10} and reverts to v1. Hermes later ships a different change: bump(v1) → version 2 again; submit merges {params, backtest_oos: 0.25, live_since, ...} into the same row, which still says rolled_back: True and li
[MEDIUM  ] #32 commit()/revert_to() write the hot-reloaded live strategy.yaml non-atomically while other threads read it
   meridian/versioning.py:38
   CLAIM: commit() and revert_to() write strategy.yaml via config.dump_yaml, a plain open('w') truncate-then-write — unlike store.write_json/write_jsonl which use tempfile+os.replace ('atomic', store.py:42). strategy.yaml is explicitly hot-reloaded ('within one bar') and is re-read concurrently by the scheduler's daily_cycle thread, the hermes standby thread, and API handlers in the same process; config.load_strategy() maps a 
   SCEN : Hermes ships a version while scheduler.advance_once() runs loop.daily_cycle in a parallel thread. The loop's load_strategy() lands between truncate and write → gets {} → params={}, version defaults to 1, params_by_regime absent. That cycle scans/manages positions on strategy.py's hardcoded fallback 
[MEDIUM  ] #33 regime.min_exposure_score@<regime> is a structurally dead knob — probes burn budget and record false permanent dead ends
   meridian/guard.py:71
   CLAIM: The guard's honesty check only rejects a base@regime override whose base is missing from flat params, but regime.min_exposure_score's ONLY consumer is regime_mod.build_regime_json, which is called with FLAT params in both the backtest replay (backtest.py:149) and the live loop (loop.py:144) — the regime cannot be known before regime detection runs. A @regime override for it is written into params_by_regime and overla
   SCEN : The live regime flips to chop and the horizon guardrail certifies it: hermes.reflect_once runs coordinate_descent_search(regime='chop'). _ucb_rank includes regime.min_exposure_score (untried → +inf, ranked first), so up to 6 of the 10-probe budget (±1..3 steps) are spent on regime.min_exposure_score
[LOW     ] #34 Best-probe selection treats a legitimate 0.0 OOS score as -1e9
   meridian/reflect.py:436
   CLAIM: coordinate_descent_search picks the best gate-clearing probe with `(c_oos or -1e9) > (best["candidate_oos"] or -1e9)`; `or` treats a candidate_oos of exactly 0.0 (a valid composite score when incumbent_oos is below -0.02) as falsy and substitutes -1e9.
   SCEN : Incumbent OOS = -0.05. Probe A clears the gate with candidate_oos = 0.0 and is set as best; probe B later clears with candidate_oos = -0.01. The comparison evaluates -0.01 > (0.0 or -1e9) = -1e9 → True, so the strictly worse probe B displaces probe A as 'best' and is the one nominated to submit() — 

===== agent result 7: 6 finding(s) =====
[HIGH    ] #35 pullback-screener is invisible to the entire skill layer despite being an armed, trading setup
   meridian/skills.py:54
   CLAIM: "pullback-screener" (screener_for maps setup 'pullback' to it, it is in ENGINE_IMPLEMENTED, and ARMED_SETUPS arms pullback trades) is missing from PIPELINES['P2_SCREEN'], from state/skills_registry.json (66 skills, not it), and has no skills/pullback-screener/SKILL.md dir. So: (a) pipeline_run P2 rows never list it in skills_invoked/declared/skipped even though evaluate_pullback runs every ticker every day; (b) catal
   SCEN : Live state already shows it: state/trades.jsonl has 2 trades with 'pullback-screener' in skill_chain, yet no P2 row in pipeline_runs.jsonl ever mentions the skill and the /api/skills catalog omits it entirely. If pullback trades go chronically negative (avg_r <= -0.15 over >= 8 trades), the determin
[HIGH    ] #36 reconcile_enablement can disable PROTECTED and natively-executed skills on key removal
   meridian/skills.py:93
   CLAIM: The reconcile loop key-gates every skill with fmp=='req'/alpaca=='req' with no exclusion for PROTECTED or ENGINE_IMPLEMENTED skills. Registry data confirms: portfolio-manager (PROTECTED, P4 constraint layer) has alpaca:'req'; vcp-screener and stockbee-momentum-burst-screener (ENGINE_IMPLEMENTED, scanned natively from keyless Cboe/Nasdaq data per adapters/data.py) have fmp:'req'. alpaca.py itself states paper_availabl
   SCEN : Operator deletes ALPACA_PAPER_KEY (api.py:335 runs reconcile_enablement exactly then): PROTECTED portfolio-manager gets enabled=False, mode='disabled', reason='ALPACA_PAPER_KEY yok' — violating the PROTECTED contract ('may NEVER be shadowed or disabled') while broker.py keeps managing positions. Lik
[MEDIUM  ] #37 record_recommendation is permanently blocked for a (skill, action) pair after the rec is first applied
   meridian/skills.py:192
   CLAIM: The dedup scan skips any row with pending=True and applied=False for the same skill+action. But apply_skill_action APPENDS a separate {applied:True} row and never mutates the original pending row, so the original row matches the dedup predicate forever. Meanwhile pending_recommendations hides that stale pending row behind the newer applied row. Net effect: once a recommendation has been recorded and applied once, an 
   SCEN : reflect.submit records {vcp-screener, shadow} from recommend_from_attribution; operator clicks apply (applied row appended), later re-activates the skill. Months later vcp-screener's avg_r degrades below -0.15 again — recommend_from_attribution fires, reflect calls record_recommendation, the loop hi
[MEDIUM  ] #38 reconcile_enablement clobbers shadow mode/reason of key-gated skills on every skills read
   meridian/skills.py:110
   CLAIM: For a key-gated skill whose keys are present, reconcile computes reason='enabled' and, since it differs from the shadow reason set by apply_skill_action ('gölge (Axis-2 incelemesi)'), overwrites mode='active' and reason='enabled' without checking info['shadow']. GET /api/skills calls reconcile_enablement before returning, so the overwrite happens on the very next dashboard read after shadowing.
   SCEN : Operator shadows pead-screener (fmp:'req', key present) via /api/skills/apply → mode='shadow', reason='gölge (Axis-2 incelemesi)'. The next GET /api/skills reverts mode to 'active' and reason to 'enabled' in the registry. app.js still shows the 'gölge' chip (it reads the shadow bool) but renders rea
[LOW     ] #39 P4_EXECUTE and P5_LEARN are never run through pipeline_run — executions are invisible in the audit log
   meridian/skills.py:61
   CLAIM: pipeline_run is invoked only for P1_REGIME, P2_SCREEN, P3_PLAN (loop.py:143/165/183); no caller anywhere passes P4_EXECUTE or P5_LEARN (grepped whole package). The module contract says the five pipelines each log every run to pipeline_runs.jsonl ('nothing the agent does is invisible'), and ENGINE_IMPLEMENTED lists drawdown-circuit-breaker and portfolio-manager as P4 invoked skills.
   SCEN : A session with entry fills, scale-outs, exits, and even a circuit-breaker trip produces zero P4_EXECUTE rows in pipeline_runs.jsonl; /api/pipeline_runs and the dashboard show P1-P3 only, and registry last_run for drawdown-circuit-breaker/portfolio-manager stays untouched forever — the two PROTECTED 
[LOW     ] #40 rs_rating gives the sole qualifying ticker a rating of 1, hard-vetoing it at the rs gate
   meridian/indicators.py:96
   CLAIM: With exactly one non-NaN return in the cross-section, ranks[0]=0 and pct = (0/max(1, 1-1))*98+1 = 1, so the only measurable ticker gets RS=1 (worst), while tickers excluded for NaN default to 50 — the single strongest (only) name ranks below names with no data at all.
   SCEN : Early in a replay or on a thin universe where only one ticker has more than RS_LOOKBACK+1 bars of history (loop.py:169 filters rets that way), that ticker gets rs=1; evaluate_entry/evaluate_pullback hard-gate rs_rating_value < entry.rs_rating_min (default 70) at strategy.py:93/180, so the candidate 

===== agent result 8: 8 finding(s) =====
[CRITICAL] #41 FETCH_END is frozen at module-import time, so the long-running scheduler silently stops advancing the portfolio after the server-start date
   meridian/dataset.py:13
   CLAIM: FETCH_END = dt.date.today().isoformat() is evaluated once when meridian.dataset is first imported. In the long-lived FastAPI/scheduler process, every later dataset.load() windows all bars to <= the process-start date via _window(), so newly closed sessions are fetched to disk but cut out of the returned frames; scheduler.advance_once() then sees latest_bar <= portfolio last_date and returns status 'current' — a perma
   SCEN : Confirmed live: scheduler_status.json shows started_at 2026-07-15, cycles=1, last_processed=2026-07-15, yet last_refetch_session=2026-07-17 and the disk caches contain bars through 2026-07-17. The scheduler refetched the 07-16 and 07-17 sessions, windowed them away with the frozen FETCH_END='2026-07
[HIGH    ] #42 Per-ticker FRESH_DAYS=3 tolerance produces a date-misaligned universe: 19 active tickers are 2 sessions stale right now while treated as fresh
   meridian/adapters/data.py:184
   CLAIM: The freshness test cached['date'].max() >= end - FRESH_DAYS(3d) is evaluated per ticker, so tickers whose cache passes the check skip refetch while others refetch to today. The universe ends up with mixed as-of dates: on the last daily cycles, stale-but-'fresh' tickers have no bar at all for the newest sessions, so their positions are not marked and they drop out of cross-sectional signals, silently.
   SCEN : Confirmed live in state/bars/: 34 tickers end 2026-07-17 but 19 active large-caps (AVGO, LLY, UNH, HD, CAT, ORCL, MA, PG, ...) end 2026-07-15 — exactly the ones whose caches pass the freshness check. The 2026-07-16 and 07-17 daily cycles ran with no bars for those 19 names: any open position in them
[HIGH    ] #43 Freshness min-date condition is unsatisfiable for 36 caches, so every load() re-downloads two-thirds of the universe from the network despite valid disk caches
   meridian/adapters/data.py:183
   CLAIM: fresh requires cached['date'].min() <= FETCH_START+7d (2021-01-08). 36 of 55 caches (including SPY) start at 2021-07-19 because Cboe's rolling window only reaches ~5 years back and FMP historical_eod is currently returning nothing for them, so no refetch can ever extend the min backwards — the condition is permanently false and the cache is permanently bypassed for those tickers.
   SCEN : Confirmed live: 36 caches have min=2021-07-19 > 2021-01-08. Every dataset.load(use_cache=True) — every CLI run of run.py/reflect.py, every process's first load, hermes — performs 36 full-history network fetches (each up to 3 retries per source) even though the disk cache already holds everything the
[HIGH    ] #44 Warmup guarantee is silently broken: 36 tickers plus the SPY index start 2021-07-19, so 252-day indicators are invalid for the first ~6 months of the IS window and nothing gates start-coverage
   meridian/dataset.py:12
   CLAIM: dataset.py documents that the 2021 warmup year makes the 252-day trend template 'valid from the first tradable day' of IS_START=2022-01-01, but the actual data for 36 names and the index begins 2021-07-19 (~115 trading days before IS_START). validate_bars checks staleness at the series end but never checks that the series reaches back to `start`, and load_many's only size gate is len>50, so the shortfall passes every
   SCEN : Walk-forward and the new eval_regime slicing over early IS (2022-01 to ~2022-07) compute 252-day highs/SMAs and SPY regime classification on partial history (NaN or truncated windows depending on min_periods). Regime labels for the first half of 2022 — a key bear stretch the laggard names were added
[HIGH    ] #45 use_cache=False discards a valid disk cache when all sources fail, and dataset._cache then pins the empty result for the rest of the session
   meridian/adapters/data.py:189
   CLAIM: With use_cache=False the cache file is never read (cached=None), so when fetch() returns empty (all three sources fail transiently) load_bars returns an empty DataFrame even though a complete cache sits on disk; the same path also writes merged=fetched, i.e. blindly overwrites the cache without the promised merge or corporate-action check. dataset.load then stores the empty result in _cache ({} is not None), and sche
   SCEN : Scheduler's once-per-session refetch runs with use_cache=False for all 55 tickers. A transient network outage at that moment yields bars={} and an empty index; latest_bar=None bypasses the 'current' guard so loop.daily_cycle runs on zero data, and every subsequent 300s poll serves the pinned empty _
[MEDIUM  ] #46 Delisted PARA and WBA pass the integrity gate as tradable because stale_ticker is only a soft issue
   meridian/adapters/data.py:97
   CLAIM: validate_bars marks a last bar >10 days old as severity 'soft', so load_many never quarantines delisted names. PARA's cache ends 2025-08-06 and WBA's 2025-08-28 (both confirmed on disk; both delisted), yet both remain in REPLAY_UNIVERSE (data.py:279) and pass the hard gate with >50 rows.
   SCEN : OOS folds running through 2025-12-31 evaluate PARA/WBA as live series that simply stop mid-fold: positions open at the last bar can never exit within the fold, and candidate-vs-incumbent comparisons in the H2-2025 fold are computed over a universe containing names that no longer traded. Additionally
[MEDIUM  ] #47 The SPY index series bypasses validate_bars and every gate applied to universe tickers
   meridian/dataset.py:36
   CLAIM: dataset.load() fetches the index with data.load_bars directly, while universe tickers go through load_many's hard-integrity quarantine and >50-row check. The single series that drives regime classification, distribution-day counts, and the new eval_regime slicing is the only one that is never validated.
   SCEN : If the SPY fetch fails with no usable cache (or the cache is truncated/spliced in a way sanitize_bars does not repair — e.g. a mixed adjustment-scale splice under the 25 percent corporate-action threshold), load() returns it unchecked: an empty index flows into scheduler.advance_once (latest_bar=Non
[LOW     ] #48 _corporate_action returns False on empty date overlap, letting a disjoint narrow-window fetch splice mixed adjustment scales into the shared cache
   meridian/adapters/data.py:225
   CLAIM: When the cached and fetched frames share no dates, _corporate_action returns False (no action detected) and load_bars merges and persists anyway. Overlap can be empty precisely in the case where the check matters most: a stale cache plus a windowed Nasdaq fallback fetch (the only non-full-history source) covering only dates after the cache ends.
   SCEN : api.py:558 (trade drill-down) calls load_bars(ticker, ts_open, ts_close). For a ticker with a stale cache, FMP failing and Cboe empty, the Nasdaq fetch returns only [ts_open, ts_close]; if the cache ends before ts_open the overlap is empty, so a split/dividend re-adjustment between the two ranges go

===== agent result 9: 6 finding(s) =====
[HIGH    ] #49 as_of() injects a phantom 'NAN' ticker into point-in-time membership (live on disk today)
   meridian/adapters/constituents.py:81
   CLAIM: The NaN-coalescing fix at lines 42-43 (_cell) only sanitizes the FETCH path (_fetch_tables). as_of() reconstructs membership from the PERSISTED cache via store.read_json, where a change-log row with removed:"nan" (a plain string) survives. Line 77 does rem = ch.get("removed","").upper() -> "NAN", which is truthy, so line 81 members.add(rem) adds a fabricated 'NAN' instrument. The in-code comment claiming this class o
   SCEN : With the ACTUAL state/sp500_constituents.json on disk right now (changes:[{date:2026-03-01, added:NVDA, removed:"nan"}]), constituents.as_of('2025-01-01') returns ['AAPL','MSFT','NAN'] — verified by running it. 'NAN' is a non-existent ticker polluting the PIT universe. The string "nan" also re-enter
[MEDIUM  ] #50 submit_plan omits the drawdown de-risk size_mult the internal broker applies — paper mirror over-sizes in every drawdown
   meridian/adapters/alpaca.py:183
   CLAIM: The internal broker sizes with plan["size_r"] * size_mult where size_mult = derisk_mult(equity, peak) (broker.py:26, loops.py:128), a continuous 1.0->0.0 ramp across 3%-8% drawdown. submit_plan sizes with plain plan.get("size_r",1.0) * RISK_PCT_PER_R * equity and never receives or applies size_mult (loop.py:232 calls submit_plan with no de-risk arg). So whenever the book is in >3% drawdown, the Alpaca paper mirror su
   SCEN : At 5.5% drawdown derisk_mult returns ~0.5, so the internal book fills ~half the shares the Alpaca bracket was submitted for. The reconciler's position audit (loop.py:~375) flags any >25% qty gap as ALARM_MIRROR_DRIFT, so this fires a spurious sizing-drift alarm on every mirrored position during draw
[MEDIUM  ] #51 _paper_base 'hard safety lock' uses substring match, not host match — a lookalike endpoint bypasses it and leaks API keys
   meridian/adapters/alpaca.py:100
   CLAIM: The lock at line 100 does `if "paper-api.alpaca.markets" not in base` and only then forces PAPER_BASE. This is a substring test, not a hostname test. An operator-set ALPACA_PAPER_ENDPOINT (a whitelisted, dashboard-settable secret) whose host merely CONTAINS the string — e.g. a subdomain lookalike or a path segment — passes the check and is used verbatim. Every order/account/close_all call then sends _headers() (APCA-
   SCEN : Verified: with ALPACA_PAPER_ENDPOINT='https://paper-api.alpaca.markets.evil.example.com', _paper_base() returns that host unchanged (not PAPER_BASE); with '.../paper-api.alpaca.markets' as a path on another host, same. submit_bracket/close_all then POST the operator's live paper key+secret to an att
[MEDIUM  ] #52 as_of() compares change-log dates as sliced strings — inverts PIT reconstruction for the documented Wikipedia date format
   meridian/adapters/constituents.py:76
   CLAIM: Line 75 does d = str(ch.get("date",""))[:10] and line 76 compares `d > date` lexically. This is only correct if dates are ISO YYYY-MM-DD, but the module's documented source is Wikipedia's change-log, whose Date column is human-readable (e.g. 'October 1, 2024'). [:10] yields 'October 1,' and the lexical compare of a letter-leading string against a digit-leading ISO date is always True, so EVERY change is treated as af
   SCEN : Verified: cache with changes:[{date:'October 1, 2024', added:'NEW', removed:'OLD'}] and as_of('2026-07-01') returns ['AAA','OLD'] — it wrongly undoes an Oct-2024 change that predates the query date, so it removes a real current member (NEW) and re-adds a delisted name (OLD). PIT membership is corrup
[MEDIUM  ] #53 as_of() ignores freshly-fetched changes on a cold cache — silently returns current membership for a past date (survivorship bias)
   meridian/adapters/constituents.py:74
   CLAIM: Line 69 reads data = store.read_json(CACHE, {}); on a cold/missing cache data is {}. Line 71-72 then calls current(), which fetches and WRITES the cache to disk, but the local `data` variable is never refreshed. The change-log loop at line 74 iterates data.get("changes", []) == [] (the stale pre-fetch snapshot), so no changes are ever applied and current membership is returned unchanged for any historical date.
   SCEN : Verified: with no cache file present and current() returning ['AAPL','ADDED_LATER'] plus a change {date:2050-01-01, added:ADDED_LATER, removed:REMOVED_LATER}, as_of('2000-01-01') returns ['AAPL','ADDED_LATER'] — it should have excluded ADDED_LATER (added after 2000) and included REMOVED_LATER. The e
[LOW     ] #54 exit_fill_price returns None for a partially_filled exit leg — reconciler's divergence audit silently no-ops on partial exits
   meridian/adapters/alpaca.py:200
   CLAIM: Line 200 only accepts a leg whose status == "filled". Alpaca take-profit/stop-loss legs can report status 'partially_filled' with a populated filled_avg_price. Such a leg is skipped and the function returns None, so in the reconciler (loop.py:386) `af = exit_fill_price(o)` is None and the code `continue`s: no alpaca_fill_price is written to the trade row and no MIRROR_DRIFT check runs.
   SCEN : Verified: order legs=[{status:'partially_filled', filled_avg_price:'101.20'}, {status:'canceled', filled_avg_price:None}] returns None. When the internal sim fully closed a trade but the Alpaca exit leg only partially filled at a divergent price, the execution-divergence audit records nothing and fi

TOTAL: 54 findings from 9 completed agents (batches Core/Learning/Data done; Surface+Ops not run)
