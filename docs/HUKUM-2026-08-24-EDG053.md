# HÜKÜM — EDG-2026-053 gelir momentumu (Rol-1, 2026-08-24)

## 0 · Nasıl geldi
Gece turu ölçümü koştu ve "hiçbir dal eşleşmedi" dedi. **Bağımsız doğrulayıcı çürüttü:**
blok-bootstrap birimi kart donduktan SONRA sessizce GÜN'den AY'a çevrilmiş ve bu seçim
kartın ateşleyip ateşlemediğini BELİRLİYOR.

## 1 · BİRİM HÜKMÜ — kart zaten söylüyordu, ölçüm çevirmedi
- Kart (`features_asof`): **"21g blok-bootstrap CI"** — 21 **GÜN**.
- Şablon sabiti (`ortak.py:48`): `BLOCK = 21  # 21 ARDIŞIK GÖZLEM GÜNÜ`.
- EDG-050'de bir gözlem = bir İŞLEM GÜNÜ idi → `BLOCK=21` doğru: 21 gün.
- **EDG-053'te gözlemler AYLIK** (nd=202 ay). `BLOCK=21`i olduğu gibi kopyalamak
  **21 AY ≈ 1,75 yıl** demektir — bu "21 gün" DEĞİLDİR.

**Rol-1 hükmü: aylık gözlemde "21 gün"ün sadık karşılığı `blok = 1`dir.** Ölçüm ajanı sabiti
kopyaladı, BİRİMİ çevirmedi. Bu bir eşik oynatması değil, bir **çeviri hatasıdır** — ama
sonucu eşik oynatmakla aynı: hüküm değişiyor.

## 2 · SADIK OKUMAYLA SONUÇ: **KURAL ATEŞLİYOR** — ÜÇ ZORUNLU ŞERHLE
`blok=1`de `ivme_ust_30pct @60g`: CI **[+0,000086, +0,008653]**, 0-dışı pozitif;
10bps sonrası net **+0,003473 > 0** → kartın DAL-1 şartı **tam eşleşiyor**
("temel-momentum sinyali VAR — ARSENAL adayı"). Doğrulayıcı bunu bağımsız bootstrap'iyle
yeniden üretti (aynı cebir, seed 20260812).

**EDG-050 emsali uygulanır: kural ateşler ama ŞERHLERLE, ve şerhler çözülmeden aday
İLERLEYEMEZ.**

| # | şerh | ölçüm |
|---|---|---|
| Ş1 | **Etki bağımsız yöntemlerde gürültünün İÇİNDE** | ay-düzeyi çıkarım CI [−0,000539, +0,004968] (0-içi) · Newey-West L=0/1/3/6 t=1,34–1,55 hepsi 0-içi · permütasyon (30 tekrar, ay sırası karıştırılmış): `lo>0` oranı yalnız **0,27** |
| Ş2 | **Kart-sonrası örneklem filtresi, duyarlılığı ölçülmemiş** | `BAYAT_CEYREK_GUN=200` guard'ı kartın `features_asof`/`kill_criteria`sında YOK; 2.388 gözlemi (~%7) düşürdü; guard'sız koşu YAPILMADI |
| Ş3 | **PIT "yıkıcı sınaması" TOTOLOJİK** | `pit_yikici_sinama` önce `filed<=t` süzüyor, sonra ilk satırı yine `filed<=t` süzen `_asof_saf`ı çağırıyor — `f(x)` ile `f(filtre(x))` karşılaştırılıyor, filtre idempotent, sonuç matematiksel olarak 0 çıkmak ZORUNDA. Sınama asla kırmızı veremezdi. *(Doğrulayıcının bağımsız denetiminde GERÇEK bir PIT ihlali de bulunamadı — yani sonuç doğru, ama iddia edilen KANIT geçersiz.)* |

## 3 · SONUÇ VE SINIR
- Kart `status: measured` **DEĞİL**, `measuring` KALIR. Hüküm alanı bu belgeye işaret eder.
- ARSENAL adayı **kaydedilir ama İLERLEYEMEZ**: Ş1-Ş3 çözülmeden hiçbir tasarım kararına girdi olamaz.
- **Sunum kusuru ayrıca kayda geçer (YASA 4 sınıfı, tek-yönlü sunum):** ölçümün duyarlılık şerhi
  YALNIZ bulguyu ZAYIFLATAN yönü anlattı ("blok 6/3/1'de negatif"); aynı tablodaki
  "blok=1'de ATEŞLEME dalı AÇILIYOR" gerçeği hiçbir cümleye dökülmedi, sayı JSON'da gömülü kaldı.
  Rol-1'e giden özet, hükmün blok seçimine bağlı olduğunu SÖYLEMİYORDU.
- **Şablon borcu:** `ortak.py`nin `BLOCK` sabiti gözlem biriminden bağımsız yazılmış. Yeni
  kartlarda blok, **gözlem birimi cinsinden** adlandırılmalı ("21 gün" değil, "N gözlem =
  X takvim süresi"). Bu, EDG-050 şablonunu kullanan her kartı ilgilendirir.

## 4 · SONRAKİ ADIM (kart-önce)
Takip kartı yazılmadan ölçüm tekrarlanmaz. Takip kartı şunları ÖNCEDEN dondurmalı:
(a) blok birimi gözlem cinsinden ADIYLA, (b) her iki okumanın da karar tablosuna yazılması,
(c) 200g guard'ının duyarlılık koşusu, (d) totolojik PIT sınamasının GERÇEK bir yıkıcı
sınamayla değiştirilmesi (ör. bilerek geleceğe sızdırılmış bir kopya kırmızı vermeli).
