# BASELINE — 2026-08-06 (Yeni tasarım programı, Faz 1)

**Rol:** ölçüm turu. **Kod yazılmadı, git koşulmadı, canlıya dokunulmadı, `serve.sh` koşulmadı.**
**Bu belge bir HÜKÜM değil bir ÖLÇÜMdür.** Her satır dosya:satıra ya da bir sayıma dayanır.
Ölçülemeyen her şey "ÖLÇÜLEMEDİ + neden" olarak yazılıdır. Hüküm Rol-1'de.

**Yer-gerçeği (satır sayıları bu turda sayıldı):**
`meridian/web/app.js` 7.511 · `index.html` 1.594 · `landing.html` 818 · `palette.js` 1.043 ·
`workflow.html` 306 · `workflow.js` 166 · `runbook.html` 129 · `landing.js` 122 · `theme.js` 100
— **toplam 11.789 satır.**

**Bağlayıcı girdi:** `DESIGN.md` § Design rules (2026-08-06, üç beyanlı sapmayla) · `PRODUCT.md`
(tek-emir-yolu çivisi + üç kanonik top-task) · `docs/UX-SADELESTIRME-DENETIMI-2026-08-06.md`
(B1–B23 — TEKRAR EDİLMEDİ, üstüne konuldu).

**Kategori kararı (girdi olarak alındı, sorgulanmadı):** Meridian bir *denetim konsoludur*; manuel
emir girişi YOKTUR. Emir-bileti / market-view / grafik-üstü-emir kolu bu ölçümün kapsamı dışında.

**Manda (operatör, 2026-08-06, bağlayıcı):** "UI'da değişmeyecek herhangi bir element olamaz" +
"neyi nerede gösterdiğin veya hangi şekilde gösterdiğin de değişebilir." Bu yüzden aşağıdaki
envanter **öğeye göre değil İŞE göre** çıkarıldı: her satır bir SORU ile başlar, bugünkü yeri ve
biçimi bir *gözlem* olarak kaydedilir, "zaten yapılmış" gerekçesi hiçbir yerde kullanılmadı.
Değişmeyen tek şey ürün yasalarıdır (tek emir-yolu · ÖLÇÜLEMEDİ≠0 · paydasız çubuk yok · uydurma
yasağı · iki zemin/gündüz varsayılan · dopamin yasağı · CSP-self · Türkçe arayüz) — bunlar İÇERİK
sınırıdır, BİÇİM sınırı değil.

---

## 0. ÖLÇÜM YÖNTEMİ VE ÖLÇÜLEMEYENLER

Bu bölüm önce gelir, çünkü aşağıdaki her sayının kapsamı buradaki sınırlarla belirleniyor.

### 0.1 Ne ölçüldü, nasıl

| Ölçüm | Araç | Kapsam |
|---|---|---|
| A — anti-desen taraması | `npx impeccable@latest detect meridian/web` (CLI **3.5.0**, `--json`) | statik HTML/CSS |
| B — depo-özgü tarama | `grep`/`awk` + geçici Node betiği (scratchpad'de, depoya yazılmadı) | 9 dosyanın tamamı |
| C — renk-rolü haritası | yorum-maskeli CSS ayrıştırma, seçici→jeton eşlemesi | `index.html` `<style>` |
| Kontrast rakamları | WCAG 2.x bağıl-luminans formülü, 8-bit sRGB | jeton değerlerinden hesaplandı |

`.impeccable/config.json` boş (hiçbir kural/dosya/değer bastırılmıyor) — yani A ölçümü
filtresizdir. `.impeccable/` `.gitignore`'da (satır 16); `detect` çalıştırması yalnız
`hook.cache.json`ı güncelledi, versiyonlu hiçbir dosya değişmedi.

### 0.2 ÖLÇÜLEMEDİ — ve nedenleri (bunlar boşluk değil, beyandır)

| # | Ölçülemeyen | Neden |
|---|---|---|
| Ö1 | **Panonun ÇALIŞAN DOM'u** — impeccable'ın URL (puppeteer) kipi | Uygulamayı yerelde YÜKLEMEK yasak: `app.js` `localStorage.meridian_api`'yi okur ve `file://`/localhost yüklemesi canlı A1 ucuna gidebilir; hafıza notu "yerel görsel doğrulama" bu sınıfı bir kez kaydetmiş (canlının Alpaca akışı düşebilir). Statik tarama `index.html`'in yalnız kabuğunu görüyor. |
| Ö2 | **`app.js`'in ürettiği DOM'daki impeccable kuralları** | impeccable JS dosyalarında 0 bulgu döndürdü (regex kipi şablon-içi HTML'i tanımıyor). 7.511 satırlık render katmanı A ölçümünün DIŞINDA — B ölçümü bu boşluğu elle kapatıyor (bkz. §B). |
| Ö3 | **Bulguların satır numaraları (A ölçümü)** | impeccable 3.5.0 JSON'da 41 bulgunun **tamamına** `"line": 0` yazıyor. Dosya adı var, satır yok. Aşağıdaki dosya:satır adresleri A için değil, B'de elle bulunanlar için geçerlidir. |
| Ö4 | **Sayfa başına ÇALIŞMA-ZAMANI kart sayısı** | Kart HTML'i yardımcı fonksiyonlarda üretilip şablona enterpole ediliyor; `RENDER.<bölüm>` blokları arası satır aralığı yardımcıları da kapsadığı için satır-sırasına dayalı atıf yanlış sonuç veriyor (ölçüldü: `market` kovası 31 kart gösteriyor, gerçekte 8). Statik emitör toplamı ölçüldü (§3), sayfa kırılımı için önceki denetimin sayımı devralındı. |
| Ö5 | **Gerçek kontrast, gerçek bileşik zeminlerde** | Jeton değerlerinden hesaplanabilir olanlar hesaplandı; ekranda fiilen üst üste binen katmanlar (ör. çekmece + perde + kart) tarayıcı olmadan doğrulanamaz. |
| Ö6 | **Alarm/olay akışında gerçek sıklık** | Canlı state okunmadı (canlı worker koşuyor olabilir; state'e dokunulmadı). Önceki denetimin "129/24s" rakamı devralınmadı, tekrar da edilmedi. |

---

## A. `impeccable detect` — kural-kural sayım

**Komut:** `npx impeccable@latest detect meridian/web` · **çıkış kodu 2** · **41 anti-desen.**
Hepsi `severity: warning`. Kategori dağılımı: `quality` 32 · `slop` 9.

### A.1 Kural × dosya matrisi (ham sayım)

| Kural | index.html | landing.html | workflow.html | runbook.html | **Σ** |
|---|---|---|---|---|---|
| `undersized-ui-text` (<11px işlevsel metin) | 0 | **17** | 2 | 0 | **19** |
| `overused-font` (geist / geist mono / roboto) | 2 | 2 | 2 | 1 | **7** |
| `cramped-padding` | 0 | **6** | 0 | 0 | **6** |
| `wide-tracking` (>0.05em gövde metni) | 0 | **5** | 0 | 0 | **5** |
| `flat-type-hierarchy` | 0 | 0 | 1 | 1 | **2** |
| `clipped-overflow-container` | **1** | 0 | 0 | 0 | **1** |
| `tiny-text` (11px gövde) | 0 | 1 | 0 | 0 | **1** |
| **Dosya toplamı** | **3** | **31** | **5** | **2** | **41** |

**Okunması gereken asimetri:** bulguların **%76'sı `landing.html`de** — panoda (`index.html`)
yalnız 3 bulgu var. Bu bir kalite hükmü DEĞİLDİR; Ö1/Ö2'nin doğrudan sonucudur: `index.html`in
statik kabuğu 1.594 satır, çizdiği arayüz 7.511 satırlık `app.js`ten geliyor ve araç oraya
bakamıyor. **`index.html`in "3 bulgu"su bir temizlik kanıtı değil, bir ölçüm kör noktasıdır.**

### A.2 Bulgu metinleri (satır yok — Ö3)

- `clipped-overflow-container` / `index.html`: "html clips a positioned child" — kırpan kap
  mutlak-konumlu bir çocuğu kesiyor (ipucu/menü/açılır katman kaçamaz).
- `undersized-ui-text` örnekleri (`landing.html`): "HESAP", "Exposure tavanı", "Mevcut exposure",
  "Distribution days", "Onay bekleyen", "HİSSE", "KURULUM", "R:R", "BOYUT", "KAPI", "GO",
  "REVIEW", "NO_GO", "04:10 ET", "GO / REVIEW / NO_GO", "IDEA → ACTIVE → CLOSED",
  "her 5 kapanan işlem" — 10px. Kuralın kendi metni bir kaçış yolunu açıkça kapatıyor:
  *"Being ON the DESIGN.md size ramp does not exempt a value here."*
- `undersized-ui-text` (`workflow.html`): "2026-07-20 SÜRÜMÜ", "YENİ" — 10px.
- `flat-type-hierarchy`: `runbook.html` 11/13/15px (oran 1,4:1) · `workflow.html` 10/11/13/16px
  (oran 1,6:1).
- `overused-font`: geist ×2 (index), geist + geist mono (landing, workflow), **roboto**
  (runbook — panonun yazı tipi bile değil).

---

## B. DEPO-ÖZGÜ TARAMA — para-güvenliği sınıfı

Impeccable'ın bilmediği, bu ürüne özgü kusur sınıfları. Her satır grep/sayım ile ölçüldü.

### B.1 `?? 0` · `|| 0` — ölçülmemişin sıfıra çökmesi

Sayım **eşleşme** başınadır (satır başına değil) ve `0.5` gibi ondalıkları dışlar (`0(?![\d.])`).

| Ölçüm | Sayı |
|---|---|
| `?? 0` (9 dosya) | **154** — hepsi `app.js`te |
| `\|\| 0` (9 dosya) | **64** — `app.js` 60 · `landing.js` 3 · `palette.js` 1 |
| Toplam (yorum satırları hariç) | **215** |
| **Bunlardan DOĞRUDAN DOM'a yazılanlar** (bir `${…}` enterpolasyonu içinde) | **162** |
| Yalnız aritmetikte kalanlar (payda, genişlik, sayaç) | **53** |
| `index.html`, `theme.js`, `workflow.js`, `landing.html`, `workflow.html`, `runbook.html` | **0** — hiç yok |

**Yöntem beyanı:** "DOM'a yazılıyor" testi, ifadenin bir şablon-değişmezi içindeki açık `${`
sayısının kapalı `}` sayısından fazla olmasıdır. İç içe şablonlarda yanılabilir; bu yüzden 162
bir **büyüklük mertebesidir**, tek tek doğrulanmış bir liste değil.

Kod bu sınıfın farkında — `app.js:2125` şunu yazıyor: *"'ölçtük, sıfır çıktı'dır ve `?? 0` bir
uydurma değil. Payda YOKSA (hiç plan yok) çubuk…"*. Yani **meşru kullanım var.** Ölçülen şey
meşruiyetin yokluğu değil, **ayrımın mekanik bir zorlayıcısının olmayışıdır**: 162 çağrı yerinin
her birinde "bu null mü, ölçülmüş sıfır mı?" kararı yazarın hafızasına bırakılmış. Önceki
denetimin B2 bulgusu (0 bekleyen onay ≠ hiç onay yok) bu sınıfın canlı vakasıdır.

Karşı-kanıt olarak kaydedilmeli: `trn()` (`app.js:202`) `null`/`NaN` için **"—"** basıyor,
`money()` (203) `null` için "—", `onk()` (211) ön-eki bile bastırıyor. Yani **jenerik biçimleyici
katmanı dürüsttür**; kaçak, biçimleyiciye girmeden önce `?? 0` ile kapatılan alanlarda.

### B.2 `fillna` / `ffill` / `bfill` / `interpolate`

| Katman | Sonuç |
|---|---|
| Tüm web katmanı (9 dosya) | **0 eşleşme** |
| Python — `meridian/**` | **11 eşleşme, 3 dosya** |

Python kırılımı:
- `meridian/adapters/data.py` — 8 eşleşme (`:377, 384, 385, 439, 508, 1969, 2002` + `edgar_shares.py:199,200`).
  Tamamı **maske üretimi** (`.fillna(False)`) ya da hacim temizliği; görünüm yolu değil.
- **`meridian/trend_shadow.py:181` — `r.loc[fv:lv] = r.loc[fv:lv].ffill()`** — bu satır ekrana
  ulaşıyor: `api.py:2681` `trend_shadow.ozet()` çağırıyor, sonuç `Öğrenme → golge` bölümünde
  çiziliyor. Fonksiyonun kendi docstring'i sınırı dürüstçe yazıyor ("KENDİ ÖMRÜ İÇİNDE ffill…
  bakma-ileri yok"). **Ölçülen boşluk: bu ileri-doldurma ekranda hiçbir yerde etiketlenmiyor** —
  web katmanında "dolduruldu"/"ffill"/"ileri taşındı" diye bir dize 0 kez geçiyor.

### B.3 `filled`/`dolduruldu` durumunun teyitten ÖNCE render edilmesi

**Aranan desen bulunamadı** — ama nedeni "temiz" değil, "yok":

- Ayna rozeti (`app.js:944-946`) üç hâl basıyor: `aynada` / `gönderilecek` / `RET`. `aynada`
  rozeti `t.alpaca_submitted` kümesinden türüyor, yani **gönderildi**yi anlatıyor, **doldu**yu
  değil — etiket ("aynada") bu ayrımı koruyor. Rozette dolum hâli **hiç yok**.
- Dolum ayrı bir yerde, ayrı bir dilde: `app.js:1775` `silahlı → gönderilmiş → dolan` oran
  çubuğu ve `app.js:3170` parçalı-dolum satırı (`filled_qty/total_qty (%fill_pct)`).
- Onay mesajı (`app.js:2019-2020`) sunucunun `armed_n` yanıtından sonra basılıyor ve açıkça
  *"Dolum bir sonraki açılışta"* diyor — teyitten önce dolum iddiası yok.

**Sonuç (gözlem):** teyitten önce "doldu" yazan bir yüzey bulunmadı; bunun yerine **aynı hattın üç
farklı yüzeyde üç farklı biçimde** anlatıldığı ölçüldü (§4).

### B.4 Radial / gauge / donut / pie bileşeni

| Aranan | Sonuç |
|---|---|
| `conic-gradient` | **0** (4 HTML + 5 JS) |
| `radial-gradient` | **0** |
| dairesel yay (`stroke-dasharray` + `<circle>` bir gösterge olarak) | **0** |
| `_donut` / `_ring` fonksiyonu | **0 — kaldırılmış** |

`app.js:2598` bunu kayda geçiriyor: *"BULLET GRAPH — `_donut` ve `_ring`'in YERİNE
(2026-08-01)"*; `index.html:1225` aynı şeyi CSS tarafında: *"eskiden burada iki dairesel gauge
vardı… ikisi de bullet"*. Kalan yuvarlaklar bir gösterge değil: `.dot` 6px durum noktası
(`index.html:378`), `<circle r=5>` kalibrasyon saçılım noktası (`app.js:5611`), ikon glifleri
(`app.js:592, 609`).

### B.5 Renk LİTERALLERİ

| Dosya | Toplam hex | Jeton bildirimi içinde | **Kural gövdesinde (ihlal)** |
|---|---|---|---|
| `app.js` | **0** | — | **0** |
| `palette.js` | **0** | — | **0** |
| `theme.js` | **0** | — | **0** |
| `landing.js` / `workflow.js` | **0** | — | **0** |
| `index.html` | 67 | 41 + yorumlar | **0** |
| `workflow.html` | 44 | 38 + yorumlar | **0** |
| `runbook.html` | 14 | 8 + yorumlar | **0** (ama ayrı bir sözlük — bkz. T9) |
| `landing.html` | 48 | 38 + yorumlar | **2** |

`#000`/`#fff`/`rgb(0,0,0)`/`rgb(255,255,255)` ham sayımı: `index.html` 6 · `landing.html` 6 ·
`workflow.html` 3 · `runbook.html` 4 — **bunların tamamı ya yorum metni ya jeton bildirimi**,
ikisi hariç:

```
landing.html:203  .btn-f{background:var(--accent);border:1px solid var(--accent);color:#fff;…}
landing.html:252  .pillc{background:var(--accent);color:#fff;…font-size:10px…}
```

**Ölçüm:** gece zemininde `landing.html:140` `--accent:#d4d0cb`. `#ffffff` üzerine `#d4d0cb` =
**1,53:1** (WCAG 2.x, hesaplandı). Yani gece temasında birincil eylem düğmesinin ve `.pillc`
rozetinin **yazısı dolgusunun üzerinde kayboluyor**. DESIGN.md'nin bu tam kusur sınıfı için
yazdığı cümle: *"no rule may contain a colour"* — ve zaten bir kez yaşanmış (üst bar, 1,27:1).

### B.6 Yalnız kırmızı/yeşille taşınan P&L sinyali

| Ölçüm | Sayı |
|---|---|
| `cls()` çağrısı (renk ataması) | 26 |
| `isr()` çağrısı (açık `+` işareti) | 11 |
| `cls()` ile aynı satırda `isr()` olan | 6 |
| `class="…pos/neg…"` doğrudan emisyon | 94 |
| `class="…warn…"` doğrudan emisyon | 79 |
| **Bunlardan tamamen KOŞULSUZ olanlar** (değere bakmadan sabit sınıf) | **164** |

`isr()` sözleşmesi (`app.js:219-224`) doğru kurulmuş ve gerekçesi kodda yazılı: *"Yokluk bir
işaret değildir."* Eksik olan **kapsam**: negatifler formatörden gelen `−` ile zaten iki kanallı,
ama **pozitifler yalnız `isr()` sarmalanan yerlerde `+` alıyor.** `isr()`siz `cls()` yerlerinde
pozitif bir para/yüzde okuması için tek ayırt edici kanal **renk**tir.

Doğrulanmış örnekler (para/yüzde, `+` yok, renk var):
- `app.js:1738` — `gerçekleşmiş <b class="${cls(kk.canli_pnl_usd)}">${money(...)}</b>`
- `app.js:5905` — `<span class="${cls(kb.realized_pnl)}">${money(kb.realized_pnl)}</span>`
  (defterin **manşet** K/Z rakamı)
- `app.js:4658` — `pctf(g.toplam_verim, 1)`, `cls()` ile boyalı

Karşı-kanıt: R değerleri (`app.js:6332`, `6067`, `4650`) `(r >= 0 ? "+" : "")` ile elle işaretli —
yani sözleşme R hattında tutuyor, para/yüzde hattında tutmuyor.

### B.7 Saat dilimi YAZILMAYAN zaman damgası

| Biçimleyici | Yer | Çıktı | Zemin etiketi |
|---|---|---|---|
| `mutlakTs()` | `app.js:1016-1021` | `"30 Tem 14:55"` | **yok** — `toLocaleString("tr-TR")`, tarayıcı yerel saati |
| `mktSaat()` | `app.js:2840-2843` | `"14:55"` | **yok** — yorumda "(yerel)" yazıyor, **ekranda yazmıyor** |
| `relTime()` | 13 çağrı | "az önce / N sa önce" | göreli, zemin gerekmez |
| ham dilim | `app.js:4753`, `5314` | `"14:55 UTC"` | **var** (2 yer) |

Tüm `app.js` içinde `UTC`/`ET`/`TSİ` dizesi **11 satırda** geçiyor ve bunların yalnız **2'si**
ekrana basılan bir damganın yanında. Ürün ABD hissesi işliyor (piyasa ET), operatör TSİ'de,
tarayıcı yerel saati basıyor: **"14:55" üç farklı şey olabilir ve ekran hangisi olduğunu
söylemiyor.**

### B.8 Para birimi YAZILMAYAN tutar

- `money()` (`app.js:203`) → `"$" + trn(x, 2)`; `mktUsd()` (2830-2836) → `$…K/M/B`.
  **Para basan her yol `$` taşıyor** — birimsiz tutar bulunamadı (45 `money()` çağrısı).
- Ölçülen sapma **başka bir eksende**: `$` bir *sembol*dür, Design rules **"explicit currency"**
  diyor. `USD` dizesi hiçbir tutarın yanında geçmiyor. Sayı biçimi `tr-TR` (`1.234,56`), sembol
  ABD konvansiyonuyla önde — tek satırda iki farklı yerelleştirme sözleşmesi.

### B.9 Canlı/kâğıt modun DOM'dan türetilebilirliği

"paper"/"kâğıt" kelimesinin **ekrana bastığı** yerlerin tamamı:

| # | Yer | Koşul |
|---|---|---|
| 1 | `app.js:394` `#statustext` → `canlı · paper · L0 · N sn önce` | **yalnız sağlıklı hâlde.** `t.halted` ise metin `"DURDURULDU"`, `t.stale` ise `"gecikmiş · N sn önce"` — **her iki hâlde de "paper" DOM'dan kayboluyor.** |
| 2 | `app.js:752` `.durum-kart` satırı → `Otonomi: L0 · paper` | yalnız o kart çizildiğinde |
| 3 | `app.js:2278` HUD çipi → `<b>${h.mode}</b>·${broker}` | broker adı `"alpaca_paper" → "alpaca"` ile **değiştirilerek**; "paper" hecesi burada silinmiş |

**Ölçüm:** moda ayrılmış hiçbir kromatik kanal, hiçbir yapısal muamele, hiçbir sayfa-seviyesi
çerçeve yok. Mod, iki metin dizesi ve bir seviye harfidir (`L0`). Design rules'un cümlesi:
*"Mode must be legible from any pixel… the costliest accident class in this domain is an operator
who believes they are in the other mode."*

### B.10 300 ms üstü geçiş / animasyon

| Dosya:satır | Değer | Ne | Anomali sinyali mi? |
|---|---|---|---|
| `index.html:159`, `landing.html:109`, `workflow.html:104` | `--t-rise:.34s` = **340 ms** | `.rise` içerik giriş animasyonu (`opacity`+`transform`) | hayır — dekoratif giriş |
| `index.html:1271` | **400 ms** | `.thermo .tube i` ısınma termometresi dolumu | hayır |
| `landing.html:360` | **450 ms** | `.fill w` kaydırma-anlatı metin dolumu | hayır |
| `index.html:386` | **2,6 s · `infinite`** | `.dot.pulse.stale` / `.halt` nabız | evet (bayat/HALT) |
| `index.html:916` | **1,1 s · `infinite`** | `.tazeleniyor::before` tazeleme şeridi | hayır — yükleme |
| `index.html:1212` | **1,1 s · `infinite`** | `.blink` (GHOST / DESYNC etiketleri) | evet |

Temel süre `--t:.15s` (150 ms) ve baskın kullanımdır — `index.html`de 20 `transition:` bildiriminin
10'u doğrudan `var(--t)`, 2'si `var(--t-rise)`, 2'si `none`.
`prefers-reduced-motion` gardı: `index.html` 4 · `landing.html` 1 · `workflow.html` 1 ·
**`runbook.html` 0** (ama o dosyada hiç `transition`/`animation` yok, yani boşluk fiilen boş) ·
`app.js` 0 (tek `transition:none`, satır 7036).

### B.11 Akan kapsayıcıda `aria-live`

**VAR (5):** `#triage-spine` (`app.js:929`, `role=status` + `polite`) · `#statuspill`
(`index.html:1451`) · `#gate-msg` (`index.html:1440`) · ⌘K kutusu (`palette.js:810`,
`assertive`) · sessiz-hat (`app.js:2435`) — sonuncusu **koşullu**: `sh.saglikli` ise `role`
basılmıyor, ve gerekçesi satır 2432'de yazılı (*"her yeniden çizimde 'bekçiler 17/17' diye
duyurulan bir satır"*). Bu, PRODUCT.md'nin beyan ettiği "kısmi ARIA kapsamı" tercihinin
uygulaması.

**YOK — ve akıyor:**
| Kapsayıcı | Tazeleme | Taşıdığı |
|---|---|---|
| `#hud` (`index.html:1450`) | `setInterval(pollHUD, 20000)` | WS KOPUK · bekçi N geciken · KEŞİF MODU · IO p95 · döngü sayacı — **`aria-label` var, `aria-live` yok** |
| `#bp-alerts` (`app.js:5126`) | her sayfa çiziminde | alarm gelen kutusu |
| `#bp-events` (`app.js:5129`) | her sayfa çiziminde | olay akışı |

`aria-busy` deseni zaten mevcut (`app.js:520, 530`) — yani mekanizma var, HUD'a bağlanmamış.

### B.12 Sayısal biçim sözleşmesi

| Ölçüm | Sonuç |
|---|---|
| `font-variant-numeric: tabular-nums` | `index.html` 26 kural · `.mono-num`, `.tbl .num`, `.trow > .num` |
| `class="…mono-num…"` emisyonu (`app.js`) | 99 |
| `class="…num…"` emisyonu (`app.js`) | 123 |
| `text-align:right` kuralı | 12 — yalnız `.tbl .num` ve `.trow > .num` |
| `trn()` ondalık argümanı | **8 ayrı değer**: 0,1,2,3,4,5,6 + argümansız (72 çağrı) |
| `pctf()` ondalık argümanı | **4 ayrı değer**: 0,1,2 + varsayılan 2 |

**Gözlem:** tabular figürler sistemin her yerinde. **Sağa hizalama yalnız tablo hücrelerinde**;
`.mono-num` (99 emisyon) `.srow`ların içinde akış hizasında. **Sütun başına sabit ondalık yok** —
ondalık sayısı 240'tan fazla çağrı yerinde tek tek seçiliyor.

### B.13 "Forbidden" listesi taraması

| Yasaklı | index | landing | workflow | runbook | app.js | Not |
|---|---|---|---|---|---|---|
| `linear-gradient` | 3 | 0 | 0 | 0 | 0 | işlevsel: 1 tazeleme maskesi + 2 `select` oku |
| `radial-gradient` / `conic-gradient` | 0 | 0 | 0 | 0 | 0 | — |
| `background-clip:text` (gradyan yazı) | 0 | 0 | 0 | 0 | 0 | — |
| `<img>` (hero görsel/illüstrasyon) | 0 | 0 | 0 | 0 | 0 | — |
| dekoratif `box-shadow` | 0 | 0 | 0 | 0 | 0 | tüm `box-shadow`lar `inset …1px` çip kenarı ya da `var(--elev)`=`none`; `index.html:358` `0 3px 0 -2px` bir **çizgi** çiziyor, gölge değil |
| `backdrop-filter: blur` | 2 | 1 | 0 | 0 | 0 | üst bar 8px · klavye katmanı 4px |

**Kural çatışması (hüküm Rol-1'de, burada yalnız kaydediliyor):** Design rules "glassmorphism"i
yasaklıyor; DESIGN.md § Composited surfaces ise şeffaf üst barı ve gece perdesinin ayrımını
**ölçülmüş bir mekanizma** olarak beyan ediyor (*"gecede modal, backdrop blur'u ve kendi saç
teliyle ayırt edilir"*). Üç `backdrop-filter` yeri bu iki kuralın kesiştiği yerdir.

---

## C. BEŞ RENK ROLÜ — UYUM HARİTASI

Yeni Design rules beş rol tanımlıyor: (1) yapı — akromatik · (2) risk/alarm şiddeti — yüksek
kroma, üç hue, **başka hiçbir şeye kullanılmaz** · (3) yön — P&L işareti, düşük kroma, CVD-güvenli,
**işaret ve oktan SONRA üçüncü sinyal** · (4) **mod kroması — kalıcı olarak kâğıt-vs-canlı'ya
ayrılmış, başka hiçbir şeye kullanılmaz** · (5) veri ölçekleri — tek-hue sıralı + CVD-güvenli
ıraksayan, asla gökkuşağı.

### C.1 Bugün kaç kromatik kanal var — ölçüm

`index.html` jeton tablosunda kromatik değer taşıyan **üç** jeton var: `--green` · `--amber` ·
`--red` (+ `-t` %10 tint, `-h` %35 saç teli varyantları). `--violet` ve `--blue` **jeton olarak
duruyor ama kromatik değil**: gündüz `#3e3c39`/`#050505`, gece `#b1afad`/`#d4d0cb` — yani sıcak
nötr. Kural gövdelerinde kullanımı: `--violet` **1 yer** (`app.js:2771`, grafik serisi),
`--blue` **0 yer**. `.t-vi` etiketi (40 kullanım) `--accent-tint`/`--accent-2` okuyor, akromatik.

**Sonuç: sistemde 3 kromatik kanal var, 5 rol dolduruluyor.**

### C.2 Hue → seçici → bugünkü rol (ham eşleme, `index.html` `<style>`)

**`--green` — 14 bağlanma noktası, en az 4 farklı rol:**

| Seçici | Satır | Bugünkü rol |
|---|---|---|
| `.pos` | 285 | **YÖN** (P&L pozitif) |
| `.pm-cell.pos`, `.pm-yield.pos` | 752, 766 | **YÖN** (matris hücre işareti) |
| `.dot`, `.hudchip .ld` | 378, 1191 | **ŞİDDET** (nabız/lamba sağlıklı) |
| `.spine::before`, `.spine.calm::before` | 704, 707 | **ŞİDDET** (triyaj sakin) |
| `.gate-msg.ok` | 344 | **BAŞARI** (kimlik kapısı) |
| `.t-go` | 997 | **HÜKÜM** (kapı verdikti GO) |
| `.s-ok`, `.gc.p`, `.ck.ok`, `.mono .ok` | 1017, 1222, 1111, 1029 | **DURUM** (canlı/geçti/işaretli) |
| `.pillc.g` | 480 | rozet dolgusu |

**`--amber` — 21 bağlanma noktası:**

| Seçici | Satır | Bugünkü rol |
|---|---|---|
| `.dot.stale`, `.spine.attn`, `.gb-alarm.asim`, `.durum-kart.uyari`, `.alarmbutce.asim`, `.sessizhat .sh-sap`, `.hudchip .ld.bad` | 387, 709-710, 651, 688, 1328, 1301, 1192 | **ŞİDDET** ✓ |
| `.t-rv` | 998 | **HÜKÜM** (REVIEW) |
| **`.pm-cell.thin`, `.pm-thin`** | 770, 772 | **VERİ GÜVENİ** (ince örneklem) — §5'in rolü, §2'nin hue'sunda |
| **`.hudchip.explore`** | 1190 | **MOD** (KEŞİF MODU) — §4'ün işi, §2'nin hue'sunda |
| `.ksgroup button.armed` | 1210 | **KONTROL DURUMU** (silahlı düğme) |
| `.pd-warn` | 806-807 | uyarı nesir bloğu |

**`--red` — 24 bağlanma noktası:**

| Seçici | Satır | Bugünkü rol |
|---|---|---|
| `.neg`, `.pm-cell.neg`, `.pm-yield.neg` | 285, 752, 766 | **YÖN** (P&L negatif) |
| `.dot.halt`, `.spine.act`, `.hudchip .ld.bad` | 387, 711-712, 1192 | **ŞİDDET** ✓ |
| `.halt`, `.halt:hover`, `.kscover`, `.kscover:hover`, `.kscover[aria-expanded]`, `.ksgroup button:hover`, satır-içi `opFlatten` | 390-392, 1200-1209, 1457 | **YIKICI KONTROL AFORDANSI** |
| `.gate-msg`, `.gate-tls` | 343, 347 | **HATA METNİ** |
| `.t-no`, `.s-rb`, `.gc.f` | 999, 1018, 1223 | **HÜKÜM / DURUM** |
| **`.side .sitem.cikis:hover`** | 298 | **OTURUM ÇIKIŞI** — gezinme rayının içinde yaşayan bir çıkış düğmesi (`app.js:757`, `cikisYap`), kayıp hue'sunda |

### C.3 Ölçülen sızıntılar

| Sızıntı | Kanıt | Yön |
|---|---|---|
| **§2 şiddet hue'su normal durumda** | `app.js:3172` `class="mono-num warn"` → `açık ${open_risk_r}R` — **koşulsuz**, açık risk her zaman amber | şiddet → nötr veri |
| **§3 yön hue'su sabit parametrede** | `app.js:950` `class="mono-num neg"` → `stop ${p.stop}` · `app.js:951` `class="mono-num pos"` → `hedef ${p.profit_target}` — **her silahlı plan satırında bir kırmızı ve bir yeşil sayı var, ikisi de bir sonuç değil bir FİYAT SEVİYESİ** | yön → plan parametresi |
| **§3 yön hue'su risk büyüklüğünde** | `app.js:3171` `class="mono-num pos"` → `gerçekleşen ${realized_risk_r}R` koşulsuz yeşil · `app.js:4649` `class="mono-num neg"` → `geri_verilen_r` koşulsuz kırmızı | yön → risk muhasebesi |
| **§4 mod, §2'nin hue'sunda** | `index.html:1190` `.hudchip.explore{border-color:var(--amber);color:var(--amber)}` — KEŞİF MODU bir çalışma kipidir, bir alarm değil | mod → şiddet |
| **§5 veri güveni, §2'nin hue'sunda** | `index.html:770` `.pm-cell.thin{box-shadow:inset 0 0 0 1px var(--amber-h)}` + `.pm-thin` rozeti — "ince örneklem" bir güven ölçüsü | veri ölçeği → şiddet |
| **§2 şiddet hue'su gezinme rayında** | `index.html:298` `.side .sitem.cikis:hover{color:var(--red)}` — oturum çıkışı düğmesi (`app.js:757`), gezinme rayının içinde; kırmızısı HALT/FLATTEN'in kırmızısıyla aynı jeton, oysa geri alınabilir bir eylem | yıkıcı-kontrol afordansı → şiddet |
| **§1 yapı, kromatik** | `app.js:4412` `class="warn"` → "En yoğun aile `<ad>`" · `app.js:4793` `class="warn"` → "`N` sembol · `pairs`" · `app.js:4593` `class="pos"` → "Uygulanmış rotasyon `<id>`" — **etiket ve kimlik dizeleri kromatik boyanmış, koşulsuz** | yapı → yön/şiddet |
| **164 koşulsuz `pos`/`neg`/`warn` emisyonu** | §B.6 sayımı | ölçek göstergesi |

### C.4 §4 mod kroması — ayrılmış kanal VAR MI?

**YOK. Doğrulandı, iki yönden:**
1. Jeton tarafı: `index.html` gündüz ve gece jeton bloklarının tamamı tarandı — `--mode`,
   `--live`, `--paper`, `--l0`/`--l1` adında bir jeton **yok**; kromatik jeton sayısı 3 ve üçü de
   §2/§3 tarafından tüketilmiş.
2. Kullanım tarafı: mod bilgisi **üç metin dizesi** olarak var (§B.9) ve bunlardan biri
   koşullu olarak kayboluyor, biri de "paper" hecesini kırpıyor.

En yakın aday `.hudchip.explore` (amber) — ama o KEŞİF modudur, kâğıt/canlı değil; ve §2'nin
hue'sunu ödünç alıyor.

### C.5 §5 veri ölçekleri — ölçülen uyum

Bu eksende sistem **kuralın istediğini fiilen yapıyor ve gerekçesi kodda yazılı.** `IC_SERI`
(`app.js:2769-2773`): üç seri `var(--accent)` / `var(--violet)` / `var(--tx3)` — **hepsi nötr, bir
luminans merdiveni** (kod yorumu ölçümü veriyor: **17,80 → 9,60 → 5,06** kart üstünde), ayrım
kanalı **kesik-çizgi deseni** (`"0"`, `"1 3"`, `"2 2"`), ve efsane deseni de basıyor. Yorum
gerekçeyi yazıyor: *"hue EKLENMEDİ çünkü hue, renk körlüğünün sildiği tam o şeydir."*

Matris hücre dolgusu (`--pm-pos: rgba(12,106,59,.08)` / `--pm-neg: rgba(179,36,44,.07)`) ise
**ıraksayan iki-hue bir ölçek** — yön için; alfa tavanı `.30`'da sabit ve `index.html:120-127`
bunun ölçülmüş bir sınır olduğunu yazıyor (gece temasında hücre rakamı en koyu bantta 4,68:1;
`.34`'te 4,45'e düşer).

---

## D. BULGU TABLOSU

Ciddiyet: **4 = para/güvenlik sınıfı** (yanlış karar aldırabilir ya da bir kontrolü görünmez
kılar) · **3 = ciddi** · **2 = küçük** · **1 = kozmetik**.
Kimlikler `T` ön ekli — önceki denetimin `B1–B23`üyle çakışmasın diye.

| id | dosya:satır | cid. | ihlal edilen kural / ilke | önerilen düzeltme |
|---|---|---|---|---|
| **T1** | `landing.html:203`, `landing.html:252` | **4** | DESIGN.md § Overview *"no rule may contain a colour"* + iki-zemin sözleşmesi | `color:#fff` → `var(--bg2)`. Ölçüldü: gece `--accent:#d4d0cb` üzerine `#ffffff` = **1,53:1**; birincil düğme yazısı ve `.pillc` rozeti gece zemininde kayboluyor. Aynı kusur sınıfı bir kez yaşandı (üst bar 1,27:1). |
| **T2** | `app.js` — 162 çağrı yeri (§B.1) | **4** | PRODUCT.md § Honesty-of-absence: *"ölçülmeyen metrik 0 olarak render edilmez"* | Ayrımı mekanikleştir: para/sayım alanları biçimleyiciye `null` geçsin (`trn`/`money` zaten "—" basıyor); `?? 0` yalnız "ölçüldü ve sıfır çıktı" beyanı olan yerde kalsın ve o beyan çağrı yerinde görünsün. |
| **T3** | `app.js:394` | **4** | Design rules: *"Mode must be legible from any pixel"* | `#statustext` üç hâlin **üçünde de** modu basmıyor: `halted` → "DURDURULDU", `stale` → "gecikmiş · N sn", yalnız sağlıklı hâlde "paper" yazıyor. Mod, durum metnine değil ayrı ve koşulsuz bir kanala bağlanmalı. |
| **T4** | `index.html` jeton blokları (satır 75–260) | **4** | Design rules §4: mod kroması *"reserved permanently… used for nothing else"* | **Ayrılmış bir mod kanalı yok** (§C.4). Üç kromatik jeton §2 ve §3 tarafından tüketilmiş. L1'e geçişte taşıyacak yapısal muamele bugün mevcut değil. |
| **T5** | `app.js:950`, `app.js:951` | **3** | Design rules §3: yön hue'su yalnız P&L işaretine | `stop` fiyatı koşulsuz kırmızı, `hedef` fiyatı koşulsuz yeşil. Bunlar sonuç değil plan parametresi; her silahlı plan satırı iki yanlış-rollü renk taşıyor. Nötr `--tx`/`--tx2` + etiketle ayır. |
| **T6** | `app.js:3171`, `app.js:3172`, `app.js:4649` | **3** | Design rules §2/§3 rol karışması | `gerçekleşen R` koşulsuz yeşil, `açık R` koşulsuz amber, `geri verilen R` koşulsuz kırmızı — üçü de büyüklük, hiçbiri hüküm. |
| **T7** | `index.html:1190` (`.hudchip.explore`) | **3** | Design rules §2: şiddet hue'su *"used for nothing else"* | KEŞİF MODU bir çalışma kipidir; amber ile taşındığında alarm bütçesinin diliyle karışıyor. Mod kanalına (T4) taşınmalı. |
| **T8** | `index.html:1450` (`#hud`) | **3** | Erişilebilirlik + akış sözleşmesi | 20 sn'de bir yeniden çizilen, WS kopukluğunu ve bekçi gecikmesini taşıyan kapsayıcıda `aria-live` yok (`aria-label` var). Kritik-durum-değişimi kuralına göre `polite` + `aria-atomic` gerek; `#bp-alerts`/`#bp-events` aynı durumda. |
| **T9** | `app.js:1016-1021`, `app.js:2840-2843` | **3** | Design rules: sayısal dürüstlük · alan-model eşleşmesi | Damgalar tarayıcı yerel saatiyle basılıyor, zemin etiketi yok. ABD piyasası (ET) + TSİ operatör + yerel tarayıcı = aynı "14:55" üç şey. Tüm 4 `mutlakTs` ve 3 `mktSaat` çağrı yeri etkileniyor. |
| **T10** | `runbook.html` (129 satır, tamamı) | **3** | PRODUCT.md § Brand: tek jeton sözlüğü, iki zemin, gündüz varsayılan | Dördüncü, dönüştürülmemiş görsel dünya: **kendi jeton sözlüğü** (`--tx3:#8a8580` → `#fbfaf9` üzerinde **3,50:1** ölçüldü; gece `#77746f`/`#17181a` = **3,82:1**), **soğuk** saç telleri `rgba(0,0,0,.10)` (Warm Rule), `prefers-color-scheme` (operatör anahtarı değil — `theme.js` **bilerek** yüklenmiyor, satır 33), **Roboto** yığını, ve rampa dışı 15/18/26px + `.92em`/`.86em`. Panodan **2 bağla** erişiliyor (`app.js:2406`, `7439`). |
| **T11** | `app.js` — 36 yer @10px (`index.html` 20 + `app.js` 16), 46 yer @11px | **3** | impeccable `undersized-ui-text` / `tiny-text` | Kuralın kendi metni rampayı kaçış yolu saymıyor. **Araç bu 36 yeri GÖRMEDİ** (Ö2) — landing'de gördüğü 17 örnekle aynı sınıf, panonun kendisinde ölçülmemiş. |
| **T12** | `trend_shadow.py:181` → `api.py:2681` → `Öğrenme#golge` | **3** | Design rules: *"Null renders as an explicit gap with a reason — never interpolated"* | `ffill()` sonucu ekrana ulaşıyor; web katmanında "dolduruldu/ileri taşındı" dizesi **0 kez** geçiyor. Doldurma sınırlı ve dürüst belgelenmiş, ama **ekranda etiketsiz**. |
| **T13** | `app.js` — `cls()` 26 / `isr()` 11 (§B.6) | **3** | DESIGN.md § The Second-Channel Rule | Pozitif para/yüzde okumaları `isr()` sarmalı olmayan yerlerde tek kanalla (renk) taşınıyor. Doğrulanan manşet vakalar: `app.js:5905` (defter K/Z toplamı), `app.js:1738` (gerçekleşmiş canlı K/Z). |
| **T14** | `index.html:298`, `app.js:4412`, `4593`, `4793` | **2** | Design rules §1: yapı akromatik | Oturum-çıkış düğmesinin hover'ı HALT/FLATTEN ile **aynı kırmızı jetonu** kullanıyor (geri alınabilir bir eylem, geri alınamayanların dilinde); aile adı ve pencere kimliği gibi **etiket dizeleri** koşulsuz kromatik. |
| **T15** | `index.html:159`, `landing.html:109`, `workflow.html:104` (`--t-rise:.34s`) | **2** | Design rules: *"Motion: ≤300ms, anomaly-signalling only"* | 340 ms, üstelik içerik giriş animasyonu — anomali sinyali değil. Üç yüzeyde de aynı jeton. |
| **T16** | `index.html:1271` (400 ms), `landing.html:360` (450 ms) | **2** | aynı | Termometre dolumu ve kaydırma-anlatı metin dolumu; ikisi de anomali sinyali değil. |
| **T17** | `index.html:386` (2,6 s ∞), `:916` (1,1 s ∞), `:1212` (1,1 s ∞) | **2** | aynı | Sonsuz döngüler. `.dot.pulse` ve `.blink` anomali sinyali (kural niyetine uyuyor, süre sınırına uymuyor); `.tazeleniyor` yükleme göstergesi (niyete de uymuyor). |
| **T18** | `app.js` — `trn()` 8 ondalık varyantı, `pctf()` 4 | **2** | Design rules: *"fixed decimals per column"* | Ondalık sayısı 240+ çağrı yerinde tek tek seçiliyor; sütun seviyesinde bir sözleşme yok. |
| **T19** | `index.html:286` (`.mono-num`) — 99 emisyon | **2** | Design rules: *"tabular figures, right-aligned"* | Tabular ✓, **sağa hizalama yalnız `.tbl .num` / `.trow > .num`de** (12 kural). `.srow` içindeki 99 `mono-num` akış hizasında. |
| **T20** | `landing.html` — 31 impeccable bulgusu (17 `undersized-ui-text`, 6 `cramped-padding`, 5 `wide-tracking`, 1 `tiny-text`, 2 `overused-font`) | **2** | impeccable kalite kuralları | Bulguların %76'sı bu dosyada. Ayrıca `landing.html`e **panodan hiçbir bağ yok** (grep: 0) — bkz. T22. |
| **T21** | `index.html` — `clipped-overflow-container` | **2** | impeccable | Kırpan kap mutlak-konumlu bir çocuğu kesiyor; ipucu/açılır katman kaçamaz. Satır **ÖLÇÜLEMEDİ** (Ö3: araç `line:0` veriyor). |
| **T22** | `landing.html` (818 satır), `workflow.html` (306 satır) | **2** | PRODUCT.md § Users: *"Not multi-tenant; no external investors, clients, or team members view this system"* | İkisi de `api.py:507`/`:593`ten servis ediliyor ama **panodan hiçbir iç bağ yok** (grep: `href` eşleşmesi 0). Tek kullanıcısı yazarı olan bir ürünün ikna yüzeyi — hangi soruya hizmet ettiği ölçülemedi. |
| **T23** | `index.html:357`, `:1160`, `landing.html:187` (`backdrop-filter:blur`) | **—** | **KURAL ÇATIŞMASI** | Design rules "glassmorphism"i yasaklıyor; DESIGN.md § Composited surfaces şeffaf üst barı ve gece perdesini **ölçülmüş** bir ayrım mekanizması olarak beyan ediyor. Ciddiyet atanmadı — bu bir bulgu değil, iki bağlayıcı metnin kesişimi. Hüküm Rol-1'de. |
| **T24** | `app.js:203`, `:2830` (`money`, `mktUsd`) | **1** | Design rules: *"explicit currency"* | Her tutarda `$` var (45 çağrı), `USD` hiç yok; sayı biçimi `tr-TR` (`1.234,56`), sembol ABD konvansiyonuyla önde. Tek satırda iki yerelleştirme sözleşmesi. |
| **T25** | 4 yüzey — `overused-font` ×7 | **1** | impeccable `slop` | geist / geist mono (index, landing, workflow) + **roboto** (runbook). PRODUCT.md 2026-08-06'da yazı tipini **açtı** — bu bulgu artık bir kısıtla çatışmıyor, girdi. |

**Dağılım: 4 × ciddiyet-4 · 9 × ciddiyet-3 · 9 × ciddiyet-2 · 2 × ciddiyet-1 · 1 kural çatışması.**

---

## 1. MEVCUT DURUM ENVANTERİ — İŞE GÖRE

Manda gereği bu envanter widget'a göre değil **işe** göre çıkarıldı. Bir öğe yalnız **ölçülmüş
değerini** taşır; biçimini, adresini ya da var olma hakkını taşımaz.

**Sorular nereden geldi:** uydurulmadı — `app.js:80-111`deki `EKRAN_SORUSU` sözlüğü zaten 27
soruyu (7 sayfa + 20 bölüm) tanımlıyor ve `soruCumlesi()` (116) her başlığın altına basıyor.
Aşağıdaki tablo o sözlüğü **top-task'lara** göre yeniden kesiyor.

**"Ölçülebilir kazanç" sütunu kuralı:** yalnız *kodda ya da bu turda ölçülmüş* bir şey yazıldı.
Ölçüm yoksa "ölçülemedi" ve nedeni yazıldı — biçimin iyi olduğuna dair bir iddia hiçbir satırda
yok.

### TASK ① — "sağlıklı mı, dün gece ne oldu?" (10 sn)

| operatörün sorduğu SORU | cevabı veren VERİ (alan/uç) | bugün NEREDE | bugün hangi BİÇİMDE | o biçimin ÖLÇÜLEBİLİR kazancı |
|---|---|---|---|---|
| Sistem şu an ayakta mı? | `/api/today` → `halted`, `stale`, `heartbeat_age_seconds` | üst bar, her sayfada sabit (`index.html:1451`) | `#statuspill`: renkli nokta + 3 hâlli metin, `role=status aria-live=polite` | Erişilebilirlik kanalı ölçülü mevcut (5 canlı bölgeden biri). Modu 3 hâlden yalnız 1'inde basıyor (T3). |
| Dün gece döngü koştu mu, ne üretti? | `/api/today` → `date`, `candidates`, `plans`, `armed`, `yas_saat`, `data_ok` | Genel Bakış → "Dün gece" kartı (`GENEL_KARTLARI[0]`, hedef `kosu`) | `.gb-kart`, tek bağ, gövdede satırlar | Kart bütçesi **teste çivili** (6 kart, `GENEL_KARTLARI` veri olarak tutuluyor — `app.js:1343-1354` gerekçeyi yazıyor). Sayfa sayımının ölçülebilir bir kapısı olan **iki** yüzeyden biri. |
| Bekleyen bir şey var mı? | `/api/today` → `inbox_count` (`api.py:_inbox_count`) | Genel Bakış → "Bugün ne var" kartı | `.gb-kart`, sayı + `portfoy#onaylar` bağı | **Ölçülen kusur devam ediyor** (önceki denetim B2): sayaç REVIEW planlarını saymıyor. |
| Alarm çaldı mı, bütçesini aştı mı? | `/api/alerts` → `groups`, `g.n` | Genel Bakış `.gb-alarm` şeridi **+** Gözetim → `alarmbutce` kutusu | iki farklı bileşen dili (§4-P5) | Sunucu tarafı gruplama ölçülü mevcut (`×N` rozeti) — sel sunum katmanında zaten toplanmış. |
| Bekçiler / veri hattı / WS akıyor mu? | `/api/hud` → `watchdog`, `stream_ok`, `io`, `askida` | üst bardaki `#hud`, 6 çip, 20 sn'de bir | `.hudchip` + `.ld` lambası + `title` ipucu | `askida` üçüncü hâli **rozet rengini bozmuyor** (kod yorumu 2280-2282 gerekçeyi yazıyor) — alarm-olmayan bir durum alarm dilinde konuşmuyor. Buna karşı: `aria-live` yok (T8), `title` klavyeyle erişilemez. |
| Sessiz mi, bir şey mi bekliyor? | `/api/hud` → `sessiz_hat` segmentleri | üst bar altı `.sessizhat` şeridi | mono metin şeridi, **sağlıklıyken görünmeze yakın** (`index.html:1281-1302`) | `role=status` **koşullu** basılıyor (`app.js:2432-2435`): sağlıklıyken duyurulmuyor — gerekçe kodda ("bekçiler 17/17 diye duyurulan bir satır"). ARIA gürültüsüne karşı ölçülü bir tercih. |

### TASK ② — "onayla & arm et, aynaya ulaştığını gör"

| SORU | VERİ | NEREDE | BİÇİM | ÖLÇÜLEBİLİR KAZANÇ |
|---|---|---|---|---|
| Onayımı bekleyen ne var? | `/api/approvals` | Portföy & Emirler → `onaylar` bölümü | gelen kutusu listesi | **Bölüm kendi sorusunu cevaplamıyor** (önceki denetim B4): REVIEW planları bu kutuda değil. |
| Bu planı onaylamalı mıyım? | plan kaydı → `gate_verdict`, `expired`, seviyeler | Koşu & Döngü → `adaylar` → satır çekmecesi | `planOnayBloguHTML`, iki adımlı `confirm` düğmenin kendisinde | **NO_GO yapısal olarak onaylanamaz**, süresi dolmuş plan onaylanamaz (`app.js:1965`) — kapı kodda, metinde değil. Onay bir **olay** olarak kaydediliyor (verdict yeniden yazılmıyor). |
| Onayım işledi mi? | `POST` yanıtı → `armed_n`, `not` | çekmece içi `#plan-onay-msg` | `.pos` yeşil satır + yönerge metni | Mesaj **sunucu yanıtından sonra** basılıyor; "Dolum bir sonraki açılışta" diyerek teyit-öncesi iddia kurmuyor. Buna karşı: çekmece kapanınca **kayboluyor** (B5). |
| Emir aynaya ulaştı mı? | `/api/today` → `armed_plans`, `alpaca_submitted`, `broker_status` | Portföy → `nextSessionCard` | tablo satırı + **rozet** (`aynada`/`gönderilecek`/`RET`) | Rozet "gönderildi"yi "doldu"dan ayırıyor (§B.3) — sözcük seçimi ölçülü doğru. |
| Kaçı doldu? | `/api/slippage` → `n_dolan`, `dolum_orani`; `/api/reconcile` → `partial_fills` | ③ EMİRLER durum kartı **+** `mutabakat` bölümü | **oran çubuğu, paydası beyanlı** + parçalı-dolum satırı | Payda **beyanlı** (`payda: "gönderilen emir"`), oran `null` ise "gönderilen emir yok — oran tanımsız" basılıyor — paydasız çubuk yasağı burada tutuyor. |
| Gönderim düğmesine bastım, ne oldu? | `/api/alpaca/submit_armed` yanıtı | Kilitler & Yapılandırma → `ayarlar` | **hiçbir biçim** | Önceki denetim B1: `#alp-msg` öğesi hiç üretilmiyor. `app.js:7101` ölçülemeyen gönderim sayısını **"0 DEĞİL"** diye yazacak metni hazır tutuyor — okuyucusu yok. |

### TASK ③ — "alarmdan nedene ve dispozisyona, ≤2 tık"

| SORU | VERİ | NEREDE | BİÇİM | ÖLÇÜLEBİLİR KAZANÇ |
|---|---|---|---|---|
| Hangi alarm çaldı? | `/api/alerts` → gruplar | Gözetim & Alarmlar → `#bp-alerts` | liste + `×N` rozeti + tek toplu eylem | Gruplama sunucuda; satır bazlı ertele/sustur yok (B17). `aria-live` yok. |
| Nedeni ne? | bekçi segmenti, `sessiz_hat`, `askida`/`n_askida` | sessiz hat + HUD çipi | metin şeridi | `askida` alanı **üretiliyor ama çizilmiyor** (B6) — nedeni yalnız `title` ipucunda. |
| Hangi bütünlük ihlali? | `/api/butunluk` → 7 desen + makullük + defter + yazar + eleme | Veri Sağlığı → `veriboru` → "Bölüm 5" | **tek kartta 5 alt-başlık, hepsi aynı görsel ağırlıkta** | Ölçüm doğru (rapor 7 düşen satır buluyor); kademe yok (B11). |
| Ne yapabilirim? | müdahale kolları | Kilitler → `mudahale` (4 kademe) + ⌘K "Müdahale" grubu (6 komut) + üst bar HALT + Ayarlar Alpaca kartı | **dört ayrı yüzey, üç ayrı onay dili** | Müdahale paneli ekranda *"üçü de aynı gövdeyi çağırır"* yazıyor — **dördüncü yüzey var ve tek `confirm()`lü** (B3). |
| Belgeye gitmem gerekir mi? | `runbookHref()` → `/runbook#<ad>` | ayrı tam sayfa | `<a>` bağı | Panoyu terk ediyor; dönüşte `go()` `scrollTo(0)` yapıyor (B14). Ayrıca yüzeyin kendisi dördüncü görsel dünya (T10). |

### Sistem geneli — biçim taşıyıcıları (rol, adres ve biçim AÇIK; yalnız ölçülmüş değer kaydedildi)

| Öğe | Bugünkü hâli | Ölçülmüş DEĞER (varsa) | Ölçülmemiş/açık olan |
|---|---|---|---|
| **Sessiz Hat** (`app.js:2360-2440`) | üst bar altı mono şerit, sağlıklıyken görünmeze yakın | ARIA'yı koşullu duyurma (2432) — gürültü kontrolü ölçülü bir tercih | `askida` okunmuyor; şeridin **biçimi ve adresi** açık |
| **⌘K paleti** (`palette.js`, 1.043 satır) | 6 grup, `grup:` alanı 31 yerde, `aria-live=assertive` | Grup sırası veri olarak tutuluyor (`GRUP_SIRA:130`); klavye yüzeyi tam (Tab/Ok/Home/End/PageUp) | 29 gezinme komutu tek grupta (B16); paletin **var olma biçimi** açık |
| **Karne matrisi** (`.pm-*`, `index.html:740-790`) | kurulum×rejim ızgarası; hücre = tıklanabilir `button` | (a) **ıraksayan alfa tavanı `.30`'da ÖLÇÜLMÜŞ sınır** (gece 4,68:1; `.34`→4,45); (b) `.pm-conf` güven izi — 3-işlemlik ortalama 55-işlemlik kadar sağlam görünmüyor; (c) `.pm-unsown` "hiç ekilmemiş" ≠ sıfır; (d) `.pm-yield` özgüllük düzeltmesi (0,2,0) — para kuralının görünür kanalı bir sıra çakışmasıyla ölüydü, ölçülüp düzeltilmiş | "ince örneklem" amber'i §2 hue'sunda (T7 sınıfı); matrisin **biçimi ve adresi** açık |
| **`.durum-kart`** (4 adet, ızgara) | tıklanabilir, çekmece açar | Kart bütçesi teste çivili (4) | Tıklanabilirlik ekranda işaretsiz (B19) |
| **`.gb-kart`** (6 adet) | tıklanamaz, ayrı `→ <alan>` düğmesi | Bütçe teste çivili (6); tek çıkış noktası `gbKart()` — "kart başına tek bağ" **sayılabilir** | Aynı reçeteden üç farklı tıklama sözleşmesi (B19) |
| **Ray / gezinme** (7 sayfa) | sol şerit + `1-7` + `g`+harf + ⌘K | Üç yol da `VIEWS`ten türüyor — ikinci bir liste yok, sessiz ayrışma imkânsız | Aynı hedefe üç tam liste (B16); rakamlar ekranda gösterilmiyor (B20) |
| **Tipografi** | Geist + Geist Mono, 9 basamak | `index.html` fiilen **9 ayrı boy** kullanıyor (10/11/12/13/14/17/20/24/28) — rampa dışı **0** değer; `app.js` satır-içi boyların **tamamı** (10/11/12/13/14) rampada | 36 yer 10px'te (T11); yazı tipi PRODUCT.md ile **açık** |
| **Jetonlar** | tek ad kümesi, iki değer kümesi | `app.js`/`palette.js`/`theme.js`/`landing.js`/`workflow.js` → **0 hex literali** (jeton sözleşmesi JS'te %100 tutuyor); `index.html`/`workflow.html` kural gövdesinde **0** renk | `landing.html`de 2 kaçak (T1); `runbook.html` ayrı sözlük (T10) |
| **Grafik serileri** (`IC_SERI`) | 3 seri: nötr luminans merdiveni + kesik-çizgi deseni | **17,80 → 9,60 → 5,06** ölçülmüş; efsane deseni de basıyor; hue bilerek eklenmemiş | Serilerin **biçimi** açık (bullet/bar/başka) |
| **Tablo düzenleri** (`.trow`, `.tbl`) | `grid-template-columns` satır içinde | `tabular-nums` + `text-align:right` tablo hücrelerinde tutuyor | Sütun başına sabit ondalık yok (T18); `.srow` sayıları hizasız (T19) |
| **Boş hâller** | `.empty` + gerekçe metni | "emir yok bir özür değil, bir bilgidir" (`app.js:935`) — boş hâl birinci sınıf; `onk()` "Lundefined" sınıfını kapatıyor | Boş hâllerin **biçimi** açık |
| **Detay katmanı** (`detayKatmani`) | varsayılan-kapalı `<details>` + `neden` satırı | "neden burada değil" sorusu **ekrandan** cevaplanabiliyor | Her yeniden çizimde kapanıyor (B15) |

---

## 2. AYNI SORUYU İKİ YERDE / İKİ BİÇİMDE CEVAPLAYAN ÇİFTLER

Yeni mimarinin en yüksek kaldıraçlı girdileri. Her satır bir **ölçülmüş** çifttir.

| # | SORU | Yer 1 · biçim | Yer 2 (·3) · biçim | Ölçüm |
|---|---|---|---|---|
| **P1** | Emir aynaya ulaştı mı? | `nextSessionCard` (`app.js:944`) — **rozet** (`aynada`/`gönderilecek`/`RET`) | ③ EMİRLER durum kartı (`app.js:1775`) — **oran çubuğu**, payda beyanlı | İkisi de **Portföy** sayfasında, aralarında 1 bölüm var (önceki denetim B18) |
| **P2** | Alarm bütçesi aşıldı mı? | Genel Bakış `gbAlarmSatiri` (`app.js:1492`) — `.gb-alarm` şeridi | Gözetim `alarmButce` (`app.js:2394`) — `.alarmbutce` kutusu | İki farklı bileşen dili, aynı jetonlar (B23) |
| **P3** | Bekçiler ne durumda? | HUD çipi (`app.js:2286-2290`) — lamba + `ok/total` | sessiz-hat "bekçiler" segmenti | ×2 (B7) |
| **P4** | Nabız taze mi? | `#statuspill` (`app.js:394`) | sessiz-hat "tazelik" segmenti | + kenar şeridi `subs.genel` → **×3** (B7) |
| **P5** | WS akıyor mu? | HUD çipi (`app.js:2283`) | ③ EMİRLER durum kartı `akisTxt` (`app.js:1771-1773`) | + `spineHTML` → **×3**; ayrıca `mutabakat` kartı (`app.js:3181-3183`) aynı gerçeği dördüncü kez basıyor |
| **P6** | Ayna sapması var mı? | `.durum-kart` satırı (`app.js:749`) — `sapma`/`ayna uyumlu` | Ayarlar Alpaca kartı (`app.js:6989-6991`) — `fiyat SAPMA · pozisyon SAPMA` | Farklı sayfalar, farklı sözcükler, aynı `rc.mirror_drift` |
| **P7** | Sistem durduruldu mu? | `#statuspill` "DURDURULDU" | `mudahale` kartı `HALTED ? ÇEKİLİ : serbest` (`app.js:2551`) | + `spineHTML` + HALT düğmesinin kendi metni (`DEVAM`/`HALT`) → **×4** |
| **P8** | Hangi kesitte kaç işlem var? | Karne matrisi `.pm-n` hücresi | `Öğrenme#karne` "Hangi araç ne kadar isabetli" kartı (`app.js:6249`) | Aynı `n`, iki biçim (matris hücresi vs. satır listesi) |
| **P9** | Bugün ne bekliyor? | Genel Bakış "Bugün ne var" kartı — `inbox_count` | `spineHTML` triyaj şeridi — 12 durum çipi | **İki farklı sayaç, iki farklı tanım** (B2/B9): kart REVIEW'ü saymıyor, şerit `pending_count`u "onay bekliyor" diye etiketliyor |
| **P10** | Otonomi seviyesi ne? | `#statuspill` `L0` | `.durum-kart` "Otonomi" satırı (`app.js:752`) | + kenar şeridi `kilitler` satırı (`app.js:717`) → **×3** |

---

## 3. PANEL / KART ENVANTERİ

### 3.1 Statik emitör sayımı (bu turda ölçüldü)

| Emitör | `app.js` | `index.html` |
|---|---|---|
| `class="card…"` | **81** (`card rise` 72 · `card` 5 · `card rise in` 4) | 1 |
| `gbKart()` — tek çıkış | 1 şablon → `GENEL_KARTLARI` 6 girdi | — |
| `.durum-kart` | 1 şablon (ızgara 4 kart) | — |
| `.pane` | 2 | — |
| `detayKatmani()` | 6 çağrı (+1 tanım) | — |
| `bolumBasHTML()` | 20 çağrı (+1 tanım) | — |
| `.srow` (etiket/değer satırı) | **171** | — |
| `.hudchip` | 8 | — |
| `.gb-alarm` | 2 | — |

**Uyarı (Ö4):** 81 statik emitörün sayfa kırılımı satır-sırasıyla **çıkarılamıyor** — yardımcı
fonksiyonlar `RENDER.<bölüm>` blokları arasında tanımlı ve satır-sırasına dayalı atıf ölçüldüğünde
yanlış çıktı verdi (`market` kovası 31 gösteriyor, gerçek 8).

### 3.2 Sayfa başına kart (önceki denetimden devralındı, tekrar ölçülmedi)

| Sayfa | Bölüm | Ölçülen kart | Bütçe |
|---|---|---|---|
| Genel Bakış | — | 6 `.gb-kart` + 1 `.gb-alarm` | **çivili** (`GENEL_KARTLARI`) |
| Veri Sağlığı | 3 | 8 kart + 1 detay katmanı + evren tablosu | yok |
| Koşu & Döngü | 2 | 4 `.durum-kart` + 4 kart + 2 `.pane` + 2 detay katmanı | durum ızgarası **çivili** (4) |
| Portföy & Emirler | 5 | 4 `.durum-kart` + 13 kart | yok |
| **Öğrenme** | **7** | **39 kart** | yok |
| **Gözetim & Alarmlar** | **1** | **3 kart** | yok |
| Kilitler & Yapılandırma | 2 | 5 kart + 1 detay katmanı | yok |

Ölçülen asimetri (devralındı): Öğrenme, Gözetim'in **13 katı** kart taşıyor. Kart bütçesi olan
yüzey sayısı: **2 / 7.**

### 3.3 Diğer yüzeyler

| Yüzey | Satır | Panel/kart | Not |
|---|---|---|---|
| `landing.html` | 818 | ölçülmedi (ikna yüzeyi, kart dili farklı) | panodan **0 iç bağ** |
| `workflow.html` | 306 | ölçülmedi | panodan **0 iç bağ** |
| `runbook.html` | 129 | ölçülmedi | panodan **2 bağ**; ayrı jeton dünyası |

---

## 4. HİÇBİR SORUYA BAĞLANAMAYAN ÖĞELER (silme adayları)

Bunlar *öneri* değil, **ölçüm sonucu adaylıktır** — hüküm Rol-1'de.

| # | Öğe | Ölçüm |
|---|---|---|
| **N1** | `spineHTML` satır 900 kolu — `pending_count > 0 && autonomy_level >= 1` | Sistem **L0**'da; koşul hiç ateşlemiyor → **ölü satır** (B9). Ayrıca `pending_count` "onay bekleyen" değil (`analytics.py:216-221`) — kodun kendi yorumu "kimseden bir şey istemez" diyor. |
| **N2** | `--blue` jetonu | Kural gövdesinde **0 kullanım** (gündüz+gece iki değer bildirimi taşıyor). |
| **N3** | `--violet` jetonu | **1 kullanım** (`app.js:2771`) ve değeri zaten nötr — hue taşımıyor. |
| **N4** | `landing.html` (818 satır) | PRODUCT.md: dış izleyici yok. Panodan **0 bağ**. impeccable bulgularının **%76'sı** burada. Hangi operatör sorusuna hizmet ettiği **ölçülemedi**. |
| **N5** | `workflow.html` (306 satır) | Panodan **0 bağ**; `flat-type-hierarchy` + 2 `undersized-ui-text`. Aynı içeriğin bir bölüm-içi karşılığı olup olmadığı **ölçülemedi**. |
| **N6** | "Bölüm 1…6" numaralı başlıklar | Numaralar dört ayrı sayfaya dağılmış; numara bir konum iddiasıdır, o konum yok (B22). |
| **N7** | `#alp-msg` referansları (`app.js:6967`, `6974`) | Hedef öğe **hiç üretilmiyor** (grep: 0 eşleşme). Kod her satırı `if (m)` ile koruyor — sessizce ölü (B1). |
| **N8** | `.tazeleniyor` sonsuz animasyonu (`index.html:916`) | Yükleme göstergesi; Design rules motion kuralı "anomaly-signalling only" diyor. Kaldırılırsa `aria-busy` (`app.js:520`) zaten aynı bilgiyi taşıyor. |
| **N9** | `runbook.html`in kendi jeton bloğu (satır 37-49) | Panonun jeton sözlüğünü **kasten** yüklemiyor (satır 33: *"paralel koşuyor"*). O paralel tur kapandı; gerekçe süresi dolmuş olabilir — **ölçülemedi** (tur durumu bu depodan okunamıyor). |

---

## 5. ROL-SIZINTI HARİTASI — ÖZET

```
                 §1 YAPI    §2 ŞİDDET   §3 YÖN     §4 MOD     §5 VERİ ÖLÇEĞİ
                (akromatik) (3 hue)    (P&L)     (kâğıt/canlı) (tek-hue/ıraksayan)
--green            ←4 yer     ✓          ✓          —            —
--amber            ←2 yer     ✓          —          ←1 yer       ←2 yer
--red              ←1 yer     ✓          ✓          —            —
(ayrılmış kanal)     —        var        yok*       YOK          kısmen var
```

`*` §3 (yön) kendi kanalına sahip değil — §2 ile aynı üç hue'yu paylaşıyor.
`§5` kısmen: `IC_SERI` tek-hue luminans merdiveni ✓ · `--pm-pos/neg` ıraksayan ✓ · ama
"ince örneklem" güven sinyali §2'nin amber'ini ödünç alıyor.

**Sayısal özet:** 3 kromatik kanal, 5 rol. `--green` 14 bağlanma / ≥4 rol · `--amber` 21 bağlanma
/ ≥5 rol · `--red` 24 bağlanma / ≥5 rol. 164 koşulsuz `pos`/`neg`/`warn` emisyonu (değere bakmadan
sabit sınıf) — bunlar tanımı gereği yön taşıyamaz, çünkü yön değişkendir.

---

## 6. HÜKMÜ KESKİNLEŞTİRECEK EKRAN FOTOĞRAFI İSTEKLERİ (3)

Aşağıdaki üçü, **statik analizin yapısal olarak göremediği** (Ö1/Ö2) hükümleri kapatır.
Hepsi canlı panodan, uygulama yerelde yüklenmeden.

1. **Öğrenme sayfası, gündüz zemini, tam sayfa (kaydırmalı yakalama), 1440×900.**
   Kapatacağı hüküm: 39 kartın *görsel* yoğunluğu ve `.srow`ların (171 emisyon) ritmi — kart
   sayımı ölçüldü ama **bir ekranın kaç okuma bölgesine bölündüğü** koddan çıkarılamıyor. T11'in
   (36 yer @10px) gerçek okunabilirlik maliyeti de burada görünür.

2. **Karne matrisi (`Öğrenme#karne`), GECE zemini, en az bir `thin` ve bir `unsown` hücre
   görünecek şekilde.**
   Kapatacağı hüküm: §C'nin en riskli sızıntısı — `.pm-cell.thin`in amber saç teli ile
   `.pm-cell.neg`in kırmızı dolgusunun **aynı hücrede** nasıl okunduğu. Alfa tavanı `.30`
   ölçülmüş ama *iki sinyalin üst üste geldiği* hâl ölçülmemiş; gece zemininde tint yönü ters
   çalışıyor (Tint-Direction Rule).

3. **Üst bar + HUD, sistem BOZUK bir hâldeyken** (WS kopuk ya da bekçi geciken ya da HALT çekili),
   gündüz zemini, sadece üst 200px.
   Kapatacağı hüküm: T3 + T7 + T8 birlikte. `#statustext` bozuk hâlde modu basmıyor, HUD'da
   `KEŞİF MODU` amber çipi alarm çipleriyle **yan yana** duruyor, ve `.dot.pulse` 2,6 s'lik
   sonsuz nabzıyla çalışıyor. Üçünün aynı 200 pikselde nasıl yarıştığı yalnız fotoğrafta görünür.

---

## 7. ÖLÇÜM ARTEFAKTLARI VE SÜRÜM KAYMASI

Bu tur `npx impeccable@latest detect` çalıştırdı. Tek yan etki: `.impeccable/hook.cache.json`
güncellendi. `.gitignore:16` `.impeccable/` dizinini dışlıyor — **versiyonlu hiçbir dosya
değişmedi.** Geçici tarama betikleri oturum scratchpad'inde kaldı, depoya yazılmadı.

**Beyan edilmesi gereken sürüm kayması:** `DESIGN.md` bu tur SIRASINDA paralel bir oturumda
büyüdü — okunduğunda 56.165 bayt, tur bitiminde **59.102 bayt** (§ Design rules'a "Redesign
scope", "Direction", "Calibration" ve yöntem paragrafı eklendi). Eklenen metin bu raporun
yöntemiyle **çelişmiyor, onu doğruluyor**: yeni paragraf envanterin *iş* ekseninde ve tam olarak
bu beş sütunla tutulmasını istiyor (*"what question the operator is asking · which data answers
it · where it lives today · in what form · what that form measurably buys"*) ve *"A shipped
element carries forward only its measured value, never its shape, its address, or its right to
exist"* diyor — §1 bu kuralla yazıldı.

Yeni metnin bu raporda **karşılığı olmayan tek kalemi**: *"new modules are permitted… a valuable
job is never dropped merely because today's data does not cover it"*. Bu rapor **mevcut** öğeleri
ölçtü; "bugün hiçbir yüzeyin cevaplamadığı iş" listesi çıkarılmadı — brief'te istenmediği için.
İki aday yine de ölçüm sırasında düştü ve §1'de duruyor: (a) *"gönderim düğmesine bastım, ne
oldu?"* — hiçbir biçimi yok (`#alp-msg` üretilmiyor); (b) *"bekçi neden eksik?"* — `askida` alanı
üretiliyor ama çizilmiyor. Tam liste ayrı bir tur işidir.
