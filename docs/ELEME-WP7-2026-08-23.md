# ELEME — WP7 Skill Katmanı (STOK KAMPANYASI eleme turu)

**Tarih:** 2026-08-23 · **Ajan:** salt-okuma eleme ajanı (tek yazım bu dosya; git koşulmadı)
**Kapsam:** WP7 kalemleri 24c · 24d · 24e · 24f · 24g + Ö-39 (künye yolu kararı)
**Hüküm yetkisi:** YOK — sınıf önerileri Rol-1'e gider; kart/ROADMAP'e dokunulmadı.

**Ölçüm koşulları (ölçüm-bağlamı tuzağına karşı adıyla):** canlı A1 `/opt/meridian`,
2026-08-23 20:06–20:35 UTC, salt-okuma ssh; defterler doğrudan okundu (`agent_calls.jsonl`,
`events.jsonl`, `skill_gorusleri.jsonl`, `skill_gorus_durum.json`, `llm_calibration.json`,
sprint kum-havuzu defterleri) + `meridian.db` **sqlite `mode=ro` + `PRAGMA query_only`**
(canlı worker koşarken sıfır yazım). `trade_plans.jsonl` canlıda `.migrated` — plan görüşleri
DB `extra_json` kolonundan sayıldı. **Dikkat:** 08-22/23 hafta sonu; o iki günün çağrı
sessizliği seans-dışı kadansla uyumludur, arıza kanıtı değildir (Pazartesi seansı teyit eder).

---

## §0 — ÖZET TABLO + SAYIM

| kalem | eski iddia (kaynak) | bugünkü ölçüm (2026-08-23 canlı) | SINIF |
|---|---|---|---|
| **24c** ana danışma yolu ÖLÜ | son 7g: 788 `agent_call`, 385 boş, **1** başarılı görüş (ARASTIRMA-SKILL-ETKIN-KULLANIM-2026-08-13:22) | son 7g: **9** çağrı (review 4: 3 dolu/1 zaman-aşımı), boş fırtınası 08-11'de bitti; aynı-gün görüş damgası 08-19 (n=9) · 08-20 (n=2) · 08-21 (n=5); danışman 08-14'te TERFİ ETTİ | **KAPAT-BAYAT** |
| **24d** pilot-S1 A/B | DOLGU "çalışan tek nokta"; `n_pairs` 4→~51→~95, terfi tabanı 30 pilotla aşılacak (ROADMAP WP7) | `n_pairs=100` (pencere dolu), terfi **pilotsuz gerçekleşti** (08-14); veto yüzeyi 9 günde 0 kez ateşlendi; iki pilot skill'i canlı ön-yükleme listesinde bile yok | **KAPAT-BAYAT** |
| **24e** çekimser teşviki | `destekle` kovası BOŞ, `r_gap=null`, hiç terfi etmez → "terfinin ASIL duvarı" (aynı doküman :24) | destekle **10** · karşı **14** · çekimser 76 (taze-100 çift); `r_gap=0.857`; **`promoted=true`** (2026-08-14T21:03 AUTHORITY_CHANGE); son-30g plan görüşlerinde çekimser **%40**'a düştü | **KAPAT-BAYAT** |
| **24f** SKILL.md↔kod bağı yok | "31 dosyada 3 atıf; `strategy.py`de `skills` geçen 0 satır" | `skills` modülünü 10 meridian modülü import ediyor; SKILL.md'ye 4 modül + app.js + 5 test atıf; `strategy.py`de 3 satır; `ENGINE_IMPLEMENTED` (skills.py:120) + skill_gorus evreni motor/SKILL.md ayrımını zaten yapıyor. Kalan çekirdek = 24h ile aynı boşluk | **BİRLEŞTİR → 24h** |
| **24g** sprint sızıntısı | sandbox canlının paylaşımlı skill dizinini buduyor (v242 ölçümü: 4 symlink söküldü) | Kapı (`sprint.kum_havuzunda` + `sync_agent_skills` atlaması) canlı kodda; **3 kum-havuzu koşumu** (08-14 · 08-15 · 08-22) sökümü BLOKE etti (`agent_skills_sync_atlandi_kum_havuzu`, n_sokulecek=4); canlı sync olayları 08-13'ten beri `pruned=[]`; `~/.hermes/skills` 30 symlink sağlam | **KAPAT-BAYAT** |
| **Ö-39** künye yolu kararı | kalibrasyon "hangi beyin isabetli"yi cevaplayamıyor; plan satırına alan = yasa değişikliği, Rol-1 YOL kararı bekliyor (ROADMAP §4-39) | Kusur canlıda BUGÜN de görünür: 08-16 backfill, Şubat/Nisan tarihli planlara damga vurdu (aylar-öncesine damga — zaman-yakınlık join'i yapısal yanlış). Karar taslağı aşağıda | **KAPAT-TASARIMDA** |

**SAYIM: KAPAT-BAYAT 4 · KAPAT-TASARIMDA 1 · BİRLEŞTİR 1 · ÖLÇ 0.**
(24e için koşullu kart-taslak §3'te — Rol-1 teşvik ölçümü isterse hazır; varsayılan öneri kapatmaktır.)

---

## §1 — 24c: "ANA DANIŞMA YOLU ÖLÜ" → KAPAT-BAYAT (kanıtlı)

788/385/1 iddiası **2026-08-13 tarihli ve penceresi 08-06..08-13** — o pencere bir **retry
fırtınasıydı**, sağlıklı hacim değil. Günlük kırım (canlı `agent_calls.jsonl`):

| gün | toplam | boş | dolu | not |
|---|---:|---:|---:|---|
| 08-09 | 156 | 155 | 1 | fırtına (12'şer deneme, boş dönüş) |
| 08-10 | 151 | 150 | 1 | fırtına |
| 08-11 | 150 | 150 | 0 | fırtına |
| 08-12 | 29 | 20 | 9 | dönüş başlıyor |
| 08-13 | 153 | 73 | 79 | v244 gecesi |
| 08-14 | 142 | 0 | **142** | yol dirildi |
| 08-16..21 | 9 | 0 | 8 (1 zaman-aşımı) | sağlıklı düşük tempo |

- Görüş üretimi **aynı-gün damgalanıyor**: `llm_opinions_stamped` 08-19 n=9 (date=08-19) ·
  08-20 n=2 · 08-21 n=5. `llm_calibration.json` `last_opinion_plan_date=2026-08-20`,
  `n_plans_with_opinion=374`; dosya mtime 08-21 20:32.
- En güçlü çürütme: bu "ölü" yol üzerinden danışman **2026-08-14T21:03:34'te TERFİ ETTİ**
  (`AUTHORITY_CHANGE` alarmı: "LLM danışman yetkisi AÇILDI — R farkı 0.638, n=100 çift").
- Son çağrı 08-21 Cuma 21:44; 08-22/23 hafta sonu — sessizlik seans-dışı.

**ÇAPRAZ ETKİ (Rol-1'e):** 24c, §5 KOVA-3/C4 **NOUS_MODEL** kaleminin güncel GEREKÇESİYDİ
("danışma yolu ölü"; ROADMAP:2234-2239 zaten "kalem KAPANMIŞ olabilir" notu taşıyor).
24c kapanırsa C4'ün son gerekçesi de düşer → C4 kapanış adayı, doğrulama Rol-1'de.
Not: son review'lar `nvidia/nemotron-3-super-120b` ile dolu döndü (08-21 satırı, 91 sn, 16 KB çıktı).

---

## §2 — 24d: PİLOT-S1 A/B → KAPAT-BAYAT (kanıtlı)

Tasarımın ÜÇ öncülü de bugün yanlış:

1. **"`n_pairs` 4→~51→~95, terfi tabanı 30 pilotla aşılır"** → taban pilotsuz aşıldı:
   `n_pairs=100` (taze pencere dolu), `promoted=true` 08-14'ten beri. Pilotun var oluş amacı
   (örneklem kuraklığını DOLGU yolundan kırmak) kendiliğinden gerçekleşti.
2. **"DOLGU yolu çalışan tek nokta"** → yol artık tek değil: review yolu dirildi (§1) ve
   deterministik görüş defteri (EDG-019 katmanı) canlı yazıyor (aşağıda §7). Ayrıca DOLGU
   vetosunun kendisi (`loop.py:1103-1122` `llm_veto_strip`) terfiden bu yana **0 kez ateşlendi**
   (olay defterinde hiç yok) — pilotun ölçeceği yüzey 9 günde tek örnek üretmedi.
3. **İki pilot skill'i** (`edge-strategy-reviewer`, `weekly-performance-digest`) bugün canlı
   review ön-yüklemesinde YOK (08-21 çağrısının `on_yukleme` listesi 7 tarayıcı/kapı skill'i)
   ve EDG-019 evreninde `llm_baglamli_motor_kosturmuyor` dışlamasındalar — A kolu tasarlandığı
   hâliyle bugünkü canlı yapılandırmaya oturmuyor.

Kalan meşru soru ("ön-yükleme görüş KALİTESİNİ değiştiriyor mu") 24b'nin zaten açık olan
ETKİ ölçümü penceresine devredilir (24b kilidi 08-22'de canlıda doğrulandı, etki ölçümü açık —
ROADMAP:182). Yeniden açılacaksa bugünkü tabandan YENİ ön-kayıt kartı ister; eski sayılarla
koşulamaz.

---

## §3 — 24e: ÇEKİMSER TEŞVİKİ → KAPAT-BAYAT (kanıtlı) + kök parametreler

**"Terfinin ASIL duvarı" iddiası canlıda çürüdü** — iddia yazıldıktan BİR GÜN sonra:

| ölçüm | 2026-08-13 (kaynak :24, :169) | 2026-08-23 (canlı) |
|---|---|---|
| `n_pairs` | 4 | **100** |
| destekle / karşı / çekimser | 0 / 1 / 3 | **10 / 14 / 76** |
| `r_gap` | null | **0.857** (destekle +0.396 · karşı −0.461) |
| `promoted` | false | **true** (2026-08-14T21:03'ten beri) |

**Bugünkü çekimser oranı:** taze-100 kalibrasyon çiftinde **%76**; tüm görüşlü planlarda
(DB, 379 plan) çekimser 278 = **%73**; **son-30g plan görüşlerinde 25 görüşün 10'u = %40**
(karşı 11, destekle 4) — yeni modelle çekimserlik belirgin düşüyor. Bağlayıcı kısıt teşvik
değil HACİMdi: yol dirilince kovalar doldu ve kapı açıldı.

**Kök parametreler (mekanizmanın tamamı 4 kod noktası; config/state'te ayar YOK):**
1. `meridian/analytics.py:1125-1128` — `LLM_PROMOTE_MIN_PAIRS=30` · `LLM_PROMOTE_MIN_BUCKET=8`
   · `LLM_PROMOTE_R_GAP=0.3` (modül sabitleri).
2. `meridian/analytics.py:1169-1173` — `r_gap` yalnız destekle/karşı'dan kurulur; çekimser
   kovası sayılır ama karara girmez.
3. `meridian/hermes.py:3797-3798` — `_opinion_history` karnesi: çekimser → **"nötr"**
   (asla "yanılgı" olmaz). **Teşvikin tek davranışsal kod noktası budur.**
4. `meridian/hermes.py:3687, 3910` — istem üç seçeneği eşit sunar.

**Ölçülebilir TEK kaldıraç: (3)** — karne eşlemesi tek satırdır ve etkisi doğrudan çekimser
payıyla ölçülür. (1)/(2)'yi oynamak eşik icadıdır (kart + Rol-1 ister).

**Koşullu kart-taslak (yalnız Rol-1 teşvik ölçümü İSTERSE; varsayılan öneri: kapat):**
`family: llm_cekimser_tesviki` · müdahale: karnede çekimsere "kaçırılan R" satırı (hermes.py:3797
eşlemesi) · metrik: çekimser payı (taban: son-30g %40 / taze-100 %76) + kova doluş hızı +
`r_gap` istikrarı · başarı: çekimser payı ≥10 puan düşer VE `r_gap≥0.3` korunur · `n_min=30`
görüş/pencere · kill: `r_gap<0.3`e düşerse geri al · **K += 1** (tek müdahale, grid yok).

---

## §4 — 24f: SKILL.md↔KOD BAĞI → BİRLEŞTİR (→ 24h)

Sayısal kanıt bayatladı: bugün `skills` modülünü **10** meridian modülü import ediyor
(api · backtest · counterfactual · hermes · scheduler · reflect · skill_gorus · loop ·
skill_evolve · skills); SKILL.md'ye 4 modül + `web/app.js` + 5 test dosyası atıf yapıyor;
`strategy.py`de "skill" geçen 3 satır var (712, 1022, 1059 — per-skill avg_r ve registry
bayrakları üstüne). `skills.py:120 ENGINE_IMPLEMENTED` "motor FİİLEN neyi koşturuyor"
dürüstlük kümesini tutuyor ve skill_gorus evreni SKILL.md-klasörü (`llm_baglamli_motor_
kosturmuyor`) ile motor-koşturulan skill'i zaten ayırıyor.

**Kalan çekirdek** — SKILL.md İÇERİĞİNİN motor davranışına sözleşme/test bağı yok ve 93 bayrak
motor registry'sine bağlı değil — **24h "skill rozeti damgası" kalemiyle AYNI yapısal boşluk**
(C10: "En acil damga"). İki kalemi ayrı tutmak aynı işi iki satırda saymaktır.
**Öneri: 24f gövdesi 24h'ye katlanır** (kanıt satırları güncellenerek); WP7 listesi bir kalem kısalır.

---

## §5 — 24g: SPRINT SIZINTISI → KAPAT-BAYAT (kanıtlı — düzeltme İNDİ ve canlıda 3 koşumda tuttu)

v242 düzeltmesi (`sprint.kum_havuzunda` yapısal ölçütü + `hermes.sync_agent_skills` B-yolu
atlaması) canlı kodda dağıtık (`/opt/meridian/meridian/hermes.py` doğrulandı). Canlı kanıt:

| kum havuzu koşumu | olay | sonuç |
|---|---|---|
| 20260813-202316 → 08-14 02:34/02:35 | `agent_skills_sync_atlandi_kum_havuzu` (warn) | söküm BLOKE: `n_sokulecek=4` (dört `fmp=req` skill — 08-13 vakasının birebir senaryosu) |
| 20260814-220214 → 08-15 04:35 (×2) | aynı | söküm BLOKE, n_sokulecek=4 |
| 20260821-220656 → 08-22 04:42/04:43 | aynı | söküm BLOKE, n_sokulecek=4 |

- Canlı (ana) `events.jsonl`'da 08-13'ten beri tüm `agent_skills_synced` olayları `pruned=[]`;
  onarım döngüsüne gerek kalmadı.
- `~/.hermes/skills` bugün: 46 girdi, **30 symlink** — 08-13 onarım sonrası kapsamla aynı.
- Sızıntının yeniden üretileceği koşul (kum havuzunda FMP anahtarı yok → küçük enabled set)
  üç koşumda da OLUŞTU ve kapı üçünde de tuttu — bu bayrak testi değil, gerçek-koşul kanıtıdır.

---

## §6 — Ö-39: KÜNYE YOLU KARARI → KAPAT-TASARIMDA (taslaklı)

Kusur bugün de canlı: 08-16'da `backfill_opinions` **2026-02-26 ve 2026-04-14 tarihli planlara**
damga vurdu (olay defteri: `llm_opinions_stamped` date=2026-02-26 n=1 · date=2026-04-14 n=2) —
"bugünkü çağrıyla aylar öncesine damga" tam ROADMAP §4-39'un tarif ettiği yapısal yanlış.
Terfi 08-14'te açıldığı için soru artık akademik de değil: yetkili danışmanın HANGİ model
olduğu kalıcı hiçbir defterde yok.

**TASLAK KARAR (Rol-1 onayına):** ROADMAP'teki iki yoldan **(b) ayrı atıf defteri** — yasa
korunur (`test_authority_boundaries_v77::test_c3`: `llm_opinion` plan satırında TEK anahtar kalır):

- Yeni append-only defter `state/plan_atif.jsonl`; satır:
  `{ts, plan_id, ticker, plan_date, kind, model, model_istenen, model_kaynagi, iz_id, backfill:bool}`
  — `iz_id` `agent_calls.jsonl` join anahtarı; `backfill=true` damgası zaman-yakınlık join'ini
  yapısal olarak düzeltir (geriye-damga artık kendini beyan eder).
- Yazan tek yer: `hermes._stamp_llm_opinions` + `backfill_opinions` (aynı çağrı içinde, kilitli).
- `ledgers.CONTRACTS` kaydı + tüketici İLK GÜNDEN: `analytics.llm_opinion_calibration`
  kovalarına model-kırılımı ekler (uyuyan-yol dersi: okuyucusuz defter açılmaz).
- **Aynı tura katlanacak künye ailesi:** WP7-31a (`hermes.py:3987` tek satır
  `cevap_veren_model()`) · WP7-31b (`active_model()` uydurma koruması) · WP7-40
  (`nous_eval` künye alanları, XS) — dört kalem tek "künye turu" olarak kapanır.
- Ölçüm çapası hazır: `tests/test_zincir_kunye_v246.py::test_n1` donduruyor; yeni defterin
  kapsam testi aynı dosyaya eklenir.

---

## §7 — KAPSAM DIŞI ama Rol-1'in GÖRMESİ GEREKEN canlı bulgular (bu turda işlem yapılmadı)

1. **EDG-2026-019 katmanı ÇALIŞIYOR ve kill#1 tetiklenmiş görünüyor — hüküm işlenmemiş.**
   Kart yorumu "ölçüm kodu HENÜZ YAZILMADI" derken `meridian/skill_gorus.py` v218'de (08-09,
   `251911e`) yazılmış; canlıda 5.500 görüş satırı (son: 08-21), iki yüzey. Rapor: terfi adayı
   **vcp-screener/cikis** (n=1260, etki +0.1228, p=0.0078, FDR-sağkalan) · emeklilik işareti
   **stockbee-exhaustion-hammer/cikis** (n=177, etki −0.2319, FDR-sağkalan NEGATİF, pencere
   sayacı yok). AMA `skill_gorus_durum.json` kill_p95: **"KILL"** (p95_pay 6.57 > tavan 0.10;
   10 örneğin 10'u pay>1.0). Kart: "katman KAPATILIR". Kod hükmü kaydediyor, uygulamıyor —
   **kill hükmünün işlenmesi (ya da pay ölçüm düzeneğinin Rol-1 denetimi) açık iş.**
   Kart `status: registered`'da duruyor; ölçüm ajanı karta dokunmaz kuralı gereği doğru,
   ama Rol-1 hüküm işleme adımı görünmüyor.
2. **Terfi yaşıyor ama vetosu hiç ateşlenmedi:** `promoted=true` 9 gündür; `llm_veto_strip`
   olayı 0. REVIEW+karşı kesişimi hiç oluşmamış olabilir — bir sonraki turda tek satırlık
   sayımla ayrıştırılabilir (kaç REVIEW planı karşı görüş aldı?).
3. Bu dokümandaki tüm sayılar tek oturumluk salt-okuma ölçümdür; Pazartesi seansı 08-22/23
   sessizliğinin kadans mı arıza mı olduğunu kesinler (§1 notu).
