# KEŞİF — WP-U (Evren/PIT Cephesi) · 2026-08-09

> **Rol:** keşif/ölçüm ajanı. **SALT ÖLÇÜM + PLAN.** Bu belge HÜKÜM taşımaz, KART oluşturmaz,
> koda/`state`'e/canlıya DOKUNMAZ. Kart oluşturma + hüküm Rol-1'e aittir.
> **Yazılan tek dosya bu belgedir.** Canlı sondaj SALT-OKUNUR yapıldı (ssh, `state/` okundu,
> `/api` sorgulandı; hiçbir yazım yok).
> **UYDURMA YASAĞI:** her sayı kaynağıyla verilir; ölçülemeyen açıkça `ÖLÇÜLEMEDİ`.

---

## 0. Özet (dönüş satırı)

- **Mevcut evren sayısı:** **251** (canlı `universe_drift.json` = 251; `REPLAY_UNIVERSE` literali =
  251). **Canlı evren ile `REPLAY_UNIVERSE` AYNI kümedir** — aralarında fark YOK; "251" zaten
  `REPLAY_UNIVERSE`'ün kendisidir.
- **En büyük operatör-bloğu (canlı kanıt-zinciri için):** **`FINVIZ_API_KEY` (Elite) YOK** →
  evren genişletme kolu **%100 ölü**: `finviz_unavailable` olayı **3.746 kez**, başarılı keşif
  (`finviz_universe`) **0 kez** (2026-07-14 → 2026-08-09). Evren hiç 251'in üstüne çıkmıyor.
  (Stratejik/kanıt-kalitesi ekseninde en değerli blok ise **delist-bar kaynağı kararı** — §4-B2.)
- **Otonom-başlanabilir ilk kart (öneri):** **EDG-2026-022 — "Evren bağlayıcı kısıt mı?"** —
  mevcut replay'de işlem üretimini bağlayan şey aday-havuzu (evren) mü yoksa `eff_max_open`
  (de-risk pozisyon tavanı) mı? Altyapı HAZIR (`backtest.py`: `candidate_log` + `eff_max_open` +
  `plan_log`). Operatör-bloğu YOK, yeni bar GEREKMEZ. Bu kart **tüm WP-U harcamasını de-risk eder**
  (FINVIZ parası, evren gerçekten bağlıyorsa harcanır).
- **Belge yolu:** `docs/KESIF-WP-U-2026-08-09.md`

---

## (i) MEVCUT DURUM ÖLÇÜMÜ

### 1.1 Evren sayısı ve paydanın kaynağı

| ölçüm | değer | kaynak |
|---|---|---|
| `REPLAY_UNIVERSE` (elle bakımlı liste) | **251** (tekil 251) | `meridian/adapters/data.py:2528` literalinden sayıldı |
| Canlı servis edilen evren | **251** | canlı `state/universe_drift.json` (`universe: 251`) |
| `RETIRED_SYMBOLS` (emekli defteri) | 8 | `data.py:2606` (ANSS, DFS, FI, HES, IPG, K, PARA, WBA) |
| Evrende emekli sembol | **0** | `universe_drift.json` (`retired_in_universe: []`) |
| Canlı `state/bars/*.csv` | 260 | = 251 evren + SPY + 8 emekli kalıntı (ssh sayımı; `CIFT-KAYNAK-TARAMASI-2026-08-09.md` #4 ile tutarlı) |

**Payda nereden geliyor:** Tek kaynak `REPLAY_UNIVERSE` literali (`data.py:2528`). Pano/kapsama
paydası `constituents.py:223,228` → `len(REPLAY_UNIVERSE)`. Liste **operatör kararıyla elle**
büyütülmüş: 2026-07-20 (40→250 likit S&P isim, sektör-dengeli), 2026-07-30 (+FISV → 251).
Tarihsel 259 payda, 8 emekli çıkınca 251'e indi (ENG-LOG:482).

**"REPLAY_UNIVERSE vs canlı 251" sorusunun cevabı:** ayrım YOK. Canlı ileri-yürüyen tarama
(`dataset.load_live`, `dataset.py:201`) tanımı gereği `REPLAY_UNIVERSE + Finviz keşfi`dir; Finviz
katkısı bugün **sıfır** (aşağıda), dolayısıyla canlı evren = `REPLAY_UNIVERSE` = 251.

### 1.2 Finviz / evren genişletme kolu — %100 ölü (ölçüldü)

| ölçüm | değer | kaynak |
|---|---|---|
| `finviz_unavailable` olay sayısı | **3.746** | canlı `events.jsonl` (2026-07-14 → 2026-08-09) |
| `finviz_universe` (başarılı keşif) | **0** | canlı `events.jsonl` (grep-count) |
| Canlı `finviz_universe.json` (bugün) | `source:none, n:0` | 2026-08-09T14:15:58Z |
| Poll kadansı | 300 sn | systemd `CYCLE_POLL_SECONDS=300` → ~144 ölü tur/gün |
| `FINVIZ_API_KEY` | **YOK** | canlı `state/secrets.json` anahtar-ad listesi (değer okunmadı) |

**Yorum:** Evren genişletme kolu üretimde **hiç çalışmadı**. Her tur `discover_universe()` boş
dönüyor (`finviz.py:263`), evren dürüstçe `REPLAY_UNIVERSE`'e iniyor. Neden: Elite token yok →
Elite yolu atlanıyor; public HTML scraping otonom döngüde varsayılan KAPALI
(`MERIDIAN_FINVIZ_PUBLIC` set değil, `finviz.py:222`, ToS/ban koruması). Bu bir arıza değil,
tasarlanmış dürüst-bozunma — ama **stratejik sonuç:** evren kalıcı olarak 251.

### 1.3 Bar kaynağı ve arşiv (evrenin "beslenme" tarafı)

| ölçüm | değer | kaynak |
|---|---|---|
| Yerel bar arşivi derinliği | **2004-01-02 → 2026-08-07** (AAPL 5.686 satır) | canlı `state/bars/aapl.csv` |
| Massive modu | `yalniz_alarm` (write_enabled=**false**) | canlı `events.jsonl` massive_mode 2026-08-09 |
| Massive grouped son durum | **HTTP 403** (2026-08-06, 08-07) | canlı `massive_grouped_failed` |
| `MASSIVE_API_KEY` | VAR | canlı `secrets.json` |
| FMP anahtarları | `FMP_API_KEY`, `FMP_API_KEY_2` VAR | canlı `secrets.json` |
| FMP bugün | **7 çağrı / 7 başarısız** | canlı `state/fmp_usage.json` (2026-08-09) |
| FMP kota tavanı (ölçülen) | ~250/gün/anahtar (509./510. çağrıda 429) | `fmp_usage.json` quota_hits 2026-08-03 |

**Yorum:** Bar zinciri şu an Massive grouped'a **güvenmiyor** (403 + `yalniz_alarm`); fiili yol
FMP→Cboe→Nasdaq→Alpaca. FMP ücretsiz katman ~250/gün/anahtar; 251 sembolün tek tam tazelemesi
kotanın tamamını yakıyor (ENG-LOG + `fmp_usage`). **2004+ derin arşiv YERELDE DURUYOR** ama
ROADMAP:448'e göre Massive planı artık ~son 2 ayı veriyor → arşiv **yeniden üretilemez kalıntı**
(yedek zinciri kritik). Bu WP-D'ye komşu, ama evren genişlemesinin bar-maliyeti burada görünür.

### 1.4 PIT (point-in-time) fundamentals — büyük ölçüde ÇÖZÜLDÜ

| bileşen | durum | kaynak |
|---|---|---|
| shares-outstanding (as-of) | **ÇÖZÜLDÜ, operatörsüz (2026-08-01, EDGAR)** | `research/edgar_facts/shares_outstanding.csv.gz` (161.856 satır, 258 sembol) |
| fundamentals (issuance/profitability) | veri indi (2026-08-01) | `fundamentals.csv.gz` (142.499 satır, 258 sembol) |
| PIT canlı köprüsü | kablolu, salt-okunur | `meridian/adapters/edgar_shares.py` (`filed<=t` kuralı) |
| EDG-016 turnover21 hükmü | **SUCCESS** (PIT-temiz) | kart EDG-2026-016; `edgar_shares.py:10` |
| kapsama | 251 evrene ölçekli (188–250/tag) | `research/edgar_facts/kapsam.csv` (`sembol_n_evren251`) |

**PIT-temiz olan (EDGAR `filed<=t`):** dolaşımdaki hisse (dei:EntityCommonStockSharesOutstanding
medyan 7g gecikme; us-gaap:CommonStockSharesOutstanding ~36g), ve fundamentals aile (Assets 250/251,
GrossProfit 122, Revenues 204, CostOfGoodsAndServicesSold 138…). Net-issuance (hisse Δ) ve gross
profitability (GrossProfit/Assets) bu setle PIT-doğru kurulabilir.

**PIT-EKSİK / YASAK proxy (CLAUDE.md md.4 "PIT'siz fundamentals proxy YASAK"):** satıcı "güncel"
fundamentals (FMP `profile`/gelir tablosu — `filed` tarihi taşımaz) proxy olarak **kullanılamaz**.
Analist-tahmin / NLP (ROADMAP 3.3) hâlâ veri-bileti (operatörde), proxy yasak.

**Önemli:** PIT fundamentals kapsaması evrenle birlikte ölçekleniyor — evren büyürse yeni semboller
için `research/edgar_facts/betikler/` (aylık, operatörsüz) yeniden çekiyor. Yani PIT-fundamentals
evren genişlemesinin **bloğu değil**, evrene **bağlı otonom kol**.

### 1.5 PIT evren üyeliği ve delist-bar kilidi (SERT KISIT)

| ölçüm | değer | kaynak |
|---|---|---|
| S&P500 tarihî üyelik | 1996 → 2026, hazır (ücretsiz, MIT) | `research/pit_universe/sp500_uyelik_tarihi.csv` (2.718 satır) |
| — kapsam | yalnız large-cap (günlük üye 487–507, **boyut alanı YOK**) | RAPOR_018 §1 |
| S&P400/600 ücretsiz PIT seti | **BULUNAMADI** | ROADMAP:436; RAPOR_018 §1 |
| dosyada hiç görünmüş isim | 1.206 | RAPOR_018 §2 |
| çıkmış isim | 703 | RAPOR_018 §2 |
| çıkmış **ve** arşivde barı olan | **12 (%1,71)** | RAPOR_018 §2 |
| EDG-016 panel penceresi kayıp oranı | **%96,57** (338/350 sıfır bar) | RAPOR_018 §4 |

**Kilit:** Survivorship-serbest BACKTEST evreni için delist edilmiş isimlerin tarihsel barları
gerekir; yerel arşivde **%96,57 boşluk** var. Bu, tüm mid/small-cap ve delist-isim kenar
ölçümlerine **kalıcı yukarı-yanlılık** bindirir. EDG-2026-018 (PIT mid-cap) tam bu yüzden
`askiya_veri_kapisi` (feasibility gate: 12 < 40 isim).

---

## (ii) AÇIK İŞ ENVANTERİ

| # | kalem | tür | durum | blok |
|---|---|---|---|---|
| U1 | Finviz canlı evren genişletme | operatör-bloklu | %100 ölü (3.746 boş tur) | `FINVIZ_API_KEY` |
| U2 | EDG-2026-018 PIT mid-cap yeniden açılış | operatör-bloklu | `askiya_veri_kapisi` | delist-bar kaynağı VEYA S&P400/600 PIT+bar |
| U3 | Survivorship-serbest backtest evreni | operatör-bloklu | %96,57 delist boşluğu | delist-bar kaynağı (Massive↑ / QC) |
| U4 | Evren gerçekten bağlayıcı kısıt mı? (tanı) | **OTONOM** | ölçülmedi | YOK (öneri: ilk kart) |
| U5 | S&P500 sağkalan kapsama/bar-edinilebilirlik haritası | **OTONOM** (altyapı) | ölçülmedi | YOK (FMP kota dikkatli) |
| U6 | `universe_drift`/constituents sağlık kontrolü kırık | altyapı-bug | canlıda `ImportError: lxml` | (kod bacağı — bu turda DEĞİŞTİRİLMEZ) |
| U7 | Massive grouped 403 + plan ~2 ay derinliği | operatör-bloklu | WP-D komşu | Massive plan kararı |
| U8 | S&P400/600 PIT üyelik kaynağı | operatör-bloklu | yok | ücretli set / SEC 13(f) CUSIP→ticker emeği |

> **U6 notu:** canlı `universe_drift.json` `status:"unknown", reason:"ImportError: Import lxml
> failed"`. Constituents çapraz-kontrolü (güncel endeks üyeliğiyle bayat-sembol taraması) canlıda
> lxml eksikliğinden koşmuyor. Bu bir **kod/dağıtım bacağı** (bağımlılık) — evren sayısını
> değiştirmiyor (251 doğru) ama "evren güncel mi" bekçisini kör bırakıyor. Bu keşif turu
> `meridian/`'a dokunmadığı için yalnız RAPORLANIR; düzeltme Rol-1/dağıtım kararı.

---

## (iii) KART GEREKSİNİMLERİ

**Ayrım (CLAUDE.md md.3):** ölçüm kodu = ön-kayıt kartı ister; altyapı = istemez.

| iş | kart ister mi? | gerekçe |
|---|---|---|
| Evrene isim/bar EKLEME (Finviz kablolama, sağkalan ekleme) | **HAYIR (altyapı)** | kenar ölçmez; yalnız aday havuzunu genişletir. Kararı Finviz/kapı DEĞİL, Meridian'ın kendi yasası verir |
| Kapsama/bar-edinilebilirlik HARİTASI (U5, EDG-018 ADIM-0 emsali) | **HAYIR (altyapı/fizibilite)** | sayı üretir, hüküm üretmez; operatör kararına girdi |
| Genişlemiş evrende KENAR yeniden ölçümü (turnover/trend/…) | **EVET (kart)** | "aynı kenar büyük evrende de var mı" bir edge iddiasıdır; K-muhasebeli |
| **Evren bağlayıcı kısıt mı? (U4)** | **EVET (kart)** | hipotez + kill + K var; ölçüm kodu koşar → önce kart |
| EDG-2026-018 | **ZATEN VAR** (yazılı, askıda) | yeniden açılış şartı: delist-bar kaynağı geldiğinde |

**Kart-ÖNCE olanlar:** U4 (tanı) ve genişlemiş-evren kenar yeniden ölçümleri. Bunlar ölçüm
kodudur → `research/cards/` ön-kayıt kartı olmadan kod YOK.

---

## (iv) OPERATÖR-BLOKLARI (adıyla)

### B1 — `FINVIZ_API_KEY` (Finviz Elite token)
- **Ne açar:** canlı ileri-yürüyen taramada momentum/kırılım aday evrenini (`finviz.py` PRESETS:
  hacim>500K, fiyat>$10, SMA50 üstü, 13h perf>+10%; tavan 150 isim). Evreni 251'in ÜSTÜNE çıkarır.
- **Olmadan ne çalışmaz:** evren genişletme kolu **%100** (ölçüldü: 3.746 boş tur / 0 başarı).
- **Maliyet:** ücretli abonelik (1 haftalık Elite trial da mümkün). Ek: eklenen isimlerin barı FMP
  zincirinden gelir → FMP kotasını yer (B3 ile etkileşir).
- **Kanıt:** ROADMAP §6.1 `FINVIZ_API_KEY` satırı; canlı `finviz_universe.json`/`events.jsonl`.

### B2 — Delist-bar kaynağı kararı (survivorship-serbest evrenin TEK meşru yolu)
- **İki tamamlayıcı yol (rakip DEĞİL — `QC-ENTEGRASYON-DEGERLENDIRMESI.md`):**
  - **(a) QuantConnect platform-içi ölçüm:** BEDAVA; AlgoSeek delist-dahil evren + Security Master;
    ölçüm platformda koşar, dışarı yalnız **hüküm-sayısı** taşınır. **SERT ToS:** barlar dışarı
    ÇIKARILAMAZ → yerel arşiv boşluğu QC ile DOLDURULAMAZ, yalnız ÖLÇÜLEBİLİR.
  - **(b) Massive plan yükseltmesi:** yerel arşivin + kendi motorumuzun tek meşru derin-tarih yolu.
- **Ne açar:** EDG-018 yeniden açılışı + tüm kenar ölçümlerinin delist-dahil/mid-cap evrende
  survivorship-serbest tekrarı.
- **Olmadan ne çalışmaz:** %96,57 delist boşluğu kalıcı; mid/small-cap kenar hükümleri "üst-sınır"
  şerhiyle sınırlı; EDG-018 askıda.

### B3 — FMP plan yükseltmesi
- **Ne açar:** insider Form-4 sayfalama + `search` ucu (ROADMAP §6.1, ücretsizde 402); ayrıca
  evren genişlerse bar-tazeleme kota baskısını hafifletir.
- **Olmadan ne çalışmaz:** insider derinliği (Y4); geniş evrende günlük bar tazelemesi kota-riskli.

### B4 — Massive plan (grouped 403 + ~2 ay derinlik)
- **Ne açar:** tek-çağrı grouped-daily bar yazımı (yalniz_alarm → yazım) + 2 aydan derin tarih.
- **Olmadan ne çalışmaz:** bar zinciri FMP kotasına bağımlı kalır; derin arşiv yeniden üretilemez
  (WP-D komşu — yedek kritik).

### B5 — S&P400/600 PIT üyelik kaynağı
- **Ne açar:** mid/small-cap PIT evren (large-cap S&P500'ün ötesi).
- **Olmadan ne çalışmaz:** PIT mid-cap kohortu kaynaktan İNMİYOR (RAPOR_018 §1) — ücretsiz set yok;
  SEC 13(f) (2004Q1+) CUSIP→ticker emeği ya da ücretli set gerekir.

> **En büyük blok — iki eksende:**
> - **Canlı kanıt-hızı ekseni:** **B1 (`FINVIZ_API_KEY`)** — en ucuz, en doğrudan, ölçülmüş %100
>   etkili. (Şartlı — bkz. §v U4.)
> - **Kanıt-KALİTESİ / strateji ekseni:** **B2 (delist-bar kaynağı)** — en stratejik, en pahalı;
>   survivorship yanlılığını yapısal olarak çözen tek yol.

---

## (v) ÖNCELİKLENDİRİLMİŞ PLAN (otonom vs bloklu)

### A) OTONOM — kod bacağı, kart-disiplinli, operatör-bloğu YOK

**A1 (İLK KART — öneri EDG-2026-022): "Evren bağlayıcı kısıt mı?"**
- **Soru:** mevcut replay'de işlem üretimini bağlayan şey aday-havuzu boyutu (evren) mü, yoksa
  `eff_max_open` de-risk pozisyon tavanı mı?
- **Neden ÖNCE:** ROADMAP:425 ölçtü — de-risk rampası günlerin **%92**'sinde aktif, çoğu gün izin
  **1 pozisyon** ("işlem üretiminin gizli boğucusu"). Yani "dar evren → az işlem" zincirinin
  **rakip bir kök nedeni** var. Evren tavanı değilse, B1'e (FINVIZ parası) harcamak boşa gider.
  Bu kart **operatör harcamasını de-risk eder.**
- **Fizibilite (ölçüldü):** altyapı HAZIR — `backtest.py:107,202` `candidate_log` (günlük taranan
  ilk adaylar) + `:224` `derisk_mult` + `:225` `eff_max_open` + `plan_log`. Yeni bar GEREKMEZ.
- **Kill/hüküm iskeleti (Rol-1 yazar):** "aday_sayısı > eff_max_open" olan günlerin oranı yüksekse
  → tavan bağlıyor, evren DEĞİL → WP-U evren-genişlemesi WP-R rampasına kadar ertelenir. Düşükse →
  evren bağlıyor → B1/B2 önceliklenir.
- **K:** tek tanı hücresi (grid değil).

**A2: S&P500 sağkalan kapsama + bar-edinilebilirlik haritası (altyapı — kart istemez)**
- Güncel S&P500 üyelerinden (503) `REPLAY_UNIVERSE`'de OLMAYANLARIN sayısı + barlarının mevcut
  zincirden (FMP/Massive/Alpaca) edinilebilirliği. EDG-018 ADIM-0 emsali (kapsama haritası).
- **Not:** bu isimler sağkalan → BACKTEST için survivorship-temiz DEĞİL; ama **canlı ileri
  trading** için meşru (ileri-yürüyüş yanlılık taşımaz). Yani A2 ancak A1 "evren bağlıyor" derse
  ve/veya canlı aday havuzunu büyütmek için değerli.
- **FMP kota uyarısı:** toplu bar çekimi kotayı yakar → küçük parti + kart-dışı ölçüm.

**A3: EDGAR facts tazeleme (zaten otonom)**
- `research/edgar_facts/betikler/` aylık; evren büyürse yeni sembolleri operatörsüz kapsar. PIT
  fundamentals bloğu YOK — evrene bağlı kol.

**Sıra:** A1 → (A1 sonucuna göre) A2 / A3.

### B) OPERATÖR-BLOKLU — karar/aksiyon operatörde

| sıra | blok | tetik-şartı | kazanım |
|---|---|---|---|
| B1 | `FINVIZ_API_KEY` | **A1 "evren bağlıyor" derse** | canlı aday evreni 251→+150'ye kadar; kanıt-hızı |
| B2 | delist-bar kaynağı (QC ölçüm / Massive↑) | stratejik karar | survivorship-serbest kenar ölçümü; EDG-018 açılır |
| B3 | FMP plan↑ | evren büyürse / insider derinliği | kota rahatlar; Y4 |
| B4 | Massive plan (403 + derinlik) | WP-D ile birlikte | bar zinciri sağlamlığı; derin arşiv |
| B5 | S&P400/600 PIT kaynağı | mid-cap hedeflenirse | PIT mid/small-cap evren |

### "Evren şu kadar genişlerse işlem/kanıt şu kadar artar" tahmini

**ÖLÇÜLEMEDİ — ve bilerek uydurulmuyor.** İki gerekçe:
1. **Rakip boğucu:** de-risk rampası günlerin %92'sinde pozisyonu 1'e kısıyor (ROADMAP:425). Aday
   havuzu büyüse bile tavan bağlıysa işlem sayısı ARTMAYABİLİR. Çarpanı ölçmeden yazmak uydurma
   olurdu.
2. `REPLAY_UNIVERSE:2540` yorumu "tüm kanıt motorları evrenle DOĞRUSAL ölçeklenir" DER; bu bir
   **iddia**, ölçüm değil — ve (1) ile potansiyel çelişir.

**Bu belirsizliğin çözümü A1 kartıdır.** A1, çarpanı (aday↔tavan bağlanması) mevcut veriyle
ölçer; ancak ondan sonra "evren +X → işlem +Y" sayısı UYDURMADAN verilebilir.

---

## Ek — sondaj kaynakları (salt-okuma)

- Kod: `meridian/adapters/{data.py,finviz.py,fmp.py,constituents.py,edgar_shares.py}`,
  `meridian/dataset.py`, `meridian/secrets.py`, `meridian/backtest.py`.
- Kart/rapor: `research/cards/EDG-2026-018-*.yaml`, `research/olcumler/wp_u_midcap/RAPOR_018.md`,
  `research/edgar_facts/{README.md,kapsam.csv}`, `research/pit_universe/`.
- Doküman: `ROADMAP.md` (WP-U §433, §6/§6.1), `docs/QC-ENTEGRASYON-DEGERLENDIRMESI.md`,
  `docs/CIFT-KAYNAK-TARAMASI-2026-08-09.md`, `MERIDIAN_ENGINEERING_LOG.md`.
- Canlı (ubuntu@130.61.126.87, `/opt/meridian`): `state/{universe_drift,finviz_universe,fmp_usage,
  secrets}.json` (secrets: yalnız anahtar-adları), `state/events.jsonl` (olay sayımları),
  `state/bars/` (derinlik), systemd `meridian.service` (env adları), `/api/*` rota listesi.
