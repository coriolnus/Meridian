# TASARIM-KANITI — 23e gün-içi pencere · 23f `gap_behavior: cancel` · 13 scale-out (2026-08-22)

**Ne bu belge:** WP1 sırası ⑦'nin (ROADMAP.md:113) üç kalemi için H0→H1 hazırlığı: her kalemin
gerçek borcu KOD + ROADMAP + donmuş defterlerden çıkarıldı, kalemlerin dayandığı sayılar yeniden
ölçüldü ya da birincil artefakttan doğrulandı. **HÜKÜM YOK** — kart/karar Rol-1'in; buradaki
"kart-adayı envanteri" bölümleri ÖNERİ değil, halihazırda beyan edilmiş eşik/kill/canlanma-koşulu
kalemlerinin dökümüdür.

**Kanıt kaynakları:**
- Kod: `meridian/broker.py`, `meridian/loop.py`, `meridian/backtest.py`, `meridian/bararchive.py`,
  `meridian/barsarchive.py`, `meridian/marketstream.py`, `meridian/codelaw.py`,
  `meridian/shadow_lifecycle.py`, `meridian/config.py`, `meridian/adapters/alpaca.py`.
- Donmuş defterler (yazılmadı, yalnız okundu):
  `research/olcumler/exe003_golge_kapsam_2026-08-22/canli_state/` (2026-08-22 14:11 çekimi:
  `entry_execution.jsonl` 30 satır · `meridian.db` immutable-ro açıldı · `goal.yaml` · `bounds.yaml`),
  `research/olcumler/e1_grid_2026-08-03/sonuc_e1.json`, yerel `state/trades.jsonl` (95 satır,
  2026-07-23 damgalı replay-dönemi kopyası).
- Canlı salt-okuma çekim (ssh-stdin, emsal exe007; damga **2026-08-22T18:42:33Z**): intraday arşiv
  kapsamı + E2/işlem defteri güncel sayıları + `strategy.yaml`/`goal.yaml` yürürlük değerleri.
  Betik scratchpad'de koştu, canlıya ve repoya dosya yazılmadı.
- Belgeler: `docs/ARASTIRMA-SLIPAJ-AZALTMA-2026-08-13.md` (§B.4/§C.2/§C.3/§D),
  `research/cards/EXE-2026-001-entry-execution.yaml`, `research/cards/EDG-2026-029-scaleout-duzeltilmis.yaml`,
  ROADMAP.md:113 · :198 · :328 · :405-410 · :417-424.

---

## 1 · 23e — GÜN-İÇİ PENCERE (altyapı kalemi)

### 1.1 Bugünkü davranış (kod satırıyla)

- **Canlı gönderim kadansı EOD'dir:** E1 icra girdileri sinyal barı **kapanışında** sabitlenir,
  "emir kapanıştan SONRA, ertesi açılıştan ÖNCE gider" (`loop.py:1876-1897`; donmuş E2 defterinde
  gönderim damgaları ~22:10Z). Gün-içi bir gönderim penceresi ne canlıda ne replayde vardır.
- **Replay dolumu ertesi günün AÇILIŞIDIR:** `backtest.py:332`
  (`broker.fill_entry(plan, per[t].loc[d, "open"], ...)`); iç motor dolum fiyatı
  `base_fill = next_open * (1.0 + self.slip)` (`broker.py:611`). Gün-içi an kavramı motorda yok —
  tek istisna 23c'nin `bar_low` parametresi (`broker.py:583-596`), o da günlük barın `low`u.
- **Veri tabanı günlük bardır:** `state/bars/*.csv` şeması `date,open,high,low,close,volume`
  (yerelde 260 dosya; ör. `aapl.csv` 5.680 satır, 2004-01-02'den). Tek çekim yolu
  `adapters/alpaca.py:1476-1492`: `/v2/stocks/bars` **`timeframe=1Day`**. `meridian/*.py` içinde
  başka bir bar-timeframe çekimi yok (tek "timeframe" geçişleri haftalık yeniden-örnekleme,
  `indicators.py:291`). **Tahta notu "bugünkü replayde MODELLENEMEZ — 1Day tek yol" kod düzeyinde
  doğrulandı.**

### 1.2 Kalemin dayandığı ölçülmüş fayda/bedel (ARASTIRMA-SLIPAJ §B.4/§C.2)

- Açılış bandı 13:30–13:45 UTC'de **146,7 bps**, 13:45–14:00'te **84,9 bps (−%42)**,
  14:00–14:15'te 74,3 bps (n=401 sembol-seans, konsolide 15-dk barlar).
- Bedel (koşulsuz): 15 dk bekleme ≈ **+3,4 bps**, 30 dk ≈ **+8,3 bps** VWAP sürüklenmesi.
- **Sinyal-koşullu sürüklenme ÖLÇÜLEMEDİ** (None; neden: koşulsuz örneklem koşullu soruyu
  cevaplamaz — §C.2'nin kendi kill beyanı).

### 1.3 1Day kısıtının GERÇEK sınırı — hangi veri VAR (bu turda ölçüldü)

"Dakika barı yolu yok" cümlesi **motor** için doğru, **veri** için artık kısmen eskimiş: iki
gün-içi arşiv canlıda birikiyor ve `unverifiable_claims` bugün tam olarak bu borcu taşıyor.

| arşiv | yazar | birim | canlı kapsam (2026-08-22T18:42Z) | yerel kopya |
|---|---|---|---|---|
| `state/intraday_bars/*.jsonl` | `bararchive.archive_frame` (hotstate içi, tek-deneme) | çerçeve `{ts, bars:{T:bar}}` | **20 dosya, 2026-07-27 → 2026-08-21, 171,9 MB** | 4 dosya (07-27..07-30) |
| `state/bars_intraday/*.jsonl` | `barsarchive.BarsArchiver` (Redis consumer-group, fsync→XACK, tekilleştirmeli) | kapanmış dakikalık bar/satır | **20 dosya, 2026-07-27 → 2026-08-21, 186,4 MB** | 4 dosya (07-27..07-30) |

Yoğunluk (canlı, `bars_intraday/2026-08-20.jsonl` — son TAM seans):
**68.416 satır · 249 tekil ticker** (canlı evren 251) · 454 tekil dakika ·
ticker başına **medyan 294 bar** (~390 dk seansın ~%75'i) ·
açılış penceresi 13:30–13:59 UTC: **5.335 satır, 249 tickerın 249'u kapsanıyor**.

Sınırlar (ölçülen):
1. **Derinlik:** arşiv 2026-07-27'de başlıyor → bugün **20 seans**. Replay defteri 2022-01-07'den
   koşuyor (donmuş DB: 885 replay_seed satırın aralığı 2022-01-07..2026-08-21) — gün-içi pencereyi
   **geçmişe doğru** modelleyecek veri depoda YOK; arşiv ancak ileriye doğru birikir
   (`ARCHIVE_KEEP_DAYS=120` takvim günü retention, `bararchive.py:37-38` — birikim tavanı da var).
2. **Feed:** akış varsayılanı IEX (`marketstream.py:37`, `MERIDIAN_DATA_FEED=iex`). ARASTIRMA §B.6
   IEX↔konsolide ölçüt hatasını **−36,8…+16,2 bps** ölçtü — 23e'nin ölçmek istediği büyüklükle
   aynı mertebe. Konsolide dakika/15-dk barı emsali var (edg038 `massive_capraz.json`) ama o yol
   dış kaynak + PIT disiplini gerektirir.
3. **Tüketici yok (beyanlı):** `codelaw.unverifiable_claims()` bu turda koşuldu → **tek kalem**
   döndü: `intraday_bars/*.jsonl` (pattern), sınıf `gelecek_tuketici`
   (`codelaw.py:585-607`). Beyanın kendi devir şartı: "Faz-5/6 ölçümü arşivi okuduğu gün bu satır
   kaldırılmalı ve gerçek tüketici yazılmalı". Adlandırılmış gelecek tüketici: **EXE-2026-002 kart
   ailesi** (dakika-hassas icra ölçümü).

### 1.4 Canlı↔replay boşluğu ve neden açık

- Boşluk simetrik DEĞİL: bugün **iki motor da** EOD kadansındadır; 23e uygulanmak istense önce
  canlı gönderim adımı gün-içi tetiğe bağlanır (§C.2: "daily_cycle'ın gönderim adımı gün-içi bir
  tetiğe bağlanır — yeni emir tipi gerekmez"), replay ise bunu ancak dakika-barı dolum sözleşmesiyle
  izleyebilir. §C.2'nin kendi tespiti: **"MODELLENEMEZ (K2)"** — ya dakika-barı arşivi + `fill`
  sözleşmesi genişlemesi (tüm karneler yeniden koşar), ya canlı A/B (paper hesapta tek kol → yapısal
  olarak zor).
- Neden açık: fayda yalnız koşulsuz örneklemde ölçüldü; koşullu etki ölçüm altyapısı olmadan
  kurulamıyor; ROADMAP bu yüzden 23e'yi "altyapı kalemi" sınıfında tutuyor (ROADMAP.md:405-407).

### 1.5 Kart-adayı ölçüm tasarımı — mevcut eşik/kill ENVANTERİ

Halihazırda beyan edilmiş (yeni öneri değil):
- **Kill (ARASTIRMA §C.2):** koşulsuz örneklemle hüküm kurulamaz — koşullu sürüklenme ölçülmeden
  pencere hükmü yok.
- **Bağlam eşiği (§D tablosu):** friksiyon başabaş **9,49 bps/bacak** (EDG-037 eğrisi); pencere
  kaleminin kıyas zemini budur.
- **Ölçülmüş büyüklükler:** menzil −%42 (146,7→84,9 bps) · bedel +3,4 bps/15 dk (koşulsuz) ·
  likidite hiçbir dilimde bağlamaz (§B.3, emir ~1e-5·ADV).
- **Veri ön-şartı (bu belgeyle güncellenen durum):** konsolide olmayan (IEX) dakikalık korpus
  bugün **20 seans × ~249 sembol**; §B.6 ölçüt-hatası payı ölçüm tasarımına girmek zorunda.
- **Sıra beyanı (§C.2 "Gerekli kart"):** önce ölçüm-altyapısı kartı (dakika-barı arşivi + gün-içi
  dolum sözleşmesi), sonra pencere grid'i.

---

## 2 · 23f — `gap_behavior: cancel`

### 2.1 Bugünkü davranış (goal + kod)

- Yürürlük değeri: `goal.yaml:84` → `gap_behavior: marketable_limit` (canlı teyit 2026-08-22T18:42Z;
  donmuş kopya `exe003_.../canli_state/goal.yaml:84` aynı).
- Yasa: `broker.entry_law` (`broker.py:163-164`) yalnız `marketable_limit|cancel` kabul eder;
  karar `entry_order_decision` (`broker.py:225-247`):
  `gap = (rp is None) or (t > 0 and rp >= t)` → gap değilse `stop_limit`, gap + `cancel` ise
  `veto`, gap + `marketable_limit` ise limit-tavanlı emir.
- **TOTOLOJİ damgası kodda duruyor** (`EZILEN-DAMGA[25b-5]`, `broker.py:225-240`): silahlı
  kurulumların hepsi `entry_trigger = sinyal barı kapanışı` verir, canlı `ref_price` aynı kapanıştır
  → `rp >= t` YAPISAL true. Canlanma koşulu da beyanlı: tetik sinyal-barı-kapanışından farklı bir
  pivota taşınırsa `stop_limit` dalı erişilebilir olur (damga o gün kalkar).
- Veto uygulaması iki motorda: ayna `loop.py:784-795` (`res.veto` → plan `broker_status="gap_veto"`,
  `EV_GAP_VETO` olayı, ret dağılımına yazılmaz); iç motor `broker.py:564-566`
  (`gap_at_submit and GAP_VETO` → `_red(EV_GAP_VETO, ...)`).

### 2.2 Canlı↔replay boşluğu (beyanlı ve birincil artefaktla doğrulanmış)

- Replay `fill_entry`e `gap_at_submit` HİÇ geçmez (`backtest.py:332-337` çağrısında argüman yok) ve
  imza varsayılanı `None`dır (`broker.py:517`); `None` beyanı: "bu motorda gönderim anı YOK
  (replay) ve veto ateşlemez — BEYAN EDİLMİŞ kapsam farkı" (`broker.py:540-543`).
- Sonuç, E1 grid'inde birincil artefakttan doğrulandı
  (`research/olcumler/e1_grid_2026-08-03/sonuc_e1.json` → `gap_bacagi`): üç hücrede de
  `mkt` ↔ `cnl` kolları **bayt-özdeş** (`digest_tum_esit: true` ×3, `entry_gap_veto_reddi: [0,0]`,
  `gap_at_submit_none: 124/176/199` = çağrıların %100'ü). Kart bunu satırına işlemiş:
  `EXE-2026-001-entry-execution.yaml:36` "GAP-BACAĞI REPLAY'DE ÖLÇÜLEMEDİ (yapısal)". Yani
  **kartın `gap_davranisi` ekseni (2 nokta, K+=2; kart :17) harcanmış ama bacak ölçülememiştir.**

### 2.3 `cancel` semantiği neyi değiştirirdi — DONMUŞ defter sayımları

**(a) Yürürlükteki (totolojik) tanımla — donmuş E2 defteri**
(`exe003_.../canli_state/entry_execution.jsonl`, 30 satır; canlı çekim 2026-08-22T18:42Z de n=30,
değişmemiş):

| sayım | değer |
|---|---|
| toplam satır | 30 (15 `motor=ayna` gönderim + 15 `motor=ic` dolum) |
| `gap_at_submit=true` | **29/30** |
| `gap_at_submit=None` | 1 (kaynak `pano` — elle gönderim satırı) |
| ayna gönderimlerinde gap | **15/15** |
| dolan girişler (fill_status=filled) ve hepsi gap | **13/13** |

→ `cancel` yürürlükteki tanımla **gönderimlerin %100'ünü** keserdi (13 gerçek dolum dahil).
ROADMAP'in "%100" kolu budur (ROADMAP.md:408-409; ARASTIRMA §C.3'ün "canlı defter 9/9" sayısı
2026-08-13 anlık görüntüsüdür, bugün 15/15).

**(b) Anlamlı tanımla ("bar tetiğin üstünde açtı") — kanonik ölçüm + bu turun kısmi çapraz sayımı:**
- Kanonik (ARASTIRMA §B.1/§C.3, n=885 replay işlemi): **452/885 (%51,07)** işlem kaybolur;
  kaybolanların net P&L'i **+11.233 $** (defter 20.685 $ → 9.452 $).
- Bu turun çapraz sayımı (donmuş `meridian.db`, immutable-ro): 885 `replay_seed` işlemin yalnız
  **352'si** `trade_plans`a joinlenebildi (tablo 500 satır tutuyor; **533 işlem plansız →
  tam yeniden-sayım bu defterden ÖLÇÜLEMEZ**, neden: plan retention). Joinlenebilenlerde
  `entry_fill > entry_trigger` **193/352 (%54,8)** — slip fiyatın içinde olduğundan hafif fazla
  sayar; kanonik %51,1 ile tutarlı, onun yerine geçmez.

### 2.4 Neden açık

ROADMAP 23f'yi "karar-gerektirmez … ELENMELİ" diye sınıflıyor (ROADMAP.md:407-410) ve ARASTIRMA §D
tablosu #10 "ELE" diyor — ama bu bir belge-düzeyi tespittir; kart hükmü yok, çünkü kartın gap
bacağı yapısal olarak boş koştu (§2.2). Açıklık tam olarak şu: **"ELENMELİ" tespitini kart/hüküm
düzeyine kim, hangi biçimde işleyecek** (yeniden ölçüm mü — ki ölçülebilir tek tanım zaten §B.1'de
ölçülmüş durumda — yoksa kayıt-düşme mi).

### 2.5 Kart-adayı ölçüm tasarımı — mevcut eşik/kill ENVANTERİ

- **Yapısal kill (kod beyanı, `broker.py:540-543`):** replay `gap_at_submit` geçirmediği sürece
  `cancel` grid noktası ölçülemez — mkt↔cnl bayt-özdeş çıkar (kanıt: sonuc_e1.json).
- **Canlanma koşulu (kod beyanı, `broker.py:238-240`):** tetik tanımı sinyal-kapanışından farklı
  pivota taşınırsa bayrak gerçek filtreye döner (`EV_STOP_LIMIT` defterde görünmeye başlar) —
  23f'nin yeniden ele alınabileceği tek beyanlı yol.
- **Harcanmış K:** EXE-2026-001 `gap_davranisi` ekseni 2 nokta (K+=2), ölçülemedi olarak kapandı.
- **Ölçülmüş etki büyüklükleri (mevcut):** %100 kesinti (yürürlük tanımı, 15/15) ·
  %51,07 / +11.233 $ (anlamlı tanım, n=885) · gap yönü simetrik (%51,07 yukarı; medyan +1,4 bps).

---

## 3 · 13 — SCALE-OUT (latent kusur)

### 3.1 Bugünkü davranış (kod satırıyla)

- **Alet KAPALI:** `exit.scale_out_frac = 0.0` (canlı `strategy.yaml:19`, teyit 2026-08-22T18:42Z;
  üretim varsayılanı `config.py:358-359` — `scale_out_r 2.0 / frac 0.0 "OFF by default"`;
  yaşam-döngüsü varsayılan tablosu `shadow_lifecycle.py:106-107`, AST-testle çivili).
- Motor yolu her seans yine ÇAĞRILIR: canlı `loop.py:1566-1575` INTRADAY(D) fazında
  `b.scale_out(pos, {high, low, open}, eff_intraday)` (rejim `prev_regime` ile çözülür); replay
  `backtest.py:363` aynı faz sırasıyla (`prev_eff`) — `frac<=0` ilk kapıda `False` döner
  (`broker.py:702-706`).
- Davranış (`broker.py:697-754`): `high ≥ entry + scale_out_r·R` → `frac` kadar `level`den bankala,
  kalan koşucuya breakeven ratchet (`trail_stop = max(trail, entry)`), `pos.scaled_out = True`;
  hedef-önce ve stop-önce muhafazakârlık kapıları; bankalanan P&L TEK kapanış satırında birleşir
  (`close_position`: `pnl = pnl_remaining + pos.banked_pnl`, `broker.py:790-797`).
- Bounds araması bu düğmeyi HÂLÂ tarayabilir: `bounds.yaml:22-23`
  (`scale_out_r 1.0-4.0 step 0.5` · `scale_out_frac 0.0-0.75 step 0.25`).

### 3.2 `scaled_out`un defterdeki izi (SAYIM)

| defter | n | `scaled_out=true` |
|---|---|---|
| donmuş canlı DB `trades` (exe003, 2026-08-22 14:11) | **893** (885 replay_seed + 8 live_paper) | **0** |
| canlı `store.read_jsonl("trades.jsonl")` (2026-08-22T18:42Z) | 893 | 0 |
| yerel replay-dönemi `state/trades.jsonl` (2026-07-23 damgalı) | 95 | 0 |
| gölge kitabı V6 (`frac=0.5` kolu, 2026-07-31'den beri; donmuş DB `shadow_books`) | n_trades 4, açık 6 pozisyon | açık pozisyonlarda 0 |

Yani şema alanı (`storage.py:74` `("scaled_out","BOOL")`, satır yazımı `broker.py:797`) canlıdır ama
**hiçbir defterde tek bir true yok** — alet kapalıyken bu beklenen görüntüdür; V6 gölge kolu
(`shadow_lifecycle.py:70-77`) kavramı `frac=0.5` ile İZLİYOR ve orada da henüz bankalama olayı
oluşmadı (kümülatif: V6 pnl 6.246,03 $ vs V1 kontrol 6.530,45 $ — kitap düzeyi gözlem, hüküm değil).

### 3.3 Bilinen bilinçli-boşluk beyanları (üçü de adresli)

1. **Ayna boşluğu:** iç kitap kısmiyi bankalarsa ayna bracket TAM boy kalır — "bilinçli ve belgeli"
   (`loop.py:146-147`); reconcile bunu drift saymaz: kapsam-dışı listesinde "`scaled` → ölçek-çıkış
   BİLİNÇLİ bir boşluktur" (`loop.py:2698`) ve karşılaştırma döngüsünde `if scaled: continue`
   "KNOWN, intentional gap, not drift (audit #16 false alarm)" (`loop.py:3344-3346`).
2. **Latent mekanik kusur (ROADMAP §WP1-C, madde 13):** bankalama barında `trail=entry_fill`
   kurulumu (`entry_fill>open` iken) koşucuyu AYNI barda ~0.7R `stop_gap`e kesiyor; büyük
   gap-kazananlar budanıyor (ENPH +12.3R→0.72R). ZORUNLULUK ŞARTI beyanı: `scale_out_frac` bir gün
   açılacaksa ÖNCE bu düzeltilir (ROADMAP.md:417-424).
3. **Ölçülmüş eleme (EDG-2026-029, status: measured, Rol-1 hükmü 2026-08-12):** düzeltilmiş haliyle
   bile CI-negatif — F1x(B) −0.0530R CI[−0.1033,−0.0091] · F2x(C) −0.0451R CI[−0.0839,−0.0116];
   düzeltmenin kendi etkisi POZİTİF (F1x−H1 = +0.0874R CI[+0.034,+0.153]; `bars_held=0` 18→2) →
   "zararın bir kısmı kusurdu AMA kalan zarar KAVRAMIN kendisi… SCALE-OUT DOSYASI TÜMDEN KAPANIR
   (bu motor/param dünyasında)".

### 3.4 Canlı↔replay boşluğu ve neden açık

- Motor yolu iki tarafta AYNI (aynı `PaperBroker.scale_out`, aynı faz sırası, aynı
  `prev_regime/prev_eff` semantiği — `backtest.py:280-285`, `loop.py:1566` yorumu); asıl boşluk
  **iç kitap ↔ Alpaca aynası** (madde 3.3-1) ve **latent kusur** (3.3-2).
- Neden açık: kalem ROADMAP'te "LATENT-KUSUR notuna indi… düzeltme ACİL DEĞİL, öncelik düşük"
  (ROADMAP.md:417-424) — kapatılmış değil, koşullu-uykuda: `frac>0` terfisi denenirse önce
  motor-düzeltme kartı şartı tetiklenir; ayrıca bounds hâlâ taradığı için mutasyon araması düğmeyi
  önerebilir (bkz. Açık Sorular).

### 3.5 Kart-adayı ölçüm tasarımı — mevcut eşik/kill ENVANTERİ

EDG-2026-029'da zaten tanımlı (yeniden ölçüm gerekirse şablon odur):
- **Kill:** eşli-ortak <60 → olculemedi · şasi bütünlüğü bozuksa hücre geçersiz · yama davranışı
  öz-sınamayla kanıtlanamazsa koşulmaz (araç turu).
- **Başarı metriği:** hücre-başına eşli ort-R farkı CI; CI>0 → "kavram kazanan — motor-düzeltme
  kartı önerilir"; CI 0-içi/altı → dosya kapanır (kapandı).
- **Zorunluluk şartı (ROADMAP):** `frac>0` her terfiden önce bankalama-barı trail düzeltmesi
  (sonraki-bar trail ya da aynı-bar stop-kontrol sırası); motor değişikliği = kart + yeniden ölçüm.
- **İzleme (mevcut, bedava):** gölge V6 kolu kavramı canlı akışta izlemeye devam ediyor
  (`shadow_lifecycle.LIFECYCLE_ONLY`).

---

## 4 · AÇIK SORULAR (Rol-1 / operatör)

1. **[23e — sıra]** §C.2'nin sıra beyanı "önce ölçüm-altyapısı kartı, sonra pencere grid'i".
   Altyapının veri bacağı fiilen 2026-07-27'den beri birikiyor (20 seans, tam evren). Rol-1: EXE-2026-002
   ailesinin ilk kartı ne zaman açılır — birikim eşiği (kaç seans?) beyan edilecek mi? (Beyan edilmiş
   bir seans-eşiği bugün YOK — bu bir boşluk tespiti, öneri değil.)
2. **[23e — feed]** IEX korpusuyla mı ölçülür, konsolide (Massive emsali) mi? §B.6'nın
   −36,8…+16,2 bps ölçüt hatası pencere etkisiyle aynı mertebede — hangisi seçilirse ölçüm tasarımı
   bunu paydasına yazmak zorunda. Konsolide yol dış kaynak + PIT disiplini ister (operatör bütçe kalemi).
3. **[23f — kapanış biçimi]** "ELENMELİ" tespiti belge-düzeyinde; kart hükmü yok (gap bacağı boş
   koştu, K+=2 harcandı). Rol-1: kayıt-düşme mi (kill-list/karar kaydı), yoksa canlanma koşuluna
   (tetik-pivotu değişimi, `broker.py:238-240`) bağlı askı notu mu?
4. **[23f — pano istisnası]** Donmuş E2'de `gap_at_submit=None` tek satır `kaynak=pano` (elle
   gönderim). `cancel` semantiği None'u da gap sayar (`broker.py:241` öncesi `gap=(rp is None) or ...`)
   — elle gönderim yolunun veto kapsamına girip girmeyeceği hiçbir yerde beyan edilmemiş.
5. **[13 — bounds tutarlılığı]** EDG-029 "dosya TÜMDEN KAPANIR" dedi ama `bounds.yaml:22-23`
   `scale_out_frac 0.0-0.75`'i hâlâ tarıyor; mutasyon araması düğmeyi yeniden önerebilir ve ROADMAP
   zorunluluk şartı yalnız metinde. Rol-1/operatör: bounds'tan daraltma mı (frac max 0.0), yoksa
   terfi-kapısında kart şartı mı? (İkisi de mevcut yasalarda otomatik değil — tespit.)
6. **[13 — ayna boşluğu ömrü]** `scaled` ayna boşluğu bilinçli/belgeli AMA alet kapalıyken ölü
   koddur; `frac>0` senaryosunda ayna bracket'ının kısmi-satış bacağı tasarlanmadan boşluk canlanır.
   Bu, motor-düzeltme kartının kapsamına dahil mi (Rol-1 kapsam kararı)?

---

*Yazan: tasarım-kanıtı ajanı, 2026-08-22. Bu belge salt kanıt/envanterdir; hiçbir kart, eşik ya da
hüküm oluşturmaz. Canlıya ve state/'e yazılmadı; tek yeni dosya budur.*
