# ELEME — WP5 (Ölçüm Altyapısı) stok kalemleri · 2026-08-23

**Usul:** STOK KAMPANYASI eleme turu; salt-okuma ajanı (Ö-49 disiplini — beyana değil koda/artefakta
bakıldı). Hüküm Rol-1'in; buradakiler DÖRT-SINIF **ÖNERİDİR**: KAPAT-BAYAT · KAPAT-TASARIMDA ·
ÖLÇ · BİRLEŞTİR/BEKLE. Kapsam: ROADMAP §2:212 + WP5 gövdesi (:764-1010) listelenen stok kalemleri.

**SINIR BEYANLARI (ölçümün kendi dürüstlüğü):**
- Yerel `state/` anlık görüntüsü **2026-07-28 tarihli** (`portfolio.json last_date`). Canlı-defter
  gerektiren iki sayı (korunum açıklanamayanı, aşınma sorgu sayacı) bu yüzden yerel-anlık-görüntü
  değeridir; kanonik değer canlıdadır ve satırında ayrıca yazılıdır.
- `conservation_report` koşumu obs.warn/alarm no-op'lanarak yapıldı (yazım sızıntısı yok);
  `oos_erosion.status` salt-okuma.
- Ö-41 AST taraması pyproject'in kendi kural tanımıyla (v246-A metodolojisi) birebir yeniden koşuldu.

---

## Eleme tablosu

| # | Kalem | Bugünkü ölçülen gerçek (kanıt) | Öneri |
|---|-------|-------------------------------|-------|
| 1 | **M2 DSR-yarısı (Ö-4 aracı)** | Araç YOK: `validation_ledger` `seri`si hâlâ `[[tarih, r_multiple]]` (`reflect.py:837`, `ledgers.py:220-226` sözleşme değişmemiş); pnl-serisi damgası da donmuş-çekim betiği de repo'da yok (grep boş). `KYS-2026-002` `status: measured_partial` — DSR yarısı kill#2 (`kart :50,:60-61`). D6 gerekçesi geçerliliğini koruyor: DSR artık anlamlı sayı — **0,0391** (`EDG-036:124,:166`), yani araç borcu bağlayıcı. | **ÖLÇ** — önce reflect şema kararı (Rol-1: pnl-serisi damgası MI, ayrı donmuş-çekim betiği Mİ), sonra KYS-002'ye **R2 revizyonu** ile DSR-yarısı yeniden ölçülür. K-maliyeti: **K+=1** (grid yok, tek taban hesabı; araç boyutu S-M). Taslak eşik: PBO yarısıyla simetrik — taban RAPORLANIR, hüküm eşiği yok (taban ölçümü); kill: seri ölçek-eşdeğerliği doğrulanamazsa (Sharpe sapması medyan >0,01 kalırsa) DSR tabanı yine yazılMAZ. |
| 2 | **M7 prescreen kod-sürümü damgası** | YAPILMIŞ: `olcum_araclari.kod_surumu_damgasi` (`:761`) + prescreen dört noktada damga yazıyor (`prescreen.py:186,243,277,390,429`); çivisi `tests/test_wpm_sasi_v173.py`. İniş: **4b84871, 2026-08-02** (blame :186 doğrulandı). Not: 2026-08-09 KESIF-WP-MKP bunu bir hafta SONRA hâlâ açık listelemişti — Ö-49 bayat-beyan sınıfının bir örneği daha. | **KAPAT-BAYAT** |
| 3 | **M8 kararları Rol-1 turu (U1-U7)** | YAPILMAMIŞ — dört bağımsız iz: ① U2 temizliği yok: **41/65 kart** hâlâ `pending-*` trial_ids taşıyor (grep sayımı; ör. `EDG-001:18`, `EDG-038:116`); ② U3 yok: `research/cards/README.md` endeksi bayat (EDG-001/002 hâlâ "Aktif/registered" başlığında); ③ U5 yok: hiçbir kartta beyan-K/harcanan-K alanı yok; ④ U6 yok: `analytics.validation_trio` n_trials'ı aşınma-defteri + k_probes'tan topluyor, kart-K'ya bağlı DEĞİL (`analytics.py:2608-2615` açıklaması). Rapor hazır: `docs/M8-K-SENKRON-RAPOR-2026-08-10.md`. | **KAPAT-TASARIMDA** — tek konsolide Rol-1 oturumu ölçümsüz kapatır. Kapanış taslağı: U1 K-düzeltme notu tarihçe-koru işlenir · U2 41 kartın pending-* alanları gerçek trial_id'lerle (yoksa `none-<neden>` damgasıyla) mekanik değiştirilir · U3 README endeksi kart durumlarından YENİDEN ÜRETİLİR (elle bakım bırakılır) · U5 şema alanı eklenMEZ (mevcut k_registry yeterli — yeni alan yeni bakım borcu) · U7 rotasyon-yerel sayaç REDDEDİLİR (ömür-boyu kalır; seçilim baskısı oturum tanımaz). TEK MİMARİ İSTİSNA: **U6 (kart-K ↔ DSR n_trials bağı)** — DSR çıktısını değiştirir; ayrı Rol-1 hükmü ister, oturumun tek "karar" maddesi budur. |
| 4 | **M9 Chen-2022 t-hurdle notu** | YAPILMIŞ: dengeleme referansı koda yazılı (`analytics.py:2590-2607` — "BU NOT BİR GEVŞETME DEĞİLDİR" + UYDURMA-YASAĞI künye şerhi) ve standartlar belgesinde (`docs/olcum_standartlari.md:348`). İniş: c9aee5e, 2026-08-10. F13 yüzey bacağı WP-UX'in — WP5'te iş kalmadı. | **KAPAT-BAYAT** |
| 5 | **M11 KATMAN-4 alan merceği → plan defteri ~20 alan** | YAPILMAMIŞ: repo'da alan-merceği ARACI hiç yok (grep: `meridian/ research/ ops/` boş) — mercek 08-07 taramasının oturum-içi metodolojisiydi; `ARTEFAKT-TARAMASI-2026-08-07.md:492-493` "yalnız dormant_setup'a uygulandı, ~20 kontrol alanı sonraki kova" beyanı bugün de doğru. Ara dönemde sınıfın değeri KANITLANDI: uyuyan-yol ve broker_status ölü-dalı tam bu körlükten çıktı (`watchdog.py:552-560` düzeltme şerhi). | **ÖLÇ** — ama kart DEĞİL, **kova-6 salt-ölçüm taraması** (4./5. kova emsali: belge-çıktılı, `meridian/` dokunulmaz, K=0 — grid yok, hipotez yok). Maliyet: bir gece-ajanı; kalibrasyon kapısı şart (kova-4'ün 1/3 dersi). |
| 6 | **2B blok-bootstrap CI standardı** | YAPILMIŞ: genel standart `olcum_araclari.blok_bootstrap_ci` (sözleşme "1.0", `:41,:482`; moving-blok, n^(1/3)); iniş 4b84871 (2026-08-02, WP-M şasi). İki beyanlı ikiz BİLİNÇLİ ayrı: `analytics._blok_bootstrap_ci` (circular, işlem-ekseni, yayımlanmış hüküm tabanı — `analytics.py:1819-1828` yazılı gerekçe) ve `benchmark_relative`in IID'si (bilinçli değişmedi, 2. ölçüt hükmü kaymasın). "Standart + beyanlı istisna" tam olarak kalemin istediği son durumdur. | **KAPAT-BAYAT** |
| 7 | **2C empirical-Bayes küçültme** | YAPILMIŞ: `analytics._empirical_bayes` (`:3304`) + `shrunk_regime_cells` (`:3355`) → `/api/diagnostics` (`api.py:4570`) → panoda çiziliyor (`app.js:5828`). İkiz `olcum_araclari.eb_kucult` beyanlı (`analytics.py:3311`). İniş 4b84871. τ²=0 bulgusu v125 arşivinde işlenmiş (ROADMAP:3553-3556). | **KAPAT-BAYAT** |
| 8 | **2D R2 holdout rotasyonu ("zamanı gelince")** | R1 uygulandı (2026-07-30, `dataset.py:58-118` tam tarihçe); R2 için CANLI TETİK ZATEN KURULU: `holdout_rotation_advice` ölçer-önerir-uygulamaz (`dataset.py:158` YASA-6 şerhi) ve panoda (`app.js:5828` `holdout_rotation`). Sorgu basıncı bugün limitin ÇOK altında: canlı 08-10 ölçümü 4 pencere; yerel 07-28 anlık görüntüde sayaç 0 (dosya beyanı: retro damga yok, sayaç 07-28'den sayar). Limit 20. | **KAPAT-TASARIMDA** — stok satırı düşürülür; kapanış paragrafı: "R2'nin sahibi ROADMAP değil `holdout_rotation_advice`tir — advisor ROTASYON ÖNERİLİR dediği gün operatör kararıyla R1 usulü tekrarlanır (maliyet beyanı: fingerprint değişir, geçmiş p/ΔS kıyaslanamaz)." Takvimle değil tetikle yaşayan bir kalemi stokta tutmak çift-defterdir. |
| 9 | **A4 tahmin-isabeti bandı** | YAPILMIŞ ve ESKİ: `prediction_accuracy_band` ilk git commit'te bile var (d9c3f24, 2026-07-31; v125 dönemi); n<3'te bant UYDURMUYOR (`A4_BAND_MIN_N`, `analytics.py:2699`), para-ölçeği sütunu v178'de eklendi. Okuyucuları gerçek: hermes kendi karnesini OKUYOR (`hermes.py:1059-1070`) + pano (`api.py:4541 hermes_scorecard`). | **KAPAT-BAYAT** |
| 10 | **kill#4 uygulaması (EXE-2026-002-R1 ön şartı)** | YAPILMIŞ: daraltma kodda — `KAPSAM_DISI_SINIFLARI=("eod_yok",)` + "BURADA OLMAYAN her eşleşmeme BOZULMA sayılır — fail-closed" (`faz5_cikis.py:42-49`); payda eod_yok'u dışlar, oran yalnız golge_bozuk+bps_yok'tan (`:355-372`). Kart R1 revizyonu işlenmiş (`EXE-2026-002…yaml:92-96`), status measured. "Ayrı tur" borcu diye taşınan iş o turda YAPILMIŞ. | **KAPAT-BAYAT** |
| 11 | **20c yönetişim asimetrisi (slot kilitli / boyut açık)** | ASİMETRİ BUGÜN DE GERÇEK: `max_open_positions` LIMIT_KEYS'te (`guard.py:51` — hermes öneremez) + goal'da "ikisi AYRILMAZ" (`goal.yaml:134`); ama `position_size_r` bounds'ta **max: 1.0** duruyor (`bounds.yaml:19`) ve LIMIT_KEYS'te YOK → öğrenme çiftin yarısını tek başına 1,0'a çekebilir. Karşı-argüman da yazılı: bounds şerhi "arama uzayı kumhavuzudur, canlı değer strategy.yaml'da" (`bounds.yaml:16-18`). Yön ölçümü hazır: EDG-026 B kolu (+775$, sharpe 0,018) vs C kolu. | **KAPAT-TASARIMDA** — operatör/Rol-1 masa-başı karar. Taslak: kumhavuzu-şerhi tek başına yetmez, çünkü kapıdan geçen bir position_size_r önerisi canlıyı değiştirir — çift SÖZLEŞMEYSE iki yakası da aynı rejimde olmalı. Önerilen kapanış: `position_size_r` LIMIT_KEYS'e alınMAZ (arama ölçmeye devam etsin), ama **goal'a çift-bağ çivisi** eklenir: slot≠20 VEYA size≠0,5 önerisi tek başına gelirse kapı `REVIEW`a düşürür (öneri ancak ÇİFT olarak, kart-önce gelir). Bounds satırı dokunulmaz kalır. |
| 12 | **20d ince marjlar / izleme** | Yapı DEĞİŞMEMİŞ, hiçbir alt kalem iş üretmiyor: `EDGE_CVAR5_MIN_R=-1.5` aynen (`analytics.py:1686`); `EXPLORE_MAX_POS=5` artık max_open kalıntısı DEĞİL, beyanlı operatör debisi (`loop.py:45` "öğrenme debisi (operatör, 2026-07-20)"); hedef üçlüsü goal'da aynen (`target_return_30d: 0.07`, `min_sharpe: 1.2`, `rollback_if_worse_by: 0.10`). Marjın bugünkü CVaR değeri canlı defter ister (yerel 07-28 — ölçülmedi, uydurulmadı). | **KAPAT-TASARIMDA** — 20b emsaliyle **KAYIT** sınıfına indirilir (karar değil, bilgi): "hedef üçlüsü gevşek + CVaR marjı ince" cümlesi WP5-E satırında izleme notu olarak kalır, stok kalemi düşer. Canlanma koşulu beyanlı: EDGE VERDICT bir ölçütü CVaR yüzünden çevirirse ya da hedef üçlüsü bir hükme girerse kalem yeniden açılır. |
| 13 | **Korunum kovası (kalan 3)** | Dedektör-tarafı `uyuyan_kurulum` terminal sınıfı HÂLÂ YOK (`watchdog.conservation_report:514-616` — sınıflar: traded/dropped/NO_GO/taze/no_fill/replay_era/unexplained; grep `uyuyan_kurulum` boş). Yerel 07-28 anlık görüntüde `unexplained=6` (CSX/UNP/NSC/RTX 07-23 mb + PKG 07-24 + ROK 07-27 hammer — HEPSİ uyuyan-kurulum REVIEW planı, yani kovanın sınıflayacağı sınıfın ta kendisi); kanonik canlı sayı 08-13'te 3 idi (EDG-036:169-172, mb 08-12'de silahlanınca düştü). AYNI SINIFIN kartı uçuşta: `EDG-2026-049` registered 2026-08-23 (K6, dormant karşı-olgu). | **BİRLEŞTİR/BEKLE** — EDG-049 hükmü + §5 icra-bağı operatör kararıyla AYNI pencereye bağlanır: 049 uyuyanların kaderini ölçecek; kova (S-boyut, kart istemez — dedektör hijyeni) o hükümden SONRA tek kod turunda iner. Şimdi inerse 049'un ölçtüğü popülasyonun sınıflaması ölçüm sırasında değişir (ölçüm-bağlamı tuzağı). |
| 14 | **Kart biçim/lint** | Somut vaka ÖLÇÜLDÜ: 65 kartın **1**'inde çift üst-düzey anahtar — `EDG-2026-038` iki `verdict:` (`:120` boş placeholder + `:122` gerçek hüküm; dup-anahtar dedektörü tüm kartlarda koşuldu, başka vaka ve parse hatası YOK). ZARAR BUGÜN YOK ama tesadüfen: PyYAML last-wins, İKİNCİ (dolu) blok kazanıyor — sıra ters olsaydı gerçek hüküm SESSİZCE yutulurdu. Lint aracı repo'da yok (ops/ tarandı). | **KAPAT-TASARIMDA** — iki satırlık kapanış: ① Rol-1 EDG-038'deki boş placeholder satırını siler (ölçüm ajanı karta dokunamaz — Rol-1 kalemi); ② dup-anahtar çivisi teste girer (bu belgede koşulan 15 satırlık dedektör aynen çivi olur; S-boyut). Ayrı "kart şablonu" turu AÇILMAZ — sınıfın bugünkü tüm nüfusu 1. |
| 15 | **Ö-41 modül-kural boşluğu (~40 dosya)** | BUGÜN YENİDEN ÖLÇÜLDÜ (AST, v246-A kuralı birebir): pyproject listesi **39 gerçek dosya** (+6 `--deselect` argümanı = ham 45); modül-kural **84 test modülü** buluyor (+`wf_fixtures.py` test-değil); boşluk **46 dosya** — 08-14'te 41'di, §2 satırındaki "~40" bugün İYİMSER, boşluk BÜYÜYOR. Alt sınıf: 46'nın ~**19**'u metin-tarayan (`__file__`+`read_text` kaba grep'i) — pyproject'in kendi şerhi gereği körce eklenemez (skor şişirir). Listede kuralı sağlamayan 1 fazlalık duruyor (test_trend_shadow_v144, beyanlı-zararsız). | **KAPAT-TASARIMDA** — karar Rol-1'in (pyproject şerhi bunu zaten söylüyor) ve masa-başı verilebilir. Taslak: davranışsal ~27 dosya (46−19) listeye girer; ~19 metin-tarayan ADIYLA dışarıda bırakılır (pyproject şerhine liste eklenir — "dışlama beyanlıdır"); süre maliyeti ilk haftalık ritüelde ölçülür, tavan aşılırsa geri alınır. `wf_fixtures.py` girmez (pytest toplamaz). Ayrıca `ops/haftalik_mutasyon.sh` öz-testi kural-liste eşitliğini doğrulamaya başlar — boşluğun üçüncü kez büyümemesi mekanikleşir. |

---

## Sayım

| Sınıf | Adet | Kalemler |
|-------|------|----------|
| **KAPAT-BAYAT** | **6** | M7 · M9 · 2B · 2C · A4 · kill#4 |
| **KAPAT-TASARIMDA** | **6** | M8 · 2D · 20c · 20d · kart-lint · Ö-41 |
| **ÖLÇ** | **2** | M2/Ö-4 (KYS-002-R2, K+=1) · M11 (kova-6 taraması, K=0) |
| **BİRLEŞTİR/BEKLE** | **1** | korunum kovası (EDG-049 hükmü + icra-bağı kararına bağlı) |

**Net:** 15 stok kaleminin 12'si ölçümsüz kapanabilir (6'sı ZATEN yapılmış işti). Ö-49 bayat-beyan
sınıfına bu turdan giren vakalar: M7 (KESIF-WP-MKP inişten bir hafta sonra açık listeledi), 2B/2C
(şasi commit'i 4b84871 ikisini de indirmişken WP5-A gövdesi açık taşıdı), A4 (ilk commit'ten beri
var), kill#4 ("ayrı turun işi" denen iş yapılmış, satır güncellenmemiş). Tek büyüyen borç: Ö-41
boşluğu 41→46.

**Yan bulgu (bu turun işi değil, kayıt):** WP5-B'nin iki görünürlük borcu (② hermes çağrı
telemetrisi pano kartı `hermes.py:2503` · ③ cf çıkış-yasası sapması `analytics.py:1380`) bu eleme
listesinin DIŞINDAydı ve durumları ölçülmedi — stok satırları WP5-B'de yaşamaya devam ediyor.
