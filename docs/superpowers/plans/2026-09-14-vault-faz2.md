# TSK-064 Faz-2 — HashiCorp Vault (repo tarafı) uygulama planı

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development (tek Opus implementer, Sonnet inceleme). Adımlar `- [ ]` kutulu. A1 kurulumu (Rol-1, sudo) bu planın DIŞINDA — bu plan yalnız REPO tarafını üretir; A1'de koşacak betikler burada yazılır ama AJAN A1'e dokunmaz.

**Goal:** Sırlar tek kasada (Vault kv-v2, A1 loopback) dursun; mevcut dosya kanalları (`/etc/meridian/*`, `/etc/hindsight/creds/*`) Vault Agent şablonlarından üretilsin; tüketici kodu DEĞİŞMESİN.

**Architecture:** Vault 2.1.0 ikili (pinli sha), `storage "file"`, `127.0.0.1:8200` TLS'siz, sertleştirilmiş `vault.service` + `ExecStartPost` oto-unseal (Shamir 1/1, anahtar dosyası `/etc/vault/unseal.key` 0400 root, değer stdin'den). `vault-agent.service` (AppRole) 7 sırrı dosyaya render eder; tek kaynak `deploy/sir_envanteri.yaml`'a eklenen `vault_kv` bölümünden politika + agent şablonu ÜRETİLİR (üretilmiş dosyalar çivili). Bekçi: `vault-sagligi.timer` → `VAULT_SEALED` alarm sınıfı.

**Spec:** `docs/TASARIM-SIR-YOL1-2026-09-03.md` §6 (Faz-2 eki; operatör K6/K6b 2026-09-07; "A ve B hemen" 2026-09-14 07:3xZ). Lisans kapısı Rol-1 tarafından GEÇİLDİ (2026-09-14: BSL 1.1 Additional Use Grant — "production use … provided your use does not include offering the Licensed Work to third parties on a hosted or embedded basis to compete" → iç sır deposu kapsam dışı).

## Global Constraints
- Sır DEĞERİ hiçbir betikte argv'ye, log'a, terminale girmez (`read -s`, stdin `-`, `--field` çıktısı yalnız sha256'ya). Çivi: betik kaynağında `echo $` ile sır değişkeni basılmıyor (regex).
- Vault sürümü pinli: `2.1.0`, zip sha256 `SHA256SUMS`tan; `vault_kur.sh` içinde sabit; sürüm değişimi = ayrı karar.
- Dinleme yalnız `127.0.0.1:8200`; `0.0.0.0` YASAK (çivi: hcl'de `address = "127.0.0.1:8200"`).
- Birimler `meridian.service` sertleştirme bloğunu taşır (NoNewPrivileges, ProtectSystem=strict, PrivateTmp, RestrictAddressFamilies=AF_INET AF_UNIX, SystemCallFilter=@system-service); `vault.service` `AmbientCapabilities=CAP_IPC_LOCK`, `ReadWritePaths=/opt/vault/data`.
- SAPMA (Rol-1 kararı): `vault-agent.service` **root** koşar (hedef dosyalar root:root 0400 tasarım gereği; ayrı kullanıcıya vermek hedef dizinleri gevşetirdi) — sertleştirme + `ReadWritePaths` yalnız hedef dizinler (`/etc/meridian /etc/hindsight/creds`). §6.3'teki `User=vault-agent` bu gerekçeyle DÜŞTÜ; docstring'e yaz.
- Dalga-1 sır kümesi (7, tek-değer dosyaları): `dash_token`, `nous_api_key`, `kapi_apikey`, `apisix_admin_key` (→ `/etc/meridian/<ad>`), `HINDSIGHT_API_DATABASE_URL`, `HINDSIGHT_API_LLM_API_KEY`, `HINDSIGHT_API_TENANT_API_KEY` (→ `/etc/hindsight/creds/<ad>`). Çok-değişkenli dosyalar (`.env-apisix`, hermes `.env`ler) Dalga-2 (ayrı kalem) — bu planda YOK, `sir_rotasyon.sh` onları yönetmeye devam eder.
- Tek-kaynak: Vault yolları/hedef dosyalar/politikalar YALNIZ `deploy/sir_envanteri.yaml::vault_kv`den üretilir; `deploy/vault/policies/*.hcl` ve `deploy/vault/agent.hcl` ÜRETİLMİŞ (başlık işareti + `--kontrol` çivisi, v437 deseni). Mevcut v439 envanter çivileri kırılmaz.
- Yasa 4/6; uydurma yasağı; çapa yasası (`dosya.py:NNN` yok); K7 sınıfı: birim şerhlerinde saat DEĞERİ yok.
- Kod yazan ajan Opus; TDD; mutasyon ≥6; kapsam v485 + v439 + v446 + v451 + v382 v401 v402.

## Dosya haritası
- Create: `deploy/vault/vault.hcl` · `deploy/vault/vault.service` · `deploy/vault/vault-unseal.service` · `deploy/vault/vault_unseal.sh` · `deploy/vault/vault-agent.service` · `deploy/vault/vault-sagligi.service` + `.timer` (5 dk) · `deploy/vault/vault_kur.sh` · `deploy/vault/vault_sir_koy.sh` · `ops/vault_politika_uret.py` (→ `deploy/vault/policies/meridian-agent.hcl`, `meridian-admin.hcl`, `deploy/vault/agent.hcl`) · `ops/vault_sagligi.py` · `tests/test_vault_faz2_v485.py`.
- Modify: `deploy/sir_envanteri.yaml` (`vault_kv` bölümü) · `deploy/ansible/roles/meridian_a1/defaults/main.yml` (`birim_kaynaklari` += `../vault/*.service`, `../vault/*.timer`; `etkin_timerlar` += `vault-sagligi.timer`) · `deploy/ansible/vars/dagit_vars.yml` (motor-dışı dosya listesi: `deploy/vault/vault_unseal.sh`→`/opt/vault/bin/vault_unseal.sh`, `vault.hcl`/`agent.hcl`→`/etc/vault/`, betikler) · `ops/runbook_uret.py` kaynağı (VAULT_SEALED alarm sınıfı — alarm sınıfları nerede kayıtlıysa ORAYA; `docs/RUNBOOK.md` + tipografi korpusu yeniden üretilir — üretilmiş belge zinciri) · `meridian/obs.py`/bekçi kaydı gerekiyorsa (ÖLÇ).

## Task 1 — Vault sunucu birimleri + oto-unseal
- [ ] Kırmızı çivi (v485): `vault.hcl` listener 127.0.0.1:8200, `tls_disable = true`, storage file `/opt/vault/data`; `vault.service` sertleştirme satırları + `ExecStartPost=/opt/vault/bin/vault_unseal.sh`; `vault_unseal.sh` değeri argv'ye koymuyor (`operator unseal -` stdin) ve health beklemesi `_servis_ayakta` deseni (dash_token_credential.sh emsali; en çok 60 s, uyku döngüsü değil — `sleep 1` ile sınırlı sayaç kabul: bu bir bekleyici döngüsü DEĞİL, servis açılış yoklamasıdır, sınırı ve gerekçesi yazılır).
- [ ] Uygula; `vault operator unseal -h` ile stdin desteğini ÖLÇ [ÖLÇÜLDÜ 2026-09-14 tur-3: YOK — TTY dışında reddeder; betik `write sys/unseal key=-` kullanır, v485 C2/C5b/C11/C12] (yerelde vault ikilisi yoksa: çivi kaynak metnini okur, davranış A1 test-ateşlemesinde — Rol-1).
- [ ] Yeşil + mutasyon (address 0.0.0.0'a çevrilince kırmızı; `-` stdin kalkınca kırmızı).

## Task 2 — Tek kaynak: envanter `vault_kv` + politika/agent üretici
- [ ] `deploy/sir_envanteri.yaml`'a `vault_kv:` listesi: `{ad, vault_yolu: secret/meridian/<ad>, hedef: <dosya>, mod: "0400", sahip: root, tuketici: <birim>}` ×7 (v439 çivisi kırılmamalı — ÖLÇ).
- [ ] `ops/vault_politika_uret.py` (KOMUT SATIRI: `--kontrol` / `--uygula`; çıkış 1 bayat): `policies/meridian-agent.hcl` (7 yol `read`), `policies/meridian-admin.hcl` (`secret/meridian/*` CRUD + `sys/health` read + `auth/approle/role/agent/secret-id` update), `agent.hcl` (auto_auth approle: `role_id_file_path=/etc/vault/agent.role-id`, `secret_id_file_path=/etc/vault/agent.secret-id`, `remove_secret_id_file_after_reading=false`; `vault { address = "http://127.0.0.1:8200" }`; her sır için `template { source/contents = "{{ with secret \"secret/data/meridian/<ad>\" }}{{ .Data.data.value }}{{ end }}" destination = <hedef> perms = 0400 }`).
- [ ] Çiviler: üretilmiş üç dosya == üretici çıktısı (bayt); envanter `vault_kv` ↔ agent şablon hedefleri birebir; hedef dosyalar mevcut `LoadCredential=` drop-in'lerinin kaynaklarıyla eşleşiyor (drop-in dosyalarından ölçülür — `deploy/oracle-a1/*.service.d/*credential*.conf`, `deploy/hindsight/…`); mutasyon.

## Task 3 — `vault_kur.sh` (A1, idempotent, Rol-1 koşar) + `vault_sir_koy.sh`
- [ ] `vault_kur.sh` adımları (`--kuru` destekli, her adım idempotent, sır basmaz): ikili `/tmp/vault_unzip/vault` → `/usr/local/bin/vault` (sha256 pin doğrulanır: ikilinin sha'sı ya da zip sha'sı sabit), `vault` kullanıcısı (system, nologin), dizinler (`/opt/vault/data` 0700 vault, `/opt/vault/bin`, `/etc/vault` 0700 root), `vault.hcl` kopyası, birimler (A0 rolü kurmadıysa `install`), `systemctl enable --now vault`, `operator init -key-shares=1 -key-threshold=1 -format=json` → python ile ayrıştırıp `unseal.key`/`root.token` 0400 root (DEĞER BASILMAZ), unseal, `secrets enable -path=secret kv-v2`, politikalar yaz, `auth enable approle`, `role/agent` (policy meridian-agent, `secret_id_ttl=0`, `secret_id_num_uses=0`, `token_ttl=1h`, `token_max_ttl=24h`), `role-id` → `/etc/vault/agent.role-id` 0400, `secret-id` → `/etc/vault/agent.secret-id` 0400, admin token (policy meridian-admin, ttl 720h) → `/etc/vault/admin.token` 0400, kök jetonu `token revoke` (son adım, `--kok-iptal` bayrağıyla açık). Her adımın kanıtı `vault status`/`vault read` çıktısının SIR OLMAYAN alanları.
- [ ] `vault_sir_koy.sh`: dalga-1 sırlarını mevcut dosyalardan Vault'a taşır: `vault kv put secret/meridian/<ad> value=-` (stdin `< dosya`), sonra `vault kv get -field=value | sha256sum` == `sha256sum dosya` (ikisi de yalnız sha basar); `--kuru` yalnız listeler.
- [ ] Çiviler: betikler `bash -n`; sır sızıntı regex (echo/printf ile `$UNSEAL`/`$TOKEN`/`$DEGER` yok; `set -x` yok); `--kuru` yolu çıkış 0 ve hiçbir yazım (sahte `vault` PATH'te stub ile); mutasyon.

## Task 4 — Bekçi + canary + dağıtım listeleri
- [ ] `ops/vault_sagligi.py`: `GET http://127.0.0.1:8200/v1/sys/health` → 200/429 sağlıklı; 503 → `VAULT_SEALED`; bağlantı yok → `VAULT_DOWN`; çıkış 0/1; `obs.warn` ile olay (meridian ithali — obs canlı deftere yazar: yalnız A1'de timer'la; testte `sandbox_state`). `vault-sagligi.{service,timer}` (5 dk, `OnCalendar` tek kaynak; şerhlerde saat değeri yok — K7).
- [ ] Alarm sınıfı kaydı: `VAULT_SEALED`/`VAULT_DOWN` RUNBOOK üreticisinin okuduğu kaynağa (ölç: `ops/runbook_uret.py` alarm sınıflarını nereden derliyor) → `docs/RUNBOOK.md` yeniden üret (v154) + tipografi korpusu (v209) — üretilmiş belge zinciri; çivi.
- [ ] `defaults/main.yml` birim glob'ları + `etkin_timerlar`; `dagit_vars.yml` motor-dışı dosyalar; v451/v446 yeşil.
- [ ] Canary REÇETESİ (Rol-1 A1'de koşar; koda girmez): `dash_token_credential.sh::faz2` deseni — Vault'ta gerçek değer, eski dosyada sahte → agent render → tüketici restart → 200; `/proc/<pid>/environ` grep 0; `vault status` Sealed=false; `systemctl show meridian -p LoadCredential`.

## Kapsam koşumu ve teslim
v485 + `tests/test_sir_credential_v439.py tests/test_ops_duzeltme_v446.py tests/test_ansible_a0_v451.py tests/test_uiux_s1b_v154.py tests/test_tipografi_rampa_v209.py` + v382 v401 v402 → `…/scratchpad/vault/final.log`, üçlü hüküm. Rapor `…/scratchpad/vault/rapor_vault_faz2.md`; dönüş: durum + test özeti + üretilmiş dosya listesi + A1 reçetesi sırası (Rol-1'in koşacağı komutlar, sır değeri yok) + kaygılar (≤6).
