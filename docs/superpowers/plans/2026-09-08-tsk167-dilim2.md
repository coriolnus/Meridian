# TSK-167 dilim-2 — B1 uygulama planı (`/api/arama` ucu + pano arama kutusu)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox syntax.

**Goal:** Dilim-1'in A1'de kurulu arama CLI'ını (`research/olcumler/edg067_hindsight_faz1/hafiza_ara.py`
→ `deploy/hindsight/hafiza_ara.sh`) panoya BİR OKUYUCU olarak bağlamak: `meridian/arama.py` (ince alt-süreç
sarmalayıcısı + kesit/süzgeç/scrub) + `meridian/api.py` `GET /api/arama` + `ui/` Hafıza yüzeyinde arama
görünümü. Korpus tazelemesi, indeks kurulumu, belge açma yüzeyi ve sohbet aracının yeniden yazımı bu planın DIŞI.

**Spec/Kaynak:** ROADMAP `[TSK-167]` gövdesi (dilim-1 19:0xZ notu + 23:0xZ hazır varlık notu) ·
`docs/TASARIM-HINDSIGHT-ENTEGRASYON-2026-08-31.md` §4/§10 (DİKKAT: §10'un sonuç satırı şeması
"observation kimliği + ispat sayısı + güven" HİNDSIGHT RECALL şeklidir ve MELEZ hükmünden sonra
BAYATTIR — gerçek şema `taban_indeks.py::en_yakin`in döndürdüğüdür: `dosya · bolum · metin · blob_sha ·
mesafe`; düzeltme Rol-1 kalemi) · TSK-060 MELEZ kararı (2026-09-07 10:12Z) · EDG-2026-067 hükmü
(2026-09-06 22:4xZ KALDI).

**Emsaller (ÖLÇ, varsayma):** `meridian/sohbet.py::_arac_hafiza_ara` + `_hafiza_betigi` (alt süreç, k
kelepçesi, "ölçülemedi" dili) · `meridian/api.py::api_sohbet` (`_auth`, `run_in_threadpool` gerekçesi,
`obs.log` künyesi — içerik yazılmaz) · `meridian/api.py::api_hindsight_recall` (200 + `neden` zarfı,
beyaz liste) · `meridian/skill_gorus_llm.py::kota_durumu` (sayaç deseni — BU UÇTA KULLANILMAZ, gerekçe
aşağıda) · `meridian/notify.py::scrub` · `ui/src/pano/yuzeyler/hafiza/Recall.tsx` (sorgu formu → sonuç,
ham skor, üç-durum çizimi) · `ui/src/pano/yuzeyler/hafiza/Dersler.tsx` + `gorunumler.ts` + `alanlar.ts`
(TSK-118 "onuncu durak" emsali) · `ui/src/pano/kabuk/search-dialog.tsx` + `komutlar.ts::AJANA_SOR_YOLU`
+ `ui/src/pano/yuzeyler/ajan/sohbet.ts::sohbetIstegiBirak` (palet metni TAŞIR, ateşlemez) ·
`docs/superpowers/plans/2026-09-07-pano-sohbet-b1.md` (biçim) · `2026-09-07-pano-sohbet-b2.md` (UI kısıtları).

## ÖLÇÜLEN ZEMİN (bu plan bunların üstüne kurulur; hiçbiri tahmin değildir)

| Ne | Ölçüm | Kaynak |
|---|---|---|
| Arama motoru | sqlite-vec + bge-m3 ONNX **taban**; Hindsight recall DEĞİL | EDG-067 hükmü 2026-09-06 22:4xZ |
| İsabet | dosya@3 %55,6 · bölüm@3 %27,8 (n=36) | `sonuc_2026-09-06/edg067_kiyas_*.json` |
| Gecikme | p50 616 ms · p95 731 ms — **SICAK süreçte** (kıyas döngüsü `taban_hazirla`yı BİR KEZ çağırır) | aynı rapor |
| Soğuk açılış | **ÖLÇÜLMEDİ** — her HTTP isteği yeni bir süreç ve yeni bir `onnxruntime.InferenceSession` doğurur | `taban_indeks.py::OnnxGomucu.__init__` |
| İndeks | `/opt/hindsight/edg067/taban.sqlite` · 3.582 chunk · HEAD `ac824e1` (2026-09-06 23:0xZ) | ROADMAP TSK-167 gövdesi |
| Tazeleme | Pazar 08:15Z `hindsight-taban-tazele.timer` → manifest_uret → taban_indeks → taban_terfi (≥%90 satır kapısı) | `deploy/hindsight/*` |
| Korpus | günlük · `ROADMAP.md%237` · `research/cards/*.yaml` · `docs/**/*.md` (RUNBOOK hariç), hepsi **git HEAD blob'u** | `manifest_uret.py` |
| CLI çıkışı | rc 0 sonuç · 1 girdi/uzantı · 2 kullanım · 3 ölçülemedi (model/şema ayrışması) | `hafiza_ara.py` başlığı + `hafiza_ara.sh` (v446 ikisini birlikte ölçer) |
| Test numarası | en büyük `vNNN` = **v454**; v455/v456 boş (koşum anında YENİDEN ölç) | `ls tests \| grep -oE 'v[0-9]+' \| sort -n \| tail -1` |

## Global Constraints

- **AUTH ZORUNLU.** `GET /api/arama` ilk satırında `_auth(request)`. Yetkisiz GET beyaz listesine
  (`tests/test_api_audit_v21.py::test_p1c`) EKLENMEZ — bu bir VERİ ucudur (depo içeriğini döndürür).
- **SALT OKUMA (Yasa 6 / codelaw).** Yeni hiçbir `state/` dosyası doğmaz, hiçbir mevcut deftere satır
  yazılmaz. Tek yazım `obs.log("arama_sorgu", …)` künyesidir ve okuyucusu MEVCUTtur (`/api/events`,
  pano olay şeridi) — beyanlı muafiyet (`DECLARED_SINKS`) GEREKMEZ. Çivi: sahte store ile yazılan dosya
  adları kümesi BOŞ.
- **SIR DIŞLAMA — İKİ KATMAN.** (a) Yapısal: korpus `manifest_uret.py`nin git-HEAD beyaz listesidir;
  `state/secrets.json` ve `.env` versiyonlanmadığı için indekse GİREMEZ. (b) Savunma derinliği: uç,
  dönen her satırın `dosya` alanını korpus önekleri kümesiyle (`docs/`, `research/cards/`,
  `MERIDIAN_ENGINEERING_LOG.md`, `ROADMAP.md%237`) süzer; küme dışı satır DÜŞÜRÜLÜR ve sayısı
  `korpus_disi_n` olarak BEYAN EDİLİR (sessizce yutulmaz). Ayrıca giden her dizge `notify.scrub`tan geçer.
- **ÇIKTI KESİTİ TAVANLI VE BEDELİ BEYANLI.** `--json` ham chunk metnini KESMEDEN verir (pencere 1500
  karakter). Uç `ARAMA_KESIT_TAVANI` (öneri 600 karakter) uygular ve HER satırda `kesit_kirpildi: bool`
  + `metin_uzunluk: int` döndürür — kırpma görünür olmadan bedel ölçülmemiş olur (bedel yasası; dilim-1'in
  K8 bulgusuyla aynı sınıf).
- **SÜRE TAVANI.** `ARAMA_ZAMAN_ASIMI_S` (öneri 60; sohbetin 180'i etkileşimli bir pano kutusu için
  fazla). Aşımda süreç öldürülür, uç `neden: "ölçülemedi: zaman aşımı (Ns)"` döner. **Sayı SEÇİLDİ,
  ÖLÇÜLMEDİ** ve ilk gerçek A1 sorgusundan sonra ölçülmüş değerle değiştirilmelidir (Görev 3).
- **KOTA YOK — VE BU BEYANLI.** Uç hiçbir model çağırmaz (`llm-cagri-kotasi` yüzeyine dokunmaz;
  hafıza: hindsight-aktif-kullanim "LLM'siz, kota yok"), bu yüzden `skill_gorus_llm.kota_durumu`
  emsali UYGULANMAZ. Ama BEDEL SIFIR DEĞİL: her sorgu A1'in 4 OCPU'sunda bir ONNX oturumu açar.
  Karşılığı bir sayaç değil bir KİLİTtir: aynı anda tek arama (`mesgul: true` ile ikinci istek süreç
  DOĞURMADAN döner — `sohbet.py` kilidinin emsali) + `obs.log` künyesi (gün içi çağrı sayısı olay
  defterinden SAYILIR, ikinci bir sayaç dosyası TUTULMAZ — tek-kaynak).
- **ALT SÜREÇ SÖZLEŞMESİ.** `subprocess.run([betik, "--json", "-k", str(k), …, soru])` — `shell=False`,
  soru argv'de. Kabuk enjeksiyonu yüzeyi YOKTUR ve bu çivilidir.
- **THREAD HAVUZU ZORUNLU.** `run_in_threadpool` (gerekçe `api_sohbet` şerhinde: tek işçi/tek döngü;
  bloklayıcı alt süreç olay döngüsünde beklerse `/api/control/halt` dahil her uç susar).
- **"BOŞ SONUÇ" ≠ "ÖLÇÜLEMEDİ".** Betik yoksa / rc≠0 ise `sonuclar: null` + `neden`; gerçekten 0 satır
  dönerse `sonuclar: []` + `neden: null`. İkisini tek gövdeye katlamak uydurma yasağının ihlalidir.
- **YOL SÖZLEŞMESİ TEK KAYNAKTAN.** Betik yolu ve env adı `meridian/arama.py`de yaşar; `sohbet.py::
  _hafiza_betigi` ona DEVREDİLİR (env adı `SOHBET_HAFIZA_ARA` KORUNUR — v440 onu monkeypatch'liyor).
  İki ayrı çözücü tutmak sessiz ayrışmadır. *(Rol-1 kararı gerekir: devir mi, kopya + ayrışma çivisi mi.)*
- **UI:** `ui/` tek yer; ham hex YASAK (yalnız `--color-*` jetonları, v208/v407); `fetch(` YASAK
  (`veri.ts::apiGet` tek kapı, v378); **yoklama YASAK** (arama `useApi` ile 15/30 sn'de bir koşmaz —
  CPU bedeli; sorgu OPERATÖR eylemiyle koşar); çapa yasağı (`dosya.tsx:NNN`).
- **BUILD EN SON ADIM.** Ajan `cd ui && npm run kontrol` koşar; `npm run build` **Rol-1'in** (dagit 5c
  mtime kapısı). Ajan: git yok, dağıtım yok, tam suite yok, `-q` yok, `serve.sh` yok, A1 ssh yok.
- **UYGULAMA YÜKLENMEZ.** Ajan tarayıcı doğrulaması YAPMAZ; canlı doğrulama Görev 3'te Rol-1'de
  (`2026-09-02-hafiza-sayfasi.md` Task 3 kuralı: "tarayıcı doğrulaması operatöre bırakılmaz").

## Task 1 — `meridian/arama.py` + `GET /api/arama` — implementer

**Files:** Create `meridian/arama.py` · Modify `meridian/api.py` (yeni uç) · Modify `meridian/sohbet.py`
(YALNIZ `_hafiza_betigi` devri, K2 onaylanırsa) · Test `tests/test_arama_api_v455.py`
(numara koşum anında `ls tests | grep -oE 'v[0-9]+' | sort -n | tail -1` ile DOĞRULANIR; v454 alınmış).

**Interfaces (Produces):**
- `arama.betik_yolu() -> str` (env `SOHBET_HAFIZA_ARA`, varsayılan `config.ROOT/deploy/hindsight/hafiza_ara.sh`)
- `arama.ara(soru: str, k: int = 5, dosya: str | None = None, *, kos=None, simdi=None) -> dict`
  → `{"sonuclar": list|None, "kunye": dict|None, "n": int|None, "korpus_disi_n": int, "sure_s": float,
     "mesgul": bool, "neden": str|None}`; `kos` enjekte edilebilir (çiviler sahte alt süreç verir).
- `arama.KORPUS_ONEKLERI: tuple[str, ...]` · `ARAMA_KESIT_TAVANI` · `ARAMA_ZAMAN_ASIMI_S` · `K_TAVANI = 20`
- `api.py`: `GET /api/arama?soru=&k=&dosya=` → yukarıdaki gövde; `400` boş soru / geçersiz `k` /
  beyaz liste dışı `dosya` öneki; `401` yetkisiz.

- [ ] Çivi 1 (kırmızı): `DASH_TOKEN` kuruluyken jetonsuz `GET /api/arama` → 401.
- [ ] Çivi 2: `soru=""` / `soru="   "` / parametre yok → 400 **ve alt süreç HİÇ ÇAĞRILMAZ** (sahte `kos` sayacı 0).
- [ ] Çivi 3: `k` kelepçesi — `0`, `-1`, `999` → 1..20 aralığına çekilir (argv'den ÖLÇÜLÜR); sayı olmayan `k` → 400.
- [ ] Çivi 4: `dosya` öneki beyaz liste dışıysa (`state/`, `/etc/`, `../`, `.env`) → 400, süreç doğmaz.
- [ ] Çivi 5: argv sözleşmesi — `--json` ve `-k` geçer, soru AYRI argv öğesidir, `shell=False`
      (sahte `kos` argv listesini yakalar; `;`/`$()` içeren soru zararsız geçer).
- [ ] Çivi 6: betik YOKken → 200 + `sonuclar: None` + `neden` "ölçülemedi: …" (0 sonuç DEĞİL).
      *(K1 kararı 503 çıkarsa çivi ona göre yazılır.)*
- [ ] Çivi 7: rc ayrımı — rc=1 · rc=2 · rc=3 ÜÇ AYRI `neden` metni üretir (CLI'ın "ÇIKIŞ KODU
      SÖZLEŞMESİ" bloğu tek kaynak; sarmalayıcıyla ayrışma zaten v446'da çivili).
- [ ] Çivi 8: zaman aşımı → süreç öldürülür, `neden` süreyi ADIYLA yazar.
- [ ] Çivi 9: **SIR** — sahte bir `secrets.ALLOWED` değeri chunk metnine konur → yanıt gövdesinde YOK (`scrub`).
- [ ] Çivi 10: **KORPUS DIŞI YOL** — sahte çıktıda `state/secrets.json` satırı → düşürülür,
      `korpus_disi_n == 1`, gövdede o yol GEÇMEZ.
- [ ] Çivi 11: kesit tavanı — 5.000 karakterlik metin → `kesit` ≤ tavan, `kesit_kirpildi is True`,
      `metin_uzunluk == 5000` (bedel beyanı).
- [ ] Çivi 12: bozuk stdout (JSON değil) → `sonuclar: None` + `neden`; `except` İŞARETLİ (Yasa 4).
- [ ] Çivi 13: `obs.log("arama_sorgu", …)` künye yazar — `k`, `n`, `sure_s`, `neden`, `soru_uzunluk`;
      **SORU METNİ YAZILMAZ** (sohbet emsali: içerik olay defterine girmez).
- [ ] Çivi 14: eşzamanlılık — kilit tutulurken ikinci çağrı `mesgul: true` döner ve İKİNCİ SÜREÇ DOĞMAZ.
- [ ] Çivi 15: Yasa 6 — sahte store ile yazılan dosya adları kümesi BOŞ; `tests/test_codelaw_v59.py` yeşil.
- [ ] Çivi 16: `sohbet._hafiza_betigi()` ile `arama.betik_yolu()` AYNI değeri döndürür (devir çivisi);
      `tests/test_sohbet_v440.py` yeşil kalır (env adı korunmuş).
- [ ] Uygulama (minimal, TDD) · [ ] mutasyon ≥5 (scrub atlama · korpus öneki süzgeci · k kelepçesi ·
      kesit tavanı · "boş sonuç ↔ ölçülemedi" katlaması) · [ ] rapor

## Task 2 — Pano arama kutusu (`ui/`) — implementer (Task 1 sonrası, ayrı ajan olabilir)

**Files:** Create `ui/src/pano/yuzeyler/hafiza/Arama.tsx` (+ gerekirse `arama.ts` saf mantık) ·
Modify `ui/src/pano/alanlar.ts` (memory.bolumler'e ONUNCU kayıt) · `ui/src/pano/yuzeyler/hafiza/gorunumler.ts`
(`HAFIZA_GORUNUMLERI` sonuna) · `HafizaYuzey.tsx` (`GOVDELER` tablosu — `Record<HafizaGorunumu, …>` tipi
eksik girdide DERLEMEYİ KIRAR, sessiz yedek yok) · `ui/src/pano/komutlar.ts` (`BOLUM_EK` anahtarları +
"Belgelerde ara…" komutu) · `ui/src/pano/kabuk/search-dialog.tsx` · Test `tests/test_arama_ui_v456.py`
(kaynak taraması; `ui/`de vitest YOK — ölçüldü `ui/package.json`: yalnız `build` ve `kontrol`).

- [ ] **Ev: Hafıza yüzeyinin ONUNCU durağı** `hafiza-arama` (başlık "Arama", soru "Bu soruya hangi
      belgeler yakın?"), **CP sıralamasının SONUNA** — TSK-118 `hafiza-dersler` emsali birebir
      (ilk sekizin CP paritesi iddiası korunur).
- [ ] **Banka seçici beyanı:** kabuk her görünümün üstüne "Banka" seçicisi çizer; bu görünüm bankayı
      KULLANMAZ (kaynak: sqlite-vec taban indeksi). Görünüm bunu bir cümleyle YAZAR — sessizce yok
      saymak, operatöre etkisi olmayan bir denetim göstermek olurdu. *(K3: alternatif, kabuğun
      seçiciyi görünüm bazında gizlemesi — kabuk değişikliği, ayrı karar.)*
- [ ] **Kutu:** metin girişi + "Ara" (Enter de gönderir) + `k` seçimi (5/10/20) + `--dosya` öneki
      seçimi (kapalı liste: hepsi · `docs/` · `research/cards/` · günlük). `apiGet("/api/arama?…")`,
      `AbortController` ile önceki istek iptal edilir. **Yoklama YOK.**
- [ ] **Sonuç satırı:** `dosya` · `bolum` · `mesafe` (HAM, yuvarlanmaz — Recall.tsx'in ölçülmüş
      gerekçesi) · `kesit`; kırpıldıysa "…(+N karakter)" beyanı; `blob_sha` künyede.
- [ ] **Okuma talimatı ekranda:** "ilk üç sonuçtan birinin doğru dosya olma oranı ölçülmüş %55,6 —
      bu bir CEVAP değil ADAY LİSTESİdir" (CLI başlığındaki hüküm; sayı tek kaynaktan, ekrana
      elle yazılan ikinci bir orana DÖNÜŞMESİN diye şerhle işaretlenir).
- [ ] **Bayatlık görünür:** `kunye.head_commit` + `uretim_ts` rozet olarak; indeks haftalık tazelenir
      (Pazar 08:15Z) ve bu satır "canlı depo" iddiası TAŞIMAZ.
- [ ] **Üç durum ayrık:** `neden` doluysa `Olculemedi` (asla "sonuç yok") · `sonuclar: []` → "eşik yok,
      sıralama saf mesafedir: liste boş" · dolu → tablo. `mesgul: true` → "başka bir arama koşuyor".
- [ ] **Belge AÇILMAZ — ve bu yazılır:** `/api/belge?yol=` UCU YOKTUR (ölçüldü). Satır yolu KOPYALANABİLİR
      olur ("yolu kopyala"); `ROADMAP.md%237` sonucu ROADMAP yüzeyine derin bağ TAŞIMAZ (kesit
      document_id'si sayfa çapası değildir). Belge görüntüleyici AYRI DİLİM.
- [ ] **⌘K:** "Belgelerde ara…" komutu — `AJANA_SOR` deseni birebir: yazılmış metni bir istek kutusuyla
      TAŞIR (`aramaIstegiBirak`, `sohbetIstegiBirak` emsali), Hafıza→Arama adresine gider ve
      **SORGUYU ATEŞLEMEZ** (paletin "hızlı erişim ≠ icra" hükmü + her tuş vuruşunda ONNX süreci
      doğurmama gerekçesi). Adres sorgusuyla TAŞIMAZ: `gorunumler.ts::sekmeliYol` bugün TEK sorgu
      anahtarı biliyor (`sekme`) ve ikincisini sessizce düşürürdü (ölçülmüş sınır).
- [ ] Çiviler (python kaynak taraması): kimlik üç dosyada da var (`alanlar.ts` · `gorunumler.ts` ·
      `GOVDELER`) · yeni dosyada `useApi(` GEÇMEZ · `fetch(` GEÇMEZ · ham hex GEÇMEZ (v208/v407
      kapsamı doğrulanır) · palet komutunun `onSelect`inde ağ çağrısı YOK · `neden` dolu dalında
      "sonuç yok" dizgesi YOK.
- [ ] `cd ui && npm run kontrol` temiz (**`npm run build` KOŞULMAZ** — Rol-1'in adımı) · [ ] rapor

## Task 3 — Rol-1: merge → suite → dağıtım → İLK GERÇEK SORGU (A1)

- [ ] Ajan dalları merge; `cd ui && npm run build` (dagit 5c mtime kapısı için EN SON adım).
- [ ] Tam suite: arka planda, `-n 4`, donmuş ağaç, üçlü hüküm (`FAILED|ERROR` boş + "N passed" + `PYTEST_EXIT=0`).
- [ ] Dağıtım reçetesi (temiz ağaç → `--dry-run` = `--check --diff` → mtime → worker durdur → dağıt → doğrula).
- [ ] **İLK GERÇEK ÖLÇÜM (bu dilimin hükmü):** A1'de panodan bir sorgu + `time ~/bin/hafiza_ara.sh
      --json -k 5 "…"` → (a) soğuk açılış süresi (ONNX oturumu her istekte yeniden kuruluyor),
      (b) uçtan uca `sure_s`, (c) EDG-067 soru kümesinden 3 soruyla göz kontrolü. Sonuç ROADMAP
      TSK-167 gövdesine + `EDG-2026-067` kurulum kaydına yazılır.
- [ ] **Karar kapısı:** soğuk açılış tavanı aşıyorsa dilim-3 açılır (sıcak yardımcı süreç / uzun ömürlü
      gömücü) — bu planda ÇÖZÜLMEZ, ölçülür.
- [ ] `docs/TASARIM-HINDSIGHT-ENTEGRASYON-2026-08-31.md` §10 arama-kutusu satırının bayat şeması
      (observation id + güven) MELEZ hükmüne göre düzeltilir.
- [ ] ROADMAP TSK-167: dilim-2 notu + statü.

## Belirsizlikler — Rol-1 kararı gerekir

1. **K1** Arıza zarfı: 200 + `neden` (recall vekili emsali, pano kararmaz) mi, 503 mü?
2. **K2** `sohbet.py::_hafiza_betigi` devri (tek kaynak, `meridian/` dokunulur) mi, kopya + ayrışma çivisi mi?
3. **K3** Hafıza kabuğunun "Banka" seçicisi arama görünümünde anlamsız — beyanla yaşasın mı, kabuk mu değişsin?
4. **K4** `ARAMA_ZAMAN_ASIMI_S` ve `ARAMA_KESIT_TAVANI` sayıları (SEÇİLDİ, ölçülmedi; A1 ilk sorgusundan sonra revize).
5. **K5** Eşzamanlılık kilidi + `obs.log` künyesi yeterli mi, yoksa günlük çağrı tavanı da mı konsun?
6. **K6** `k` üst sınırı 20 (sohbet emsali) — pano için daha dar (10) mı?
7. **K7** Arama görünümü Hafıza altında mı kalsın, yoksa ⌘K'nın "Belgeler" grubuna ayrı bir yüzey mi olsun?

---

## Rol-1 hükümleri (2026-09-08 16:5xZ) — K1–K7 (kaynak: sohbet B1 emsalleri, recall vekili zarfı, hafıza: hindsight-aktif-kullanim; hafıza: benzer kayıt yok)
1. **K1** Arıza zarfı: **200 + `neden`** (recall vekili emsali; pano kararmaz; çivi 6 buna göre).
2. **K2** **DEVİR**: `sohbet._hafiza_betigi` → `arama.betik_yolu()` (env adı `SOHBET_HAFIZA_ARA` korunur; v440 yeşil kalır). Kopya + ayrışma çivisi DEĞİL.
3. **K3** Banka seçicisi: görünüm tek cümleyle **beyan eder**; kabuk değişikliği ayrı dilim.
4. **K4** `ARAMA_ZAMAN_ASIMI_S=60`, `ARAMA_KESIT_TAVANI=600` — SEÇİLDİ; Task 3'te A1 ilk gerçek sorgusuyla ölçülüp revize edilir (ROADMAP'e yazılır).
5. **K5** Eşzamanlılık kilidi (`mesgul`) + `obs.log` künyesi yeterli; günlük tavan YOK (LLM yok, kota yok).
6. **K6** API `K_TAVANI=20` (sohbetle ortak sabit, tek kaynak); pano kutusu 5/10 sunar (20 sunmaz).
7. **K7** Arama görünümü Hafıza yüzeyinin onuncu durağı (TSK-118 emsali); ⌘K komutu metni taşır, ateşlemez.
