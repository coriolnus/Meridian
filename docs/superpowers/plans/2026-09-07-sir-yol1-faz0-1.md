# TSK-064 Sır yönetimi YOL-1 — Faz-0 + Faz-1A/1B uygulama planı (tek dalga)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox syntax.

**Goal:** A1'deki systemd-doğal iki servisin (meridian, hindsight-api) sırlarını `EnvironmentFile`/düz `.env` okumasından systemd
`LoadCredential` kanalına taşımak (sır ortama girmez), envanteri çivilemek, geçişi farksal ölçümle ve geri-alımla yapmak.
**Spec:** `docs/TASARIM-SIR-YOL1-2026-09-03.md` §1–§4 (envanter, sınıflar, Faz-0/1A/1B, kapılar); operatör K3 (2026-09-03): Faz-0 ile
Faz-1A/1B TEK DALGA; operatör 2026-09-07 "bugün başla". Emsal: TSK-049 `deploy/oracle-a1/dash_token_credential.sh` + `meridian.service.d/50-dash-credential.conf`
+ `meridian/api.py::_read_dash_token` + `tests/test_wph_dash_credential_v184.py`.

## Global Constraints
- Sır DEĞERİ hiçbir dosyaya/loga/terminale yazılmaz; testlerde sahte değer; envanter YALNIZ ad taşır.
- İki kanal aynı anda canlı ilkesi (TSK-049): credential kanalı EKLENİR, env kanalı geçiş doğrulanana kadar KALIR; credential ÖNCE okunur.
- Farksal ölçüm (canary): eski kanala SAHTE değer, gerçek değer YENİ kanaldan → servis iş yapıyorsa yeni kanal gerçekten okunuyor; `/proc/<pid>/environ` grep 0.
- Rotasyon: TSK-049 hükmü "rotasyon + kanal geçişi aynı pencerede". KAPI_APIKEY (apisix tüketici anahtarı) ve HINDSIGHT TENANT/DB parolası bizim → aynı pencerede
  rotate edilir; NOUS_API_KEY ve HINDSIGHT LLM_API_KEY OpenRouter'da → operatör yeni anahtar üretince rotate; üretmezse geçiş farksal ölçümle yapılır, rotasyon
  BEYANLI olarak ertelenir (Rol-1 hükmü 2026-09-07, plan §Task 3).
- `meridian/` dokunulur → tam suite Rol-1'de; birimler kurulduğu gün elle test-ateşlenir (§9); dagit [1c] birim ayrıklığı raporlar, kurulum elle (bakım penceresi).
- Çapa yasağı, Yasa 4/6, `-q` yok, seri pytest, ajan git yok.

## Task 1 — Faz-0 envanter çivisi + Faz-1B meridian credential-önce okuma (implementer)
**Files:** Create `deploy/sir_envanteri.yaml` (dosya → değişken ADLARI + sınıf A/B/C/D + kanal; spec §1 tablosundan birebir, değer yok) ·
Modify `meridian/secrets.py` (yeni `credential_oku(ad) -> str | None`: `$CREDENTIALS_DIRECTORY/<ad>` varsa okur (strip), yoksa None; `get()` sırası
**credential → env → dosya → GCP** olur; `status()` hangi kanaldan okunduğunu ad bazında söyler — farksal ölçümün okuyucusu) · Modify `meridian/hermes.py::_nous_headers`
DEĞİŞMEZ (zaten `secrets.get` kullanıyor → otomatik kazanır; çivi ile kanıtla) · Create `deploy/oracle-a1/meridian.service.d/53-nous-kapi-credential.conf`
(`LoadCredential=NOUS_API_KEY:/etc/meridian/nous_api_key` + `LoadCredential=KAPI_APIKEY:/etc/meridian/kapi_apikey`; 50-dash emsali şerhiyle: kaynak yoksa birim açılmaz —
bilerek) · Create `deploy/oracle-a1/sir_credential_gecis.sh` (`durum | --faz1 <ad> | --faz2 <ad> | --geri-al <ad>`; dash_token_credential.sh deseninin GENELLEŞTİRİLMİŞİ:
`/etc/meridian/<dosya>` 0400 root, systemd ≥247 kapısı, `_servis_ayakta`, farksal ölçüm: `--faz2` eski `.env` satırına sahte değer yazıp gerçek değerle credential'dan
sağlık/iş kanıtı ister — NOUS için kanıt `hermes.ping_brain` 200 sınıfı, KAPI için `/llm/v1/models` 200; başarısızlıkta otomatik geri alım) · Tests
`tests/test_sir_credential_v439.py`: credential-önce sırası, CREDENTIALS_DIRECTORY yokken env, `status()` kanal beyanı, envanter yaml ↔ spec §1 ad listesi eşitliği
(ayrışma çivisi), drop-in dosyasında iki LoadCredential satırı, geçiş betiği `bash -n` + `durum` alt komutu sahte dizinle. Kırmızı-önce + ≥3 mutasyon.
- [ ] Envanter yaml + çivi (kırmızı → yeşil) · [ ] `credential_oku` + `get` sırası (kırmızı → yeşil) · [ ] drop-in + betik · [ ] mutasyon kanıtı · [ ] rapor

## Task 2 — Faz-1A hindsight-api + vekil (implementer, Task 1 sonrası)
**Files:** Create `deploy/hindsight/hindsight-api.service.d/50-creds.conf` (`LoadCredential=` ×3: HINDSIGHT_API_DATABASE_URL, HINDSIGHT_API_LLM_API_KEY,
HINDSIGHT_API_TENANT_API_KEY ← `/etc/hindsight/creds/<ad>` 0400 root) · Create `deploy/hindsight/hindsight-api-baslat.sh` (ExecStart sarmalayıcı: `$CREDENTIALS_DIRECTORY`
altındaki üç dosyayı ortama koyup `exec /opt/hindsight/venv/bin/hindsight-api …` — upstream `_FILE` desteği A1'de ÖLÇÜLENE kadar B-sınıfı yarım kazanım, beyanlı; ölçüm
Rol-1: `hindsight-api --help` / kaynak) · Modify `deploy/hindsight/hindsight-api.service` (`ExecStart` sarmalayıcıya; `EnvironmentFile` KALIR — faz-2'de 3 sır satırı
`.env`den çıkar, 29 ayar kalır) · Modify `meridian/api.py::_hafiza_anahtari` (credential-önce: `secrets.credential_oku("HINDSIGHT_API_TENANT_API_KEY")`, yoksa
`_env_anahtari`) + `deploy/oracle-a1/meridian.service.d/54-hafiza-credential.conf` (`LoadCredential=HINDSIGHT_API_TENANT_API_KEY:/etc/hindsight/creds/HINDSIGHT_API_TENANT_API_KEY`)
· Tests `tests/test_hafiza_yuzeyi_v375.py` genişletme (credential-önce) + `tests/test_sir_credential_v439.py` ek çiviler.

## Task 3 — A1 geçişi (Rol-1, bakım penceresi; kod main'de + tam suite + dağıtım sonrası)
1. Faz-0: `chmod 600 /opt/apisix/.env-apisix` (F9 beyanı), envanter yaml ↔ canlı ad listesi (`cut -d= -f1`) kıyası.
2. Faz-1B: `sir_credential_gecis.sh --faz1 NOUS_API_KEY` / `KAPI_APIKEY` (kaynak dosyalar 0400 root; değer `read -s` ile — terminale basılmaz) → drop-in kur → restart →
   `--faz2` farksal ölçüm → `.env`den satırlar kaldırılır (faz-2), `/proc/<pid>/environ` grep 0. KAPI_APIKEY aynı pencerede rotate (apisix tüketici anahtarı; `ops/apisix_uygula.py`).
3. Faz-1A: `/etc/hindsight/creds/*` → drop-in + sarmalayıcı → restart → `/health` + `/api/hermes` vekil 200 → `.env`den 3 sır satırı çıkar. TENANT ve DB parolası rotate.
4. Rotasyon-OpenRouter: operatör yeni NOUS/LLM anahtarı verirse aynı pencerede; vermezse beyanlı erteleme (ROADMAP notu + hafıza).
5. Her birim elle test-ateşleme; dagit `--dry-run` [1c] beyanı; ROADMAP TSK-064 Faz-0/1 DONE notu + günlük.
