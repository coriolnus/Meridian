# TSK-187 — Canlı/replay `r_multiple` paydası: hisse-başı GİRİŞ RİSKİ (uygulama planı)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development (tek Opus implementer, Sonnet inceleme). Adımlar `- [ ]` kutulu.

**Goal:** Canlı ve replay işlem satırındaki `r_multiple`, girişte donan dolar bütçesine değil `adet_taban × (dolum − stop)` paydasına bölünsün — gölge motoruyla aynı birim; adet aynadan benimsenince R hisse-başı tutarlı kalsın.

**Architecture:** `meridian/broker.py::Position` yeni alan `qty_taban` (girişte = qty; `loop._adet_benimse` benimsediğinde = yeni adet; `scale_out` DOKUNMAZ). `PaperBroker.close_position` paydası `pos.qty_taban * pos.r_per_share`; `risk_dollars` boyutlandırma/maruziyet için aynen kalır. İşlem satırı `extra_json` içine `r_payda="giris_riski"` + `r_payda_usd` damgası (eski satırlar damgasız = bütçe). Replay aynı sınıfı kullandığı için otomatik geçer.

**Spec:** `research/cards/EDG-2026-091-canli-r-paydasi-kaynagi.yaml` (`hukum_2026_09_13`, `karar_operatore` (b)); operatör kararı 2026-09-13 21:1xZ. Geriye dönük yeniden hesap YOK (ayrı karar).

## Global Constraints
- Yasa 4: her `except`/geri-düşüş `# sessiz-yutma: <≥20 karakter gerekçe>`; `r_per_share <= 0` ya da `qty_taban <= 0` → `r_multiple = 0.0` + `obs.warn("r_payda_gecersiz", …)` (sessiz değil).
- Yasa 6: `r_payda`/`r_payda_usd` alanlarının okuyucusu EDG-088 PK(2) betiği + karne notu — çivide beyan.
- Uydurma yasağı; tek-kaynak: payda tanımı TEK yerde (`Position.r_payda_usd()` yardımcı ya da `close_position` içinde tek satır — mfe/mae ile aynı `r_per_share`).
- Çapa yasası: `dosya.py:NNN` yok. Motor dosyası → tam suite Rol-1'de → dağıtım #43.
- Ölçek-çıkışı: `exit.scale_out_frac` üretimde 0 ama kod yolu var — `qty_taban` ölçek-çıkışında DEĞİŞMEZ (pnl = banked + kalan, orijinal adet ölçeğinde).

## Dosya haritası
- Modify: `meridian/broker.py` (`Position` alanı + `fill_entry` ataması + `close_position` paydası/damga + durum serileştirme/yeniden yükleme — ÖLÇ: pozisyonlar `state/`e nasıl yazılıp okunuyor; eski kayıtta `qty_taban` yoksa yüklemede `qty_taban = qty` + `obs.log("r_payda_gocu", …)` BİR kez).
- Modify: `meridian/loop.py::_adet_benimse` (`poz.qty_taban = yeni` — `poz.qty = yeni` satırının yanında).
- Test: `tests/test_r_paydasi_giris_riski_v482.py` (yeni).
- Mevcut çivilerde bütçe-R bekleyen beklenen değerler (backtest/broker/analytics testleri) — her değişen beklentinin el hesabı test docstring'inde.

## Task 1 — `Position.qty_taban` + yükleme göçü
- [ ] Kırmızı çivi: `Position(...)` `qty_taban` alanı; eski sözlükten yükleme `qty_taban` yoksa `qty`ye eşitlenir ve olay yazılır (`sandbox_state`).
- [ ] Uygula: alan (default 0), `fill_entry` içinde `qty_taban=qty` (ADV/notional daraltmalarından SONRAKİ qty), yükleme göçü.
- [ ] Yeşil + mutasyon (göç satırı silinince kırmızı).

## Task 2 — `close_position` paydası + damga
- [ ] Kırmızı çivi: sentetik VRTX — giriş 559,37 / stop 530,34 / qty 17 → benimseme 38 → çıkış 529,38: `r_multiple == round(pnl / (38 × 29,03…), 3)` (el hesabı docstring'de); eski bütçe formülüyle ~2× fark; `extra_json.r_payda == "giris_riski"`, `r_payda_usd` el hesabıyla ±0,01.
- [ ] Kırmızı çivi: ölçek-çıkışlı işlem — `qty_taban` değişmez, R = (banked + kalan) / (qty_taban × rps).
- [ ] Kırmızı çivi: `r_per_share <= 0` → R 0.0 + `r_payda_gecersiz` uyarısı.
- [ ] Kırmızı çivi: replay ↔ gölge paritesi — aynı dolum/stop/çıkış için `golge_icra._kapanis_satiri` R'si ile `close_position` R'si ±0,001.
- [ ] Uygula; `mfe_r`/`mae_r` DEĞİŞMEDİ çivisi.
- [ ] Yeşil + mutasyon ≥5 (payda eski formüle dönünce, damga silinince, qty_taban benimsemede güncellenmeyince, scale_out qty_taban'ı değiştirince, guard silinince).

## Task 3 — `_adet_benimse` + kapsam
- [ ] `poz.qty_taban = yeni`; çivi: benimseme sonrası `qty_taban == qty`.
- [ ] Bütçe-R bekleyen mevcut çiviler: değişen her beklenti el hesabıyla güncellenir, docstring'e "TSK-187: payda giriş riski" notu; sayı uydurulmaz.
- [ ] Kapsam koşumu (ön planda seri): v482 + `tests/test_*broker*` + `tests/test_*backtest*` + `tests/test_*loop*adet*`/`*benims*` + `tests/test_edg088_pk2_gercek_v472.py` + `tests/test_edg091_r_paydasi_v479.py` + analytics/karne çivileri (grep `r_multiple`) + v382 v401 v402 + codelaw; log `…/scratchpad/tsk187/final.log`, üçlü hüküm.

## Teslim
Rapor `…/scratchpad/tsk187/rapor_tsk187.md`; dönüş: durum + tek satır test özeti + VRTX sentetik R (eski/yeni) + değişen beklenti sayısı + kaygılar. Merge/suite/dağıtım Rol-1.
