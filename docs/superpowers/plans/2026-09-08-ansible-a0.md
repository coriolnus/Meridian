# TSK-176 Faz A0 — Ansible rolü `meridian_a1` (tek host, yakınsama; dagit'e dokunmaz) — uygulama planı

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use checkbox syntax.

**Goal:** `deploy/ansible/` altında, A1'in VM-içi durumunu (paketler · dizinler · venv · systemd birimleri/timer'ları/drop-in'leri · polkit · unattended-upgrades ·
hermes SOUL/config · bot kum havuzları · sır DOSYALARININ varlık/izin denetimi · sağlık kontrolü) repodan TEK KAYNAKLA yakınsayan, idempotent (2. koşum `changed=0`),
`--check --diff` ile kuru koşulabilen rol + playbook; pytest çivileriyle "rolün kopyaladığı dosya kümesi == deploy/ altındaki birim/drop-in kümesi".
**Spec:** `docs/TASARIM-ALTYAPI-KOD-2026-09-08.md` §2 (Ansible), §3 matrisi, §4 kapılar, §5 A0 satırı, §8 kararlar (K1: A1 fazında dagit'in yerine geçecek; A0 dagit'e DOKUNMAZ).
**Envanter:** scratchpad `a0_envanter.md` (deploy.sh 16 adım + 5 betik; dosya eşleme; 6 tek-kaynak riski) + `a1_birim_envanteri.txt` (A1 canlı: 21 service, 11 timer, 11 drop-in; canlı↔repo fark yalnız yorum).

**Architecture:** Kontrolcü = operatörün Mac'i (repo checkout'u), hedef = A1 (`ubuntu`, `become`). Rol dosyaları repodaki `deploy/oracle-a1/`, `deploy/hindsight/`, `deploy/apisix/`,
`deploy/hermes/` kaynaklarını `ansible.builtin.copy` ile (`.j2` YOK — tek kaynak, aynı bayt) hedefe taşır; birim listesi ELLE yazılmaz, `with_fileglob` ile deploy/'dan türetilir
ve pytest çivisi glob'un her birim/drop-in dosyasını kapsadığını ölçer (dagit `[1c]`/F9'un kör noktası — .timer ve drop-in'ler — burada kapanır). Restart POLİTİKASI: A0 hiçbir
uzun ömürlü servisi (meridian, barsarchive, hindsight-api, apisix, hindsight-cp) yeniden BAŞLATMAZ (bakım penceresi = A1 fazı/dagit); yalnız `daemon-reload` (handler), timer'lar
`enabled+started`, polkit yalnız kural değişince restart. Sır DEĞERİ yazılmaz/üretilmez: credential dosyaları ve `.dash.env` `stat`+`assert` ile (var, mod, sahip) DENETLENİR;
yoksa playbook durur ve hangi betiğin (dash_token_credential.sh / sir_credential_gecis.sh) koşulacağını söyler. Docker konteynerleri birim dosyalarının işidir (docker_container YOK).

**Tech Stack:** ansible-core (dev-grubu, uv), ansible-lint (production profile), `ansible.builtin.*` + `ansible.posix` YOK (gerekmiyor), pytest çivileri (YAML'ı `yaml.safe_load` ile okur).

## Global Constraints
- Rol kaynağı = repodaki dosyanın kendisi (`src: "{{ playbook_dir }}/../oracle-a1/…"`); `.j2` ve içerik kopyası YASAK (tek-kaynak yasası). İstisna yok.
- Sır değeri: rol hiçbir sır üretmez/yazmaz/okumaz; `no_log: true` sır dosyasına dokunan (stat) görevlerde; `-v` çıktısında değer YOK (çivi: playbook YAML'ında `openssl rand`, `read`, `content:` ile sır adı geçen görev YOK).
- Restart: `meridian.service`, `meridian-barsarchive.service`, `hindsight-api.service`, `hindsight-cp.service`, `apisix.service`, `apisix-etcd.service`, `meridian-litestream.service` için `state: restarted/started` YASAK (yalnız `enabled: true`, `daemon_reload`); çivi bunu YAML'dan ölçer.
- Check-mode yalancı yeşil beyanı: playbook başına `pre_tasks` debug satırı: "check-mode 'command/shell' görevlerini ATLAR; 'temiz' hükmü yalnız copy/file/systemd için" (TASARIM §2.1).
- Brifing/bekçi/karne kadans kapısı (deploy.sh 6c-e/11a-c) birebir: timer `enabled` DEĞİLSE dosya kopyalanmaz ve enable edilmez (fact: `ansible.builtin.service_facts` + `when`), "dokunulmadı: neden" debug'ı; `MERIDIAN_BRIFING_DEVRI=1` eşdeğeri `-e brifing_devri=true`.
- Ops sözleşmesi = komut satırı: `deploy/ansible/README.md` üç komutu verir: `ansible-playbook -i deploy/ansible/inventory.ini deploy/ansible/site.yml --check --diff` · aynı komut `--check`siz · `ansible-lint deploy/ansible`. Rol-1 dışında kimse A1'e koşmaz (CLAUDE.md §2 satırı Rol-1 ekler).
- Yasa 4: `ignore_errors`, `failed_when: false` YASAK (gerekçeli `block/rescue` + `debug` serbest); Yasa 6: rol yalnız hedefe yazar, artefakt üretmez.
- Test numaraları SABİT: v451 (`tests/test_ansible_a0_v451.py`). Ansible bağımlılığı: `uv add --group dev --no-sync "ansible-core>=2.17,<2.19" "ansible-lint>=24"` (uv.lock değişir; Rol-1 `uv sync`). Çiviler `ansible-playbook --syntax-check` ve `ansible-lint`i `shutil.which` ile bulamazsa **pytest.fail** ("koşamıyorum ≠ yeşil"; venv'e dev-grubu kurulunca vardır).
- Çapa yasağı; `-q` yok; seri pytest; git yok; dağıtım yok; A1'e ssh YOK (ajan) — playbook A1'e karşı yalnız Rol-1 tarafından koşulur.

---

### Task 1 — İskelet + envanter + syntax çivisi (implementer)
**Files:** Create `deploy/ansible/ansible.cfg` (`[defaults] inventory=inventory.ini, interpreter_python=/usr/bin/python3, retry_files_enabled=False, stdout_callback=yaml, host_key_checking=True; [ssh_connection] pipelining=True`) ·
`deploy/ansible/inventory.ini` (`[a1]\na1 ansible_host=130.61.126.87 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/oci-a1.key`) · `deploy/ansible/site.yml`
(`hosts: a1`, `become: true`, `gather_facts: true`, `pre_tasks`: check-mode beyanı + `service_facts`, `roles: [meridian_a1]`) · `deploy/ansible/roles/meridian_a1/{defaults/main.yml,handlers/main.yml,tasks/main.yml}` ·
`deploy/ansible/README.md` · Modify `pyproject.toml` (+uv.lock) · Test `tests/test_ansible_a0_v451.py`.
**Interfaces (Produces):** `defaults/main.yml`: `repo_kok: /opt/meridian` · `meridian_kullanici: ubuntu` · `brifing_devri: false` · `bot_adlari: [sef, bekci, karne]` · `birim_kaynaklari:` (glob listesi:
`../../oracle-a1/*.service`, `../../oracle-a1/*.timer`, `../../hindsight/*.service`, `../../hindsight/*.timer`, `../../apisix/*.service`) · `dropin_dizinleri:` (`meridian.service.d`, `meridian-tick-watchdog.service.d`,
`meridian-fail-notify.service.d`, `meridian-litestream.service.d`, `hindsight-api.service.d`) · `zorunlu_sir_dosyalari:` (`/etc/meridian/dash_token`, `/etc/meridian/nous_api_key`, `/etc/meridian/kapi_apikey`,
`/etc/hindsight/creds/HINDSIGHT_API_DATABASE_URL`, `/etc/hindsight/creds/HINDSIGHT_API_LLM_API_KEY`, `/etc/hindsight/creds/HINDSIGHT_API_TENANT_API_KEY` — mod 0400 root) + `/opt/meridian/.dash.env` (0600 ubuntu).
- [ ] Çivi 1 (kırmızı): `site.yml` + rol YAML'ları `yaml.safe_load` ile açılır; `ansible-playbook --syntax-check -i deploy/ansible/inventory.ini deploy/ansible/site.yml` çıkış 0 (`shutil.which` yoksa `pytest.fail`).
- [ ] Çivi 2: `birim_kaynaklari` glob'ları deploy/ altındaki HER `*.service`/`*.timer` dosyasını kapsar (ölü `deploy/litestream.service` hariç — çivi bunu adıyla dışlar ve
      "silinmeli" notu düşer) ve `dropin_dizinleri` deploy/ altındaki her `*.service.d/*.conf`'u kapsar (kümeler eşit; eksik/fazla → kırmızı).
- [ ] Çivi 3: YAML metninde yasak desenler YOK: `state: restarted` / `state: started` ile uzun ömürlü birim adları; `ignore_errors`; `failed_when: false`; `openssl rand`; `.j2`.
- [ ] `uv add --group dev --no-sync …` · `ansible-lint deploy/ansible` (production) temiz — çivi 4 (`shutil.which('ansible-lint')` yoksa fail).
- [ ] Uygulama · rapor.

### Task 2 — Görevler: paketler · dizinler · venv · birimler · drop-in'ler · polkit · unattended · hermes · kum havuzları (implementer, Task 1 sonrası)
**Files:** Create `roles/meridian_a1/tasks/{paketler,dizinler,venv,birimler,dropinler,polkit,hermes,denetim}.yml`; `tasks/main.yml` bunları `import_tasks` ile sırayla; `handlers/main.yml`
(`daemon-reload` → `systemd_service daemon_reload: true`; `polkit-yeniden-baslat` → `systemd_service name=polkit state=restarted`).
- `paketler.yml`: `apt` (update_cache, cache_valid_time 3600) `curl ca-certificates rsync redis-server docker.io jq git unattended-upgrades` (deploy.sh 1 + A1 ölçümü; Postgres A2'de) ·
  `systemd_service redis-server enabled+started` (deploy.sh 1 aynen) · `copy 52meridian-unattended-upgrades → /etc/apt/apt.conf.d/` 0644.
- `dizinler.yml`: `/home/ubuntu/backups` (ubuntu 0755) · `/opt/meridian/var/bots/{{ ad }}` 0700 ubuntu (loop bot_adlari) · `/etc/meridian` 0755 root · `/etc/hindsight/creds` 0755 root.
- `venv.yml`: uv varlık `stat ~/.local/bin/uv` → yoksa `command` resmi installer **SÜRÜM PİNLİ** URL (`https://astral.sh/uv/{{ uv_surum }}/install.sh`; `uv_surum` defaults'ta — Rol-1 A1'de `uv --version` ile ölçüp yazar; çivi: boş bırakılamaz) ·
  `command: {{ uv }} sync --frozen` `chdir=/opt/meridian` `become_user=ubuntu` `changed_when: "'Installed' in r.stdout or 'Uninstalled' in r.stdout"` (uv çıktısı ölçülerek; ölçülemezse `changed_when: false` + beyan) ·
  doğrulama `command: .venv/bin/python -c "import meridian.api, meridian.loop"` `changed_when: false`.
- `birimler.yml`: `with_fileglob` `birim_kaynaklari` → `copy dest=/etc/systemd/system/{{ item | basename }}` 0644 root, `notify: daemon-reload`; **kadans kapısı**: brifing/bekci/karne
  `.service/.timer` için `when: (ansible_facts.services[ad ~ '.timer'].status | default('') == 'enabled') or brifing_devri` — değilse `debug` "dokunulmadı"; enable listesi (A1 ölçümü, `defaults`
  `etkin_birimler`): meridian, meridian-barsarchive, meridian-litestream, apisix, apisix-etcd, hindsight-api, hindsight-cp → `enabled: true` (state YOK); timer'lar `etkin_timerlar` (A1 ölçümü 11 timer) → `enabled+started`
  (kadans kapısına tabi üçü hariç → `when` aynı). `meridian-learn.service`, `meridian-aylik-bucket-kopya.service` disabled kalır (ölçüm).
- `dropinler.yml`: loop `dropin_dizinleri` × `with_fileglob` → `file directory /etc/systemd/system/{{ dizin }}` + `copy` 0644 `notify: daemon-reload`.
- `polkit.yml`: `copy 50-meridian-sprint.rules`, `51-meridian-birim-anahtari.rules` → `/etc/polkit-1/rules.d/` 0644 `notify: polkit-yeniden-baslat` (deploy.sh'ın her-koşuda-restart'ı BİLEREK daraltıldı — plan beyanı; README'ye).
- `hermes.yml`: `copy deploy/hermes/SOUL.md → ~ubuntu/.hermes/SOUL.md backup=yes owner=ubuntu` · `copy config.yaml → ~ubuntu/.hermes/config.yaml backup=yes` · hermes ikili `stat` (üç yol) → yoksa **kurmaz**,
  `debug` reçete (deploy.sh 5'in curl|sh'ı A0'a GEÇMEZ — sha kapısı yok, Bulgu 5) · profil dizinleri `stat` + `debug` rapor (deploy.sh 12; ROL DIŞI).
- `denetim.yml`: `stat` loop `zorunlu_sir_dosyalari` (`no_log: true`) → `assert` (exists ∧ mode ∧ owner) `fail_msg` hangi betik koşulmalı · `.dash.env` 0600 ubuntu · `tick_watchdog.sh` 0755 ·
  sağlık `uri http://127.0.0.1:8080/healthz status_code=200 retries=5 delay=3` (`delegate_to: a1` — hedefte koşar; playbook zaten hedefte) · `command systemctl is-active …` `changed_when: false` (rapor, düşürmez? — HAYIR: `meridian.service` aktif değilse `assert` düşer; timer'lar için rapor).
- [ ] Çiviler (v451 ek): (a) `birimler.yml` kadans kapısı `when` ifadesi üç birim için VAR (regex); (b) `venv.yml` installer URL'si `uv_surum` içerir ve defaults'ta boş DEĞİL; (c) `denetim.yml` sır dosyası görevlerinde `no_log: true`;
      (d) `hermes.yml`'de `curl` YOK; (e) handlers'ta yalnız `daemon-reload` ve `polkit-yeniden-baslat`.
- [ ] `ansible-lint` temiz · `--syntax-check` 0 · rapor (A1'e KOŞULMADI beyanı).

### Task 3 — Rol-1: `uv sync` → merge → tam suite → A1'e `--check --diff` (kuru) → beklenen fark: yalnız 3 yorum-farklı birim + (varsa) eksikler → gerçek koşum (bakım penceresi gerekmez: restart yok) →
2. koşum `changed=0` (idempotency kanıtı) → README + CLAUDE.md §2 satırı (`ansible-playbook` → Rol-1, A1 penceresi) → ROADMAP TSK-176 A0 DONE(tarih·GEÇTİ) → dagit `[1c]` "yalnız raporla" notu A1 fazına devir.
