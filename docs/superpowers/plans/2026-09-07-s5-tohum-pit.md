# S5 — Replay tohumunun PIT üyelikle yeniden kurulması (TSK-159, varyant A) — Uygulama Planı

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Adımlar `- [ ]` ile izlenir.

**Goal:** Canlı `trades`/`trade_plans` defterindeki tohum dilimini (885 satır, sv=90, kaynak=replay_seed, 2026-08-13 partisi) aynı
parametre/hedef/takvimle, `uyelik=as_of` (varyant A: hiç-üye 6 sembol hariç) ile üretilmiş PIT tohumuyla değiştirmek; canlı satırlar ve
öğrenme geçmişi bit-aynı korunur; eski tohum arşivlenir; işlem operatör onaylı bakım penceresinde (worker durmuşken) uygulanır.

**Spec:** docs/TASARIM-PIT-EVREN-YENIDEN-TOHUMLAMA-2026-09-06.md §3.2/§6; emsal: research/cards/EDG-2026-036-*.yaml (UYGULAMA_PROSEDURU,
asama2_UYGULANDI); ölçüm: research/cards/EDG-2026-082-*.yaml (A: 829 işlem, sızıntı 0, kapı 1/1).

**Operatör kararı (2026-09-07 sabah):** "S5 tohum değişimini onaylıyorum, A varyantı ile devam et."

**Rol-1 ruling'leri (hafıza kontrolü: recall 07:4xZ, EDG-036 prosedürü, kart_benzer EDG-079/082/036):**
1. `kaynak` damgası `replay_seed` KALIR (tasarım metnindeki `replay_seed_pit` UYGULANMAZ): EDG-036'da ölçülen "13 tüketici kaynak-kör";
   yeni bir kaynak değeri `ledgerstamp.GECERLI` dışında kalıp her satırı `belirsiz` yapar. PIT tohumu `extra_json.tohum_parti =
   "EDG-082A-2026-09-07"` + `extra_json.pit_uyelik="as_of"` + `extra_json.varyant="A"` ile ayırt edilir; strateji sürüm uzayı sv=91
   (08-13'ün sv=90 gerekçesi: ebeveyn tabanı kirlenmesin, geri-alma kapısı açılmasın).
2. Eski tohum satırları canlı defterden ÇIKARILIR ama ARŞİVLENİR (08-13 ile aynı: `backups/tohum-arsiv-<ts>.jsonl` + sha256 +
   research artefaktı); iki tohumu aynı defterde tutmak tüketicileri çift saydırırdı.
3. `equity_curve.points` DOKUNULMAZ; kırılma `reset_isaretleri` zarf anahtarına beyanla yazılır (sermaye.py sözleşmesi) — API
   yoksa yazılmaz ve beyan düşülür (08-13'te yazılmamıştı).
4. `run.replay_seed` ÇAĞRILMAZ (canlı satırları siler, planları ezer — EDG-036 kill). Yazım yalnız `store` kapısından (file_lock +
   write_jsonl), elle SQL yok.
5. Bakım penceresi: 2026-09-07 ≥20:45Z (akşam brifingi sonrası, seans dışı); worker durdurulur, DB yedeği alınır, betik `--uygula`,
   doğrulama kapıları, worker başlatılır. Kuru koşum (`--dry-run` varsayılan) gündüz A1'de worker koşarken yalnız OKUR ve rapor yazar.

## Global Constraints
- Yasa 4 (sessiz except yok), Yasa 6 (her yazımın okuyucusu: sonuç JSON/MD + günlük), uydurma yasağı (ölçülemeyen None+neden).
- Çapa yasağı: `dosya.py:NNN` hiçbir yerde. ops sözleşmesi KOMUT SATIRIdır. pytest yalnız kapsam, ön planda.
- Canlı yerel state'e yazma yasağı: testler `sandbox_state`; betik `--uygula` olmadan hiçbir bayt yazmaz.

---

### Task 1: `ops/tohum_pit_yenile.py` — kuru koşum + uygulama betiği (implementer, worktree)
**Files:** Create `ops/tohum_pit_yenile.py`; Test `tests/test_tohum_pit_yenile_v433.py` (çakışma kontrolü: `ls tests | grep v433`).
**Interfaces:** `backtest.replay(params, bars, index, goal, start, end, strategy_version=91, with_gate_detail=True, uyelik=<A>)`;
`data.as_of_pit_durumu`/`constituents.as_of` ile A üyelik fonksiyonu (EDG-082 olcum.py `uyelik_fonksiyonlarini_kur` ile AYNI türetim);
`ledgerstamp.stamp_rows(rows, ledgerstamp.REPLAY_SEED)`; `store.file_lock`, `store.read_jsonl`, `store.write_jsonl`, `store.read_json`;
`health.stale(...)`; `config.goal()`, `run.bootstrap_v01()` (params + version kaynağı EDG-082 ile aynı); `dataset.load()`.
**CLI:** `uv run python ops/tohum_pit_yenile.py [--uygula] [--baslangic 2022-01-01] [--bitis <son tam seans>] [--rapor-dizin research/olcumler/edg082_pit_tohum/s5_2026-09-07]`
**Adımlar (her biri kanıtlı):**
- [ ] KAPI-0: worker: `health.stale(run.RESEED_HEARTBEAT_FRESH_S)` False ise `--uygula` REDDEDİLİR (kuru koşum serbest); `portfolio.armed` doluysa red.
- [ ] Mevcut defteri oku: trades (kaynak sayımı: live_paper N_l, replay_seed N_s, belirsiz N_b), trade_plans (kaynak/llm_opinion damgalı olanları belirle), portfolio.realized_pnl, equity_curve.points sayısı — hepsi rapora "onceki" bloğuna.
- [ ] Replay A: EDG-082 ile aynı üyelik türetimi (as_of, hiç-üye hariç), aynı params/goal/takvim; `strategy_version=91`; sonuç işlemleri `stamp_rows(..., REPLAY_SEED)` + `extra_json` damgaları (tohum_parti, pit_uyelik, varyant, params_sha256, guncel_liste_sha256, motor künyesi git sha, friksiyon şerhi metni EDG-036 DAMGA_KARARI ile aynı).
- [ ] Kıyas raporu: eski tohum (885) ↔ yeni tohum: n, avg_r, yıl bazında n, çıkış nedeni dağılımı, hiç-üye sızıntı 0 kontrolü (EDG-082 `eslesen` mantığı), `kapi` özeti (OOS composite) — JSON + MD.
- [ ] `--uygula`: (1) eski tohum satırları + eski tohum planları `backups/tohum-arsiv-<ts>.jsonl` (+`.sha256`) olarak arşivlenir; (2) yeni defter = canlı satırlar (bit-aynı) + yeni tohum; `store.file_lock("trades.jsonl")` içinde `write_jsonl`; (3) plan defteri: canlı/llm_opinion damgalı planlar KORUNUR + yeni tohum planları (replay `with_gate_detail` çıktısı; 08-13 şeması), aynı kapı; (4) equity_curve: `points` dokunulmaz; `reset_isaretleri`ne `{tur:"tohum_degisimi", ts, tohum_parti, onceki_n, yeni_n}` eklenir (sermaye.CURVE_MARK_KEY sözleşmesi; zarf `store.read_json/write_json`); (5) scoreboard DOKUNULMAZ.
- [ ] Yazım-sonrası DOĞRULAMA (betik kendi yapar, rapora yazar): canlı satır sayısı ve içerik hash'i aynı · realized_pnl aynı · yeni tohum n · `ledgerstamp.seed_boundary()["replay_end"]` = yeni tohumun max ts_close · `ledgerstamp.counts()` belirsiz 0 · korunum raporu (`watchdog.conservation_report` varsa) açıklanamayan sayısı · `analytics` DSR/PBO değeri (varsa) · rollback kapısı girdileri (EDG-036 olcum_asama1_kapilar deseni: `_would_have` öncesi karar noktası) — hepsi rapora; eşik betikte YOK (hüküm Rol-1).
- [ ] TDD (v433, sandbox_state): (a) `--dry-run` hiçbir bayt yazmaz (mtime/sha aynı); (b) nabız taze → `--uygula` red; (c) sahte replay (monkeypatch `backtest.replay` → 3 işlem) ile `--uygula`: canlı satırlar bit-aynı, eski tohum arşiv dosyasında (n ve sha), yeni satırlar `kaynak=replay_seed` + `extra_json.tohum_parti` + sv=91, plan defterinde llm_opinion damgalı plan korunmuş, `reset_isaretleri` eklenmiş, `points` aynı; (d) `seed_boundary` yeni max ts_close; (e) rapor JSON şeması. Önce KIRMIZI, sonra yeşil, 2 mutasyon (canlı satır koruma dalı; arşiv yazımı) ısırır.
- [ ] Rapor: `.superpowers/sdd/2026-09-07-s5/task1-report.md` (yazamazsan mesajda ver).

### Task 2 (Rol-1): kuru koşum A1'de (gündüz, worker koşarken — yalnız okur) → rapor incelemesi → 20:45Z bakım penceresi: worker stop → DB yedek (`backups/meridian.db.bak-tohum-<ts>`) → `--uygula` → doğrulama kapıları → worker start → healthz → ROADMAP TSK-159 S5 DONE + EDG-082 kart notu + pitlaw sınıf kararı (TSK-156 dilim-2 b) + günlük + push.

## Task 1 hükümleri (Rol-1, 2026-09-07 08:3xZ — implementer DONE_WITH_CONCERNS)
- Sapma 1 (`config.load_strategy` ↔ `bootstrap_v01`): KABUL — kuru koşum "tek bayt yazmaz" sözü; strategy.yaml yoksa dur (rc 1).
- Sapma 2 (rapor sözleşmesi: dry-run stdout, `--uygula` rapor-dizin ister): KABUL (Yasa 6).
- Soru 1 (T%05d çakışması): yeni tohum T00001'den başlar, canlı T00096+ → `--uygula` sonrası AYNI pencerede
  `ops/trade_id_yeniden_numarala.py` (TSK-150 aracı, eşleme defteri state/trade_id_eslesme.jsonl) kuru koşum → uygula; rapordaki
  `trades_kimlik.cift_n` yeniden numaralama sonrası 0 olmalı (Task 2 kapısı).
- Soru 2 (plan kimliği çakışması): canlı plan kazanır, tohum planı düşer ve sayılır — KABUL.
- Soru 3 (plan ayrımı dört ölçüt: tohum_parti · llm_opinion · korunan işlemin plan_id'si · date > eski sınır): KABUL; rapor ölçüt
  başına sayım taşır.
- Soru 4 (equity_curve.points eski tohumu taşır; reset işaretine egri_son_nokta konmadı; seed_boundary YOL-2 güven "orta"):
  KABUL, BEYANLI AYRIŞMA — eğrinin tohum-dönemi yeniden üretimi TSK-035/§2-18 kadanslı yazar kalemine not (yeni kalem açılmadı).
- Bar kaynağı üretim yolu (`dataset.load`) ≠ EDG-082 minimal temizlik: yeni tohum n'i 829'dan sapabilir — rapor kıyası bunu
  ölçer; beklenen sapma küçük, büyükse (>%5) uygulama DURUR ve fark teşhis edilir.

- Hafıza atfı (CLAUDE.md §2 kapısı, 08:3xZ): memory/ — tohum/replay_seed/trade-id kararına dair kayıt yok (yalnız surdurme-durumu:
  equity_curve planı §2-9 operatörde → Soru 4 beyanıyla uyumlu); `ops/kart_benzer.py` → EDG-079 KALDI (as_of kuramama 0/885,
  sızan pay ≤%2), EDG-082 GEÇTİ (bu değişimin ölçüm temeli), EDG-036 (2026-08-13 tohum yenileme emsali: sv=90 gerekçesi,
  13 kaynak-kör tüketici), EDG-075 KALDI (Wikipedia HTML kaynağı; oldid 1370891103 donuk). A1 recall: kuru koşum bitince
  (CPU çakışması — reflect-kanary-sessiz-saat dersi) eklenir.
- Soru 1 ek nüans: `trade_id_yeniden_numarala.py` çifti SEQ ile ayırır (küçük seq kimliğini korur, büyük seq yeniden numaralanır),
  KAYNAK ile değil. Yeni tohum satırları canlıdan SONRA yazılacağı için büyük seq'i alır → yeniden numaralamada CANLI satırlar
  (T00096…) kimliğini KORUR, çakışan yeni-tohum satırları en büyük numaranın üstünden ardışık numara alır. Bu tercih edilen
  sonuçtur (canlı kimlikler plan/bildirim/karne atıflarında sabit kalır); tohum kimlik dizisinin kesikli olması kozmetiktir ve
  rapora beyan edilir. Byte-eşitlik değişmezi bu durumda canlı satırları korur.
- A1 recall (08:55Z, low, 36 s, 8 sonuç) atfı: [1][2] `sermaye.py` eğriye nokta eklemeyi bilerek reddeder — `seed_boundary()`
  eskiden eğrinin son noktasından okurdu; bugün (TSK-035) sınır DOĞRUDAN `trades.kaynak` damgalı satırların en geç ts_close'undan
  ölçülüyor → yeni tohum yazılınca sınır kendiliğinden yeni tohumun ucuna gelir, eğri noktasına dokunmak gerekmez (Soru 4
  hükmüyle uyumlu; recall 2026-09-05 tarihli kayıtlar bayat — egri_son_nokta 2026-09-04, 898 nokta: eğri YAZILIYOR). [4][5] EDG-036
  emsali: 885 tohum + 2 canlı, 7/8 kapı, equity_curve artığı §2-9'a devredildi. [3] chop OOS 27 < 30: tohum yenilemesi bunu
  ÇÖZMEZ — beklenti yazılmaz. [6] analitik tohumu TRAINING sayar, canlı kanıt değil.
- **Ruling 6 — pencere sonu (`--bitis`)**: plan "son tam seans" diyordu; A1 ölçümü (`ledgerstamp.seed_boundary()`, 08:56Z):
  eski tohumun en geç ts_close'u **2026-07-24** (replay_end, güven orta, reset işareti 07-20 ayrışık), canlı satırlar 2026-08-05'ten
  başlar → emsalde tohum ile canlı dönem ÖRTÜŞMEZ. Son bar (09-04) alınsaydı yeni tohum 08-05..09-04 canlı dönemine sim işlem
  yazar, 16 canlı satırla aynı haftalarda ikinci popülasyon oluşurdu. HÜKÜM: `--bitis 2026-07-24` (tek kaynak: seed_boundary
  ölçümü; ikinci literal değil), yeni tohum eski tohumun penceresini birebir kapsar, canlı dönem yalnız canlı satırlardan oluşur.
  Bedel: chop OOS örneklemi artmaz (recall [3] zaten "çözmez" diyor). 08:54Z başlayan bitis=son-bar kuru koşumu 08:58Z'de
  durduruldu, doğru pencereyle yeniden başlatıldı (/tmp/s5_kuru3.log). Kabul bandı: n ∈ [800, 884] — türetme 885×(829/871)=842
  ±%5 (EDG-082 A/taban oranı aynı pencereye uygulanmış); sızıntı 0; KAPI-0 kuru koşumda serbest; yazım sonrası doğrulama kapıları.
- Operasyon notu (09:3xZ): dagit rsync `--delete` A1'de repo-dışı `research/olcumler/edg082_pit_tohum/s5_2026-09-07/` dizinini
  SİLER. Sıra: rapor yazılır → `scp -r` ile yerele → `git add` + commit → ancak sonra dağıtım. `--uygula` raporu için de aynı.
- **Kuru koşum SONUCU (10:23Z, 85 dk, RC=0)** — rapor `research/olcumler/edg082_pit_tohum/s5_2026-09-07/rapor.{json,md}` (A1'den scp, repoya
  alındı). n 885→843 (Δ-42, band [800,884] içinde; türetilen beklenti 842), avg_r 0,0763→0,0754, medyan_r -0,18→-0,162, kazanma
  %36,3 aynı, pnl$ 20.685→18.503. SIZINTI yeni 0/0/0 (eski tohum aynı üyelikle 95 üye-olmayan). Plan 121 tohum düşer / 390 korunur.
  Kapı oos_score 0,3004 ölçüldü. KAPI-0 kuru koşumda: nabız TAZE (worker koşuyor) + 1 silahlı plan (MU) → `--uygula` şu an REDDEDİLİR;
  20:45Z'de worker durur, MU'nun silahlı kalıp kalmadığı ÖLÇÜLÜR (silahlıysa: plan seans içinde tüketilmemişse pencere ertelenir —
  `--kapi-atla` yalnız Rol-1 hükmüyle ve gerekçesi yazılarak). HÜKÜM: rapor KABUL, uygulama 20:45Z penceresinde.
- **Pencere operasyonu iki parça (12:0xZ hükmü)**: KAPI-0 `health.stale(run.RESEED_HEARTBEAT_FRESH_S=900)` fail-closed → worker
  durduktan sonra nabız 15 dk bayatlamadan `--uygula` reddedilir; bekleme döngüsü yasak (§7) → PARÇA A 20:45Z (ön-ölçüm: armed
  listesi + nabız yaşı → `systemctl stop` → sqlite backup API ile `backups/meridian.db.bak-tohum-<ts>`), PARÇA B ≥21:00Z ayrı çağrı
  (kapı ön-kontrolü → `--uygula` → rapor/arşiv → `trade_id_yeniden_numarala` kuru/uygula/kontrol → son ölçüm → start/healthz). Betikler
  oturum scratchpad'inde (s5_pencere.sh / s5_pencere_b.sh). Armed dolu çıkarsa: plan seans içinde tüketilmemişse `--kapi-atla` YOK,
  pencere ertelenir ve gerekçe yazılır. Worker ~25 dk kapalı (seans dışı; 22:04Z brifing ayrı birim, etkilenmez).
- **Ruling 7 (14:4xZ) — pencere BUGÜN, tek betik, `--kapi-atla` ile**: ÖLÇÜLDÜ (A1 `data._sessions`): 2026-09-07 XNYS seansı DEĞİL (Labor
  Day; 09-04 ve 09-08 seans) — bugün olay/karar akışı yok (events.jsonl son kayıt 10:06Z, intraday_decisions son 09-04). "≥20:45Z (seans
  dışı)" şartının niyeti seans dışında çalışmaktı; tatil günü niyeti sağlar → pencere ŞİMDİ. KAPI-0 iki koşulundan (a) nabız: worker
  `systemctl stop` ile durduruluyor (900 s bayatlama beklemek yerine durmuşluk `pgrep` ile doğrulanır); (b) armed: `portfolio.armed=[MU]`
  (Cuma silahlanmış, Pazartesi açılışı olmadı) — `run.replay_seed`in kapısı plan defterini SİLDİĞİ için vardı; bu betik canlı planlara
  dokunmaz (plan çakışmasında canlı kazanır, `portfolio` dokunulmaz, candidates/scoreboard dokunulmaz) → MU'nun zemini çekilmez. İki
  koşul da elle sağlandığından `--kapi-atla` verilir; rapor `kapi.calisti=False, neden=--kapi-atla` taşır, bu hüküm gerekçesidir.
  Hafıza: memory `meridian-scoreboard-write-safety` (worker koşarken state'e yazma — stop ile sağlanır); kart_benzer/recall: ek kayıt yok.
  Bedel: worker ~90 dk kapalı (replay A1'de 85 dk) — tatil günü, 20:32Z günlük denetim ve 22:04Z brifing etkilenmez. 20:43Z/21:03Z
  zamanlayıcıları iptal edilir.
- **Ruling 8 (16:3xZ) — KAPI-0 `--kapi-atla` ile ATLANMIYOR (ölçüldü: 16:27Z uygula denemesi RC=1, rapor "KAPI-0 REDDETTİ — hiçbir bayt
  yazılmadı"; `--kapi-atla` yalnız yazım-sonrası oos kapısını atlar). İki koşul GERÇEKTEN sağlanacak: (a) nabız: worker 16:27:16Z'de
  durdu → 16:42:16Z'den sonra `health.stale(900)` True (beklenir, döngü kurulmaz); (b) armed: `portfolio.armed=[MU]` GEÇİCİ olarak yan
  dosyaya alınır (`backups/portfolio_armed-<ts>.json`, doc_json kopyasıyla), `armed=[]` yazılır (worker durmuş, Rol-1 state yazımı §3
  matrisine uygun), `--uygula` koşar ve AYNI nohup zincirinde — RC ne olursa olsun — armed yan dosyadan GERİ YÜKLENİR ve geri yükleme
  doğrulanır (MU listede). Sonra id yeniden numaralama → son ölçüm → worker start/healthz. Gerekçe ruling 7 ile aynı: MU canlı plan,
  betik canlı planlara dokunmaz; kapı `run.replay_seed` mirasıdır. Sınıflandırıcı zamanlayıcı kurmayı reddetti → operatör 16:45Z'de
  "devam" der ya da 20:43Z zamanlayıcısı uyandırır.
- **Ruling 9 (18:0xZ) — tick-watchdog worker'ı geri başlattı**: ÖLÇÜLDÜ (journal): `meridian-tick-watchdog.timer` (15 dk) 17:12:55Z'de
  "durum 2847 s bayat (eşik 2700 s) → meridian.service yeniden başlatılıyor" ile worker'ı AÇTI; ilk deneme bu yüzden de düşerdi (nabız
  yeniden taze). Pencere boyunca `meridian-tick-watchdog.timer` DURDURULUR (zincir [a]'da ölçülür, her çıkış yolunda [h] geri açılır);
  bekçinin restart yetkisi yalnız pencere süresince askıdadır — bedel: pencere içinde worker çökerse otomatik yeniden başlatma yok
  (zincir zaten worker'ı bilerek kapalı tutuyor). Zincir A1'de `systemd-run --on-active=16min` ile tek atım tetiklenir (nabız 900 s
  bayatlamasını beklemek için yoklama döngüsü yerine systemd zamanlayıcısı); [a] nabız taze bulursa rc 3 ile durur, yeniden tetiklenir.

## SONUÇ (19:5xZ) — UYGULANDI
Zincir 18:19:50–19:44:27Z: [a] kapı ✓ · [b] MU yan dosya · [c] --uygula RC=0 (84,5 dk) · [d] MU geri (eşit) · [e] yeniden numaralama GEREKSİZ (çift 0;
canlı id'ler 09-05'te T00886+ — T00001–T00843 çakışmadı); `--kontrol` eski eşleme defterinin arşivlenen seq'lerini hayalet buldu → defter backups'a
arşivlendi · [f] trades 859 (843+16), plans 1032 (642 yeni; 310 çakışmada canlı kazandı; 390 sv3/4/5 eski-dönem planı miras), seed_boundary 07-24 ·
[g] worker active, healthz 200 (6 sn'de 503, sonra 200) · [h] tick-watchdog timer active. Açık kalem: eski-dönem 390 plan (08-13 tohumunun da
bıraktığı) — ayrı hijyen kalemi adayı; equity_curve tohum dönemi (§2-18) beyanlı ayrışma sürüyor.

