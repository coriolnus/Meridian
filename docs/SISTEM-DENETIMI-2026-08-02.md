# SİSTEM DENETİMİ — 2026-08-02 (gece, otonom)

> Operatör talimatı: "bütün sistemi baştan aşağı gözden geçirmeni istiyorum, sistem
> içerisindeki bütün bileşenler". Yöntem: 8 salt-okuma denetçi ajanı (alt-sistem başına
> bir) + şiddet≥3 her bulgu için bağımsız adversarial doğrulayıcı. Toplam 34 ajan,
> 937 araç çağrısı, ~4093k token. Bilinen-borç tekrarı yasaktı; aşağıdakiler YENİ bulgular.
> Şiddet ölçeği: 4=para/veri kaybı aktif · 3=canlı davranış kusuru · 2=beyan-davranış ayrışması · 1=hijyen.

**Bilanço: 25 doğrulanmış ciddi bulgu (şiddet-3: 20, şiddet-2 doğrulanmış: 5) + 48 hafif bulgu + 1 çürütülen.**

## Rol-1 triyaj hükmü (2026-08-02 gecesi)

Yirmi beş doğrulanmış bulgu ÜÇ kovaya ayrıldı. Ayırma ilkesi: **"davranışı beyan edilene getiren
hata-yolu düzeltmesi"** (sessiz-güvenli — gece onaysız kapatılır, kârlılık-programı yetkisi
kapsamında) ile **"karar/emir semantiğini değiştiren düzeltme"** (operatör sabah görmeli) ayrımı.

### KOVA A — Gece kapatılacak (sessiz-güvenli; karar/emir semantiğine dokunmaz)

| Bulgu | Ne | Dosya kümesi |
|---|---|---|
| C20 + hafif-kardeşi | Bacak `asked` işaretlemesi cevaptan SONRAYA; `outcomes` hata/boş sözleşmesi (`error:` öneki) | data.py |
| C25 (sayaç yarısı) | Kazanç-takvimi gün-başına başarı/başarısızlık sayacı + kısmi-pencere olayı (FMP eşiği KOVA B'de) | data.py, earnings.py |
| C2 | Takvim-düşüşü sessiz yutması → süreç-başına bir `obs.warn` (sessiz-hat entegrasyonu S2R-sonrası kuyrukta — api.py'ye bu gece dokunulmaz) | scheduler.py |
| C6 | Watchdog olay penceresi satır-limitinden `events_since(gun)`a | watchdog.py |
| C7 | Kanal bağlıyken teslimat başarısızlığı sayacı (`notify_undelivered`) | obs.py |
| C21 | `integrity_report` sıralaması: parity persist'li üçlünün ÖNÜNE + dedektör-başına hata yakalama | watchdog.py |
| C22 | `determinism_report` fail-closed (`ok=False` + `olculemedi` + uyarı) | watchdog.py |
| C15 | Sprint durum-damgası korunumu (`_write_live_status` damga alanlarını ezmesin) — **bu geceki 00:11 sprintinin "taze_aday_birikimi" tetiği de büyük olasılıkla bu kusurun eseri** | sprint_run.py, sprint.py |
| C4 | dbmigrate hata yolu: şema transaction İÇİNE + başarısız DB `meridian.db.failed-<ts>` kenara (aktif-boş-DB sınıfı kapanır) | dbmigrate.py, storage.py |

### KOVA B — Operatör onayına (davranış/karar-değiştiren; sabah raporunda tek tek)

1. **C9 (en kritik):** iç motorun çıkış kararları aynaya iletilmiyor — time_stop/regime_flip/erken-itlaf
   sonrası aynada pozisyon AÇIK kalıyor (kalıcı yetim + sembol kilidi). Düzeltme emir yolu değiştirir.
2. **C8:** pano "gönder" düğmesi E1 yasasını by-pass ediyor (de-risk/dedup/E2 yok) — döngüdeki ayna
   bloğuna delegasyon gerekir; emir yüzeyi olduğu için onaya.
3. **C23:** silahlı planlar HALT/breaker'da olaysız düşüyor + HALT aynadaki canlı emri iptal etmiyor.
4. **C10:** TIF=day gün-boyu limit vs iç motorun "yalnız açılışta dolar" modeli — semantik seçim.
5. **C11+C18:** replay/gölge `fill_entry`e ATR bacağını geçmiyor → iki-motor dolum-limiti ayrışması.
   Düzeltme tarihsel karne rakamlarını DEĞİŞTİRİR — operatör görmeli; diferansiyel çivi testiyle iner.
6. **C19:** günlük devre kesici iki motorda farklı pencere (canlıda fiilen yalnız gecelik-boşluk kesici).
7. **C13:** `exit.early_kill_pivot` canlıda yapısal ölü (pivot kablosu yok) + terfi kapısı bu sınıfı
   reddetmiyor — iki satır kablo ya da kapı; ikisi de karar-çekirdeği değişikliği.
8. **C24:** `HEAT_HARD_R=4.5` kod sabiti vs goal.yaml zarfı 5,0R — operatör kalemi mi bounds knob'u mu?
9. **C12:** Y3 portföy tavanları (sector_cap/heat_cap) kablosuz: ya üretici alanları eklenir ya
   bounds'tan düşülür (UYDURMA-YASAĞI sınıfı: aranabilir-ama-ölü knob).
10. **C3:** `run.py:worker()` ikinci kadans uygulaması — emeklilik kararı (Dockerfile/README yolu değişir).
11. **C5:** `MERIDIAN_DB=off` geri-alma kolu değil; `dbmigrate --geri-al` tasarımı gerekir.
12. **C14:** bileşik öneri kuyruğu "measured"a hiç geçmiyor — prescreen'e kuyruk-kimliği geri-yazımı
    (öğrenme halkasının kapanışı; tasarım kararı ister).
13. **C16:** arka-plan yansıması canlı-dışı rejim kanıtıyla GLOBAL parametre ship edebiliyor — politika.
14. **C17:** arama sondaları aşınma defterine sonda-başına yazıyor — "oturum başına bir soru" politikası
    iki yönden de çözülebilir; beyanla seçilmeli.
15. **C1:** `MERIDIAN_BIND_HOST` hayalet girdi (parolasız-genel-arayüz kapısı ateşlenemez) — api.py
    S2R çakışması nedeniyle gece dokunulmadı; küçük ama gerçek güvenlik kalemi.
16. **C25 (FMP yarısı):** kısmi pencerede FMP yedeğine düşme eşiği — kota etkisi operatör kararı.

### KOVA C — Hafif bulgular (şiddet ≤2, doğrulanmamış; küçük kuyruk)

Rapor sonundaki alt-sistem bölümlerinde; öne çıkanlar: bacak-arızası `outcomes["alpaca"]="empty"`
(C20 ile birlikte kapanıyor) · `integrity_apply` olayının `dataset.load_cached` yanlış beyanı ·
`gap_scan` yarım-gün takvimsizliği (sahte akış-kesintisi alarmı) · kazanç-takvimi iki-dal birleştirme
semantiği (Nasdaq yalnız-ekler / FMP değiştirir — ikisi de yanlış tarafta) · kopyala-yapıştır YASA-4
gerekçeleri.

### Denetçinin sağlam buldukları (özet)

Sekiz denetçi de "sağlam" listesi verdi (bölümlerde tam metin): barsarchive'ın fsync→XACK sırası ve
disk-tabanlı tekilleştirme; `_calendar_scan` kitlesel-uyuşmazlık REDDİ (hüküm operatöre); FetchError
URL-taşımama + `_paper_base` hostname-karşılaştırması (sır-sızıntı önlemleri) başta olmak üzere
kapıların TASARIMI genelde ölçüm-tabanlı bulundu; bulgular kapıların etrafındaki hata yollarında.

## Doğrulanmış ciddi bulgular (adversarial doğrulamadan geçti)

### C1 [şiddet 3 · api-scheduler] MERIDIAN_BIND_HOST hayalet girdi: genel-arayüze parolasız açılmayı reddeden kapı ateşlenemez

- **Yer:** meridian/api.py:66-76
- **Sınıf:** ölü/kablolanmamış mekanizma · güvenlik duruşu

**Kanıt:**

api.py:66 `host = os.environ.get("MERIDIAN_BIND_HOST", "127.0.0.1")` → `public_bind = host not in (...)` → parola yoksa `raise RuntimeError`. Ama GERÇEK bağlanma adresini bu değişken belirlemiyor: `serve.sh` (uvicorn Popen satırı) `--host','127.0.0.1'` sabit; `deploy/oracle-a1/meridian.service:ExecStart` `--host 127.0.0.1` sabit; `docker-compose.yml` dashboard `["uvicorn",...,"--host","127.0.0.1",...]` sabit. Repo genelinde MERIDIAN_BIND_HOST'u OKUYAN iki yer var (api.py:66, auth_cli.py:57), YAZAN hiçbir başlatıcı yok. Yani panoyu gerçekten dışarı açmanın tek yolu komut satırını elle `--host 0.0.0.0` yapmak ve o durumda değişken yine boş → `public_bind=False` → ne `RuntimeError` ne de satır 76'daki `public_bind` uyarısı çıkar. Bu, 8 satır yukarıda (58-65) 'HAYALET BAYRAK KALDIRILDI (MERIDIAN_TRUST_PROXY)' diye kutlanan sınıfın birebir tekrarı. deploy/README-oracle.md:111 operatöre 'bağlanma adresi MERIDIAN_BIND_HOST ile değiştirilir' diyor — değiştirmiyor. Test yok (tests/ içinde MERIDIAN_BIND_HOST/public_bind geçmiyor).

**Öneri:** Bağlanma adresini tahmin etmeyi bırak: ya başlatıcılar `--host "$MERIDIAN_BIND_HOST"` versin (tek kaynak), ya da lifespan içinde gerçekten bağlanılan soketi (uvicorn config/server) okuyup kapıyı ONA bağla.

**Adversarial doğrulama:** Bulgu DOĞRULANDI, çürütülemedi. Kanıt zinciri: (1) meridian/api.py:66 kapıyı YALNIZ `os.environ.get("MERIDIAN_BIND_HOST","127.0.0.1")`den türetiyor (`public_bind = host not in (...)`), yani gerçek sokete değil, beyan edilen bir değişkene bakıyor; (2) depo genelinde `BIND_HOST` için tam üç geçiş var — api.py:66, auth_cli.py:57 (yalnız `status` çıktısı basar) ve deploy/README-oracle.md:111 — YAZAN hiçbir yer yok: meridian.service'te 25-42 arası 10 `Environment=` satırı var ama BIND_HOST yok, serve.sh 33-38'de 6 `export` var, BIND_HOST yok, .env.example'da da yok; (3) gerçek bağ her başlatıcıda sabit: serve.sh:39, docker-compose.yml:34, deploy/oracle-a1/meridian.service:43, ops/com.meridian.agent.plist:19, README.md:76 — hepsi `--host 127.0.0.1`. Sonuç: panoyu dışarı açmanın tek yolu olan "komut satırını elle `--host 0.0.0.0` yap" senaryosunda değişken boş kalır → `public_bind=False` → ne satır 69'daki `RuntimeError` ne satır 76'daki `obs.warn` ateşlenir; ve token/parola kurulu değilse `_auth`ın no-op olduğu tests/test_authority_boundaries_v77.py:1020-1030'da açıkça kanıtlandığı için HALT/Flatten yüzeyi kimliksiz açılır — yani kapının var olma nedeni olan tek senaryoda kapı yoktur. Değişken çift yönlü kopuk: tersine, BIND_HOST=0.0.0.0 verilip başlatıcı 127.0.0.1'de kalırsa güvenli bir bağda ASILSIZ `RuntimeError` atılır. Denetçinin kaçırdığı iki bağlam kararı değiştirmiyor: (a) ALEYHTE değil LEHTE ek kanıt — deploy/Caddyfile:8-9 dördüncü bir yanlış-güvence noktası olarak yanlış nedensel zinciri birebir yazıyor ("`--host 0.0.0.0` ... verilirse zaten api.py parola kurulu değilse açılmayı reddeder"), meridian.service:39 da benzerini ima ediyor; (b) HAFİFLETİCİ — denetçi "test yok" derken MERIDIAN_BIND_HOST/public_bind için haklı (grep boş), ama tests/test_authority_boundaries_v77.py:1032-1040 gerçek savunmayı çiviliyor: serve.sh, docker-compose.yml ve meridian.service'in ham `--host` satırlarını tarayıp `0.0.0.0` olmadığını doğruluyor. Bu çivi ops/com.meridian.agent.plist'i, elle komut satırı çağrısını ve A1'de bir systemd drop-in'ini kapsamıyor; ayrıca korudugu şey KAPI değil, başlatıcı dosyalarıdır — dolayısıyla "kablolanmamış güvenlik kapısı + üç/dört yerde var olmayan korumayı anlatan belge" kusuru ayakta kalıyor ve bu, aynı fonksiyonda 8 satır yukarıda (api.py:58-65) "HAYALET BAYRAK KALDIRILDI (MERIDIAN_TRUST_PROXY)" diye kutlanan sınıfın aynı ailesidir (bir derece hafif: TRUST_PROXY'yi SIFIR kod okuyordu, bunu iki yer okuyor ama sıfır yer üretiyor ve okunan değer korunan olguyu belirlemiyor). Şiddet 4 değil 3: bugün canlıda fiilî açık yok (tüm başlatıcılar loopback, compose portu da 127.0.0.1:8080'e bağlı, Caddy önde, 8080 kapalı) — kusur yanlış güvenlik hissi, yanlış operatör belgesi ve ateşlenemeyen bir kapıdır.

### C2 [şiddet 3 · api-scheduler] _last_closed_session() sessiz yutması yanlış gerekçeli — takvim düşerse TÜM seans-sonrası kadans sessizce durur

- **Yer:** meridian/scheduler.py:102-115
- **Sınıf:** YASA 4 kör noktası · sessiz fail-open · canlılık≠ilerleme

**Kanıt:**

`except Exception:  # sessiz-yutma: geç bağlanan yardımcı modül/çağrı; asıl karar bu değere bağlı DEĞİL ve çağıran yokluğu yedek değerle karşılıyor` → `return None`. Gerekçe gerçeğe aykırı: `last_closed` şunların HEPSİNİ kapıyor — terminal atlama (598), merdiven adım-1 (604), sık faz `_dense` (633), onarım/sip düzeltici (636), aynı-akşam bacağı `_leg_ready` (642), öğrenme kadansı (850), Y4 (865), haftalık üçlü (881). None dönerse `_dense=False`, `_chasing=False` → `fresh` KALICI False; ne `obs.warn` çıkar (kardeş `_leg_ready` satır 129'da `if not session: return None` ile erken döndüğü için onun tek-seferlik uyarısı da ATEŞLENMEZ), ne de yedek bir değer vardır. Bu arada `_persist()` her poll'de taze `updated` yazdığı için tick-bekçisi (scheduler_status.updated>45dk) susar ve 'güncel' dalı nabzı tazelediği için /healthz 200 döner. Mühendislik günlüğündeki ASILI-TİCK vakasının ('watchdog tick İÇİNDE olduğundan sustu') aynı sınıfı, farklı kapıdan.

**Öneri:** Süreç başına BİR kez `obs.warn("session_calendar_unavailable", ...)` bas ve `latest_session=None` hâlini `_sessiz_hat`ın veri segmentine sapma olarak sok — takvimsiz bir zamanlayıcı 'sağlıklı' görünemez.

**Adversarial doğrulama:** İDDİA DOĞRULANDI (çürütülemedi). (1) MEKANİZMA: `/Users/erdemozturk/AI-Trading/meridian/scheduler.py:581` `last_closed = _last_closed_session()`; None dönerse `:598` (terminal atlama) ve `:604` (merdiven adım-1) `if last_closed and …` ile düşer, `:631-633`te `_chasing = bool(refetch_chase) and refetch_chase == last_closed` → refetch_chase bir seans dizgisi olduğundan None'a asla eşit olamaz → False, `_dense = last_closed is not None and …` → False, dolayısıyla `_sparse=False` ve `fresh = _dense or _sparse` KALICI False. Bu tek bayrak `:636` onarım/sip düzelticisini, `:642` `use_cache=not fresh` (ağ çekimi hiç yapılmaz), ve `:643`ten `:903`e kadar UZANAN tüm bloğu — earnings (`:717`), arming (`:769`), selfreview (`:780`), yetim süpürme (`:799`), nous_eval (`:826`), öğrenme kadansı (`:850`), Y4 (`:865`), haftalık üçlü (`:881`), skill revizyon (`:890`), crosscheck (`:900`) — kapatır. Ayrıca cache hiç tazelenmediği için `latest_bar <= already` sonsuza dek doğru kalır ve `loop.daily_cycle` de koşmaz. (2) SESSİZLİK: `_leg_ready(None)` `:132-133`teki `if not session: return None` ile try bloğuna GİRMEDEN döner, yani kardeş `same_evening_gate_unavailable` uyarısı gerçekten ateşlenmez; "güncel" dalı `:911-922`de nabzı her poll tazeler (→ /healthz 200) ve `:923` `_persist()` taze `updated` yazar (→ RUNBOOK'taki `meridian-tick-watchdog.timer`'ın scheduler_status.updated>45dk kapısı susar). (3) GEREKÇE GERÇEĞE AYKIRI: `:114`teki "asıl karar bu değere bağlı değil ve çağıran yokluğu yedek değerle karşılıyor" cümlesi bu bölgede yanlıştır — yedek değer YOK, ve karar TAMAMEN bu değere bağlı. Cümle üstelik site-özel bir muhakeme değil, repoda 13 yerde HARFİ HARFİNE aynı olan bir şablon damgadır (hermes_runtime:376/425, api:1780/2450/2509, loop:766, fmp:126/134, scheduler:114/902, hermes:2003/2030, secrets:147); `codelaw.MARKER_RE` yalnız ≥20 karakter GEREKÇE VARLIĞINI ölçer, doğruluğunu değil — yani YASA 4 bu satırı mekanik olarak geçiriyor. (4) TEST KORUMASI YOK: `_last_closed_session`i monkeypatch'leyen 12 testin (test_data_pipeline_v67:379/423, test_ayni_aksam_bacagi_v133:587/669/700, test_temizlik_kablolama_v137:468/483, test_sip_gecmis_seans_v139:513, test_ogrenme_otomasyonu_v136:301, test_regime_patch:461, test_bottleneck_v12:32/57, test_audit_fixes:94) HEPSİ gerçek bir seans dizgisi döndürür; None yolu hiç koşulmaz. Canlı yüzey de gerçek: `deploy/oracle-a1/meridian.service:26` `MERIDIAN_AUTOSTART_CYCLE=1` — A1'de kadansı koşturan yol tam da bu. DENETÇİNİN KAÇIRDIĞI HAFİFLETİCİ (severity'yi 4'ten düşüren): `advance_once:567` HER poll'de `watchdog.check_and_alarm()` çağırıyor ve `watchdog.EXPECTED` yalnız `fresh` bloğundan ulaşılan nabızları İZLİYOR — `crosscheck`/`shadow_fit`/`axis2_cycle`/`y4_collect` 4 gün, `earnings_refresh`/`arming_eval`/`validation_report`/`massive_verify`/`shadowlaw_drift` 9 gün, artı daily_cycle'dan beslenen `cf_advance`/`p5_calibrations`/`mirror_reconcile` 4 gün. Yani kesinti SONSUZA KADAR sessiz değil: ~4 gün sonra çoklu MECHANISM_STALE alarmı yazılır (gerçi kök nedeni değil semptomu adlandırır, ve watchdog.py:688 teslim boşluğunu ayrıca not ediyor). İkinci hafifletici: en olası tetikleyici olan ImportError senaryosunda `adapters/data.py:226-234` aynı pencerede gürültülü `bar_calendar_unavailable` uyarısı üretir. Üçüncü hafifletici: tetikleyici ÖLÇÜLMÜŞ değil — `pandas-market-calendars>=4.4` pyproject:14'te sert bağımlılık, yani `except Exception` yalnız takvim katmanı arızasında (mcal sürüm/şema/tz kırılması) devreye girer.

### C3 [şiddet 3 · api-scheduler] run.py:worker() üretimde koşmayan İKİNCİ kadans uygulaması ve düzeltilmiş üç kusuru hâlâ taşıyor

- **Yer:** meridian/run.py:290-323 (+ modül docstring satır 6)
- **Sınıf:** ölü/çağrılmayan mekanizma · yorum-davranış sürüklenmesi · canlı-vs-replay ayrışması

**Kanıt:**

Docstring: '24/7 worker. ... Meant to run under systemd + tmux on the VM.' Canlı gerçek: `deploy/oracle-a1/meridian.service` → `Environment=MERIDIAN_AUTOSTART_CYCLE=1` + `ExecStart=... uvicorn meridian.api:app`, yani A1'de kadans `scheduler.advance_once`. Buna rağmen `Dockerfile:31` ve `docker-compose.yml:16` hâlâ `python -m meridian.run`, `README.md:79` onu 24/7 yol olarak öneriyor. O yolda yaşayan üç düzeltilmiş kusur: (a) satır 310-313 `sched = nyse.schedule(..., end_date=str(today))` + `sessions[-1]` → BUGÜNÜN seansını gece yarısından itibaren 'kapanmış' sayar; `scheduler._last_closed_session` docstring'i (106) bunu 'audit #12' diye düzeltilmiş ilan ediyor. (b) satır 314 `dataset.load(use_cache=False)` — `load_live()` DEĞİL; yani canlı ileri-yürüyen yolda Finviz keşfi ve aynı-akşam Alpaca bacağı YOK (dataset.py:201-221 'YALNIZ scheduler'ın canlı çağrısı kullanır'). (c) satır 315-317 `daily_cycle` çağrıldıktan sonra `last_processed = last_closed` KOŞULSUZ atanıyor — kapsama kapısı yüzünden noop dönse bile seans kalıcı atlanır; merdiven/onarım geçidi yok. Ayrıca hiçbir seans-sonrası kadans (öğrenme, Y4, haftalık üçlü, arming, selfreview, orphan sweep, nous, sprint, earnings, intraday gap) bu yolda yok. tests/ içinde `run.worker()` çağıran tek bir test yok.

**Öneri:** Ya `worker()`ı geri-alınabilirlik notuyla emekli et ve Dockerfile/compose/README'yi uvicorn+AUTOSTART_CYCLE yoluna çevir, ya da gövdesini `scheduler.advance_once()` çağrısına indir — iki kadans yasası tek depoda yaşayamaz.

**Adversarial doğrulama:** Çürütme denemesi başarısız oldu; iddianın her ayağı kodda doğrulandı. (1) ÜRETİMDE KOŞMUYOR: çıplak `python -m meridian.run` (→ `worker()`) yalnız `Dockerfile:31`, `docker-compose.yml:16` ve `README.md:79`'da geçiyor; canlı/yerel başlatma yollarının HEPSİ uvicorn+zamanlayıcı: `deploy/oracle-a1/meridian.service` (`MERIDIAN_AUTOSTART_CYCLE=1` + `ExecStart=... uvicorn meridian.api:app`), `ops/com.meridian.agent.plist:18` (uvicorn) ve `serve.sh:35,44` (AUTOSTART_CYCLE=1 + uvicorn) — `serve.sh:22` ve `deploy/oracle-a1/deploy.sh:114` `meridian.run`'ı YALNIZ `--replay` tohumu için çağırıyor, `api.py:168` ise `scheduler.start()`'ı bağlıyor. (2) ÜÇ KUSUR AYNEN DURUYOR: (a) `run.py:310-312` `end_date=str(today)` + `sessions[-1]` takvim indeksinden okuyor, yani bugünün seansını GECE YARISINDAN itibaren kapanmış sayıyor — `scheduler._last_closed_session` (satır 102-113) tam bu hatayı `market_close <= now` filtresiyle "audit #12" diye düzeltilmiş ilan ediyor; (b) `run.py:314` `dataset.load(use_cache=False)` çağırıyor, `load_live` DEĞİL — `dataset.py:201-221` docstring'i "YALNIZ scheduler'ın canlı çağrısı kullanır" diyor ve aynı-akşam Alpaca bacağının kapısı (`session=`) yalnız `load_live` üzerinden veriliyor (`scheduler.py:642`), dolayısıyla bu yolda ne Finviz keşfi ne aynı-akşam bacağı var; (c) `run.py:315-317` `daily_cycle` dönüşünü hiç sorgulamadan `last_processed = last_closed` yazıyor — `loop.daily_cycle` kapsama kapısı yüzünden `noop`/`waiting_for_universe`/`refused_regressive` dönse bile (loop.py:376, 387, 426) o seans bir daha denenmiyor; zamanlayıcıdaki sık/seyrek merdiven, `_repair_once_per_session`, terminal-atlama alarmı ve çoklu-seans yetişme döngüsü (`scheduler.py:974-983`) bu yolda YOK. (3) SEANS-SONRASI KADANSLARIN HİÇBİRİ yok: öğrenme, Y4, haftalık üçlü, arming, selfreview, orphan sweep, nous, sprint, earnings, intraday gap — hepsi `scheduler.advance_once` içinde (satır 636-967), `worker()` yalnız nabız + `daily_cycle` yazıyor. (4) TEST KORUMASI YOK: `tests/test_run_audit_v39.py` yalnız `--replay`/`--once`/CLI dağıtımını kilitliyor; repo genelinde `run.worker()` çağıran tek bir test yok. Tek hafifletici bağlam ve tek düzeltme: README'nin GCP/Docker bölümü 2026-07-30'da "⚠️ STALE, NOT the canonical path" bannerıyla beyan edilmiş (ama sayılan üç sapmanın hiçbiri bu kadans ayrışması DEĞİL), buna karşılık `README.md:79` "Run it locally" bölümünde ve o banner'ın DIŞINDA, `uv run python -m meridian.run`'ı çekincesizce "the 24/7 worker" diye öneriyor; ayrıca `codelaw.py`'de `run.py` hakkında hiçbir beyan/muafiyet yok. Denetçinin tek abartısı "seans KALICI atlanır" ifadesi: `last_processed` yalnız süreç belleğinde (zamanlayıcının aksine `_persist`/`_DURABLE` ile diske yazılmıyor), yani atlama süreç ÖMRÜ boyunca kalıcı, yeniden başlatmada sıfırlanıyor — hükmü değiştirmiyor. Ek doğrulayıcı kanıt: `HEARTBEAT_STALE` alarmının repo'daki TEK ateşleyicisi `run.py:304` ve `docs/RUNBOOK.md:59` operatörü oraya yönlendiriyor — yani runbook, üretimde asla ateşlenemeyecek bir alarmı belgeliyor.

### C4 [şiddet 3 · defter-durum] dbmigrate hata yolu, BOŞ ama AKTİF bir DB bırakıyor — altı defter birden sessizce boş okunuyor

- **Yer:** meridian/dbmigrate.py:161-162, 186-192, 221-226 · meridian/storage.py:230-250, 268-286
- **Sınıf:** hata-yolu sağlamlığı / sessiz fail-open / iddia-davranış ayrışması

**Kanıt:**

apply() transaction'a girmeden ÖNCE `storage.connect(create=True)` + `ensure_schema(c)` çağırıyor; ensure_schema kendi COMMIT'ini atıyor (storage.py:245) ve `_SCHEMA_OK`e yazıyor (249). O andan itibaren `active()` (storage.py:274-286) 'dosya var + şema sürümü var' gördüğü için True döner — 'veri taşındı mı' HİÇ sorulmaz. KAYNAK_BOZUK (188-192) ve PARİTE_TUTMADI (221-226) dallarında ise yalnız `ROLLBACK` atılıp `return` ediliyor; DB dosyası şemasıyla YERİNDE kalıyor.
ÖLÇÜM (MERIDIAN_ROOT=scratchpad sandbox, canlı state'e dokunulmadı): bozuk portfolio.json ile `dbmigrate.apply()` → `ok=False`, 'hiçbir varlık taşınmadı … Kaynak dosyalar YERİNDE'. Hemen sonrasında:
  db dosyası var : True | şema sürümü : 1 | active(trades) : True
  store.read_jsonl("trades.jsonl") n = 0   (diskteki trades.jsonl satır sayısı: 2)
  store.read_json("scoreboard.json") = VARSAYILAN · store.read_json("portfolio.json") = VARSAYILAN
Yani rapor 'hiçbir şey olmadı' derken arka uç zaten değişmiştir. Sonuç zinciri ölçülü: `loop._load_broker` (loop.py:114-121) `st = store.read_json(PORTFOLIO, None)` → None → taze `PaperBroker(START_EQUITY)`, `_id=0`, `positions={}` — açık pozisyonlar kitaptan düşer ve yeni işlem kimlikleri DB'dekilerle çakışır. Bu tam olarak `storage.connect` docstring'inin ADIYLA andığı hâl: 'migrasyon yapılmadan yapılmış gibi görünen bir geçiş, en tehlikeli hâl' (storage.py:136-141).
TEST BOŞLUĞU: tests/test_storage_v142.py:116-122 ve 129-131 geri almayı `storage.read_entity(...) == []` ile ÇİVİLİYOR ve kaynak dosyaların durduğunu doğruluyor — ama bir soru eksik soruyor: aynı anda `store.read_jsonl` ne dönüyor? Dönen değer [] ve test bunu 'tamamı geri alındı' diye okuyor.
PRECONDITION (dürüstlük notu): A1'de migrasyon 2026-07-31'de BAŞARIYLA koştu, yani bugün ikinci koşu `zaten_tasindi` der ve bu yol tetiklenmez. Risk pencereleri: H7 restore tatbikatı sonrası yeniden migrasyon, ENTITIES'e yeni varlık eklenmesi, ve DB'siz herhangi bir kurulum (Mac kopyası dahil — burada meridian.db YOK).

**Öneri:** Şemayı ancak migrasyon transaction'ının İÇİNDE kur; her hata-dönüş yolunda (KAYNAK_BOZUK, PARİTE_TUTMADI, beklenmedik istisna) `storage.close_all()` + DB dosyasını `meridian.db.failed-<ts>` diye kenara al ki `active()` False'a dönsün — ve bu davranışı 'başarısız migrasyondan sonra store hâlâ DOSYAYI okur' diyen bir testle çivile.

**Adversarial doğrulama:** ÇÜRÜTÜLEMEDİ — iddia kodda da ölçümde de doğrulandı. (1) Kod: `dbmigrate.apply()` transaction'ı açmadan (dbmigrate.py:167 `BEGIN IMMEDIATE`) ÖNCE `storage.connect(create=True)` + `ensure_schema(c)` çağırıyor (160-161); `ensure_schema` kendi `BEGIN…COMMIT`ini atıp (storage.py:234,245) `_SCHEMA_OK`e yazıyor (249) — yani DB dosyası + `schema_version` satırı KALICI olarak diske iniyor. KAYNAK_BOZUK (188-192) ve PARİTE_TUTMADI (221-226) dalları yalnız `ROLLBACK` + `return` yapıyor; hiçbir yerde dosyayı silen/geri alan bir adım yok (arşivleme zaten COMMIT sonrası, 239-246). (2) Kapı: `storage.active()` (268-286) SADECE "dosya var mı + `schema_version` None değil mi" soruyor; `migrated_at` okuma yolunda HİÇ sorulmuyor — repo genelinde `grep migrated_at|source_digest` yalnız dbmigrate'in kendi raporunda ve tests/test_storage_v142.py:160'ta geçiyor. `store.db_backed` = `storage.active` (store.py:48) ve `read_json` (286-289) / `read_jsonl` (329-331) DB koluna girince dosyaya GERİ DÜŞMÜYOR. (3) Bağımsız ölçüm (iki ayrı sandbox, `MERIDIAN_ROOT` ile; canlı state'e dokunulmadı): bozuk `portfolio.json` → `ok=False`, "hiçbir varlık taşınmadı … Kaynak dosyalar YERİNDE" — hemen sonrasında db dosyası var=True, şema=1, `active(trades)`=True, `db_backed`=True, `store.read_jsonl("trades.jsonl")`=0 satır (diskte 2), `scoreboard/portfolio`=VARSAYILAN; TAZE bir süreçte (yani `_SCHEMA_OK` önbelleği boşken, kusur disk durumundan geliyor) aynı sonuç. `read_entity` yamasıyla PARİTE_TUTMADI dalı da birebir aynı: active=True, n=0, diskte 2. (4) Sonuç zinciri ölçüldü: diskte `cash=91234.5, last_id=42, positions=[AAPL]` iken `loop._load_broker` (loop.py:114-121) `cash=100000.0, _id=0, positions={}` döndürdü — yani açık pozisyon kitaptan düşüyor ve işlem kimlikleri sıfırdan yeniden başlıyor. Bu tam olarak `storage.connect` docstring'inin (136-141) ADIYLA yasakladığı hâl. (5) Test boşluğu doğru: test_storage_v142.py:118-122 ve 131 geri almayı `storage.read_entity(...) == []`/`is None` ile — yani HAM DB katmanıyla — çiviliyor; `store.read_jsonl`/`db_backed` hiç sorulmuyor, oysa aynı dosya 212-215'te bu sözlüğü kullanıp `db_backed False → read_jsonl == []`i "DÜRÜST boşluk" diye ayırt ediyor, dolayısıyla eksiklik üslup değil kör nokta. (6) BULGUYU GÜÇLENDİREN ek kanıt: yanlış inanç ops yoluna yazılı — `deploy/oracle-a1/bakim_h9.sh:37-39` migrasyon düşerse "servisler DOSYA arka ucuyla başlatılıyor" deyip `systemctl start meridian` koşuyor; ölçüme göre bu cümle YANLIŞ, worker BOŞ DB ile başlardı. `serve.sh:20` kapısı da (`[ ! -s state/meridian.db ]`) korumaz: boş DB 65536 bayttır, seed koşmaz. Tek hafifletici: `MERIDIAN_DB=off` (elle, operatörün durumu bilmesi gerekir) ve `watchdog.monotonicity_report` (trades sayısı izleniyor, 1291) — ama o bir HALT değil, boş turdan SONRA yanan bir bayrak. (7) Dürüstlük payı (şiddeti 4 değil 3 tutan tek sebep): A1'de altı varlık 2026-07-31'de migrated_at damgası aldığı için bugün ikinci koşu `zaten_tasindi` der ve bu dallara girmez; kusur CANLIDA LATENT'tir. Tetiklenme penceresi gerçek ve dar değil: `meridian.db` bulunmayan her ortam — bu Mac kopyası (state/'te meridian.db YOK, doğrulandı), temiz kurulum, `state-premigration-*.tar.gz` (bakim_h9.sh:33) üzerinden geri yükleme/H7 tatbikatı ve DB'siz bir konakta ENTITIES'e yeni varlık eklenmesi. Düzeltme maliyeti düşük (ensure_schema'yı transaction'a almak, hata dalında yeni doğan DB'yi silmek ya da `active()`i migrated_at'e bağlamak), tetiklendiğindeki hasar defter bütünlüğü sınıfında.

### C5 [şiddet 3 · defter-durum] `MERIDIAN_DB=off` 'acil geri dönüş anahtarı' defterleri geri getirmiyor; defterleri boşaltıyor ve geri-alma kolu hiç yok

- **Yer:** meridian/storage.py:20-23, 263-265 · meridian/dbmigrate.py:21-24, 239-246, 293-320 · ROADMAP.md:272
- **Sınıf:** yarım/kablolanmamış mekanizma · yorum-davranış sürüklenmesi

**Kanıt:**

İki docstring de bunu acil geri dönüş anahtarı ilan ediyor: storage.py:23 '`MERIDIAN_DB=off` acil geri dönüş anahtarıdır (dosyalar `.migrated` ekiyle DURUYOR)' ve dbmigrate.py:22-24 '`MERIDIAN_DB=off` acil anahtarının anlamlı olması için dosyaların DURMASI gerekir'. ROADMAP.md:272 durumu 'MERIDIAN_DB=off acil anahtarı hazır' diye kaydediyor.
ÖLÇÜM (temiz sandbox, BAŞARILI migrasyon sonrası): arşiv ['trades.jsonl.migrated','scoreboard.json.migrated','portfolio.json.migrated','equity_curve.json.migrated']
  DB açık → trades n = 2 | portfolio.cash = 94457.91
  DB=off  → trades n = 0 | portfolio = VARSAYILAN | scoreboard = VARSAYILAN | equity = VARSAYILAN
Sebep: dosyalar `.migrated` adında; `store._path` `state/trades.jsonl`e bakıyor, yok, varsayılana düşüyor. Anahtarı çeken operatör 'eski hâle döndüm' sanırken altı defterin tamamı boş okunur ve ilk yazım YENİ, ayrışık dosyalar doğurur (last_id sıfırlanır → kimlik çakışması). `dbmigrate.main` (293-320) yalnız --uygula/--json/--durum/--zorla taşıyor; `.migrated`ları geri adlandıran hiçbir kod/CLI/betik yok.
Test bu boşluğu çiviliyor: tests/test_storage_v142.py:208-215 `assert store.read_jsonl("trades.jsonl") == []  # dosya .migrated oldu — DÜRÜST boşluk`. Boş defter dürüst olabilir; 'acil geri dönüş' DEĞİLDİR.
İKİNCİL: arşivleme döngüsü (dbmigrate.py:241-246) `if p.exists() and not hedef.exists()` şartlı ve `zaten_tasindi` dalı (173-176) varlığı `tasinan`dan çıkarıyor — yani kaynak dosya bir kez geri konursa `--uygula` onu bir daha ARŞİVLEMEZ; DB ile dosya yan yana okunabilir kalır ve rapor `ok=True` der (dbmigrate.py:22-24'ün 'iki okunabilir gerçek kaynak olmaz' vaadinin tersi).

**Öneri:** `dbmigrate --geri-al` ekle (DB'yi kenara al + `.migrated` → asıl ad) VE `disabled_by_env()` True iken DB dosyası varken kaynak dosyalar yoksa `obs.warn` ile beyanlı olarak reddet/uyar — sessizce varsayılana düşme.

**Adversarial doğrulama:** Çürütemedim — iddia hem kodda hem canlı ölçümde birebir doğrulandı. Temiz sandbox'ta (config.STATE yamalı, canlı state'e tek bayt yazılmadı) `dbmigrate.apply()` 4 varlığı parite-digest'le taşıdı ve arşivledi (`['trades.jsonl.migrated','scoreboard.json.migrated','portfolio.json.migrated','equity_curve.json.migrated']`); DB açıkken `store.read_jsonl("trades.jsonl")` n=2 ve `portfolio.cash=94457.91`, `MERIDIAN_DB=off` verildiği anda AYNI çağrılar trades n=0, portfolio/scoreboard/equity ise çağıranın VARSAYILANI döndü, üstelik `store.stamp`=(0,0) ve `store.mtime`=None oldu (yani tazelik dedektörleri de körleşti). Zincir tam olarak iddia edildiği gibi: `storage.disabled_by_env` (storage.py:263-265) `active()`i False yapar → `store.db_backed` False → `store.read_json`/`read_jsonl` (store.py:286-300, 329-331) `_path(name)` = `config.STATE/trades.jsonl`e bakar, dosya `.migrated` adında olduğu için yoktur ve sessizce varsayılana düşer. Ardından ilk yazım gerçekten AYRIŞIK yeni dosyalar doğurdu (dizin listesinde `portfolio.json` ve `trades.jsonl`, `*.migrated`ların YANINDA belirdi; dosya defteri n=1, DB defteri n=2 — `last_id` sıfırlanmış bir kitapla kimlik çakışması yolu açık). Geri-alma kolu aranıp bulunamadı: depo genelinde `MERIDIAN_DB` yalnız 5 yerde geçiyor (storage.py:23/264-265, dbmigrate.py:22, ROADMAP.md:272, tests/test_storage_v142.py:209-213) ve `.migrated` dosyalarını geri adlandıran hiçbir fonksiyon, CLI bayrağı (`dbmigrate.main` yalnız --uygula/--json/--durum/--zorla), ops betiği (ops/ altında 9 betik) veya docs/RUNBOOK.md kaydı yok; yani storage.py:23'ün "acil geri dönüş anahtarıdır (dosyalar `.migrated` ekiyle DURUYOR)" ve dbmigrate.py:22-24'ün "anahtarın anlamlı olması için dosyaların DURMASI gerekir" cümleleri ile ROADMAP.md:272'nin "MERIDIAN_DB=off acil anahtarı hazır" durum kaydı, kolu tek başına çekecek operatör için YANLIŞtır — dosyaların BAŞKA BİR ADLA durması anahtarı geri dönüş yapmaz, yalnız elle `mv` yapıldıktan sonra yapar ve o adım hiçbir yerde yazılı değil. İkincil iddia da ölçümle doğrulandı: kaynak dosyayı geri koyup `apply()` tekrar koştuğumda rapor `ok=True, yazildi=False, arsivlenen=[]` dedi, parite satırı `zaten_tasindi` (dbmigrate.py:173-176 dalı `tasinan`a eklemiyor, 241-246 arşiv döngüsü de yalnız `tasinan`ı geziyor), `trades.jsonl` yerinde kaldı ve DB otoriter okunmaya devam etti — yani dbmigrate.py:22-24'ün "aynı anda İKİ okunabilir gerçek kaynak bırakmayız" gerekçesi bu yolda tutmuyor: DB açıkken dosya sessiz bir yem, `MERIDIAN_DB=off` anında ise otorite oluyor. Şiddeti 4 değil 3 tutuyorum çünkü (a) veri KAYBOLMUYOR — arşivlerin içeriği bütün (tests/test_storage_v142.py:172 bunu ölçüyor) ve tek `mv` ile geri gelir, (b) davranış test-beyanlı (test_storage_v142.py:208-215 "DÜRÜST boşluk"), yani gizli bir bozulma değil belgeli bir boşluk, (c) `watchdog.monotonicity_report` trades sayısı ve `peak_equity` gerilemesini bayrağa çevirirdi, (d) hiçbir otomatik yol anahtarı kendiliğinden çekmiyor; buna karşılık 2'nin üstünde çünkü kol tam da kriz anında (canlı para sisteminde, H9 2026-07-31'de canlıya girdi) çekilir ve o an operatöre "eski hâle döndüm" derken altı defterin tamamını boş gösterip ayrışık bir ikinci kitap doğurur.

### C6 [şiddet 3 · gozetim] Korunum dedektörünün olay penceresi satır-limitli: K1'in BROKER_REJECT düzeltmesi bugünkü defterde ÖLÜ, alarm yanlış yanıyor

- **Yer:** meridian/watchdog.py:265 (`store.read_jsonl("events.jsonl", limit=8000)`), :298 (`limit=20000`)
- **Sınıf:** canlı-vs-ölçüm ayrışması / kendi dosyasında yazılı dersin uygulanmaması

**Kanıt:**

Aynı dosya satır 390-407'de `events_since()`i tam bu yüzden yazmış ("satır-limitli her tüketici geçmişe kör oldu") ama `conservation_report` iki yerde hâlâ satır limiti kullanıyor. ÖLÇÜM (yerel state/events.jsonl, 27.403 satır): `limit=8000` yalnız 2026-07-29T00:32→08-01 arasını kapsıyor. `plan_id` taşıyan 4 BROKER_REJECT olayının satır indeksleri 7983/7985/7987/12199 — yani dosyanın SONUNDAN 19.420/19.418/19.416/15.204 satır geride, hepsi pencerenin DIŞINDA. Simülasyon: kodun penceresiyle dropped=0 → unexplained=10; tam defterle dropped=4 → unexplained=6. Dördü tam da K1 notunun (watchdog.py:271-278) adıyla saydığı UNP/NSC/TMO/RTX. `state/integrity_alarmed.json` şu an "conservation" mandalını taşıyor — yani bu YANLIŞ alarm canlıda ateşlenmiş. İkinci yüz: `live_start` = 20000 satırlık pencerenin ilk `daily_cycle`ı = 2026-07-22, defterdeki GERÇEK ilk daily_cycle ise 2026-07-10; aradaki 12 günde açıklanamayan bir plan `replay_era` ("körlük, sızıntı değil") sayılıp sessizce düşer — docstring'in tek gerçek bulgusu olarak andığı GS 07-14 sızıntısı bugün tam bu yoldan yutulurdu.

**Öneri:** İki okumayı da `events_since(gun)` ile değiştir (aynı dosyada hazır) ve `live_start`ı satır penceresinden değil defterin tamamından hesapla.

**Adversarial doğrulama:** ÇÜRÜTÜLEMEDİ — ana yüz kod ve ölçümle birebir doğrulandı. `store.read_jsonl` her iki arka uçta da SON N satırı döndürür (store.py:354 `rows[-limit:]`; DB yolunda storage.py:380 `ORDER BY seq DESC LIMIT ?` + reverse), yani watchdog.py:265'teki `limit=8000` 27.403 satırlık defterin son %29'udur. Yerel state/events.jsonl üzerinde bağımsız ölçüm (salt-okuma betiği): 4 BROKER_REJECT olayının hepsi `plan_id` taşıyor ve satır indeksleri 7983/7985/7987/12199 — sondan 19.420/19.418/19.416/15.204 geride; pencere yalnız 2026-07-29T00:32 → 08-01T15:49 arasını kapsıyor, pencere içinde 0 BROKER_REJECT var. conservation_report'un mantığını birebir taklit eden simülasyon: kodun penceresiyle dropped=0, unexplained=10; tam defterle dropped=4, unexplained=6 — düşen 4 satır tam da K1 notunun (watchdog.py:271-278) adıyla saydığı P-2026-07-23-UNP/NSC/TMO + P-2026-07-27-RTX. Yani K1 düzeltmesi bugünkü canlı defterde ÖLÜ; yorumun iddia ettiği davranış üretimde gerçekleşmiyor. Çürütme denemelerinin hepsi başarısız: (a) başka yerde kablolu DEĞİL — conservation_report planların `broker_status` alanına hiç bakmıyor, oysa aynı 4 plan trade_plans.jsonl'da `broker_status: "failed_broker_rejection"` taşıyor (loop.py:878) ve bu alan zaten satır 259'da yüklenmiş `plans` listesinde, pencereden BAĞIMSIZ; loop.py:863'ün yazdığı `gap_veto` da hiç okunmuyor; (b) bilinçli-beyanlı DEĞİL — 8000/20000 sayılarını gerekçelendiren tek bir yorum yok, aksine aynı dosyada aynı gün (K1, 2026-07-30) yazılan `events_since()` (satır 390-407) "satır-limitli her tüketici geçmişe kör oldu... maliyet ~sıfır, satır limiti I/O tasarrufu ETMİYOR" diyor ve parity_report bu yeni yola geçirilmiş, conservation_report geçirilmemiş; (c) test-korumalı DEĞİL — tests/test_kopukluk_kapatma_v122.py:87 senaryosu `sandbox_state` (conftest.py:403, boş tmp dizin) içinde 1 plan + 1 olayla koşuyor, 8000 satırlık pencere hiç zorlanmıyor, dolayısıyla test yeşilken üretim kırık; (d) defter döndürme/kırpma yok (27.403 satır). Denetçinin İKİ ABARTMASI düzeltilmeli: (1) "bu YANLIŞ alarm canlıda ateşlenmiş" fazla güçlü — tam defterle de unexplained=6 > 0 olduğu için `integrity_alarmed.json`'daki "conservation" mandalı yine kurulurdu; hatalı olan alarmın VARLIĞI değil, sayısı (10 yerine 6) ve K1'in çözdüğü 4 satırın panoda hâlâ "kayıtsız kayboldu" diye görünmesi; mutation.py:313 jetonu da ikili olduğu için değişmezdi. (2) `live_start` yüzü yapısal olarak gerçek (pencereyle 2026-07-22, tam defterle 2026-07-10) ama BUGÜN ölü: 07-10..07-21 bandındaki 4 planın hepsi daha önceki dallardan çıkıyor (P-2026-07-14-GS NO_GO, MNST işlem oldu, iki MMM NO_GO), replay_era iki koşumda da 242 — yani anılan GS 07-14 örneği bugün replay_era'dan değil NO_GO dalından düşer; bu ikinci yüz uyuyan bir kusur, aktif değil. Not: ölçüm YEREL state (mtime 2026-08-01 18:49) üzerinde yapıldı; A1 kapsam dışıydı, ancak canlı defter en az bu kadar uzun olacağı için pencere orada daha da dardır. Şiddet 3: aktif ve operatöre görünen bir bütünlük dedektörü yanlış sayı üretiyor ve beyan edilen K1 düzeltmesi üretimde çalışmıyor, ama hiçbir işlem kararı değişmiyor, alarm durumu aynı kalıyor ve ikinci yüz şu an etkisiz.

### C7 [şiddet 3 · gozetim] Kanal BAĞLIYKEN teslimat başarısızlığı hiçbir sayaca girmiyor: `alarm_delivery` ve `notify_channel` satırları yeşilken sıfır alarm ulaşıyor olabilir

- **Yer:** meridian/obs.py:154-156 (ve karşıtı :121-132)
- **Sınıf:** alarm yolu uçtan uca kopukluğu

**Kanıt:**

`if notify.send(f"⚠️ {token}: {message}"): sent[token] = time.time(); store.write_json(...)` — False dalında HİÇBİR ŞEY yok. `notify_undelivered.json` sayacı yalnız `not notify.configured()` dalında (obs.py:121-132) artıyor. Yani "kanal yok" ölçülüyor, "kanal var ama Telegram/webhook cevap vermiyor" ölçülmüyor. Sonuç: watchdog.parity_report :689-721 `alarm_delivery` satırı `_kalan == 0` gördüğü için ok=True, :734-740 `notify_channel` satırı `notify.configured()` True olduğu için ok=True — her iki satır yeşilken operatöre tek bir alarm gitmemiş olabilir. Tek iz `notify.py:145 obs.warn("notify_delivery_failed", ...)`, ve o olayın ÜRETİM tarafında okuyucusu yok (grep: yalnız tests/test_notify_obs_audit_v34.py). Bu, obs.py'nin kuruluş ayrımının ("alarm yazıldı ≠ alarm ULAŞTI") tam da adını koyduğu vakada kırılması.

**Öneri:** `notify.send` False dönerse aynı `_bump` desenini `notify_undelivered.json`a uygula (ör. `token` + `_teslim_hatasi` ayrı sayaç) ki `alarm_delivery` satırı bu hâli de görsün.

**Adversarial doğrulama:** Bulgu doğrulandı; çürütme denemesi başarısız oldu. Kod aynen iddia edildiği gibi: `obs.py:154-156` `if notify.send(...): sent[token]=...; store.write_json("notify_sent.json", sent)` — False dalı YOK, yani yapılandırılmış bir kanal cevap vermediğinde hiçbir sayaç artmıyor, hiçbir artefakt yazılmıyor. `notify_undelivered.json`ı besleyen tek kod yolu `obs.py:121-132`'dir ve o dal `if not notify.configured():` altındadır (grep: dosyaya yazan başka çağıran yok; okuyanlar `watchdog.py:689` ve `api.py:1408`). Zincirin geri kalanı da kapatmıyor: `notify.py:145 obs.warn("notify_delivery_failed", ...)` olayının ÜRETİM tarafında ANAHTARLI okuyucusu yok — literal ad yalnız `tests/test_notify_obs_audit_v34.py:46,56,63`'te geçiyor, panonun `EV_TR` çeviri sözlüğünde bile yok (app.js:937 bloğunda eşleşme sıfır), dolayısıyla en fazla `/api/events`in son 80 satırlık genel akışında ham adla ve `level="warn"` olduğu için kırmızısız akıp gidiyor; `watchdog.alarm_budget` onu yalnız anonim "low" sayacına ekliyor. Bir DÜZELTME payı: `parity_report`'ta `alarm_delivery` satırı `if _tot:` kapısına bağlı (watchdog.py:699), yani kanal hep bağlıyken ve hep başarısızken satır "yeşil" olmaz, HİÇ ÇIKMAZ — ki `ok = all(r["ok"] for r in rows)` (watchdog.py:768) için sonuç aynı kapıya çıkar; satırın gerçekten yeşil göründüğü hâl ise iddiadaki ACK-soğurulmuş yığın hâlidir (`_kalan == 0` → "birikmiş N alarmın tamamı okundu", :704-706, test_alarm_delivery_v71.py:142-157). `notify_channel` satırı ise tam da iddia edildiği gibi `ok=_cfg` (watchdog.py:734-736) olduğu için `configured()` True iken koşulsuz yeşil ve "uzak bildirim kanalı bağlı" diyor; pano da aynı yalanı tekrarlıyor (app.js: `a.channel_configured ? "uzak kanal bağlı"` ). Bilinçli-beyan veya test koruması da yok: test_alarm_delivery_v71.py'nin "kanal bağlı" senaryosu `send`i **True** dönecek şekilde monkeypatch'liyor (satır 47, 179), yani başarısız-teslimat + bağlı-kanal bileşimi hiçbir testte ölçülmüyor; `test_o1b`'deki "bilerek sessiz" istisna listesi boş, yani sessizlik bir karar olarak da yazılmamış. Yedek yüzeyler kaybı sınırlıyor ama boşluğu kapatmıyor: alarm `events.jsonl`'a her hâlükârda yazılıyor ve `notify.inbox()` ACK'lenmemiş olarak sayıyor — ancak hiçbir dedektör `pending`i okumuyor (grep: watchdog/selfreview/analytics'te tüketici yok) ve gelen kutusu tam da bildirim zincirinin var oluş sebebini (paneli AÇMADAN haberdar olmak) gerektiriyor; ayrıca başarısızlıkta `notify_sent.json` yazılmadığı için susturma penceresi hiç kurulmuyor, yani her alarmda yeniden denenip yeni bir sessiz `warn` satırı düşüyor. Şiddeti 4 değil 3 tutuyorum: veri/alarm KAYBI yok (defter + yerel gelen kutusu ayakta, `/api/notify/test` ile elle yoklama var), kırılan şey modülün kendi kurucu ayrımının ("alarm yazıldı ≠ alarm ULAŞTI") ikinci yarısında YANLIŞ-YEŞİL üretmesi — HALT/ROLLBACK/HEARTBEAT_STALE gibi "beni uyandır" sınıfı için operatörün yanlış güvenle uyumasına yol açar.

### C8 [şiddet 3 · icra-entegrasyon] Pano düğmesi E1 yasasını by-pass ediyor: de-risk çarpanı yok, ATR/ref girdisi yok, dedup yok, E2 satırı yok

- **Yer:** meridian/api.py:2310-2325 (çağıran: meridian/web/app.js:5131-5136)
- **Sınıf:** iki-motor ayrışması / kablolanmamış ikinci emir yolu

**Kanıt:**

`results = [{"ticker": p["ticker"], **alpaca.submit_plan(p, eq)} for p in armed]` — submit_plan varsayılanlarıyla çağrılıyor: size_mult=1.0, atr=None, ref_price=None. Döngü yolu (loop.py:837-839) tam tersini yapıyor: `size_mult=derisk_mult(eq_now, meta['peak_equity'])` (yorumda: 'without it the mirror over-sized ~2x in every drawdown', audit #50) + `atr=_lw.get('atr'), ref_price=_lw.get('ref_price')` (silahlanma anında sabitlenmiş entry_law girdileri). Sonuçlar: (1) %5 düşüşte iç motor 0,6 çarpanla doldururken bu düğme TAM boyut gönderiyor (ölçtüm: derisk_mult(95k,100k)=0.6); (2) atr=None → limit yalnız %1 pct_cap ile kurulur, iç motor ise fill anında ATR'li limiti uygular → aynı planda İKİ FARKLI tavan; (3) `meta['alpaca_submitted']` ne okunuyor ne yazılıyor → aynı plan hem düğmeyle hem döngüyle gönderilebilir ve döngü bunu bilmez; (4) hiçbir _entry_exec_write yok → E2 slipaj defterinde bu gönderimin izi olmaz; (5) `eq = ... else 100_000.0` sabit kaçağı: hesap okunamazken döngü _MirrorUnreachable ile REDDEDERKEN (loop.py:819-827) bu uç hayali 100k üzerinden boyutlandırıp gönderiyor. app.js:5131 tek tık, teyit yok.

**Öneri:** Uç noktayı kendi gönderim mantığını kurmak yerine döngüdeki ayna bloğunun tek fonksiyona çıkarılmış hâline (dedup + entry_law + derisk + E2 yazımı + unreachable reddi) delege et.

**Adversarial doğrulama:** Çürütme denemesi başarısız — bulgu doğrulandı, hatta iddia edilenden geniş. `meridian/api.py:2310-2325` gövdesinin TAMAMI okundu; sarmalayıcı/dekoratör/ara katman yok, `results = [{"ticker": p["ticker"], **alpaca.submit_plan(p, eq)} for p in armed]` satırı `submit_plan(plan, equity, size_mult=1.0, atr=None, ref_price=None)` (alpaca.py:399-400) varsayılanlarıyla çağrılıyor. (1) De-risk: `derisk_mult` (broker.py:178-189, DERISK_FLOOR_DD=0.08) döngüde loop.py:838'de uygulanıyor, api.py'de `peak_equity` kelimesi HİÇ geçmiyor (grep: api.py'de 0 eşleşme) — oysa alan canlı state'te mevcut (state/portfolio.json `peak_equity: 102520.45`), yani veri elinin altındayken kullanılmıyor; %5 düşüşte 1.0-(0.05-0.03)/(0.08-0.03)=0.6 yerine tam boyut gider. (2) `atr=None` → `entry_limit_price` (broker.py:118) `off=pct_off` dalına düşer, limit yalnız `limit_pct_cap=0.01` ile kurulur; iç motor `_lw.get("atr")` ile ATR'li limiti uygular → aynı planda iki tavan. (2b) DENETÇİNİN KAÇIRDIĞI, DAHA AĞIRI: `ref_price=None` → `entry_order_decision`'da `gap=(rp is None)` TRUE olur (broker.py:145) ve canlı `state/goal.yaml:54 gap_behavior: marketable_limit` altında mod `stop_limit` DEĞİL `marketable_limit` seçilir; `submit_bracket(..., entry_type=("limit" if mode=="marketable_limit" else "stop_limit"))` (alpaca.py:432) gereği kırılım teyidi bekleyecek buy-stop, tetiğin %1 üstünde ANINDA dolabilir bir limit emrine dönüşür — hem giriş yasası hem gap vetosu tümden devre dışı kalır. (3) Dedup: `alpaca_submitted` api.py'de ne okunuyor ne yazılıyor (grep tüm repo: yalnız loop/watchdog/analytics/testler); döngü loop.py:829/885'te bunu dedup kümesi sayıyor ve loop.py:148-150 yorumu tek ağın Alpaca'nın duplicate-client_order_id reddi olduğunu beyan ediyor — ki bu ağ işi KÖTÜLEŞTİRİYOR: düğmeden sonra döngü aynı planı gönderip ret alınca loop.py:878 `pl["broker_status"]="failed_broker_rejection"` damgalayıp planı silahlı kümeden DÜŞÜRÜYOR, yani broker'da canlı bracket dururken iç defter planı yok sayıyor (sistemin kendi "iki defterin ayrışması vetodan büyük risktir" hükmüne aykırı). (4) E2: `_entry_exec_write` yalnız loop.py:39/484/490/841'de; bu uçtan geçen gönderim slipaj defterinde hiç görünmez. (5) `eq = ... else 100_000.0`: `account()` docstring'i (alpaca.py:243-244) birebir "çağıran varsayılan bir sermayeye düşmeden önce transport()['ok']'e bakmalı (aksi halde hayali 100k üzerinden boyutlandırır)" diyor — döngü loop.py:819-827'de `_MirrorUnreachable` ile reddederken bu uç tam da yasaklanan şeyi yapıyor (pratik tetik dar: tam kopuşta submit de düşer, ama `account()` her istisnada None döndüğü için 4xx/parse hatasında emir yolu ayakta kalabilir). Ek olarak uç `config.BROKER` kapısını da atlıyor: ayna yapılandırmayla KAPALIYKEN bile düğme Alpaca'ya emir gönderir. Test koruması yok — `submit_armed` yalnız tests/test_authority_boundaries_v77.py:954/967'de geçiyor ve orada sadece 401/self-POST yetkisi sınanıyor, hiçbir parite testi yok. Bilinçli-beyan da yok: docstring aksine "the scheduler also does this each cycle" diyerek eşdeğerlik iddia ediyor. UI'da `app.js:5087` düğmesi `alpacaSubmit`e bağlı ve `alpacaClose`un aksine teyit diyaloğu YOK — tek tık. (Denetçinin verdiği app.js:5131-5136 satır numarası kaymış; gerçek çağıran app.js:5163-5167 — tek yanlış detay bu, hükmü değiştirmiyor.) Şiddeti 4'e değil 3'e çekiyorum: kusur gerçek ve icra bütünlüğünü çok yönlü deliyor, ancak yıkım yarıçapı sınırlı — yalnız PAPER hesap, `_auth` jetonu şart, `health.halted()` kapısı çalışıyor, tetik otonom değil operatörün kendi tıklaması ve iç defter tek gerçek olarak kalıyor.

### C9 [şiddet 3 · icra-entegrasyon] Çıkış kararları aynaya HİÇ iletilmiyor: time_stop/regime_flip/erken-itlaf çıkışı kalıcı 'motor yetimi' üretiyor ve sembolü kalıcı olarak karar dışı bırakıyor

- **Yer:** meridian/loop.py:435-439, 554-555, 623-625, 1311-1322; meridian/adapters/alpaca.py:479-499
- **Sınıf:** iki-motor ayrışması / hunide kendi kendine açlık

**Kanıt:**

İç çıkış yolu: `if dec.exit_now: meta['pending_exits'][t] = dec.exit_reason` (loop.py:554-555) → ertesi açılışta `b.close_position(t, per[t].loc[d,'open'], reason, dstr)` (loop.py:437). Bu yolda TEK BİR ayna çağrısı yok; repoda pozisyon kapatan tek Alpaca yolu `close_all` (alpaca.py:479, operatör jetonlu) — `grep -rn "'sell'\|v2/positions"` sat/pozisyon-kapatma yolu bulmuyor. strategy.py:1109-1113 bu çıkışları üretiyor (`time_stop`, `regime_flip`; +early_kill_pivot/giveback). Ayna bracket'ı yalnız TP/SL taşıdığı için pozisyon AÇIK kalır → reconcile onu `engine_orphans` sayar ve HER TURDA ALARM basar (loop.py:1317-1321) → dahası bu küme `_mirror_busy`ye giriyor (loop.py:623-625) ve P3 o sembol için bir daha karar üretmiyor (loop.py:695-696 hard NO_GO). Mevcut defterde time_stop çıkışların 33/95'i (AUDIT-2026-07-31-TRADING §ED-1) — yani sınıf marjinal değil. Henüz canlıda materyalize olmadı çünkü E1 öncesi ayna 95/95 reddediliyordu; E1 ile dolumlar gerçekleşince ilk zaman-stopunda ateşlenir. NOT: scale_out'un ayna boşluğu bilinçli ve belgeli (loop.py:1301-1303) ama çıkış boşluğu hiçbir yerde belgeli değil — reconcile yorumu yalnız GİRİŞ tarafı sebepleri sayıyor ('breaker day, slot race, gap guard').

**Öneri:** İç çıkış kararı verildiğinde aynada aynı sembolün bracket'ını kapat (parent iptal + pozisyon kapatma emri, sahiplik önekiyle sınırlı) ya da en azından çıkış-kaynaklı yetimi ayrı sınıflandırıp `_mirror_busy`den düşür.

**Adversarial doğrulama:** İDDİA KOD SEVİYESİNDE DOĞRULANDI. (1) Çıkış üretimi: strategy.py:1093/1107/1111/1113 `early_kill_pivot`, `giveback`, `time_stop`, `regime_flip` için `ManageDecision(True, ...)` döner; loop.py:554-555 bunu yalnız `meta["pending_exits"][t]`ye yazar; loop.py:435-439 ertesi açılışta SADECE `b.close_position(...)` + `_persist_trade(...)` çağırır — bu iki blokta ve intraday dokunuş çıkışında (loop.py:522-524) tek bir `alpaca.*` çağrısı yoktur. (2) Ayna tarafında çıkış yolu gerçekten YOK: prodüksiyon çağıran taraması (`close_position`/`_touch_exit` grep'i) canlı hatta yalnız loop.py:437 ve 524'ü, alpaca.py'de ise pozisyon kapatan tek uç olarak `close_all` (alpaca.py:479, `CLOSE_ALL_CONFIRM` jetonlu, üstelik yabancı sembolleri de düzleştirdiği için otomasyona kapalı) verir; `cancel_open_entries` (alpaca.py:354) ve `mirror_stream._maybe_cancel_entries` (mirror_stream.py:315-327) yalnız DOLMAMIŞ giriş emirlerini iptal eder ve dolmuş parent'a bilerek dokunmaz; `intraday_cycle` emir göndermez (modül başlığı: "Emir GÖNDERMEZ"); `_llm_veto_filter`ın `cancel_order`ı (loop.py:196) `filled_qty<=0` şartıyla yalnız giriş öncesidir. (3) Sonuç zinciri de iddia edildiği gibi: reconcile `local`i o turun SONUNDAKİ `b.positions`tan alır (loop.py:955-958), çıkış açılışta uygulandığı için sembol artık `local`de değildir; alpaca.py:255 pozisyonu hâlâ döndürür, `engine_syms` P- önekli dolmuş parent'ı görür (loop.py:1314-1316) → `orphans` (1317-1322) HER TURDA `ALARM_MIRROR_DRIFT` basar ve bu küme loop.py:623-625'te `_mirror_busy`ye girip loop.py:691-696'da `severity: hard` NO_GO'ya dönüşür. (4) Beyan asimetrisi de doğru: `scaled_out` boşluğu loop.py:1301-1303'te açıkça "KNOWN, intentional gap" diye yazılıdır, çıkış boşluğu için kodda hiçbir not yoktur ve 1311-1313'teki yorum sebep olarak yalnız giriş tarafını sayar ("breaker day, slot race, gap guard"); hiçbir test çıkışın aynaya iletildiğini iddia etmez. ŞİDDETİ 4'TEN 3'E ÇEKEN ÜÇ NÜANS: (a) "kalıcı" tam doğru değil — ayna bracket'ının TP/SL bacakları canlı kaldığı için yetim pozisyon fiyat bir bacağa değince kendiliğinden kapanır ve stop koruması sürer (çıplak risk yok); (b) `all_orders = orders(status="all", limit=200, direction=desc)` (loop.py:1233) penceresinden düşen eski dolum sembolü `engine_orphans`tan `external`e (alarmsız, `_mirror_busy` dışı — loop.py:1323-1324) kaydırır, yani hem "HER TURDA ALARM" hem "kalıcı NO_GO" sınırlıdır (bu hâli daha sessiz, ama daha az gürültülü); (c) yetim sınıfının OTOMATİK onarımı AUDIT-2026-07-18.md:10'da "bilinçli ertelenen" olarak kayıtlıdır ("motor yetimi tespit+alarm VAR; Alpaca bracket'ını otomatik iptal etmek gerçek emir politikası gerektirir — operatör 'Tüm pozisyonları kapat' ile manuel; L0'da bilinçli tercih") — yani sınıf tümüyle beyansız değil, ama o beyan çıkış-kaynaklı ve yüksek frekanslı bu yolu adlandırmıyor. Buna karşılık ağırlaştırıcılar gerçek: defterin 33/95 çıkışı time_stop (AUDIT-2026-07-31-TRADING §4/§ED-1) ve `regime_flip` `regime_ok` düştüğü turda AÇIK TÜM pozisyonlar için aynı anda ateşler (loop.py:545-555) — yani tek bir rejim dönüşünde portföyün tamamı yetim kalabilir; ayrıca hesap kâğıt (alpaca.py `_paper_base` sert kilidi) olduğu için parasal zarar değil, aynanın icra-sadakati ölçümünün ve karar hattının bozulması söz konusudur.

### C10 [şiddet 3 · icra-entegrasyon] TIF=day gün boyu duran limit emri, iç motorun 'yalnız açılışta dolar' modeliyle uyuşmuyor — kaçan işlem aynada dolabilir

- **Yer:** meridian/broker.py:40-44, 333-341, 57; meridian/adapters/alpaca.py:304-319, 430-433
- **Sınıf:** canlı-vs-simülasyon ayrışması (icra modeli)

**Kanıt:**

İç motor: `if next_open > _limit: return _red(EV_MISSED_LIMIT, ...)` — dolum penceresi TEK AN (açılış). Ayna: `time_in_force = 'day'` (broker.ENTRY_TIF) ile limit/stop_limit emri → açılış limitin üstündeyse emir DÜŞMEZ, seans boyunca durur ve fiyat limite geri gelirse dolar. Yasa metni yalnız tek yönü garanti ediyor ('hiçbir motor limitin ÜSTÜNDE dolum yazamaz', broker.py:40) — limitin ALTINDA gün içi dolum simetrisi hiç ele alınmamış. Frekans önemsiz değil: ROADMAP §1 E4 okuması 'giriş-gap p90 +100bps → %1 limit tavanı bağlayıcı olacak'. Sonuç: iç defter `entry_missed_limit` yazarken ayna pozisyona giriyor; o pozisyon `local`de olmadığı için trail senkronu da almaz (loop.py:1330) ve orijinal stop/TP ile motorsuz kalır. TIF seçimi kartta yalnız 'GTC bayat tetik taşır' gerekçesiyle DAY'e çekilmiş; aynı seans içi bekleme sorusu kayıtlarda hiç geçmiyor.

**Öneri:** Ya emri açılışa kilitle (tif=opg / IOC benzeri açılış-limit semantiği) ya da iç motorun modelini gün-içi limit dolumuna genişlet; ara çözüm olarak açılış sonrası dolmamış giriş emirlerini süpürerek iptal et.

**Adversarial doğrulama:** Bulgu ÇÜRÜTÜLEMEDİ; çürütme denemelerinin her biri iddiayı güçlendirdi. (1) MEKANİZMA GERÇEK: iç motorun dolum penceresi tek bir andır — `PaperBroker.fill_entry` yalnız `next_open` skalerini alır ve `if next_open > _limit: return _red(EV_MISSED_LIMIT, ...)` (broker.py:333-341); ayna tarafında ise `submit_plan` → `submit_bracket` gövdesi `time_in_force = ENTRY_TIF = "day"` (broker.py:57; alpaca.py:308-319) ile `limit` ya da `stop_limit` emri gönderir. Canlı yolda mod pratikte `marketable_limit`tir: `entry_trigger` sinyal barının kapanışıdır ve `ref_price` de aynı kapanış olduğu için `gap = rp >= t` daima doğrudur (broker.py:145-151), yani gönderilen şey seans boyunca duran DÜZ BİR LİMİT EMRİdir. Açılış limitin üstündeyse iç motor `entry_missed_limit` yazar, aynadaki emir DÜŞMEZ; fiyat gün içinde limite dönerse dolar. (2) BAŞKA YERDE KABLOLU DEĞİL: `entry_missed_limit` dalında loop.py:488-505 yalnız defter satırı + `obs.warn` yazar, hiçbir iptal çağrısı yoktur; plan `meta["armed"] = carried` ile silahlı kümeden düşer (loop.py:506) ve dolayısıyla uzlaştırmanın (1.2) "ölü emirli silahlı planı ayıkla" bloğuna da hiç uğramaz (loop.py:1254-1264). Depodaki TÜM otomatik `cancel_order`/`cancel_open_entries` çağıranları taradım: LLM vetosu (loop.py:196), varsayılan KAPALI WS-kopuş devre kesicisi (mirror_stream.py:315-327, `MERIDIAN_WS_DISCONNECT_CANCEL_ENTRIES!=1` ise erken döner) ve panodaki elle düğme (api.py:1325-1331) — hiçbiri bu dala bağlı değil. (3) BİLİNÇLİ-BEYANLI DEĞİL: yasanın metni gerçekten tek yönlüdür ("hiçbir motor limitin ÜSTÜNDE dolum yazamaz … sınır brokerdadır", broker.py:40-41) ve `fill_entry` docstring'i kapsam farklarını ismen beyan ederken (`gap_at_submit=None` = "bu motorda gönderim anı YOK", broker.py:301-304) gün-içi bekleme penceresinden hiç söz etmez; kart EXE-2026-001'de de TIF gerekçesi yalnız "GTC ertesi güne bayat tetik taşır"dır (satır 13), aynı seans içi bekleme sorusu yoktur. Üstelik deponun KENDİ yazılı kuralı bunu yasaklıyor: "AYNA TUTARLILIK YASASI: plan Alpaca'ya gönderildiyse önce ORADA iptal doğrulanır; doğrulanamazsa plan düşürülMEZ — iki defterin ayrışması vetodan büyük risktir" (loop.py:180-181) — bu yasa LLM veto dalına uygulanmış, `entry_missed_limit` dalına uygulanmamıştır. (4) TEST-KORUMALI DEĞİL: test_icra_gercekligi_v141.py yasanın tek-yerde-yaşadığını (AST), üç gap yolunu ve defter muhasebesini çivilerken dolum-PENCERESİ simetrisine dair tek bir iddia içermez; test_mutborc_broker_entry_law_v148.py yalnız `ENTRY_TIF == "day"` sabitini doğrular. (5) FREKANS ÖNEMSİZ DEĞİL: ölçüm raporlarında `entry_missed_limit` iç motorun BASKIN ret sınıfıdır (karne: 26/36; vols: 13/17, 14/21) — gap-up'ın gün içinde geri çekildiği alt küme bunun anlamlı bir parçasıdır. SONUÇ/SONUÇ ZİNCİRİ de doğrulandı: ayna dolarsa sembol `local`de olmadığı için trail senkronu (yalnız `local` üzerinde döner, loop.py:1330) hiç dokunmaz, pozisyon yalnız gönderim anındaki statik bracket TP/SL bacaklarıyla kalır; zaman-stopu/rejim-çıkışı/scale-out uygulanmaz; E2 dolum defterine iç defterin ALMADIĞI bir işlemin slipaj satırı yazılır (`_patch_entry_slippage`, loop.py:1164-1185) ve ayna özkaynağı üzerinden boyutlandırma kirlenir. Şiddeti 4 değil 3 yapan tek şey, olayın SESSİZ olmaması: dolan yetim `engine_orphans` olarak ALARM_MIRROR_DRIFT ("motor yetimi") üretir (loop.py:1311-1322), sembol `_mirror_busy`ye girip yeniden planlanmayı bloklar (loop.py:624-625) ve `analytics.entry_execution_summary`nin "mutabakat" bloğu ayrışmayı plan_id bazında sayar; ayrıca hesap bugün kağıt hesaptır (sermaye kaybı değil ölçüm/defter bütünlüğü zararı). Yine de bu, tespit-sonrası bir alarmdır, önleyici bir kelepçe değildir ve Faz-6 gerçek emir kapısına giden yolda aynı asimetri sermaye ilgili hale gelir.

### C11 [şiddet 3 · karar-cekirdegi] E1 giriş-limit yasası yarım kablolu: `atr` yalnız CANLI motorda geçiliyor, replay/gölge geçmiyor → iki motor farklı dolum sınırıyla koşuyor

- **Yer:** meridian/backtest.py:200-202 · meridian/loop.py:475-477 · meridian/shadow_lifecycle.py:259 · meridian/broker.py:334,105-119
- **Sınıf:** canlı-vs-replay ayrışma

**Kanıt:**

loop.py:475 `b.fill_entry(..., atr=_lw.get("atr"), gap_at_submit=...)`; backtest.py:200 `broker.fill_entry(plan, per[t].loc[d,"open"], ..., size_mult=..., adv=..., pivot=armed_pivots.get(t,0.0))` — atr YOK; shadow_lifecycle.py:259 aynı şekilde yok. broker.entry_limit_price: `off = min(mult*a, pct_off) if a > 0 else pct_off` → atr=None iken limit DAİMA %1 tavanı, atr verilince min(0.5·ATR14, %1). Replay `sig.atr`ye sahip (armed_pivots yan haritasının birebir emsali var) ama taşımıyor. ÖLÇÜM (60 sembol, 322.635 gerçek bar, state/bars/*.csv + ind.atr): barların %37,9'unda 0,5·ATR < %1·fiyat, yani o barlarda canlı limit replay'inkinden SIKI (ATR/close p25=0,0176 → 88bps vs 100bps). Somut vaka: tetik 100, ATR14=1,2 → canlı limit 100,60 / replay limit 101,00; açılış 100,80'de replay DOLDURUR, canlı `entry_missed_limit` yazıp geçer. ROADMAP §1 zaten "giriş-gap p90 +100bps → %1 limit tavanı bağlayıcı olacak" diyor, yani sınır marjinal değil. Test tarafı boşluğu doğruluyor: test_icra_gercekligi_v141 broker fonksiyonlarını atr=1.0 ile TEKİL çağırıyor, hiçbir test replay'in atr geçirdiğini sınamıyor.

**Öneri:** backtest.replay ve shadow_lifecycle, `armed_pivots` deseninin aynısıyla bir `armed_atr` yan haritası taşıyıp `fill_entry(..., atr=...)` geçirsin; iki motorun limitinin eşitliğini çivileyen bir diferansiyel test eklensin.

**Adversarial doğrulama:** ÇÜRÜTÜLEMEDİ — iddia birebir doğrulandı. Tüm meridian paketinde `fill_entry(..., atr=...)` geçiren TEK çağıran vardır: `grep -n "atr=" meridian/*.py` yalnız loop.py:476'yı (`atr=_lw.get("atr")`) döndürüyor; backtest.py:200-202 (`broker.fill_entry(plan, per[t].loc[d,"open"], ..., size_mult=..., adv=..., pivot=armed_pivots.get(t,0.0))`) ve shadow_lifecycle.py:259-260 (`b.fill_entry(plan, bar["open"], date, eq_now, size_mult=..., adv=..., pivot=float(entry.get("pivot") or 0.0))`) argümanı hiç vermez, `fill_entry` imzasında (broker.py:279-282) varsayılan `atr: float | None = None`'dır ve fonksiyon ATR'yi BAŞKA hiçbir yerden (plan sözlüğünden de) okumaz — dolayısıyla broker.py:105-119'daki `off = min(mult*a, pct_off) if a > 0 else pct_off` replay/gölgede DAİMA %1 tavanına, canlıda min(0,5·ATR14, %1)'e düşer ve broker.py:334-341'deki `next_open > _limit → EV_MISSED_LIMIT` kapısı iki motorda FARKLI eşikle koşar. Kaçırılmış bağlam yok: (a) beyan yok — broker.py:296-306 docstring'i `gap_at_submit=None`'ı açıkça "replay'de gönderim anı YOK, BEYAN EDİLMİŞ kapsam farkı" diye ilan ederken `atr` için yalnız "None = ölçülemedi (uydurma ATR yok)" semantiği verir; oysa replay'de ATR ÖLÇÜLMÜŞTÜR (`sig.as_row()["atr"]`, backtest.py:290; strategy.py her EntrySignal'e `atr=float(a)` yazıyor) ve `armed_pivots` (backtest.py:161,357) ile shadow'un `{"plan":..., "pivot":...}` yan haritası (shadow_lifecycle.py:218-220,260) taşımanın birebir emsalini zaten kurmuştur — yani "ölçülemedi" dalı ölçülmüş bir değere yanlışlıkla uygulanıyor; (b) kayıt yok — test_differential_v60.py'nin beyan listeleri (LIVE_ONLY_PLAN_FIELDS / LIVE_ONLY_GATE_CHECKS, satır 35-60) yalnız plan alanları ve kapı kontrollerini kapsar, icra girdilerini hiç kıyaslamaz, backtest.py:203-209'daki "replay canlının BASİTLEŞTİRİLMİŞ modelidir" notu ise sayılı üç kaleme (carry-armed, keşif kipi, cf_backfill slot) bağlıdır, limit yasasına değil — üstelik broker.py başlığı `counterfactual.advance`in bilerek gevşek kaldığını AYRICA beyan eder, yani bu depoda kasıtlı ayrışma beyan EDİLİR, burada edilmemiştir ve modül başlığının kendi hükmü "TEK YASA, İKİ MOTOR ... hiçbir motor limitin ÜSTÜNDE dolum yazamaz"dır; (c) test yok — test_icra_gercekligi_v141.py broker fonksiyonlarını yalnız `atr=1.0` ile TEKİL çağırır (satır 65-66, 96-98, 136, 148, 156) ve dosyada replay/backtest yolunu koşan tek satır bile yoktur (tek "replay" geçişi satır 142'de gap_at_submit beyanıdır). Denetçinin ölçümünü bağımsız yeniden ürettim (60 sembol, 322.635 bar, state/bars/*.csv): barların **%39,3**'ünde 0,5·ATR < %1·fiyat (ATR/close p25=0,0172, p50=0,0223) — denetçinin %37,9/0,0176 rakamıyla uyumlu, sınır marjinal değil ve ROADMAP.md:35 "giriş-gap p90 +100bps → %1 limit tavanı bağlayıcı olacak" der, yani gap dağılımı tam da iki limitin arasındaki bandı doldurur. Şiddeti 4 değil **3** tutuyorum çünkü ayrışmanın yönü canlıyı KORUYUCU taraftadır (canlı daha sıkı; sermaye kaybettiren bir dolum hatası değil): zarar ölçüm bütünlüğündedir — replay ve gölge, canlının reddedeceği girişleri doldurup dolum oranını şişirir, `entry_missed_limit` sayısını sıfırlar, böylece parametre araması, cf_fidelity ve gölge→canlı terfi kararları sistematik olarak iyimser bir icra modeli üzerinden verilir; düzeltme dar ve mekaniktir (armed_pivots/pivot desenine paralel bir `armed_atr` yan haritası + iki çağrıya `atr=` eklemek).

### C12 [şiddet 3 · karar-cekirdegi] Y3 portföy tavanları (`portfolio.sector_cap`/`heat_cap`) bounds.yaml'da aranabilir ama HİÇBİR üretim çağıranı gereken portföy alanlarını göndermiyor — knob açılsa bile yapısal olarak bağlayamaz

- **Yer:** meridian/guard.py:399-429 · meridian/loop.py:640-650 · meridian/backtest.py:311-315 · meridian/cf_backfill.py:79-80 · meridian/shadow_variants.py:242-249
- **Sınıf:** ölü/kablolanmamış knob + yanlış-hipotez-uzayı

**Kanıt:**

guard._y3_portfolio_caps `portfolio["equity"]`, `portfolio["sector_notional"]`, `portfolio["heat_pct"]`, `plan["notional"]`, `plan["risk_dollars"]` okur. Beş üretim çağıranın portföy sözlüğü TAM olarak şu anahtarları taşıyor: open_positions, sector_counts, day_pnl_pct, open_risk_r, max_corr (loop.py:640; backtest.py:311; cf_backfill.py:79 `flat_pf`; shadow_variants.py:242; mutation.py:169) — beşinde de equity/sector_notional/heat_pct YOK, plan sözlüklerinde notional/risk_dollars YOK. Sonuç: cap_pct>0 iken `nav=0` → soft "NAV ölçülemedi — tavan uygulanmadı"; heat_cap>0 iken `isi=None` → soft "ısı ölçülemedi". Yani knob'un ulaşabildiği TEK etki her adaya bir REVIEW bayrağı eklemek; amaçlanan sert tavan hiçbir yapılandırmada ateşleyemez. Bu, guard.validate_change'in `regime.*@regime` için ADIYLA reddettiği sınıfın aynısı ("aday incumbent'la birebir replay olur, kapı reddeder, deftere SAHTE kalıcı çıkmaz sokak yazılır") ama korumasız. tests/test_mutborc_guard_iy3_portfolio_caps_v148.py bu alanları ELDE kurup guard'ı doğrular (pf = {"equity":100_000,"sector_notional":{...},"heat_pct":20.0}) — üreten tarafı sınayan test yok, boşluk tam oradan kaçmış.

**Öneri:** Ya loop/backtest/cf/shadow portföy sözlüğüne equity+sector_notional+heat_pct (ve plana notional/risk_dollars) eklensin ve üretici↔tüketici sözleşmesini çivileyen bir entegrasyon testi yazılsın, ya da iki satır bounds.yaml'dan çıkarılıp "kablolanmamış" olarak emekli edilsin.

**Adversarial doğrulama:** İDDİA DOĞRULANDI (çürütülemedi) ve mekanizması iddia edilenden DAHA sert çıktı. (1) Kablo gerçekten yok: `guard.classify_gate` (meridian/guard.py:256-354) portföy sözlüğünü HİÇ zenginleştirmeden `_y3_portfolio_caps(plan, portfolio, params, _chk)`e geçiriyor (guard.py:354), yani ara katman yok. `classify_gate`in ÜRETİMDEKİ tüm çağıranları tam olarak beş tane (loop.py:666, backtest.py:323, cf_backfill.py:94, mutation.py:171, shadow_variants.py:251; ayrıca `check_trade` guard.py:238 — onun da üretim çağıranı YOK, yalnız testler) ve beşinin de portföy sözlüğü birebir {open_positions, sector_counts, day_pnl_pct, open_risk_r, max_corr} anahtarlarını taşıyor; `equity`/`sector_notional`/`heat_pct` beşinde de YOK, plan sözlüklerinde `notional`/`risk_dollars` YOK. `heat_pct` dizgisinin repo genelinde meridian/ altında guard.py DIŞINDA tek bir ÜRETİCİSİ bile yok (`analytics.portfolio_heat` guard.py:2360'ta var ama yalnız api.py:1972'de panoya gidiyor, kapıya değil) — yani bu bir bağlam kaçırma değil, ölü uç. Bilinçli-beyan savunması da tutmuyor: shadow_variants'ta `_book_state` zaten `bk["equity"]`yi üretiyor (shadow_variants.py:181) ama 242'deki portföy sözlüğüne konmuyor — veri bir anahtar uzakta duruyor. (2) AMPİRİK KANIT (üretim-şekilli sözlükle koştum): params={} → GO, y3 satırı yok; params={sector_cap:25, heat_cap:6} → yine GO; hatta sector_cap=0.1/heat_cap=0.1 (mümkün en sıkı tavan) → yine GO. Yani knob'un ulaşabildiği etki denetçinin dediği "her adaya bir REVIEW bayrağı" bile DEĞİL: `_chk(..., failed=False, ...)` çağrıldığı için satır `passed=True, value=None, note=None` olarak kaydediliyor ve `soft` listesine HİÇBİR ŞEY eklenmiyor — hüküm birebir aynı kalıyor. Bu, "yapısal olarak bağlayamaz" iddiasını ZAYIFLATMAZ, GÜÇLENDİRİR: aday replay incumbent'la gerçekten bit-bit özdeş çıkar. (3) Sonuç zinciri de gerçek: knob state/bounds.yaml:71-72'de (sector_cap min0/max30/step5 → 25 adım üstünde; heat_cap min0/max8/step1 → 6 adım üstünde) ve hermes önerilerini `config.bounds()`tan çekiyor (hermes.py:149), `validate_change`in no-op kontrolü ise DEĞER no-op'u (`old==new`, guard.py:190-192), DAVRANIŞ no-op'u değil — dolayısıyla `portfolio.sector_cap: 0→25` önerisi tüm şekil kapılarını geçer, prescreen sıfır delta ölçer, `rejected_by_backtest` deftere yazılır ve guard.py:194-198'deki "already tried and failed" kuralı meşru bir risk kontrolünü KALICI olarak kara listeye alır. Bu, guard.py:134-140'ın `regime.*@regime` için ADIYLA reddettiği sınıfın ("candidate replays byte-identical, gate rejects, false permanent dead end lands in the ledger") aynısıdır ve burada korumasızdır. (4) Test boşluğu da doğrulandı: hem tests/test_mutborc_guard_iy3_portfolio_caps_v148.py:107 hem de sözde beyan testi tests/test_hafta3b_v125.py:453-459 alanları ELDE kuruyor (`pf={"equity":100_000,"sector_notional":{...},"heat_pct":20.0}`, plan'a `notional`/`risk_dollars` ekli) ve o kurulumla NO_GO'yu doğruluyor — üreten tarafı sınayan tek test yok, boşluk tam oradan kaçmış. EK BULGU (denetçinin kaçırdığı, aynı kökten): guard'ın kendi beyanı "NAV ölçülemedi → bu SESSİZ KALMAZ; açık bir knob'un ölçüm yapamadığı tur 'tavan tuttu' diye okunamaz" diyor, ama `_chk` `why` dizgisini yalnız `failed=True` iken saklıyor (guard.py:285) — `failed=False` verildiği için satır karar ağacına `passed=True, note=None` diye, yani tam olarak "tavan tuttu" görünümünde düşüyor; koruyucu test (test_Y3_isi_tavani_olculemezse..._SESSIZ_KALMAZ, test_hafta3b_v125.py:463-472) yalnız satırın VAR olduğunu assert ediyor, mesajın korunduğunu değil — test yeşilken beyan ihlal ediliyor. ŞİDDET 3 (4 değil): bugün canlı zarar yok, çünkü canlı stratejide iki knob da 0 ve bu default-off durumu ayrı bir çiviyle (test_hafta3b_v125.py:446-448) ölçülüyor; hasar latent — arama knob'u önerdiği gün sahte kalıcı çıkmaz sokak, ve bir operatör "sektör/ısı tavanını açtım" sanıp hiçbir koruma almadığı hâlde panoda (api.py:1071 `enabled: true`) açık görmesi.

### C13 [şiddet 3 · karar-cekirdegi] `exit.early_kill_pivot` replay/gölgede ateşliyor, canlıda YAPISAL olarak ateşleyemez — kapıdan geçip strategy.yaml'a inerse canlıda sessiz no-op olur (sahte-terfi yolu)

- **Yer:** meridian/loop.py:475-477,550-552 · meridian/strategy.py:255-272,1093 · meridian/backtest.py:200-202,249-252 · state/bounds.yaml:53
- **Sınıf:** canlı-vs-replay ayrışma / sahte-terfi

**Kanıt:**

İKİ bağımsız kopukluk: (a) loop.py:475 `fill_entry`i `pivot=` argümanı OLMADAN çağırır → `Position.pivot` daima 0.0; (b) loop.py:550 `manage_position`a geçen sözlük `{"entry","stop","trail_stop","r_per_share"}` — `pivot` anahtarı YOK. early_kill_pivot_exit `position.get("pivot")` None → `return False`. Replay tarafı ikisini de taşır (backtest.py:202 `pivot=armed_pivots.get(t,0.0)`, backtest.py:250 sözlükte `"pivot": pos.pivot`), shadow_lifecycle de taşır. bounds.yaml:53 `exit.early_kill_pivot {min:0,max:1}` → Hermes önerebilir, prescreen/replay GERÇEK bir ΔS ölçer, kapı kabul eder, strategy.yaml'a iner ve canlı motor onu OKUR ama uygulayamaz. Kod bu farkı BEYAN ediyor ("canlı planlar bu alanı taşımıyor... bayrak açılsa bile canlıda ateşlemez") — beyan edilmeyen şey terfi yolunda hiçbir kapının bunu durdurmaması: validate_change `regime.*@regime` için tam bu sınıfı reddederken bu knob için sessiz.

**Öneri:** Ya loop `fill_entry(pivot=sig.pivot)` + manage_position sözlüğüne `pivot` eklensin (iki satır), ya da canlı motorun okuyamadığı knob'ları terfi kapısında reddeden bir kontrol (validate_change'in regime.* dalının emsali) konsun.

**Adversarial doğrulama:** ÇÜRÜTÜLEMEDİ — iddianın her iki kopukluğu da kodda birebir duruyor ve terfi yolu gerçekten kapısız. (a) `meridian/loop.py:475-477` `fill_entry`i `pivot=` argümanı OLMADAN çağırır; `broker.py:281` imzasında `pivot: float = 0.0` ve `broker.py:388` `pivot=float(pivot or 0.0)` → canlı `Position.pivot` daima 0.0 (durum geri yüklemesi de kurtarmaz: `loop.py:120` `Position(**p)` sıfırı aynen geri yükler). (b) `loop.py:550-552`'nin `manage_position`a verdiği sözlük yalnız `{entry, stop, trail_stop, r_per_share}` taşır → `strategy.py:1093` → `early_kill_pivot_exit` (`strategy.py:260-262`) `position.get("pivot")` None okur ve `return False`. Replay iki kanalı da taşır (`backtest.py:357` `armed_pivots[sig.ticker]=sig.pivot` → `backtest.py:200-202` `pivot=armed_pivots.get(t,0.0)` → `backtest.py:249-252` sözlükte `"pivot": pos.pivot`), gölge-v2 de taşır (`shadow_lifecycle.py:260,294`). Kopukluk BİLİNÇLİ ve dört yerde BEYANLI (`strategy.py:250-254`, `broker.py:232-238`, `backtest.py:151-160` "dikiş, 3b'ye devir", `shadow_lifecycle.py:86-87`) ve `tests/test_g3b_cikis_v124.py:195-202` bugünkü canlı yolun ateşlemediğini ÇİVİLİYOR — ama denetçinin asıl iddiası bu değil: beyan edilmeyen şey terfi yolunun açık olması, ve o da doğrulandı. `guard.validate_change` düz (flat) öneride hiçbir canlı-kablo kontrolü yapmaz; `old = current_params.get(base)` = None olduğu için no-op dalı (`guard.py:191`, `old is not None` şartı) atlanır, `exit.early_kill_pivot` `state/bounds.yaml:53`'te tanımlı olduğu için `bounds` kontrolü de geçer — oysa AYNI fonksiyon `guard.py:134-140`'ta tam bu sınıfı (`regime.*@regime` yapısal etkisizliği, denetim #33) ADIYLA reddeder; hatta `@regime` varyantı `guard.py:131`'deki "base flat params'ta yok" kuralıyla kapalıdır, kapalı olmayan tek yol DÜZ öneridir. Sonra `reflect.py:765` `versioning.bump` → `versioning.py:53` `child["params"][variable]=new` düz params'a yazar, `reflect.py:772` ölçümü `backtest.walk_forward` (yani pivotun KABLOLU olduğu replay motoru) üzerinden yapar, `reflect.py:862` `versioning.commit` strategy.yaml'a iner ve canlı `config.resolve_params` → `manage_position` onu OKUR ama uygulayamaz. Yol teorik de değil: `reflect.py:1214` arama uzayı `list(bounds.keys())`'in tamamıdır, `_ucb_rank` hiç denenmemiş düğmeleri +inf ile ÖNE alır, `reflect.py:1224` `cur = params.get(var, lo)` = 0 ve `+1` yönü doğrudan `exit.early_kill_pivot=1` sondası üretir; `hermes.propose_virgin_knob` de bakir düğme listesinden aynı knob'a uzanır; ROADMAP.md:131 ve `research/cards/EDG-2026-003` bu düğmeyi (P=0,606, gölge-v2 V3 birikimi) HÂLÂ AKTİF terfi hattında tutuyor. Yakalayabilecek tek yapısal kontrol olan `test_differential_v60.test_plan_dicts_have_the_same_field_set` de bunu göremez, çünkü pivot bilerek plan sözlüğünün DIŞINDA (yan harita) taşınıyor — yani parite testini geçmek için seçilen tasarım, tam olarak bu ayrışmayı testin kör noktasına yerleştiriyor. Şiddet 4 değil 3: bugün knob `state/strategy.yaml`'da YOK, canlı davranış değişmemiş, ship gerçekleşse bile canlı etki sessiz NO-OP olduğu için doğrudan sermaye riski yok; zarar epistemiktir ama depo yasasının merkezindedir ("backtest sayıları yalan olur", §4) — karne/kalibrasyon/bandit ölçülemeyen bir ΔS'i kazanç sayar, aylık değişiklik kotası yanar ve `rollback.evaluate_outcomes` gerçekleşmeyen tahmini o değişkenin suçu gibi deftere yazar.

### C14 [şiddet 3 · ogrenme-katmani] Bileşik öneri kuyruğu hiç kapanmıyor: 'measured' yazan tek bir üretim yolu yok (öner→ölç→öğren halkası açık)

- **Yer:** meridian/hermes_composite.py:226,239 (mark 'measuring') · meridian/prescreen.py:388 · meridian/ledgers.py:170-179
- **Sınıf:** ölü/kablolanmamış mekanizma (yarım kablo)

**Kanıt:**

spawn_pending satırı `mark(..., "measuring", pid=...)` yapıp prescreen'i AYRI sürece atıyor; prescreen sonucu YALNIZ `--workdir`e yazıyor (`(workdir / SONUC_DOSYA).write_text(...)`, prescreen.py:388) ve o dosyayı depoda okuyan hiç kimse yok (`grep -rn 'prescreen-|SONUC_DOSYA|sonuc.json' meridian/` → yalnız yazan taraf). Depo genelinde `mark(..., "measured")` çağrısı SADECE testlerde var (tests/test_nous_eval_v131.py:531). Yani `queue_status.n_olculen` yapısal olarak hep 0, `son_olcum` hep None, satırlar süreç ölse bile 'measuring' kalıyor; nous_eval._akibet o satır için sonsuza dek 'ÖLÇÜLÜYOR (prescreen süreci koşuyor)' döndürür (nous_eval.py:457). Buna rağmen haftalık bütçe HARCANIYOR (`_budget_take` spawn'dan önce). ledgers.py:170-173 sözleşmesi tam tersini şart koşuyor: 'kuyruğun tam-döngülü olması (pending → measuring → measured) yalnız o alanla denetlenebilir; eksikse öner→ölç→öğren halkasının koptuğu görünmez kalır.'

**Öneri:** prescreen'e kuyruk-kimliği taşıyan bir geri-yazım ekleyin (ör. `--queue-id` ile bitişte `hermes_composite.mark(id, 'measured', result=özet)`), ayrıca gece kancasında 'measuring' satırların pid'ini yoklayıp ölmüş süreci `measure_failed` olarak damgalayın.

**Adversarial doğrulama:** Çürütme denendi ve BAŞARISIZ: bulgu gerçek, üstelik denetçinin yazdığından bir kat daha sert. (1) Üretim yolunda `mark(..., "measured")` YOK — repo genelinde `"measured"` durum sabitini yazan tek çağrı `tests/test_nous_eval_v131.py:531`; okuyan taraf ise üç yerde (`hermes_composite.py:122` `n_olculen`, `nous_eval.py:454` ÖLÇÜLDÜ dalı, `hermes.py:719` kanıt paketi `measured`), yani yapısal olarak hep 0/None. (2) Devir "prescreen sürecine bırakıldı" savunması İMKÂNSIZ: `hermes_composite.spawn_pending` alt sürece yalnız `--composite` ve `--workdir` geçiyor (satır 222-223); `prescreen.main` argümanları `--candidates/--composite/--workdir/--resume/--live-state` ile sınırlı, KUYRUK KİMLİĞİNİ hiç almıyor, `hermes_composite`'i import etmiyor ve sonucu yalnız `(workdir / SONUC_DOSYA).write_text(...)` ile /tmp'ye yazıyor (prescreen.py:388) — `grep -rn 'prescreen_sonuc\|tmp/prescreen'` tüm depoda yalnız YAZAN tarafı ve docstring örneklerini buluyor, hasat eden ne bir modül ne bir `ops/*.sh` var. Yani süreç sonucu geri yazamaz; bunu yapacak bilgi ona verilmemiş. (3) Modülün KENDİ beyanı iddiayı doğruluyor ve yerine getirilmiyor: hermes_composite.py:21 "Kuyruğa yazar, bütçeyi sayar, süreci başlatır, **sonucu deftere yazar**" — son yan cümlenin karşılığı olan kod yok; ledgers.py:170-173 sözleşmesi de "pending → measuring → measured ... eksikse öner→ölç→öğren halkasının koptuğu görünmez kalır" diye tam tersini şart koşuyor. Bu bilinçli-beyanlı bir boşluk DEĞİL: `codelaw.DECLARED_SINKS` yalnız `composite_budget.json` için süreli beyan taşıyor (codelaw.py:320), kuyruğun kapanmayan döngüsü için hiçbir yerde (DECLARED_SINKS, MERIDIAN_ENGINEERING_LOG.md, test) "devredildi" kaydı yok; `tests/test_hafta3b_v125.py` yalnız enqueue/bütçe/spawn-komut-biçimini çiviliyor, kapanışı hiç sınamıyor — yani test-korumalı da değil, aksine tek "measured" örneği testin kendi elle attığı damga. (4) İkincil zararlar da doğrulandı: `_budget_take()` spawn'dan ÖNCE çağrılıyor (satır 209) → haftalık 3 yoklama harcanıyor; ölü süreç için pid canlılığı denetleyen bir reaper yok (`grep -rn 'measuring'` → yalnız iki yazan, bir okuyan; watchdog'da bileşik kuyruk kontrolü yok), bu yüzden `nous_eval._akibet` beyne süreç aylar önce ölmüş olsa bile sonsuza dek "ÖLÇÜLÜYOR (prescreen süreci koşuyor)" diyor (nous_eval.py:457) — bu, doğrulanmamış bir olguyu olmuş gibi bildirmesi bakımından uydurma yasağına da değiyor, ve `onceki_akibet` doğrudan prompta girdiği için beyin her hafta aynı öneriyi yeniden üretme döngüsüne kilitleniyor (modülün var oluş sebebinin tam tersi).

### C15 [şiddet 3 · ogrenme-katmani] Sprint kadansının 'taze aday' tabanı çocuk sürecin ilk durum yazımında siliniyor → haftalık disiplin yerine HER GECE sprint

- **Yer:** meridian/sprint_run.py:77-80 (status payload) · meridian/sprint.py:227-240, 348, 363-365
- **Sınıf:** yorum-davranış sürüklenmesi + kadans kaçağı

**Kanıt:**

sprint.start() `n_hyp_at_start` (ve `cfg`) damgasını sprint_status.json'a yazıyor ve yorum bunun neden şart olduğunu söylüyor: 'Damga olmadan `taze = len(hyps) − 0` olurdu ve tetik her gece yanardı — haftalık disiplin sessizce kaybolurdu.' Ama çocuk süreç `status(**kw)` ile dosyayı BİRLEŞTİRMEDEN, sabit bir payload'la EZİYOR (`_write_live_status({"pid","sid","started_at","sbroot","eval_start","cutoff","updated", **kw})`) ve o payload'da `n_hyp_at_start` YOK. Canlı doğrulama: `state/sprint_status.json` anahtarları = ['cutoff','eval_start','loop_closed','n_v1','note','phase','pid','sbroot','search','shipped','sid','started_at','updated'] — damga yok. should_run: `taze = len(hyps) - int(st.get('n_hyp_at_start') or 0)` = 41 − 0 = 41 ≥ SPRINT_MIN_NEW_HYP(5) → `gun < 7` olsa bile tetik yanar. Yani ilk sprintten sonra 22:00-06:00 penceresindeki her poll'de yeni sprint başlar (4 işçilik walk-forward, canlı karar makinesiyle aynı VM'de).

**Öneri:** `_write_live_status` mevcut dosyayı okuyup üzerine yazsın (ya da `n_hyp_at_start`/`cfg` alanlarını payload'a taşısın); testle çivilenmesi gereken invariant 'çocuk yazımından sonra da damga okunabilir' olmalı.

**Adversarial doğrulama:** Bulgu ÇÜRÜTÜLEMEDİ; zincirin her halkası doğrulandı. (1) YAZAR TARAFI: `meridian/sprint.py:238-241` damgayı `store.write_json(STATUS_FILE, {..., "cfg": conf, "n_hyp_at_start": n_hyp})` ile CANLI dosyaya yazıyor ve 227-229'daki yorum şartı açıkça beyan ediyor ("Damga olmadan `taze = len(hyps) − 0` olurdu ve tetik her gece yanardı"). Ama bu yazım `subprocess.Popen`DAN (223) SONRA gelir ve çocuk süreç aynı canlı dosyaya `MERIDIAN_SPRINT_STATUS = str((live / STATUS_FILE).resolve())` (222) ile bağlanır; `sprint_run.py:77-80`'deki `status(**kw)` sabit bir sözlük kurar, `_write_live_status` (22-36) da `json.dump(payload, f)` + `os.replace` ile dosyayı BİRLEŞTİRMEDEN EZER — `store` katmanına hiç uğramaz, hiçbir merge yok. Çocuğun ilk `status()` çağrısı (99, `phase="baseline"`) koşulsuzdur (total==0 dalı da 89'da yazar), yani damga HER sprintte, ilk ilerleme yazımında yok olur. Scratchpad'de izole tekrar ürettim: ebeveyn anahtarları `[cfg, cutoff, eval_start, n_hyp_at_start, phase, pid, sbroot, sid, started_at]` → çocuktan sonra `[cutoff, eval_start, n_v1, phase, pid, progress, sbroot, sid, started_at, total, updated]`; `n_hyp_at_start` False, `cfg` False. Canlı `state/sprint_status.json` de bu imzayı taşıyor (`phase/n_v1/search` = yalnız çocuğun yazdığı anahtarlar, damga yok). (2) OKUYUCU TARAFI: `should_run` (342-365) `st = status()`ten okur ve `status()` (59-105) yalnız `{**st, active, runs, ...}` döner — damgayı başka hiçbir yerden tamamlamaz; `grep -rn "n_hyp_at_start"` tüm depoda SADECE 4 isabet verir (yazar 240, okuyucu 348, ve iki TEST satırı). `taze = len(hyps) - int(st.get("n_hyp_at_start") or 0)` → canlı `state/hypotheses.jsonl` 41 satır → taze = 41 − 0 = 41 ≥ `SPRINT_MIN_NEW_HYP` (5), dolayısıyla 363'teki `gun < 7 AND taze < 5` kısıtı ASLA tutmaz ve tetik "taze_aday_birikimi" ile yanar. (3) CANLI KABLOLAMA GERÇEK: `api.py:169-170` `scheduler.start(poll_seconds=300)` ile daemon'u açıyor, `scheduler.py:956-960` boşta ("current") dalında `_sp.maybe_start(mesgul=...)` çağırıyor ve daemon thread canlıyken `_mesgul=None` olur (959). Tek eşzamanlılık kapısı `st.get("active")`; bu yalnız PARALEL sprinti engeller, RATE'i değil — sprint bitince bir sonraki 300 sn'lik poll'de yenisi başlar, yani 22:00-06:00 penceresinde ART ARDA sprintler. Yan hasar sadece CPU değil: her `start()` canlı state ağacını yeni bir kum havuzuna kopyalayıp `SANDBOX_KEEP=3` ile budadığından (158-174), bir gecelik zincir `_sandbox_runs`ın TEK okuduğu `sprint_runs.jsonl` geçmişini tamamen aynı gecenin 3 koşusuyla değiştirir. (4) TEST/BEKÇİ KORUMASI YOK: `tests/test_ogrenme_otomasyonu_v136.py:438,444` damgayı `store.write_json` ile ELLE dosyaya yazıyor — `start()`→çocuk→`status()` yolu hiç koşturulmuyor, yani damgayı yok eden yazım tamamen test dışı; `watchdog.py:58`'deki `"sprint_cadence": 9*24*3600` ise yalnız "antrenman tamamen durdu" yönünü yakalar, FAZLA sık koşmayı değil. Beyan da yok: kod yorumu haftalık disiplini korumayı taahhüt ediyor, davranış tersini yapıyor.

### C16 [şiddet 3 · ogrenme-katmani] Arka plan yansıması canlı-dışı rejimin kanıtıyla GLOBAL parametre ship edebiliyor — docstring'in emniyet beyanı kodda karşılıksız

- **Yer:** meridian/hermes_runtime.py:58-64, 328-337 · meridian/hermes.py:2644-2669
- **Sınıf:** canlı-vs-replay ayrışması / beyan-davranış çelişkisi

**Kanıt:**

_bg_ready_regime docstring'i: 'Güvenlik: ship yalnız params_by_regime[o rejim]'i değiştirir — canlı davranış rejim dönene dek değişmez.' Kod bunu iki yoldan delmiyor sadece, delebiliyor: (a) reflect_once içinde `search_regime = live_reg if live_reg in VALID_REGIMES and live_reg != 'trend_up' else None` — bg rejimi trend_up ise arama GLOBAL koşar ve global params'a ship eder; (b) `propose_with_llm`/`propose_virgin_knob` global (@'sız) bir değişken önerdiğinde `preg is None` olduğu için sertifika kontrolü (hermes.py:2648-2653) hiç devreye girmez ve global ship yolu açıktır. Canlı state bunu somut yapıyor: regime.json='chop', trades.jsonl'da trend_up 57 işlem / 2023-04-21→2026-07-20 (ufuk fazlasıyla dolu) → `_bg_ready_regime` chop'ta yaşarken 'trend_up' döndürür ve o tur GLOBAL ship yetkisiyle koşar; ufuk kapısı ise chop için HİÇ sertifika vermemiştir.

**Öneri:** bg yansımalarında ship yüzeyini rejimle sınırlayın: bg rejimi trend_up ise ya turu atlayın ya aramayı `regime=trend_up` ile zorlayın, ve bg turunda global (@'sız) önerileri reddedin (sertifika kontrolünü `preg is None` hâline de uygulayın).

**Adversarial doğrulama:** Çürütme denemesi başarısız; iddianın ASIL bacağı (a) kodda ve canlı state'te birebir doğrulandı. Zincir: `deploy/oracle-a1/meridian.service:28` `MERIDIAN_AUTOSTART_HERMES=1` → süreç-içi `_run` döngüsü koşar; `MERIDIAN_BG_REFLECT` hiçbir yerde set edilmiyor, varsayılan "1" (hermes_runtime.py:329) → arka plan dalı canlıda AÇIK. `_bg_ready_regime`i saf haliyle gerçek deftere uyguladım: `state/regime.json`='chop', `state/hermes_status.json`'da `bg_reflect_by_regime` ANAHTARI YOK (taban=0), 95 işlemin 57'si trend_up ve 2023-04-21→2026-07-20 yayılıyor → `_horizon_ok(...,regime='trend_up',min_trades=5)`=True, n_new=57 (trend_down 0, high_vol 3 elenir) → fonksiyon **'trend_up' döndürür**. Sonra hermes.py:2668-2669 `live_reg = target_regime` ('trend_up') ve `search_regime = live_reg if ... and live_reg != 'trend_up' else None` → **None**; `search_and_submit(regime=None)` (reflect.py:1209-1225) '@'siz `pvar` üretir, `versioning.bump` (versioning.py:46) '@' yoksa **düz `params`a** yazar. `state/strategy.yaml`'da `params_by_regime` DÖRT REJİMDE DE BOŞ (`{}`), dolayısıyla `config.resolve_params` düz params'ı olduğu gibi döndürür — global ship, chop canlıyken chop davranışını ANINDA ve birebir değiştirir; oysa chop'un kendi kapısı `n-last_at=95-95=0 < every=5` ile HİÇ sertifika vermemiştir. Yani docstring'in "ship yalnız params_by_regime[o rejim]'i değiştirir — canlı davranış rejim dönene dek değişmez" beyanı, tam da en çok kanıt biriktiren rejim (trend_up) seçildiğinde karşılıksızdır ve bu istisna dilimi kural değil, %60 olasılıklı normal hâldir. Karşı-kablolama yok: `guard.validate_change`'de canlı-rejim/sertifika kontrolü yok, `submit`/`_submit_locked` yalnız `@` son ekinden eval_regime türetiyor, `reflect_once` "bu rejim CANLI mı yoksa ARKA PLAN mı" bilgisini hiç almıyor — hermes.py:2663-2667'deki bilinçli global-istisna gerekçesi ("trend_up defterin ~%90'ı") YALNIZ canlı trend_up varsayımı altında geçerli, arka plan çağrısında sessizce yanlış. Test koruması da yok, tersine: `tests/test_throughput_v7.py:39` ve `tests/test_hermes_runtime_audit_v29.py:100` bg'nin trend_up dönmesini AMAÇLANAN davranış olarak çiviliyor, hiçbir test ship'in rejim-kapsamlı kaldığını doğrulamıyor. İddianın (b) bacağı (global/'@'siz öneride `preg is None` → sertifika kontrolü hiç çalışmaz, hermes.py:2648-2653) kod olarak da doğru ama hermes.py:2645-2647 yorumunda "(or be global)" diye AÇIKÇA beyan edilmiş bir muafiyet; bağımsız bir kusur değil, aynı tek kök nedenin (canlı/arka plan ayrımının hiç taşınmaması) ikinci çıkışı — arka plan turunda o muafiyet de sertifikasız global ship'e dönüşür. Şiddeti 4 değil 3 tutmamın tek nedeni hafifletici bir olgu: global ship yine de tam üretim penceresinde purged+embargolu OOS kapısından (incumbent vs candidate, `eval_regime=None` → tüm defter, chop işlemleri dahil) geçiyor; yani ölçümsüz değil, ufuk/zaman-kümelenmesi güvencesi bypass ediliyor — ki bu tam olarak audit #27'nin LLM yan kapısı için kapattığı sınıfın ana arama yolunda yeniden açılmasıdır.

### C17 [şiddet 3 · ogrenme-katmani] Arama/ısınma sondaları resmî aşınma + doğrulama defterine SONDA BAŞINA yazıyor; kodun kendi yorumu 'oturum başına bir soru' diyor

- **Yer:** meridian/reflect.py:1291 (record_erosion=True, sonda döngüsünün İÇİNDE) · reflect.py:286-291, 378-382 · hermes_runtime.py:126-127
- **Sınıf:** yorum-davranış sürüklenmesi (kapı yasasını besleyen sayaçta)

**Kanıt:**

reflect.py:1289-1291 satırı 'RESMÎ: arama oturumu pencereye BİR soru sorar (K aday tek beyanla taşınır)' diyor, hemen altında ise HER sondada `_gate_eval(inc, cand, k_probes=total, record_erosion=True)` çağrılıyor; oysa aynı fonksiyonun 286-291 ve 378-382 yorumları 'sayım/defter yazımı yalnız resmî değerlendirmede... arama döngüsünde her çağrıda sayan bir defter kaç kez fonksiyon çağrıldığını ölçerdi' diyor. Ölçüm: state/validation_ledger.jsonl 204 satır, hepsi 2026-07-30 07:11→14:04 arası (7 saat) ve etiketleri tek-düğme sonda adımları ('stop_loss_atr_mult=2.2', 'entry.w_rs=0.2'), n_trials 368→594; state/oos_erosion.json tek parmak izinde queries=554 (EROSION_QUERY_LIMIT=20). Hiçbir şey ship etmemesi gereken ısınma sprinti de bu yoldan geçiyor (hermes_runtime.py:126 `coordinate_descent_search(..., budget=10)` → sonda başına resmî kayıt), yani 'Nothing ships' beyanı defter tarafında doğru değil. Sonuç: aşınma ek marjı ilk arama gecesinde kalıcı olarak açılıyor ve DSR'nin `n_trials`'ı yüzlerle besleniyor — gerçek-para modunda `dsr_kapi` fail-closed olduğu için bu, hiçbir adayın geçemeyeceği bir kapı demek.

**Öneri:** Aramada `record_erosion=False` koşup oturum sonunda TEK resmî kayıt düşürün (ya da tersine yorumları/limitleri sonda-başına sayıma göre yeniden türetin) — hangisi seçilirse seçilsin `k_probes` ile aşınma sayacının çifte cezasını beyanla çözün.

**Adversarial doğrulama:** Bulgu doğrulandı ve çürütülemedi. `meridian/reflect.py:1291` gerçekten sonda döngüsünün İÇİNDEDİR (döngü `for i, (var, new) in enumerate(probes, 1):` 1272'de başlar, `evaluated += 1` 1292'de aynı gövdededir) ve `record_erosion=True` ile çağrılır; hemen üstündeki 1289-1290 yorumu ise "RESMÎ: arama oturumu pencereye BİR soru sorar … Oturum içindeki her prob ayrı ayrı sayılsaydı aynı yük iki kez cezalandırılırdı" der — yani kod kendi beyanının tam tersini yapar. `record_erosion=True` iki yan etkiyi birden açar: `oos_erosion.note→record` sayacı koşulsuz +1 artırır (oos_erosion.py:~150, session-içi tekilleştirme YOK) ve `validation.record_candidate` deftere satır ekler (reflect.py:479-514) — oysa `validation.py:337-339` ve `oos_erosion.status` docstring'i (oos_erosion.py:~162) ile modül beyanı (2) "arama döngüsü binlerce kez çağırıyor; her çağrıda yazan bir defter 'kaç kez fonksiyon çağrıldı' ölçerdi… ön-elemenin yükü AYRI kanaldan `k_probes` ile taşınır" diye açıkça yasaklar. Başka yerde kablolama/bilinçli-beyan/test koruması YOK: tüm testler `_gate_eval`i doğrudan çağırır (test_hafta3a_v119.py:538-540 yalnız `record_erosion=False`ın yazmadığını çivi ler), `coordinate_descent_search`in kaynağını sınayan bir codelaw çivisi yoktur, sandbox istisnası da geçerli değildir (yalnız `prescreen.py:211` `config.STATE`i yönlendirir; `hermes_runtime.py:126` ısınması ve `hermes.py:2689` canlı yansıma canlı STATE üzerinde koşar). ÖLÇÜM KANITI kesin: `state/validation_ledger.jsonl`ın 204 satırının hepsi tek parmak izinde (720a577fcbb1e54c), tek günde 07:11→14:04 arası yazılmış ve `erosion_queries` satır başına BİRER artıyor (351,352,353 … 552,553,554) iken aynı oturumun `k_probes`ı sabit kalıyor (17/27/40) — yani aynı K yükü hem sayaca hem beyana yazılıp `_n_trials = erosion.queries + k_probes` (reflect.py:386) ile ikinci kez cezalandırılıyor; n_trials 368→594'e tek günde tırmanıyor ve 204 satırda yalnız 60 ayrık etiket var (aynı aday 6 kez tekrar sayılmış, PBO popülasyonu da kopyalarla kirleniyor). Sonuç: `EROSION_QUERY_LIMIT=20` bir tek ısınma gecesinde (planlanan sonda `max(budget*4,40)`) aşılır ve +0.01 ek marj ömür boyu açık kalır; "Nothing ships" beyanı submit tarafında doğru ama defter/sayaç tarafında yanlıştır. Şiddeti 4 değil 3 yaptım çünkü etki YÖNÜ muhafazakârdır (kapıyı gevşetmez, sıkar), DSR bugün kâğıt modunda advisory'dir (`dsr_kapi(live=False)` bloklamaz) ve ROADMAP'in kendi ölçümü N=32'de bile 122/122 DSR ≤0.95 olduğunu gösterdiğinden gerçek-para fail-closed sonucunun tek nedeni bu şişme değildir; ayrıca R1 rotasyonu sayacı sıfırladığı için marj ŞU AN 0.0'dır — ama mekanizma yerinde durduğundan restart sonrası ilk arama gecesinde aynı yere geri döner.

### C18 [şiddet 3 · olcum-sasisi] Replay giriş limitini ATR bacağı olmadan kuruyor — öğrenme kapısı canlının reddettiği dolumları yazıyor

- **Yer:** meridian/backtest.py:200-202 (karşılaştırma: meridian/loop.py:475-477, meridian/broker.py:105-119)
- **Sınıf:** canlı-vs-replay ayrışması (icra yasası)

**Kanıt:**

E1 yasası (broker.py:37-40) tek yasa iki motor diyor: `limit = tetik + min(atr_mult·ATR14, pct_cap·tetik)`; `entry_limit_price` (broker.py:118) `off = min(cfg['limit_atr_mult']*a, pct_off) if a > 0 else pct_off`. CANLI: `loop.py:475-477` → `b.fill_entry(..., atr=_lw.get('atr'), gap_at_submit=...)`, ATR silahlanma anında `entry_order_decision(..., atr=c.get('atr'))` ile sabitleniyor (loop.py:731-732). REPLAY: `backtest.py:200-202` → `broker.fill_entry(plan, per[t].loc[d,'open'], str(d.date()), eq_now, size_mult=size_mult, adv=_adv(per[t], d), pivot=armed_pivots.get(t, 0.0))` — `atr=` HİÇ geçilmiyor, oysa değer elde: `strategy.Signal.atr` (strategy.py:282) ve replay onu candidate_log'a zaten yazıyor (backtest.py:290). Sonuç: replay'de limit = tetik×(1+%1), canlıda limit = tetik + min(0,5·ATR14, %1) yani DAİMA ≤ replay'inki. `fill_entry` docstring'i `atr=None`u 'ölçülemedi' diye beyan ediyor (broker.py:299-300) — replay'de ölçülemez DEĞİL, ölçülmemiş. Büyüklük ölçümü (salt-okuma, state/bars'tan 25 rastgele sembol, ATR14/close): sembol-medyanlarının medyanı 0,0213; barların ~%41'inde ATR14/close < %2, yani o barlarda 0,5·ATR < %1 ve canlı limit KESİN olarak daha sıkı. Karşılaştırma noktası: karne ölçümünde `dolu/fill_cagrisi` E1 öncesi 0,9758 → E1 sonrası 0,8481 (research/olcumler/karne_olcum/RAPOR.md:147-152), yani pct-only limit bile dolumların ~%12'sini kesiyor; ATR bacağı eklendiğinde replay daha da az dolduracak. Yani §4'ün 'backtest sayıları dürüst' sözleşmesi giriş tarafında iyimser yönde bozuk.

**Öneri:** `sig.atr`yi `armed_pivots` deseniyle bir yan haritada taşı ve `fill_entry(..., atr=...)`e geçir; ayrıca `reject_out` sözlüğünü geçirip `entry_missed_limit` sayısını replay çıktısına ekle ki iki motorun kaçırdığı dolumlar kıyaslanabilsin.

**Adversarial doğrulama:** Bulgu DOĞRULANDI, çürütülemedi. `meridian/backtest.py:200-202` `broker.fill_entry`i yalnız `size_mult/adv/pivot` ile çağırıyor; `atr=` ve `gap_at_submit=` hiç geçmiyor — dosyanın tamamında `atr` yalnız bir yerde geçiyor (satır 290, candidate_log yazımı), yani değer replay'de ELDE (strategy.EntrySignal.atr, strategy.py:275-282) ama icra yoluna verilmiyor; üstelik `armed_pivots` (backtest.py:161,357) tam olarak bu amaç için kurulmuş "plan defterine değil yana taşı" desenini zaten kanıtlıyor, yani teknik engel de yok. Canlı taraf tam tersi: `loop.py:731-732` silahlanma anında `BR.entry_order_decision(..., atr=c.get('atr'))` ile kararı sabitliyor ve `loop.py:475-477` onu `fill_entry(..., atr=_lw.get('atr'), gap_at_submit=...)` diye geçiriyor. Sonuç `broker.entry_limit_price` (broker.py:118) üzerinden tek yönlü sapma: `off = min(mult·ATR, pct_off) if a>0 else pct_off` olduğu için replay limiti canlınınkinden DAİMA ≥, ATR bacağı bağladığında KESİN büyük; state/goal.yaml:45-50 yürürlükteki değerleri (0.5 / %1) teyit ediyor, yani sapma yapılandırmayla kapanmıyor. Beyan/koruma savunması da tutmuyor: `fill_entry` docstring'i `gap_at_submit=None` için AÇIKÇA "bu motorda gönderim anı YOK (replay) — BEYAN EDİLMİŞ kapsam farkı" diyor ama `atr=None` için yalnız "ölçülemedi" diyor (broker.py:299-304) — replay'de ölçülemez değil, ölçülüp geçilmemiş; EXE-2026-001 kartında replay/backtest kelimesi hiç geçmiyor; ve E1'in AST çivisi `test_icra_gercekligi_v141.py:45` YALNIZCA `alpaca` ile `loop` modüllerini geziyor, `backtest` çivilenmemiş (test_differential_v60 ise plan ŞEMASINI kontrol ediyor, icra girdilerini değil). Kapsam denetçinin dediğinden GENİŞ: aynı eksik `shadow_lifecycle.py:259` (öğrenme/aday yaşam döngüsü), `mutation.py:187` ve `intraday_shadow.py:218` için de geçerli. DÜZELTME — büyüklük iddiası abartılı: rastgele barların ~%41'i değil; state/candidates.jsonl'in 323 GERÇEK aday satırını state/bars ile birleştirip ölçtüm, medyan ATR14/kapanış %2,57 ve ATR bacağının bağladığı (ATR/px < %2) aday oranı %22,3 (entry_trigger taşıyan 23 canlı adayda 0/23 — kurulum seçimi doğası gereği yüksek-ATR isimleri seçiyor); ayrıca bağladığında bile fark yalnız (%1 − 0,5·ATR/px) genişliğinde dar bir banda düşen açılışları çeviriyor, yani karnedeki 0,9758→0,8481 düşüşüyle (o düşüş pct tavanının KENDİSİNİN etkisi) kıyaslanacak bir kayıp değil. Yön ve sözleşme ihlali yine de gerçek: replay iyimser tarafta, §4'ün "backtest sayıları dürüst" ve E1'in "tek yasa, iki motor" sözleşmesi giriş tarafında beyansız ve testsiz kırık.

### C19 [şiddet 3 · olcum-sasisi] Günlük kayıp devre kesicisi iki motorda FARKLI pencere ölçüyor — canlıda fiilen yalnız gecelik boşluk kesicisi

- **Yer:** meridian/backtest.py:191-195 ve 212 (karşılaştırma: meridian/loop.py:432,442,952; meridian/shadow_lifecycle.py:250,265)
- **Sınıf:** canlı-vs-replay ayrışması (sermaye koruma kapısı)

**Kanıt:**

REPLAY: `day_start_equity = broker.equity(marks_open_on(d))` (backtest.py:212) — yani D'nin AÇILIŞ özkaynağı; ertesi gün `breaker_tripped = (eq_now - day_start_equity)/day_start_equity <= -max_daily_loss` (backtest.py:195) açılış(D)→açılış(D+1) penceresini, yani D'nin TÜM seansını ölçüyor. CANLI: `meta['day_start_equity'] = b.equity(_marks(per, d))` (loop.py:952) ve `_marks` KAPANIŞ fiyatlarını kullanıyor (loop.py:1094-1095); breaker ise `day_pnl_pct = (b.equity(marks_open) - day_start_equity)/day_start_equity` (loop.py:442) → kapanış(D-1)→açılış(D), yani YALNIZ gecelik boşluk. Eşik `max_daily_loss_pct: 3.0` (state/goal.yaml:82): portföy düzeyinde −%3'lük bir GECELİK boşluk pratikte hiç oluşmaz, dolayısıyla canlı kesici fiilen atıl; buna karşılık replay aynı eşikle bir seansın tamamındaki −%3'te ateşler ve giriş bloke eder. Üçüncü motor (gölge-v2, shadow_lifecycle.py:265) replay konvansiyonunda — yani canlı tek başına ayrık. Modül başlığı 'Replays through the exact same strategy.py + broker.py used live, so backtest numbers are honest (§4)' diyor; bu kapı için doğru değil. `tests/` içinde iki motorun breaker penceresini çiviyen tek bir test yok (grep 'day_start_equity' → yalnız fikstür kullanımı).

**Öneri:** Tek yasa: `day_start_equity`yi iki motorda da AYNI faza yaz (canlıda da OPEN(D) markıyla) ya da farkı beyan edip `test_differential_v60`e breaker-penceresi çivisi ekle — hangisi seçilirse seçilsin canlı tarafın bugünkü 'gecelik boşluk kesicisi' davranışı adıyla belgelenmeli.

**Adversarial doğrulama:** ÇÜRÜTÜLEMEDİ — iddia edilen davranış kodda birebir var ve hiçbir yerde kablolu/beyanlı/test-korumalı değil. (1) Pencere farkı doğrulandı: `meridian/broker.py:261-268` `equity(marks)` = start_equity + realized_pnl + Σ qty·(mark − entry), yani mark seçimi AÇIK POZİSYONLAR İÇİN MADDİDİR (marks'ı yok saymıyor, dolayısıyla "her iki motor da yalnız realize kârı ölçüyor" çürütmesi düşüyor). REPLAY `backtest.py:212` `day_start_equity = broker.equity(marks_open_on(d))` (marks_open_on = `per[t].loc[d,"open"]`, satır 179-181) ve `:195` kesiciyi yine açılış markıyla ölçer → pencere açılış(D)→açılış(D+1), yani D seansının TAMAMI + gecelik boşluk. CANLI `loop.py:952` `meta["day_start_equity"] = b.equity(_marks(per, d))` ve `_marks` (`loop.py:1093-1094`) `per[t].loc[d,"close"]` döner → kapanış(D); `loop.py:431-432,442` ise okurken `marks_open` (D'nin AÇILIŞI) kullanır → pencere kapanış(D)→açılış(D+1), YALNIZ gecelik boşluk. İki motorda da kesici tek bir kez, açılış fazında değerlendirilir ve sıralama aynıdır (bekleyen çıkışlar önce: `backtest.py:185-195` ≡ `loop.py:435-443`), dolayısıyla fark tam olarak D'nin gün-içi hareketidir. (2) Gölge-v2 gerçekten replay tarafındadır: `shadow_lifecycle.py:265` `bk["day_start_equity"] = b.equity(_marks_open())` ve `:230-236` yorumu açıkça "AÇILIŞ MARKI (replay `marks_open_on`) … (denetim #1)" der — canlı üç motorda TEK BAŞINA ayrıktır. (3) Bu bilinçli bir tasarım değil, KISMİ DÜZELTME kalıntısıdır: `AUDIT-2026-07-18.md:25` tam da `day_start_equity`nin kapanışla işaretlenmesini ileri-dönüklük olarak raporlamış; düzeltme backtest ve shadow_lifecycle'da hem OKUMA hem YAZMA tarafını açılışa çevirmiş, `loop.py`de ise yalnız OKUMA tarafı çevrilmiş (`:429-431` yorumu "audit #1"e adıyla atıf yapar) — yazma tarafı `:952` kapanışta unutulmuş. Depo bilinçli ayrışmaları gürültülü biçimde beyan ediyor (`backtest.py:203` "KASITLI REPLAY↔CANLI FARKI", `loop.py:645-649` guard'a arm anında `day_pnl_pct: 0.0` verilmesi "audit #15" olarak yazılı ve İKİ MOTORDA SİMETRİK); bu ayrışma için ne `:212`de ne `:952`de tek satır beyan var. (4) İkinci bir canlı kesici yok: `intraday_cycle.py` Faz-4a'dır, `_handle_symbol` yalnız karar satırı yazar ("Faz 4a: gözlem tamam, KARAR YOK", `:230`) ve tek kapısı `_health.halted()`tir (`:149`); `intraday_shadow.py:131-132` kesiciyi heartbeat'in `day_pnl_pct`inden okur — yani aynı boşluk-penceresini miras alır ve zaten ölçüm katmanıdır; `guard.py:300` sert kontrolü her iki motorda da 0.0 ile beslenir. (5) Test koruması yok: `tests/test_differential_v60.py` yalnız PLAN/kapı üretimini eşler (plan id, alan kümesi, gate_check sırası), icra-riski tarafına hiç bakmaz; `test_health_versioning_gaps_v49.py:81-83` yalnız saf eşik fonksiyonunu (`circuit_breaker_tripped(-lim)`) sınar, pencereyi değil; `test_parity_v56.py` kapsam/sadakat dedektörleridir. Etki: `RISK_PCT_PER_R=0.01`, `max_position_r 1.0`, `max_open_positions 5` ile toplam stop-riski ≤%5 ve brüt maruziyet tipik olarak özkaynağın ~%60'ı düzeyindedir; portföy düzeyinde −%3'lük bir GECELİK boşluk ≈ tüm defterde eşzamanlı −%5 gap gerektirir — imkânsız değil ama seans-içi −%3'ten mertebe olarak daha nadirdir, yani canlı kesici fiilen atıldır. Tek düzeltmenin `loop.py:952`de `_marks` → açılış markı olması ve canlının şu an autonomy_level 0 (kâğıt) olması, ayrıca kesicinin yalnız YENİ GİRİŞİ bloklaması (pozisyon düzleştirmemesi) nedeniyle 4 değil 3: yazılı sert limit (`state/goal.yaml:82 max_daily_loss_pct: 3.0`) canlıda karşılığını bulmuyor ve öğrenme kapısı ile gölge-terfi makinesi, canlının uygulamadığı bir risk rejimi altında ölçüm yapıyor — `backtest.py:1-2` başlığındaki "backtest numbers are honest (§4)" iddiası bu kapı için yanlıştır.

### C20 [şiddet 3 · veri-hatti] Aynı-akşam bacağı: sembol CEVAP GELMEDEN 'soruldu' işaretleniyor — tek kısmi/başarısız istek o seansın bacağını süreç ömrü boyunca öldürüyor

- **Yer:** meridian/adapters/data.py:1230-1244 (ve 1094-1095)
- **Sınıf:** hata-yolu sağlamlığı / canlı-vs-replay ayrışması

**Kanıt:**

`_alpaca_session_bars` isteği ATMADAN ÖNCE işaretliyor:
```
need = [s for s in _leg_universe(ticker) if s.upper() not in asked]
if need:
    asked.update(s.upper() for s in need)          # ← cevap daha gelmedi
    res = alpaca.same_evening_bars(need, session) or {}
```
`alpaca.same_evening_bars` arıza hâlinde ASLA fırlatmaz; `{"source": None, "bars": {}}` döner (alpaca.py:969) ve `snapshots()` chunk başına `return out or None` ile KISMİ sonuç döndürür (alpaca.py:801-812, DATA_CHUNK=100 → 253 sembol = 3 chunk). Yani 2. chunk 429 alırsa ~150 sembol 'asked' kümesine girmiş ama memo'ya HİÇ girmemiş olur. Repo genelinde `_ALPACA_ASKED`i temizleyen ÜRETİM yolu yok (grep: yalnız iki test fikstürü). scheduler yeni seansta `massive.reset_cache()` çağırıyor (scheduler.py:~611) ama bu defterlere dokunmuyor. Sonuç: sık faz 8 denemesi ve seyrek faz denemeleri o sembolleri bir daha SORAMAZ → `_session_coverage` 0,90 altında sıkışır → merdiven `_declare_terminal_skip`e kadar yürür; bu, bacağın kapatmak için yazıldığı 'sahte SEANS ATLANDI' sınıfının ta kendisi. Test kapsaması da yapısal olarak kör: test_ayni_aksam_bacagi_v133.py:44 ve test_sip_gecmis_seans_v139.py:44 her testte `_ALPACA_MEMO/_ALPACA_ASKED`i sıfırlıyor, yani çağrılar-arası kalıcılık hiç sınanmıyor.

**Öneri:** `asked`i istekten SONRA ve YALNIZ gerçekten cevap dönen semboller için işaretle (cevapsız kalanlar bir sonraki poll'de yeniden sorulsun; tekrar-dövme koruması zaten `_data_cooled` soğumasında var).

**Adversarial doğrulama:** Bulgu GERÇEK ve çürütülemedi; üstelik proje venv'iyle canlı repro ile doğrulandı. Zincir tam olarak iddia edildiği gibi: `data._alpaca_session_bars` (data.py:1237-1240) `asked.update(...)`'ı `alpaca.same_evening_bars(...)` çağrısından ÖNCE yapar; `same_evening_bars` arıza hâlinde fırlatmaz, `{"source": None, "bars": {}}` döner (alpaca.py:969) ve altındaki her iki katman da KISMİ sonuç döndürür — `snapshots` chunk arızasında `return out or None` (alpaca.py:807, DATA_CHUNK=100, alpaca.py:557) ve sip yolundaki `daily_bars` aynı kalıpta (alpaca.py:875), üstelik `same_evening_bars` kısmi sip sonucunda `if bars:` ile ERKEN döner, yani eksik kalan semboller iex yedeğine bile düşmez. Repro (`_leg_universe` = 253 sembol; 1. chunk cevaplıyor, 2./3. chunk arızalı): 1. çağrı → memo 100, asked 252; SAĞLIKLI sağlayıcıyla yapılan 2. çağrı → toplam istek hâlâ 1, memo hâlâ 100, 152 sembol kalıcı olarak eksik. Yani `_data_cooled` soğuması (300 sn, alpaca.py:559) dolduktan sonra bile sık faz (her 300 sn poll) ve seyrek faz (30-60 dk) denemeleri o semboller için AĞA HİÇ ÇIKMAZ: `need` boş kalır. Üretimde `_ALPACA_ASKED`i temizleyen yol yok (grep: yalnız tests/test_ayni_aksam_bacagi_v133.py:44 ve tests/test_sip_gecmis_seans_v139.py:44 fikstürleri) ve o fikstürler her testten önce sıfırladığı için çağrılar-arası kalıcılık yapısal olarak sınanmıyor — test koruması yok. Sonuç: `dataset.load_live` → `data.load_bars`'ın zincir sonundaki bacağı (data.py:1866-1886) o seans için ölür, `_fetch_alpaca_session` boş döner, `outcomes["alpaca"]="empty"` yazılır (429'u "boş" diye etiketler; hatta diğer kollar temiz-boş dönerse hükmü `symbol_unknown` tarafına iter, data.py:1905/1962) ve scheduler'ın `_session_coverage` ölçümü 0,90 altında sıkışabilir → merdiven `_declare_terminal_skip`e (scheduler.py:602) yürür; bu, bacağın kapatmak için yazıldığı SAHTE "SEANS ATLANDI" sınıfının ta kendisidir. Denetçinin DÜZELTİLMESİ gereken iki yan iddiası: (a) etki "süreç ömrü boyunca" değil, SEANS ANAHTARI ömrüncedir — defterler `{seans: ...}` anahtarlı olduğundan yeni seans temiz bir kümeyle başlar (repro: yeni anahtar None), dolayısıyla (b) `massive.reset_cache()`in bu defterlere dokunmaması bir kusur değil, ilgisizdir. Ayrıca hafifletici olarak zincir kaynakları (fmp/cboe/nasdaq/massive) her `fresh` poll'de yeniden denenir ve `repair_coverage()` seans başına bir kez grouped ile deliği kapatmayı dener; kaybedilen şey, tam da zincir geciktiğinde devreye girsin diye yazılmış YEDEĞİN poll-başına yeniden deneme hakkıdır. Bu yüzden kusur gerçek ve sessizdir, ama veri bozulması değil, bir seanslık yedek körlüğüdür.

### C21 [şiddet 2 · gozetim] `integrity_report` üç tabanı parity'den ÖNCE ilerletiyor: parity düşerse gerileme alarmsız biçimde yeni tabana emilir

- **Yer:** meridian/watchdog.py:776-779 (dict değerleri soldan sağa değerlenir; parity EN SON), :1053 çağrısı
- **Sınıf:** hata-yolu sağlamlığı / dedektörün kendi kör noktası

**Kanıt:**

`determinism_report(persist=True)`, `monotonicity_report(persist=True)` ve `ownership_report(persist=True)` sözlük içinde `parity_report()`ten ÖNCE değerlenir, yani yeni taban DİSKE YAZILMIŞ olur. `parity_report` ise korumasız çağrılar taşıyor: `_lg.validate_live` (:517-518), `_rc.report()` (:583-584), `_sv.report()` (:591-592), `store.read_jsonl("trades.jsonl")` (:476) — bunların hiçbiri try/except içinde değil (codelaw/brain blokları ise korumalı, yani asimetri bilinçsiz). Biri fırlarsa `check_integrity_and_alarm` hiçbir alarm üretmeden düşer (loop.py:1035 `v3_learn_layer_failed` diye yutar) ve `integrity_alarmed.json` da yazılmaz. Sonraki turda prev==cur olduğu için gerileme SONSUZA DEK kaybolur — bu, persist tasarımının kendi gerekçesinin (watchdog.py:339-344, 1264-1269: "iki okuma ARASINDA olan bir gerileme sessizce yeni tabana emilir") birebir gerçekleşmesidir. Ek yorum-davranış çelişkisi: :1053 "taban YALNIZ burada ilerler (tek sahip)" diyor, oysa mutation.py:310 de `integrity_report(persist=True)` çağırıyor ve aynı dosyanın :787-789 yorumu bunu kabul ediyor.

**Öneri:** `parity_report()`i sözlükte persist'li üçlünün ÖNÜNE al ya da her dedektörü tek tek yakala (düşen dedektör `{"ok": False, "error": ...}` dönsün) ve :1053'teki "tek sahip" cümlesini mutation.py gerçeğiyle düzelt.

**Adversarial doğrulama:** Bulgunun ÇEKİRDEĞİ çürütülemedi, kod birebir doğrulandı: watchdog.py:776-779 bir sözlük literalidir, Python değerleri yazım sırasına göre değerler, yani `determinism_report(persist=True)` (:777), `monotonicity_report(persist=True)` (:778) ve `ownership_report(persist=True)` (:779) `parity_report()` (:779 sonu) ÇAĞRILMADAN ÖNCE tamamlanır; üçünün de taban yazımı kendi içinde kıyastan ÖNCEDİR (determinism :353-354 write_json → kıyas :357-366; monotonicity :1303-1304 → kıyas :1309-1321). `parity_report` gerçekten korumasız fırlatma yüzeyleri taşıyor — `store.read_jsonl("events.jsonl")` (:414), `store.read_jsonl("trades.jsonl")` (:476, ve bu ad storage.py:94 `_TABLE`'da SQLite'a bağlı: `read_rows` busy_timeout 5 sn sonrası OperationalError fırlatabilir), `_lg.validate_live` (:517-518), `intraday_stamp_report()` (:576), `_rc.report()` (:583-584), `_sv.report()` (:591-592) — oysa hemen yanlarındaki codelaw (:599-617) ve beyin (:622-684) blokları try/except içinde; asimetri gerçek. Fırlarsa `check_integrity_and_alarm` :1053'te ölür, hiçbir `obs.alarm` üretilmez, :1157'deki `integrity_alarmed.json` yazımı da atlanır ve loop.py:1035-1036 bunu yalnızca `obs.warn("v3_learn_layer_failed")` diye yutar (warn NOTIFY_TOKENS'ta değil, obs.py:98-110 — operatöre gitmez). Tabanı başka yazan yok (sermaye.py:429 MONOTONIC_FILE'ı yalnız OKUR), dolayısıyla o turdaki bar küçülmesi/defter kısalması bir sonraki turda prev==cur olduğu için KALICI olarak kaybolur — 2026-07-22'de tam bu sınıf için yazılmış persist kapısının (:339-344, :1264-1269) artık kendi istisna yolunda tekrarlanması. ÜÇ DÜZELTME: (a) "üç taban" abartılı — `ownership_report` tabanı yapışkandır (`st[f] = bool(has or seen.get(f))`, :1351), kayıp alan bir sonraki turda YENİDEN tespit edilir, yani kalıcı emilim yalnız determinism ve monotonicity için geçerli; (b) mutation.py:310 "ikinci sahip" çelişkisi bağlam kaçırmasıdır — mutation.py `_LIVE_STATE` koruması ve `_state_dir` yönlendirmesiyle (mutation.py:54-80, :609-635) canlı state'e değil geçici kopyaya yazar, üstelik :787-789 yorumu bunu zaten beyan ediyor; (c) denetçinin görmediği KISMİ telafi: `_wd5.beat("p5_calibrations")` çağrısı loop.py:1034'te aynı try içinde ve check'ten SONRA olduğu için sürekli bir parity arızası EXPECTED penceresi (4 gün, watchdog.py:39) dolunca `check_and_alarm` üzerinden MECHANISM_STALE üretir — yani KALICI arıza sonsuza dek sessiz değildir; ama TEK SEFERLİK bir istisna, ertesi turda nabız yeniden atıldığı için tamamen sessiz kalır ve o turun gerilemesi geri getirilemez. Bu davranışı sınayan hiçbir test yok (test_integrity_v15.py:141-146 yalnız şekil sözleşmesini doğruluyor).

### C22 [şiddet 2 · gozetim] `determinism_report` bar dizini okunamadığında `ok=True` dönüyor — sessiz bar mutasyonu dedektörü tek bir stat hatasıyla kapanıyor

- **Yer:** meridian/watchdog.py:347-350
- **Sınıf:** sessiz varsayılan / fail-open + YASA-4 işaretinin gerekçesi yanlış

**Kanıt:**

`except Exception:  # sessiz-yutma: sonuç KAYDA GEÇİYOR — dönen detay "kontrol atlandı" der, "temiz" demez` / `return {"ok": True, "detail": "bar dizini okunamadı — kontrol atlandı"}`. Gerekçe fiilen YANLIŞ: (a) hiçbir `obs.warn` basılmıyor, yani olay defterine hiçbir şey geçmiyor; (b) TÜM tüketiciler yalnız `ok`a bakıyor — `check_integrity_and_alarm` :1086 `if not rep["determinism"].get("ok")`, mutation.py:315 aynısı, pano app.js:3518 `${v.appended ?? 0} salt-ekleme defteri doğrulandı` (dönüşte `appended` yok → "0 defter doğrulandı", satır YEŞİL) ve app.js:3517 `_patOK` de `v.ok !== false` ile yeşile boyuyor. `detail` metnini okuyan tek bir tüketici yok. Üstelik önceki turda mandala girmiş `determinism` jetonu `now` kümesine girmediği için düşer — yani gerçek bir mutasyon alarmı da sıfırlanır.

**Öneri:** Okunamayan dizinde `{"ok": False, "olculemedi": True, ...}` dön (fail-closed) ve aynı yerde `obs.warn("determinism_bars_unreadable", ...)` bas.

**Adversarial doğrulama:** İDDİA DOĞRULANDI (ama şiddeti abartılmış). Kod aynen iddia edildiği gibi: meridian/watchdog.py:347-350'de çıplak `except Exception:` hiçbir `obs.warn` basmadan `{"ok": True, "detail": "bar dizini okunamadı — kontrol atlandı"}` döner ve dönüşte `skipped`/`unmeasured` gibi makine-okunur hiçbir bayrak yoktur. Tüketici zincirini tükettim — dedektörün SADECE üç tüketicisi var ve ÜÇÜ de yalnız `ok`a bakar: watchdog.py:1086 `if not rep["determinism"].get("ok")` (yani alarm HİÇ kurulmaz; `detail`i okuyan :1090 satırı ok=False dalında olduğu için bu yoldan asla çalışmaz), mutation.py:315 birebir aynısı, ve pano app.js:3547 `_patOK`'in son dalı (`v.ok !== false && !(v.regressions||[]).length && !(v.shrunk||0)`) → YEŞİL "temiz", app.js:3550 `_patNote` ise `${v.appended ?? 0}` ile "0 salt-ekleme defteri doğrulandı" basar. `detail` metni api.py:2103 üzerinden tarayıcıya GİDER ama onu render eden tek bir eleman yoktur (app.js:3607'deki `v.detail` codelaw ihlal listesine aittir, bütünlük satırlarına değil); yani satır içi yorumun "sonuç KAYDA GEÇİYOR" iddiası FİİLEN YANLIŞTIR — sonuç hiçbir deftere, hiçbir panele, hiçbir karara geçmez ve tests/test_integrity_v15.py:88-124 bu dalı hiç sınamaz. YİNE DE üç noktada denetçi fazla ileri gitti ve şiddet 3'ten 2'ye iner: (1) erken dönüş `store.write_json(FINGERPRINT_FILE, ...)` (satır 354) ÖNCESİNDE olduğu için TABAN İLERLEMEZ — küçülmüş bar dosyası küçük kalır, bir sonraki başarılı çağrı aynı bozulmamış tabana karşı kıyaslar ve alarmı ÜRETİR; yani kanıt yok olmaz, tespit bir tur ERTELENİR, "dedektör kapanıyor" değil; (2) mandal argümanı ters yönde çalışır: jeton `now`a girmeyip düşünce sonraki turda `tok not in prev` sağlanır ve alarm YENİDEN patlar — sonuç sessizlik değil, mükerrer gürültüdür; (3) erişilebilirlik dar: `Path.glob` olmayan/okunamayan dizinde istisna ATMAZ boş döner (o yol zaten `vanished` → ok=False alarm dalına düşer), `_write_bars` (adapters/data.py:106-118) `mkstemp(suffix=".tmp")`+`os.replace` ile atomiktir ve `*.csv` glob'unu kirletmez, `BARS/*.csv` silen kod yoktur (bararchive.py:78 `intraday_bars/*.jsonl` budar) — dolayısıyla dal ancak gerçek bir G/Ç-izin arızasıyla (A1'de blok-birim hıçkırığı, bozuk sembolik bağ) tetiklenir. Özetle GERÇEK kusur şudur: ölçülemeyen bir hüküm "temiz" kılığında panoya yeşil basılıyor ve YASA 4'ün ruhuna aykırı biçimde yorum, olmayan bir kaydı beyan ediyor; düzeltmesi tek satırlık `obs.warn("determinism_check_skipped", ...)` + dönüşe `"skipped": True` alanı + `_patOK`/`_patNote`'ta "ölçülemedi" (yeşil değil, nötr) durumudur.

### C23 [şiddet 2 · icra-entegrasyon] Silahlı planlar kapı-dışı koşullarda OLAYSIZ düşürülüyor; ayna emri o sırada canlı ve HALT onu iptal etmiyor

- **Yer:** meridian/loop.py:462, 468, 506 (+640 vs 455); meridian/api.py:2557-2578; meridian/analytics.py:3759-3765
- **Sınıf:** YASA 4/6 kör noktası + iki-motor ayrışması

**Kanıt:**

`if not halted and not breaker and not data_bad and size_mult > 0:` ... `meta['armed'] = carried` — koşul tutmazsa `carried` boş kalır ve TÜM silahlı planlar hiçbir olay/defter satırı/`broker_status` damgası olmadan yok olur. Aynı sessizlik plan başına da var: `if len(b.positions) < eff_max_open and t not in b.positions:` — else dalı YOK. Üstelik silahlanma tavanı ile dolum tavanı FARKLI: arm `slots = limits['max_open_positions'] - len(b.positions)` (640), fill `eff_max_open = max_positions_at(...)` (455). Ölçtüm: %5 düşüşte 6→4, %7'de 6→1, %8'de size_mult=0 (blok komple atlanır). Bu planların ayna emirleri ÖNCEKİ akşam gönderildi ve kapanan seans boyunca CANLIYDI → dolabilir; HALT ucu (api.py:2567 `health.set_halt(True)`) `cancel_open_entries` ÇAĞIRMIYOR (o ayrı bir elle uç: api.py:1325). Kontrast aynı dosyada: gap-veto düşürmesi hem olay hem `pl['broker_status']='gap_veto'` yazıyor, gerekçesi de yazılı — 'silahlı bir planın kaybolma sebebi defterden okunabilmeli' (loop.py:858-863). Ölçüm de kör: `entry_execution_summary` mutabakatı `if not o: continue` ile iç satırı olmayan ayna satırlarını sayımdan düşürüyor (analytics.py:3763) — yani kartın (c) ölçütü tam bu sınıfı göremiyor.

**Öneri:** Düşürülen her silahlı plana olay + E2 defter satırı + `broker_status` yaz (ör. `armed_dropped_halt` / `armed_dropped_throttle`) ve halt/breaker dalında aynadaki dolmamış giriş emirlerini `cancel_open_entries` ile kapat.

**Adversarial doğrulama:** İDDİANIN ÇEKİRDEĞİ DOĞRULANDI, HİKÂYESİ KISMEN ÇÜRÜTÜLDÜ. Kod-olguları birebir doğru: loop.py:461 `carried = []`, 462 `if not halted and not breaker and not data_bad and size_mult > 0:`, 506 `meta["armed"] = carried` — üçü de aynı 8-boşluk seviyesinde (awk ile teyit), yani koşul tutmazsa TÜM silahlı planlar plan-başına olay, `entry_exec` satırı ve `broker_status` damgası olmadan silinir; plan-başı sessizlik 468'deki else'siz `if len(b.positions) < eff_max_open and t not in b.positions:` dalında da var; tavan farkı gerçek (arm 640: `limits['max_open_positions']`, fill 455: `max_positions_at`) ve sayılar broker.py:178-200 + `DERISK_FLOOR_DD=0.08` ile birebir çıkıyor (dd %5→6/4, %7→6/1, %8→size_mult=0, blok komple atlanır); api_halt (api.py:2557-2578) `cancel_open_entries` çağırmıyor; analytics.py:3763 `if not o: continue` iç satırı olmayan ayna satırlarını (c) mutabakatından düşürüyor. Üstelik denetçinin görmediği EK bir kusur var: blok atlandığında `_carry_armed_without_bar` HİÇ çağrılmadığı için GS-1140 düzeltmesinin "barı yayınlanmamış planı bir seans taşı" koruması da sessizce kayboluyor, ve bu yolu koruyan tek bir test yok (tests/test_ops_v11.py:178 yalnız fonksiyonu izole çağırıyor). ANCAK nedensel hikâye ÇÜRÜDÜ: (1) ayna emri TIF="day"dir (broker.py:57 `ENTRY_TIF`, entry_order_decision→submit_plan→submit_bracket) ve D-1 akşamı gönderilip D seansında yaşar; `meta["armed"] = carried` ise D barını işleyen AKŞAM turunda koşar — yani düşürme anında emir çoktan dolmuş ya da sönmüştür, "o sırada canlı" değildir; (2) asıl zararlı sonuç (ayna doldu, iç defter düşürdü) BEYANLI ve ALARMLI: loop.py:1311-1322 tam bu senaryoyu adıyla anıyor ("can fill on Alpaca after the internal side dropped its plan (breaker day, slot race, gap guard) — that is split-brain and must ALARM", denetim #14), `engine_orphans` üretip ALARM_MIRROR_DRIFT atıyor ve o semboller loop.py:624 `_mirror_busy` üzerinden yeni karar üretimini de kapatıyor; (3) plan-başı sessiz kayıp için üretime kablolu bir dedektör zaten var — watchdog.conservation_report() (255-325; "P4 buharlaşması tam olarak buydu"), watchdog.py:776'dan raporlanıyor ve pano app.js:3538'den okuyor: düşen planlar `unexplained`e düşer; (4) arm-geniş/fill-kısıtlı asimetrisi CANLIYA ÖZGÜ DEĞİL — backtest.py:194-212 birebir aynısını yapıyor (arm `slots = max_open - len(positions)`, fill `eff_max_open` + `size_mult>0` + breaker, ardından koşulsuz `armed = []`) ve loop.py:645-649 "breaker enforced at fill, not at arm" diye açıkça beyan ediyor; (5) HALT'ın iptal etmemesi ucun kendi cevabında beyanlı ("new entries stop within one bar"), ayrı operatör kapısı var (api.py:1325 `/api/control/cancel_open`) ve tests/test_authority_boundaries_v77.py:590-626 hiçbir otomatik yolun düzleştirme/iptal yapmamasını sınırla koruyor; ayrıca HALT akşam turundan önce basılıysa loop.py:815 `not halted` yüzünden emir zaten hiç gitmez. Geriye kalan gerçek kusur bu yüzden "sessiz para kaybı" değil, GÖZLENEBİLİRLİK/ÖLÇÜM BORCU + bir davranış boşluğudur: aynı dosyada loop.py:858-863'te yazılı olan ilke ("silahlı bir planın kaybolma sebebi defterden okunabilmeli") kapı-dışı düşürmelere uygulanmamış, (c) mutabakatı bu sınıfı görmüyor ve taşıma koruması atlanan turda devre dışı kalıyor — sonuç ancak post-hoc korunum raporundan/yetim alarmından okunabiliyor.

### C24 [şiddet 2 · karar-cekirdegi] Sert portföy-ısısı tavanı 4,5R kodda sabit: operatörün goal.yaml zarfıyla (5 poz × 1,0R = 5,0R) çelişiyor ve canlı defterde 390 planın 18'ini NO_GO yapmış

- **Yer:** meridian/guard.py:251-252,312-315 · state/goal.yaml:80-81
- **Sınıf:** gizli eşik / hipotez uzayının körü

**Kanıt:**

guard.py:252 `HEAT_HARD_R = 4.5` — ne goal.yaml `limits` bloğunda (operatörün değişmez zarfı) ne bounds.yaml'da (arama uzayı). goal.yaml: max_open_positions=5, max_position_r=1.0 → tam boyutta 5. pozisyon 5,0R > 4,5R olduğu için YAPISAL olarak imkânsız; yani ilan edilen "5 pozisyon" tavanı tam-konviksiyonda 4'tür. Teorik değil, ÖLÇÜLDÜ: state/trade_plans.jsonl'da 390 satırın 18'i (%4,6) `portföy ısısı sert tavanı %4.5R aşıyor (~5.1R açık risk)` ile NO_GO — ör. 2025-02-28 T/ABBV/GILD (size_r 0,81-0,83). Aynı gün 'max 5 pozisyon dolu' 20 kez ayrı sebep olarak görünüyor, yani iki tavan farklı zamanlarda bağlıyor. Bu, mühendislik defterinde ADI KONMUŞ sınıfın (`broker.max_positions_at` rampa eşikleri "bounds'ta DEĞİL kodda sabit → hipotez uzayının körü") guard'daki İKİNCİ örneği; rampa örneği kayıtlı, bu değil (HEAT_HARD_R ROADMAP.md ve LOG'da hiç geçmiyor).

**Öneri:** HEAT_HARD_R/HEAT_REVIEW_R/CORR_REVIEW ya goal.yaml limits'e (operatör kalemi, değişmez) ya bounds.yaml'a (ölçülebilir knob) taşınsın; hangisi seçilirse seçilsin `max_open_positions × max_position_r` ile tutarlılığı bir testle çivilensin.

**Adversarial doğrulama:** İddianın kod çekirdeği ve yapısal sonucu DOĞRULANDI, ama denetçinin ölçüm çerçevesi şiddeti şişiriyor. Doğrulanan: `meridian/guard.py:252` `HEAT_HARD_R = 4.5` modül-düzeyi sabittir, yalnız 313-315'te okunur ve HİÇBİR kanaldan geçersiz kılınamaz — `classify_gate`'in `params` argümanı yalnız `entry.min_score` ile iki Y3 knob'una ulaşır (`_y3_portfolio_caps`), ve `tests/test_gate_statistics_v74.py:191-201` guard'ın içe aktarımlarını `{dataclasses, typing, config}` ile kilitlediği için dışarıdan enjeksiyon yolu yok. `state/goal.yaml:78-89` `limits` bloğunda ısı/open_risk anahtarı YOK (autonomy_level, max_position_r=1.0, max_open_positions=5, max_daily_loss_pct, max_sector_exposure_pct, no_trade_before_bars, kill_switch_file — hepsi bu kadar); `state/bounds.yaml:72`'deki `portfolio.heat_cap` FARKLI bir büyüklüktür (açık risk / NAV %, varsayılan 0 = kapalı, `_y3_portfolio_caps` bayrağı arkasında atıl) — yani R-tabanlı sert tavan gerçekten arama uzayının dışında. Aritmetik de tutuyor: `open_heat = open_risk_r + plan.size_r` ve `open_risk_r` NOMİNAL size_r toplamıdır, stop'a göre erimez (`meridian/loop.py:651-652`: `sum(p.size_r for p in b.positions.values()) + sum(a["size_r"] for a in meta["armed"])`), dolayısıyla 4×1,0R açık + 1,0R yeni = 5,0R > 4,5R → NO_GO; ilan edilen 5-pozisyon zarfı tam boyutta ERİŞİLEMEZ ve bu tam senaryo `tests/test_batch_k.py:9-18`'de beklenen davranış olarak çivilenmiş. 18/390 sayısı da birebir doğru (`gate_checks` içinde `heat_hard.passed=False` → tam 18; defter 2023-01-20 → 2026-07-28, sürüm 3-4). `HEAT_HARD_R`/"4,5R" ROADMAP.md, MERIDIAN_ENGINEERING_LOG.md, DESIGN.md, PRODUCT.md, README.md ve AUDIT-*.md'de HİÇ geçmiyor; sınıf ise LOG:103-105'te yalnız `broker.max_positions_at` için adlandırılmış — yani ikinci örnek gerçekten kayıtsız. DENETÇİNİN KAÇIRDIĞI: "18 planı NO_GO yaptı" nedensel değil, eşzamanlılık sayısıdır — 18'in 17'sinde AYNI planda başka bir sert kural da düşmüş (14× yalnız `max_open_positions`, 2× `max_open_positions`+`sector_cap`, 1× yalnız `sector_cap`), o planlar ısı tavanı hiç olmasaydı da NO_GO'ydu. Isı tavanının TEK bağlayıcı sert kısıt olduğu vaka defterde tam BİR tanedir: 2025-12-19 / C, heat 4,58R, open_positions=4. Yani marjinal etki %4,6 değil 1/390 = %0,26. Denetçinin verdiği örnekler (2025-02-28 T/ABBV/GILD, hepsi open_positions=5) bulgunun EN ZAYIF tarafı; asıl kanıt atlanmış olan tek vakadır, çünkü 4 açık pozisyonla 5.'yi reddederek tavanın ilan edilen zarfın ALTINDA bağladığını ampirik olarak gösteren tek satır odur. Ayrıca "tam-konviksiyon" senaryosu bugünkü boyutlandırıcıda hiç oluşmuyor (defterde size_r min 0,25 / medyan 0,79 / maks 0,97; 1,0R hiç yok), yani çelişki ilkesel olarak gerçek ama pratikte ~0,85R ortalamalarda bağlıyor. Sonuç: bulgu ayakta — sabit, operatörün değişmez zarfıyla yapısal olarak çelişiyor, arama uzayında yok ve adı konmuş sınıfın kayıtsız ikinci örneği; ama ölçülü etki tek plan, sabit muhafazakâr yönde (risk ekleyemez, yalnız keser) ve tek-kaynak/test-korumalı olduğu için yönetişim+hipotez-uzayı kusuru, korrektlik/güvenlik kusuru değil.

### C25 [şiddet 2 · veri-hatti] Kazanç takvimi: Nasdaq gün-başına hatalar sessizce yutuluyor ve KISMİ pencere 'başarı' sayılıyor — FMP yedeği yalnız TAM boşlukta devreye giriyor

- **Yer:** meridian/adapters/data.py:2496-2508 · meridian/earnings.py:191-199
- **Sınıf:** sessiz fail-open + yorum-davranış sürüklenmesi

**Kanıt:**

`nasdaq_earnings_window` her günü ayrı istek yapıyor ve gün bazında yutuyor:
```
except Exception:  # sessiz-yutma: ... çağıran boş sonuç üzerinden yedek kaynağa düşer
    pass
```
Ama çağıran (`earnings.refresh`) yedeğe YALNIZ `if not rows:` iken düşüyor — yani 20 günün 19'u patlasa ve 1'i satır döndürse yol 'başarılı' sayılır, FMP hiç denenmez ve kaç günün kaçırıldığı HİÇBİR yerde sayılmaz (`earnings_refreshed` olayı yalnız new_rows/total basıyor). Yutma gerekçesi bu yüzden kısmi durumda FAKTİK OLARAK YANLIŞ. Asimetri kanıtı: kardeş yol `refresh_from_fmp` aynı riski `earnings_refresh_partial` + önbellekten koruma ile açıkça ele alıyor (earnings.py:264-276); Nasdaq yolunda karşılığı yok. Olasılık spekülatif değil: bu depo api.nasdaq.com'dan bugün HTTP 403 aldığını kendi kaydında yazıyor (ROADMAP: `loop._universe_drift_check bugün HTTP 403 yüzünden 'unknown' dönüyor`). Sonuç: yeni duyurulan bir kazanç tarihi kaçan gün-diliminde kalırsa `in_blackout` o sembolde False döner (veri yokken fail-open) ve motor bilanço gününe pozisyonla girer — modülün kendi deyimiyle 'BU SESSİZLİK PARA KAYBETTİRİR'. NOT: ROADMAP §7'de kayıtlı olan borç 'haftalık kadans kaçarsa marj 0'a iner' (ileri pencere 14→21 ile kısmen kapatıldı); bu bulgu ONDAN AYRI — kadans kaçmasa bile tek turda kısmi kapsama üretiyor.

**Öneri:** `nasdaq_earnings_window` başarılı/başarısız gün sayısını döndürsün ve `refresh` kapsama eksikse (ör. istenen iş günlerinin <%90'ı cevapladıysa) FMP yedeğine düşsün + `earnings_refresh_partial` muadili bir olay yazsın.

**Adversarial doğrulama:** Çürütemedim — iddia edilen davranış kodda birebir var ve başka hiçbir yerde kablolu değil. `meridian/adapters/data.py:2496-2509`: `nasdaq_earnings_window` `pd.bdate_range` üzerinde gün-başına döner, her günü `try/except Exception: pass` ile sarar, hiçbir başarısızlık SAYACI tutmaz ve yalnız `out` döner; yani "20 iş gününün 19'u düştü" ile "hepsi geldi" çağırana AYNI şekilde görünür. `meridian/earnings.py:191-199`: `rows` evren filtresinden sonra BOŞ değilse `src="nasdaq"` kalır, dosya yazılır, `_snapshot` düşer ve `obs.log("earnings_refreshed", new_rows=…, total=…, time_known=…)` basılır — kaç gün-diliminin kaçtığına dair alan yok; FMP yedeği (`if not rows`) ve YASA-4 uyarısı `earnings_refresh_empty` YALNIZ TAM boşlukta tetiklenir. Dolayısıyla satır-içi yutma gerekçesindeki "çağıran boş sonuç üzerinden yedek kaynağa düşer" cümlesi kısmi durumda faktik olarak yanlıştır (YASA 4 ihlali), ve kardeş yollardaki karşılığı gerçekten yok: FMP yolu `earnings_refresh_partial` + önbellekten koruma basıyor (earnings.py:262-276), `adapters/insider.py` aynı "sayfa/gün başına istek" desenini `tavana_carpti` + gerçek çağrı sayısıyla dürüstçe raporluyor. Yukarı akıştaki hiçbir bekçi bu deliği kapatmıyor: `scheduler.py:724-763` `n > 0` görünce `watchdog.beat("earnings_refresh")` atıp hafta damgasını tüketiyor (kısmi tur "başarı" sayılıyor, `earnings_calendar_gave_up` hiç konuşmuyor); `watchdog.py:203-208` yalnız `coverage()["inert"]` (future_dates == 0) yani TAM körlüğü yakalıyor; `coverage()["ileri_gun"]` birleştirilmiş takvimdeki MAKSİMUM tarihten türediği için önceki haftaların satırlarıyla sağlıklı görünürken bu turda kaçan gün-dilimleri görünmez kalıyor; `known()`/`COVERAGE_NOTE` (earnings.py:436-442, loop.py:684-686) yalnız sembolün HİÇ tarihi yokken not düşüyor, "sembol biliniyor ama yeni duyurulan tarih kaçtı" hâlinde sessiz. `tests/test_wpd_kalanlar_v147.py` ve `test_earnings_audit_v26.py` içinde gün-bazlı kısmi başarısızlığı beyan eden ya da çiviyle sabitleyen TEK bir test yok; `integrity_registry.py:136-142`'deki earnings kaydı da yalnız E1/E2'yi (kapsam damgası + bayat takvim) sayıyor. Bulguyu ZAYIFLATAN iki bağlam (şiddeti düşürme gerekçem): (1) Nasdaq yolu FMP'nin aksine dosyayı EZMEZ, `prev` ile BİRLEŞTİRİR (earnings.py:220-224) — kısmi hata veri KAYBETTİRMEZ, yalnız YENİ satırı kaçırır ve kaçan gün [bugün-7, bugün+21] penceresinde kaldığı sürece bir sonraki haftalık tazelemede kendini onarır; zarar ancak kaçan rapor tarihi bir sonraki tazelemeden önce karartma penceresine girerse gerçekleşir. (2) Denetçinin kanıt olarak gösterdiği HTTP 403 kalıcı bir arızadır ve `_get_json`'ın 3 denemesiyle birlikte TÜM günleri düşürür → `rows` boş → FMP yedeği + `earnings_refresh_empty` yani ZATEN ele alınmış yol; kısmi hâl için tur-ortası kısılma/geçici zaman aşımı gerekir ve bunun bu uçta gerçekleştiği ÖLÇÜLMEMİŞ. Yine de mekanizma ve görünürlük boşluğu kesindir: kaçan gün sayısı hiçbir yerde sayılmadığı için o ölçüm bugün YAPILAMAZ; ucuz düzeltme, adaptörün kaçan gün sayısını döndürüp `refresh`in bunu `earnings_refreshed`e alan olarak basması (ve eşik üstünde yedeğe düşmesi/uyarması) olurdu.

## Çürütülen bulgular (doğrulayıcı reddetti — kayıt amaçlı)

### [olcum-sasisi] `mtm_dd_veto` MUTLAK düşüşü GÖRELİ marja kıyaslıyor: `ihlal` ölçülen 4 kolun 4'ünde de True — kartın kill ölçütü ayırt edici değil

**Çürütme:** Mekanik çekirdek doğru ama denetçinin kurduğu zarar zinciri üç ayrı yerden kopuyor. (1) BEYAN: backtest.py:454-469 bloğun başında "BU BLOK HÜKÜM VERMEZ — adı 'veto' ama kapı değildir, VETONUN EŞİĞİNE GÖRE OKUNAN BİR RAPORDUR... Kapının kendi düşüş vetosu (reflect._gate_eval, aday↔incumbent FARKI üzerinden) DEĞİŞMEDİ ve bu blok ona hiçbir girdi vermez" diyor; eşiğin DD_VETO_MARGIN'den gelmesinin gerekçesi de açıkça yazılı ("ikinci bir eşik tanımlamak aynı yasanın ikinci uygulaması olurdu"). Dönüş sözlüğü `hukum_verir: False`, `esik_kaynagi` ve `kapsam` alanlarını taşıyor (backtest.py:485-491) — yani mutlak-vs-fark okuması kaçırılmış bir semantik hata değil, adı konmuş bir tercih. (2) TEST KORUMASI + TÜKETİCİSİZLİK: tests/test_para_v3_borclari_v145.py:336-343 `test_dusus_vetosunun_KENDISI_bu_bloktan_girdi_ALMAZ` çivisi `_gate_eval` kaynağında "mtm_dd_veto" geçmesini yasaklıyor; :330-334 bloğun walk_forward'da tek kez ve yalnız dönüş sözlüğünde çağrıldığını çiviliyor. Depo genelinde `ihlal` alanını okuyan HİÇBİR tüketici yok: grep sonucu tüm referanslar backtest.py:474/796 (üretim), testler, ROADMAP.md:424 ve iki rapor dosyası — API/pano/kapı yolu sıfır. (3) İDDİANIN ÇEKİRDEĞİ ZAMANSAL OLARAK İMKÂNSIZ: bayrağın "EDG-2026-003 kill ölçütünün tek sayısal dayanağı" olduğu ve "rampa lehine dejenere karar ürettiği" savı ters yönde — kart 2026-07-31 03:19'da `status: measured` damgalanmış ve kill#2 hükmü rampa-açık↔rampa-kapalı FARKINDAN okunmuş ("serbest bırakınca incumbent DD %4,6→%9,7"), blok ise ROADMAP.md:421-424'e göre 2026-08-01 ~09:45'te inmiş (PARA-v3 ①② turu); bloğun kendi yorumu da bunu söylüyor: o bulgu "ancak ÖLÇÜM BETİĞİYLE elde ediliyordu (EDG-2026-003 kill#2 tam olarak böyle ölçüldü)". Bloğu gerçekten tüketen kart BASE-2026-001'dir ve o kartın `success_metric`i "EŞİK YOK", `kill_criteria`sı "yok (karne kartı)"dır; Rol-1'in karne hükmü metninde mtm_dd_veto'ya dayanan tek bir cümle yok. (4) SAHA KANITI ŞİŞİRİLMİŞ: "4 kolun 4'ü" bağımsız değil — sonuc.json'da tam_guncel, butunluk_dislamasiz ve tam_guncel_pk aynı `digest_tum: ba44ae93…` ve aynı `equity_digest: 8039eb1e…` değerini taşıyor (bütünlük-dışlaması etkisi kartta "tanımlı-sıfır" beyanlı, _pk zaten determinizm pozitif kontrolü); yani iki farklı eğri var (0,072 ve 0,0708) ve ikisi de 0,04'ün üstünde — bu bayrağın bozukluğu değil, sistemin M2M düşüşünün ~%7 olmasıdır, ayrıca raporun bilgi taşıyan alanı zaten ham sayı ile ihlal günü sayısıdır (447'ye karşı 693 — kollar arasında AYIRT EDİYOR). Geriye kalan tek meşru çekirdek etiket düzeyinde kalıyor: mutlak okuma için doğal çıpa state/goal.yaml:13'teki `max_drawdown: 0.08` bütçesi olurdu ve onunla 0,072 "temiz" okunurdu — yani `ihlal` boolean'ı olduğundan alarmist görünüyor; ama bu, hükme girmeyen, kaynağı alanla açıklanmış, testle kapıdan yalıtılmış bir rapor alanının okunabilirlik nüansıdır, ölçüm bütünlüğü kusuru değil.

## Alt-sistem özetleri + hafif bulgular (şiddet ≤2, doğrulanmamış)

### veri-hatti (toplam bulgu: 10)

Veri hattının kapıları (takvim/hayalet, karantina-v2, bütünlük defteri, kaynak damgası, dikiş kuralı) gerçekten ölçüm-tabanlı ve çoğu yerde beyanlı; bulguların hiçbiri bu kapıların TASARIMINDA değil, kapıların ETRAFINDAKİ hata yollarında ve beyanların kodla uyuşmadığı satırlarda. En ağır iki bulgu canlı-tekrarlanabilir: (1) aynı-akşam bacağı sembolleri cevap gelmeden 'soruldu' işaretlediği için tek bir kısmi/başarısız Alpaca isteği o seansın bacağını süreç ömrü boyunca öldürüyor — bacağın kapatmak için yazıldığı 'sahte SEANS ATLANDI' sınıfını geri getiriyor; (2) kazanç takvimi Nasdaq gün-başına hatalarını sayısız yutup KISMİ pencereyi başarı sayıyor ve FMP yedeği yalnız TAM boşlukta devreye girdiği için karartma kapısı sessizce fail-open kalabiliyor. Geri kalanlar orta/düşük şiddette beyan-davranış ayrışmaları (barrepair'in `dataset.load_cached` iddiası, gap_scan'in tatil-bilmeyen beklentisi, kazanç takviminde iki farklı birleştirme semantiği, kopyala-yapıştır YASA-4 gerekçeleri) ve bir bellek-defteri sızıntısı.

**Sağlam bulunanlar (denetçi özellikle doğruladı):**

- barsarchive._consume'un dayanıklılık sırası GERÇEKTEN doğru: fsync → XACK, yazım patlarsa ACK ATILMIYOR (giriş PEL'de kalıyor) ve tekilleştirme kümesi süreç belleğinden değil DİSKTEN kuruluyor (_DayIndex.__init__). 'En-az-bir-kez teslim + idempotent yazım' iddiası kodla birebir tutuyor; ayrıca NOGROUP onarımı (TTL'li anahtarda hafta sonu grup ölümü) sayaçlı ve uyarılı — 'kendini onardı' cümlesi ölçülebilir kılınmış.
- data._calendar_scan'in KİTLESEL UYUŞMAZLIK REDDİ, bir bütünlük kapısının nadiren yaptığı şeyi yapıyor: seri büyük oranda takvim dışıysa kapı hiçbir satırı düşürmüyor, hükmü operatöre bırakıyor ve reddin KENDİSİNİ rapora ayrı kalem olarak yazıyor ('0 hayalet satır' ile 'kapı adjudike etmedi' karışmıyor). Eşik seçilmemiş, iki dağılımın arasına ölçülerek konmuş (en yüksek gerçek pay %0,1233 vs bdate_range serilerinde %3,3-5).
- Sır sızıntısına karşı iki sessiz ama doğru karar: `data.FetchError` URL'i BİLEREK taşımıyor (FMP anahtarı sorgu parametresinde gidiyor ve httpx hata metni tam URL içeriyor), `alpaca._paper_base` ise alt-dize yerine HOSTNAME karşılaştırması + şema yükseltmesi yapıyor — ikisi de 'bir gün loglanırsa' senaryosunu önceden kapatmış.

**Hafif bulgular:**

#### [ş2] Bacak isteği PATLADIĞINDA `outcomes` defterine 'empty' yazılıyor — modülün kendi 'HATA ≠ BOŞ' yasası deliniyor ve sembol delist adayı sayacına giriyor

- **Yer:** meridian/adapters/data.py:1866-1880 (ve hüküm: 1904-1906) · **Sınıf:** yorum-davranış sürüklenmesi / hata-yolu sağlamlığı

```
try:
    adf, _leg_src = _fetch_alpaca_session(ticker, _leg)
except Exception as e:
    adf = pd.DataFrame()
    _bar_warn("same_evening_leg_failed", e, ticker=ticker.upper())
...
if adf is None or adf.empty or not _leg_src:
    outcomes["alpaca"] = "empty"
```
Zincirin diğer kolları hatayı `error:{reason}` diye ayrı kaydediyor ve hüküm tam bu ayrıma dayanıyor: `errored = [n for n,o in outcomes.items() if o.startswith("error")]` → `verdict = "source_error" if errored else "symbol_unknown"`. Bacak kolu hiçbir zaman `error:` yazmadığı için, diğer kaynakların hepsi temiz-boş döndüğü ve bacağın İSTEĞİ patladığı turda hüküm `symbol_unknown` çıkar; `_record_no_data` serisi artar ve 5. turda `DATA_QUALITY` alarmı 'evren bakımı gerekiyor olabilir' der — yani ağ arızası 'sembol öldü' kanıtına dönüşür. Bu, satır 1893-1903'te uzun uzun anlatılan ve düzeltildiği söylenen hatanın aynısıdır. Ayrıca `_bar_warn` olay ADI başına bir kez sustuğu için 250 sembolde bacak patlasa defterde TEK uyarı kalır. Anahtar adı da tutarsız: başarıda `outcomes[_leg_src]` ('alpaca_iex'/'alpaca_sip'), arızada sabit 'alpaca'.

**Öneri:** Bacak kolunu da zincirin sözleşmesine sok: istisna → `outcomes[_leg_src or 'alpaca'] = f"error:{type(e).__name__}"`, gerçek boş cevap → 'empty'.

#### [ş2] `bars_integrity` yazımının operatöre bastığı olay, `dataset.load_cached`in güvensiz dönemi dışladığını söylüyor — kod tam tersini beyan ediyor

- **Yer:** meridian/barrepair.py:274-277 (karşı-kanıt: meridian/adapters/data.py:557-568) · **Sınıf:** yorum-davranış sürüklenmesi (dışa dönük yanlış beyan)

barrepair.integrity_apply olayı:
```
detail="ölçüm yolları (component_ic, cf_backfill, dataset.load_cached) artık "
       "güvensiz dönemi DIŞLIYOR; türetilmiş artefaktlar YENİDEN ÜRETİLMELİ"
```
`data.measurement_bars` docstring'i ise aynı iddiayı ölçülmüş gerekçeyle REDDEDİYOR: '`dataset` YOLU BİLEREK BAĞLANMADI … YALNIZ `load_cached()`e bağlamak DAHA KÖTÜ olurdu … Yani walk-forward/prescreen/reflect tabloları HÂLÂ kirli dönemi görüyor.' Kod bunu doğruluyor: `measurement_bars` çağıranları yalnızca component_ic.py:346,371 ve cf_backfill.py:135,148 (repo geneli grep); `dataset.load_cached` (dataset.py:285-302) yalnız `sanitize_bars` + `_window` çağırıyor. Yani onarımın kendi olayı, kirli kalan tabloları temiz ilan ediyor — bu deponun UYDURMA YASAĞI'nın hedeflediği türden bir iddia, üstelik türetilmiş artefaktların yeniden üretilip üretilmeyeceğine karar veren satırda.

**Öneri:** Olay metninden `dataset.load_cached`i çıkar ve hangi yolların HÂLÂ güvensiz dönemi gördüğünü aynı satırda açıkça yaz.

#### [ş2] Seans-içi boşluk dedektörü beklentiyi tatil/yarım-gün bilmeyen takvimden üretiyor — erken kapanış günlerinde sahte 'akış kesintisi' alarmı

- **Yer:** meridian/barsarchive.py:592-597 · meridian/barclock.py:106-117 · **Sınıf:** canlı-vs-replay ayrışması / alarm dürüstlüğü

`gap_scan` beklenen dakikaları şöyle kuruyor:
```
while m < bitis:
    if barclock.is_market_open(m):
        beklenen.append(m)
```
`barclock.is_market_open` ise kendi docstring'inde 'TATİLLER hariç (yaklaşık)' diyor ve bunu şu gerekçeyle meşrulaştırıyor: 'Alpaca zaten kapalıyken bar göndermez, o yüzden bu yalnız bir kolaylık kapısıdır'. Bu gerekçe `is_admissible` için doğru, `gap_scan` için TERSİNE ÇEVRİLMİŞ: gap_scan'in semantiği 'bar yoksa kesinti var'dır, yani barın gelmemesi tam da alarm sebebidir. Somut vaka NYSE yarım günleri (13:00 ET kapanış — Şükran ertesi, 24 Aralık, 3 Temmuz): o gün dosya VAR (sabah barları yazılmış), 13:03-16:00 ET arasında pencere tamamen kapanış sonrasına düşer, `beklenen` ~58 dakika üretir, `dolu` boştur → `tur="akis"` boşluğu → scheduler.py:537 `intraday_gap_detected` uyarısı ('mrd:bars bir RING'tir, o dakikalar geri gelmez'). Tam tatil günü 'arsiv_yok' ile kurtuluyor, yarım gün kurtulmuyor. Doğru takvim zaten bağımlılıkta ve iki yerde kullanılıyor (data._sessions → XNYS, scheduler._leg_ready → mcal.schedule ile GERÇEK market_close).

**Öneri:** gap_scan beklenen dakikaları XNYS `schedule()`ün o güne ait market_open/market_close aralığından üretsin (takvim okunamazsa `durum="takvim_yok"` ile hüküm vermesin).

#### [ş2] Kazanç takvimi birleştirme semantiği iki dalda farklı; ikisi de yanlış tarafta ve `refresh` docstring'i yalnız birini anlatıyor

- **Yer:** meridian/earnings.py:178-187 (beyan) · 220-232 (Nasdaq dalı) · 262-293 (FMP dalı) · **Sınıf:** yorum-davranış sürüklenmesi / veri bütünlüğü

Beyan (refresh docstring): 'Sonuç MEVCUT csv ile BİRLEŞTİRİLİR (kaynak değişimi geçmiş çapaları silmez).' Gerçek: (a) NASDAQ DALI yalnız EKLER — `merged` önceki tüm (ticker,date) çiftlerini taşır ve hiçbir tarihi DÜŞÜRMEZ. Kazanç tarihi revize edilip kaydığında ESKİ tarih dosyada kalır ve `in_blackout` hayalet bir tarih için de karartma uygular; `coverage().future_dates` şişer. Modülün kendi PIT bloğu (301-311) 'Kazanç takvimi, borsanın en çok REVİZE EDİLEN takvimlerinden biridir (tarih kayar…)' diyerek olguyu kabul ediyor ama yol revizyonu asla EMEKLİYE AYIRMIYOR. (b) FMP DALI ise tersine DEĞİŞTİRİR: `rows` yalnız FMP'nin döndürdükleri + `failed` ticker'ların önbellek satırları; başarıyla çekilen bir ticker'ın FMP'de olmayan (Nasdaq'tan birikmiş) tarihleri `for t, d in sorted(set(rows))` yazımında sessizce DÜŞER. Yani docstring'in 'kaynak değişimi geçmiş çapaları silmez' cümlesi tam da kaynak değişimi anında ihlal ediliyor ve `days_since_report` (strategy.py:641, 794 PEAD çapası) o sembollerde köreliyor.

**Öneri:** Tek bir birleştirme yasası yaz (her iki dal aynı fonksiyondan geçsin) ve ileri tarihlerde revizyonu emekliye ayırma kuralını ekle: tazeleme penceresi İÇİNDE kalan ve kaynağın artık bildirmediği gelecek tarihler düşülsün, pencere dışı geçmiş çapalar korunsun.

#### [ş2] `earnings._load` OKUMA HATASINI dosyanın mtime'ıyla birlikte önbelleğe alıyor — karartma kapısı 'bu tur' değil, mtime değişene kadar kapalı kalıyor ve uyarı bir kez basılıyor

- **Yer:** meridian/earnings.py:64-111 (özellikle 71-72 ve 95-110) · **Sınıf:** hata-yolu sağlamlığı / YASA 4 kör noktası

```
mtime = path.stat().st_mtime
if mtime == _CACHE_MTIME:
    return _CACHE                 # ← hata sonucu da buraya sığınıyor
...
except Exception as e:
    obs.warn("earnings_calendar_unreadable", ..., detail="karartma penceresi bu turda DEVRE DIŞI")
    out, times = {}, {}
_CACHE, _CACHE_MTIME, _TIMES = out, mtime, times     # BOŞ sonuç, dosyanın mtime'ıyla ÇİVİLENDİ
```
Uyarı metni 'bu turda' diyor; davranış 'dosya yeniden yazılana kadar' (kadans haftalık). Sonraki her `_load()` erken dönerek ne yeniden dener ne yeniden uyarır → kalıcı bir arıza tek satır olay üretir, `in_blackout` herkes için False döner ve `coverage()` de aynı boş önbelleği okuduğu için `inert=True` sinyali panoya taşınsa bile alarm tekrarlanmaz. İkinci etki: haftalık `refresh` de `prev = _load()` ile aynı boş sözlüğü alır, `merged` yalnız yeni satırlardan kurulur ve birikmiş takvim `os.replace` ile DİSKTEN silinir.

**Öneri:** Hata dalında `_CACHE_MTIME`i None bırak (bir sonraki çağrı yeniden denesin) ve okunamayan takvimi `refresh`in birleştirme tabanı olarak kullanma.

#### [ş2] Kopyala-yapıştır YASA-4 gerekçeleri iki kritik yolda yolu YANLIŞ anlatıyor ('yardımcı/telemetri yolu; başarısızlığı karara girmez')

- **Yer:** meridian/adapters/data.py:2347 (load_many) · meridian/adapters/data.py:2261 (_corporate_action) · **Sınıf:** YASA 4 kör noktası / yorum-davranış sürüklenmesi

(1) `load_many` — evrenin TEK yükleme yolu:
```
try:
    df = load_bars(t, start, end, use_cache=use_cache)
except Exception:  # sessiz-yutma: yardımcı/telemetri yolu; başarısızlığı karara girmez ...
    df = pd.DataFrame()
```
Bu yol ne yardımcıdır ne telemetridir: ticker evrenden düşer (`failed`), istisnanın TÜRÜ ve METNİ tamamen kaybolur, geriye yalnız `universe_shrunk` içindeki isim listesi kalır — yani 'kaynak yok' ile 'kodda hata/bozuk CSV' ayırt edilemez (tam olarak `bar_sources_all_empty`in `verdict` alanıyla çözülen sınıf).
(2) `_corporate_action` — aynı boilerplate, ama dönen False bir KARARDIR: bölünme algılanmaz, önbellek sıfırlanmaz, `_bump_wf_rev()` çağrılmaz ve iki farklı düzeltme ölçeği `keep="last"` ile birbirine EKLENİR — D1 dikiş bloğunun (2151-2157) canlıda yaşandığını yazdığı hata. Yani 'başarısızlığı karara girmez' cümlesi yanlış: fail-open tarafında karara girer.

**Öneri:** İki yerde de gerçek yolu anlatan gerekçe yaz ve istisnayı `_bar_warn` ile ADIYLA kaydet (load_many'de ticker + hata türü, _corporate_action'da ölçek-kararının kanıtsız verildiği).

#### [ş1] İki modülün başlık docstring'i emekli mimariyi anlatıyor — dosyayı ilk açanın okuduğu ilk cümle yanlış

- **Yer:** meridian/adapters/data.py:1-6 · meridian/earnings.py:1-6 · **Sınıf:** yorum-davranış sürüklenmesi

data.py başlığı: 'Primary: Cboe delayed_quotes historical … Fallback: Nasdaq historical API.' Gerçek zincir `fetch` içinde: massive → massive_hist → fmp → cboe → nasdaq → aynı-akşam Alpaca bacağı (data.py:1779-1816, 1862-1886); Cboe artık dördüncü kol, birincil değil ve bacak/kaynak damgası mimarisi başlıkta hiç geçmiyor. earnings.py başlığı: 'If state/earnings.csv exists (rows: ticker,date …)' ve '…simply activates the day an earnings feed (FMP) writes the file' — dosya 2026-08-01'den beri `ticker,date,time` başlıklı ÜÇ sütunlu (earnings.py:229) ve birincil kaynak Nasdaq, FMP yedek (earnings.py:179-182).

**Öneri:** İki başlığı bugünkü zincire ve dosya şemasına göre güncelle (gövdedeki ayrıntılı bloklar zaten doğru — yalnız başlıklar bayat).

#### [ş1] `_ALPACA_MEMO`/`_ALPACA_ASKED` seans başına sınırsız büyüyor — kardeş sızıntı (`massive._MEM_SNAPSHOT`) bulunup kablolandı, bu ikisi atlandı

- **Yer:** meridian/adapters/data.py:1094-1095 · **Sınıf:** ölü/eksik kablolama (kaynak sızıntısı)

`_ALPACA_MEMO: dict = {}` ve `_ALPACA_ASKED: dict = {}` seans anahtarlı ve repo genelinde temizleyen ÜRETİM çağıranı yok (grep: yalnız iki test fikstürü). 7/24 koşan worker'da her seans ~253 sembollük bir bar sözlüğü + bir ad kümesi ekleniyor ve hiç boşalmıyor. Aynı tur içinde scheduler'a `massive.reset_cache()` çağrısı TAM BU GEREKÇEYLE eklendi (scheduler.py ~611: 'memo {tarih: {~12.400 sembol: bar}} … aylarca koşan bir süreçte bu, hiç boşalmayan bir bellek defteridir'), ama aynı yeni-seans kancasında bu iki defter unutuldu. Büyüklük massive'inkinden çok küçük (dolayısıyla şiddet 1), fakat sınıf aynı ve temizlik noktası zaten hazır.

**Öneri:** Yeni seans kovalaması başlarken (scheduler'ın `massive.reset_cache()` çağrısının yanında) geçmiş seansların `_ALPACA_MEMO`/`_ALPACA_ASKED` girdilerini düşür.

### karar-cekirdegi (toplam bulgu: 9)

Karar çekirdeğinin beş dosyası (strategy/score/guard/broker/indicators) satır satır okundu; zincirin matematiği ve "ölçülemeyen None kalır" disiplini beklediğimden sağlam çıktı, kırılganlık ise KABLOLAMA katmanında toplanıyor — kural tek yerde yazılı ama motorlar onu farklı girdilerle besliyor. En ağır üç bulgu aynı sınıfın örnekleri: E1 giriş-limit yasasında `atr`ı yalnız canlı motorun geçirmesi (replay %1 tavanıyla, canlı min(0,5·ATR,%1) ile doluyor — 322.635 gerçek barın %37,9'unda sınırlar ayrışıyor), Y3 portföy tavanlarının bounds'ta aranabilir ama hiçbir çağıranın gerekli portföy alanlarını göndermemesi yüzünden yapısal olarak bağlayamaması, ve `exit.early_kill_pivot`ın replay'de ateşleyip canlıda sessiz no-op olması. Ayrıca operatörün goal.yaml zarfında görünmeyen 4,5R'lik sert ısı tavanı canlı defterde 390 planın 18'ini gerçekten NO_GO yapmış — yani gizli eşik teorik değil, bağlayıcı.

**Sağlam bulunanlar (denetçi özellikle doğruladı):**

- broker._touch_exit'in iki kademeli çıkış sırası (açılış KESİN, bar-içi belirsiz → stop önce) ve scale_out'un "hedef önce / stop-önce muhafazakârlığı" dalları: ikisi de sayısal kanıtla gerekçelendirilmiş (1,6R→3,2R ve 2,0R yerine 3,0R yazılan gerçek vakalar, CF-2025-10-28-SWKS dahil) ve mutasyon aramasının bedava-R üretecek kombinasyonu (scale_out_r > profit_target_r) bilinçli olarak kapatılmış — denetimde en sık bulunan 'iyimser bar-içi muhasebe' hatası burada zaten avlanmış.
- indicators.trend_template'in 'bilinmiyor' maskesi sabit bir bar sayısından değil SERİLERİN kendisinden türetiliyor (`unknown = sma50.isna() | ... | lo_52w.isna()`), yani TREND_TEMPLATE_WARMUP değişse bile maske otomatik tutarlı kalıyor; aynı dosyada volume_ratio'nun paydasından bugünün çıkarılması ile rvol20'nin paydaya bugünü DAHİL etmesi arasındaki tersliğin gerekçesi (biri sert eşiğin böleni, öteki ölçülmüş bir ölçek) yazılı — 'aynı kavram iki tanım' tuzağı bilerek ve beyanla yönetilmiş.
- guard.validate_change'in `base@regime` dalı, override'ı sessizce düşecek iki vakayı (base canlı params'ta yok / base `regime.*` ile başlıyor) DÜRÜSTÇE reddediyor ve gerekçesinde 'aday incumbent'la birebir replay olur, deftere sahte kalıcı çıkmaz sokak yazılır' diyor — depoda yapısal-etkisizliğe karşı doğru refleksin şablonu bu; ayrıca DISCIPLINE_MIN_RR=2.0 iddiasının dayanağı (state/bounds.yaml `exit.profit_target_r min: 2.0`) kontrol edildi ve gerçekten tutuyor, yani 'taban tavanı geçemez' cümlesi bir varsayım değil doğrulanmış bir sözleşme.

**Hafif bulgular:**

#### [ş2] `guard.check_trade`ın üretimde hiçbir çağıranı yok ama modül başlığı onu "çalışma-zamanı risk zarfı yüzeyi" diye ilan ediyor; ayrıca REVIEW dalında soft gerekçeleri sessizce düşürüyor

- **Yer:** meridian/guard.py:1-7,226-244 · **Sınıf:** ölü mekanizma + yorum-davranış sürüklenmesi

Çağıran taraması (meridian/ + tests/): `guard.check_trade(` yalnız tests/test_guard_audit_v27.py ve tests/test_gate_statistics_v74.py'de; meridian/ içinde tek eşleşme tanımın kendisi. Canlı/replay/cf/gölge yollarının hepsi doğrudan `classify_gate` çağırıyor (loop.py:666, backtest.py:323, cf_backfill.py:94, shadow_variants.py:251, mutation.py:171). Buna rağmen guard.py:4-6 "Two surfaces: ... check_trade(plan, portfolio, regime, goal) — runtime risk-envelope check for a would-be entry" diyor. Ek olarak dönüş satırı `return TradeVerdict(not extra, extra)`: verdict=="REVIEW" ve extra boşken ok=True, reasons=[] döner — classify_gate'in ürettiği soft gerekçeler (ısı, korelasyon, lider-sektör, skor bandı) tamamen kaybolur. Bugün zararsız çünkü kimse çağırmıyor; yeniden kablolanırsa REVIEW bayrakları sessizce yok olur.

**Öneri:** Başlıktaki "iki yüzey" cümlesi "check_trade = classify_gate'in parite koşum yüzeyi (üretim çağıranı yok)" olarak düzeltilsin ve REVIEW dalı soft gerekçeleri de taşısın (`TradeVerdict(not extra, list(reasons)+extra)`).

#### [ş2] Kısmi satışta bankalanan K/Z `pnl_dollars`/`r_multiple`e katılıyor ama `pnl_pct`e KATILMIYOR — alfa/beta ve gece/gündüz atribüsyonu eksik getiri okuyacak

- **Yer:** meridian/broker.py:431-435,465-477,506 · meridian/analytics.py:591,4057 · state/bounds.yaml:20 · **Sınıf:** ölçüm doğruluğu / gizli-uyanan hata

scale_out docstring: "The banked P&L is folded into the SINGLE final closed-trade row (one trade, qty-weighted R)". Gerçekte close_position `pnl = pnl_remaining + pos.banked_pnl` (satır 500) ve `r_multiple = pnl/risk_dollars` bankalananı içerir, ancak satır 506 `"pnl_pct": round((exit_fill - pos.entry)/pos.entry, 5)` YALNIZ kalan bacağın yüzdesidir — bankalanan bacak ve azaltılmış qty hesaba girmez. Tüketiciler pnl_pct'i "işlemin getirisi" olarak okuyor: analytics.py:591 (SPY kıyaslı alfa/beta atribüsyonu, yöntem notu satır 550: "işlem getirisi = pnl_pct") ve analytics.py:4057 (E4 gece/gündüz ayrıştırması). Bugün gizli çünkü `exit.scale_out_frac` varsayılanı 0; ama bounds.yaml:20 aralığı 0-0.75 ve gölge V6 kolu zaten frac=0.5 ile koşuyor — knob açıldığı gün atribüsyon sessizce kayar.

**Öneri:** pnl_pct ya bankalanan bacağı da içeren ağırlıklı getiri olarak hesaplansın ya da adı/beyanı "kalan bacağın getirisi" diye netleştirilip atribüsyon tüketicileri pnl_dollars/başlangıç-notional'a çevrilsin.

#### [ş2] `score._span_days` iki zaman damgası bulamazsa 30 güne SESSİZCE düşüyor — aynı fonksiyonun istisna dalı YASA-4 gereği uyarı basıyor, veri-yok dalı basmıyor

- **Yer:** meridian/score.py:37-57 · meridian/rollback.py:124-125,312-313 · **Sınıf:** YASA 4 kör noktası / asimetrik sessiz varsayılan

satır 43-45: `if len(ts) < 2: return 30.0` — hiçbir kayıt, hiçbir uyarı. Hemen altında satır 52-57 aynı 30.0 fallback'i için `_span_warn` çağrılıyor ve gerekçesi yazılı: "burası SESSİZ KALAMAZ — süre, yıllıklandırmanın paydasıdır... skor sistematik olarak kayar ve hiçbir yerde hata görünmez". İki dal aynı hasarı üretir, biri konuşur biri susar. Ulaşılabilirlik: `score.score()` (span_days almayan tek genel sarmalayıcı) rollback.py:124/312'den kapı kararına girer; ts_open/ts_close taşımayan (sentetik/gölge/eski şema) satır kümesi gelirse realized_30d = total_return olur ve 'ölçülemedi' hiçbir kanala düşmez.

**Öneri:** `len(ts) < 2` dalı da `_span_warn` benzeri bir beyanla (obs.warn + neden) dönsün — sayı aynı kalsın, sessizlik kalksın.

#### [ş1] İki kurulumda `stop_mult` yerel değişkeni hesaplanıp hiç kullanılmıyor — `stop_loss_atr_mult` knob'unun o stopları şekillendirdiği izlenimi veriyor

- **Yer:** meridian/strategy.py:479 · meridian/strategy.py:602 · **Sınıf:** ölü kod / yanıltıcı knob izi

strategy.py:479 `stop_mult = _f(params, "stop_loss_atr_mult", 2.0)` — evaluate_entry'de stop satır 483'te `structural_stop(...)` ile kurulur ve o fonksiyon knob'u KENDİ okur; yerel değişken hiçbir yerde okunmaz. strategy.py:602 aynı satır evaluate_momentum_burst'te; oradaki stop satır 609 `float(low.iloc[-1]) - 0.25*a` yani knob'a hiç bağlı değil. Karşılaştırma: aynı satır pullback (545→548), episodic_pivot (661→664) ve canslim'de (964→967) GERÇEKTEN kullanılıyor — yani okuyucu için beşi ayırt edilemez hâlde.

**Öneri:** İki ölü atama silinsin; momentum_burst'ün stopunun knob'a bağlı OLMADIĞI (patlama barı dibi = yapı çizgisi) tek satır yorumla yazılsın.

#### [ş1] `ARMED_SETUPS` bloğundaki yorum "scan_entry calls all three" diyor — kurulum sayısı yediye çıkmış

- **Yer:** meridian/strategy.py:983-985,1012-1014 · **Sınıf:** yorum-davranış sürüklenmesi

satır 984-985: "Every screener is still EVALUATED on every ticker (scan_entry calls all three)". scan_all satır 1012-1014 yedi fonksiyon koşturuyor: evaluate_entry, evaluate_momentum_burst, evaluate_pullback, evaluate_episodic_pivot, evaluate_exhaustion_hammer, evaluate_pead, evaluate_canslim. Aynı yorum bloğu ölçüm gerekçesini de üç kurulumluk dünyaya göre anlatıyor (breakout+pullback=0.281 vs +momentum_burst) — sonradan eklenen dört kurulumun silahlanma gerekçesi orada yok.

**Öneri:** Sayı "yedi" olarak düzeltilsin ve dört yeni kurulumun neden DORMANT kaldığı (ölçüm referansıyla) aynı blokta bir satırla belirtilsin.

### icra-entegrasyon (toplam bulgu: 10)

İcra çekirdeği (E1 giriş yasası) yapısal olarak sağlam: limit/gap kararı tek fonksiyonda yaşıyor, AST testiyle çivili ve ayna tarafında host/sahiplik/monotonluk sınırları gerçek kodla korunuyor — bulgularımın hiçbiri "yasa yanlış" demiyor. Ağırlık merkezi girişin ETRAFINDA: (a) panodaki elle gönderim düğmesi yasayı by-pass ediyor (de-risk çarpanı, ATR/ref girdileri, dedup, E2 satırı yok), (b) çıkış kararları aynaya HİÇ iletilmiyor, (c) silahlı planlar kapı-dışı koşullarda olaysız düşerken ayna emri canlı kalıyor, (d) TIF=day'in gün-boyu duran limiti iç motorun "yalnız açılış" dolum modeliyle uyuşmuyor — dördü de aynı son ürünü veriyor: iç defterde olmayan, motorun yönetmediği ayna pozisyonu. İkinci katman ölçüm dürüstlüğü: E2 slipaj yaması yanlış seansın açılışını yazabiliyor, iki-motor mutabakat sayımı tam da sessizce düşen planları görmüyor, yamanın kendi sayaçlarının okuyucusu yok.

**Sağlam bulunanlar (denetçi özellikle doğruladı):**

- broker.entry_order_decision / entry_limit_price saf (ağ yok, saat yok) ve 'tek yasa iki motor' iddiası YORUMLA DEĞİL AST TESTİYLE çivili: tests/test_icra_gercekligi_v141.py:41 hem alpaca hem loop modülünde ENTRY_LIMIT_* sabitlerinin kullanımını yasaklıyor. Bu depoda defalarca görülen 'aynı yasanın ikinci uygulaması' sınıfını yapısal olarak imkânsız kılan nadir bir disiplin.
- adapters/alpaca.py:189-233 _paper_base(): host kilidi substring değil urlparse.hostname karşılaştırması (audit #51 kimlik-sızıntı vektörü kapalı) ve şema yükseltmesi SESSİZ DEĞİL — süreç başına bir kez uyarıyor, yani yanlış yapılandırma 'düzeltilmiş' gibi görünmüyor. Aynı olgunluk replace_order_stop:464 (stop gevşetmeyi SINIRIN KENDİSİ reddediyor, çağırana güvenmiyor) ve cancel_open_entries:370 (ENGINE_COID_PREFIX sahiplik süzgeci) için de geçerli.
- loop.py:1217-1222 _skip(): mutabakat atlandığında broker_reconcile.json 'checked=False + skip_reason' ile ÜSTÜNE yazılıyor — 'atlama da bir sonuçtur' ilkesi gerçekten uygulanmış; çoğu sistemde bayat artefakt taze konuşur, burada konuşamıyor.

**Hafif bulgular:**

#### [ş2] `alpaca_submitted` çift-gönderim dedup kümesi rastgele budanıyor ('son 200' değil)

- **Yer:** meridian/loop.py:885 (+832-833, 876-882, 149-151) · **Sınıf:** sessiz veri-yapısı hatası

`meta['alpaca_submitted'] = list(submitted)[-200:]` — `submitted` bir SET; set'in `list()` sırası hash sırasıdır, dolayısıyla `[-200:]` 'en yeni 200' DEĞİL rastgele 200'dür. Ölçtüm: 260 elemanlı bir set'te EN SON eklenen kimlik tail'de değildi (60 kimlik keyfi düştü). Kimliği düşen ama hâlâ silahlı bir plan bir sonraki turda `if pl['id'] in submitted` kontrolünden geçemez ve YENİDEN gönderilir: iyi ihtimalle Alpaca duplicate client_order_id ile reddeder → plan `failed_broker_rejection` damgalanıp silahlı kümeden DÜŞÜRÜLÜR (876-882) ve sahte bir BROKER_REJECT alarmı basılır; kötü ihtimalle (eski emir terminal ve sağlayıcının coid tekillik penceresi geçmişse — ŞÜPHE, doğrulanmadı) gerçek bir ikinci bracket girer. Kodun kendi yorumu niyeti yazıyor: 'losing it re-fires brackets after every restart' (loop.py:149).

**Öneri:** Kümeyi sıralı tut (ör. dict/liste ile ekleme sırasını koru) ve budamayı gerçekten en yeni N kimlik üzerinden yap.

#### [ş2] Bar-yokluğunda taşınan plan mekanizması ayna modunda fiilen ölü (DAY emri süresi doluyor → reconcile aynı turda strip ediyor)

- **Yer:** meridian/loop.py:297-317, 1249, 1256-1264 · **Sınıf:** yorum-davranış sürüklenmesi / başka katmanın nötrlediği mekanizma

`_carry_armed_without_bar` sözleşmesi: 'Bar'ı olan planlar dolum adayı; olmayanlar BİR seans taşınır (olaylı)'. Ama taşınan planın ayna emri TIF=day ile o seans içinde terminal olur ve aynı turun SONUNDA koşan mutabakat, `DEAD = {rejected, canceled, cancelled, expired, done_for_day}` kümesine giren her ayna emrinin silahlı planını düşürüyor: `if o and str(o.get('status','')).lower() in DEAD: out['stripped'].append(...)`. Sıra kanıtı: fill fazı (462-506) → P3 → ayna gönderimi → reconcile (955). Yani `armed_no_bar_carried` olayının hemen ardından aynı turda `reconcile_strip_armed` gelir; ikinci seans hiç yaşanmaz. Alternatif dal daha kötü: emir dolduysa iç motor bar olmadığı için doldurmamıştır → doğrudan motor yetimi.

**Öneri:** Taşıma kararını ayna emir durumuyla açıkça uzlaştır (taşınacaksa emri yeniden gönder, gönderilmeyecekse taşımayı mirror modunda kapat ve gerekçeyi tek olayda yaz).

#### [ş2] E2 slipaj yaması, dolumun hangi seansta gerçekleştiğini sormadan O ANKİ turun açılışını yazıyor

- **Yer:** meridian/loop.py:1146-1191 (özellikle 1164-1183), 959-962 · **Sınıf:** ölçüm sadakati / sessiz yanlış-atıf

Fonksiyonun sözü: 'ödediğimiz fiyat, o seansın RESMÎ AÇILIŞINA göre'. Kod ise `for r in rows: if r.get('motor') != 'ayna' or r.get('fill') is not None: continue` ile dolum tarihi ne olursa olsun TÜM eski bekleyen satırları alıyor ve `op = (opens or {}).get(r.get('ticker'))` ile ÇAĞRILDIĞI TURUN açılışını yazıyor (`opens` loop.py:961'de `d` seansından üretiliyor). Emrin `filled_at`/`o.get('filled_at')` alanı hiç okunmuyor, satırda da yalnız `fill_kaydedildi=dstr` var. Normal akışta yama dolum seansında koşuyor, ama mutabakat taşıma arızasında erken dönüyor (1240-1246, 1273-1281) — bir günlük kesinti, ertesi gün yamayı YANLIŞ seansın açılışına bağlar ve `fill_vs_resmi_acilis_bps` (E3 ampirik bandının girdisi) sessizce kirlenir. Test yalnız aynı-gün ve idempotanlık yollarını çiviliyor (tests/test_icra_gercekligi_v141.py:213-219).

**Öneri:** Emrin `filled_at`ini `bar_session_date` ile seansa çevir; `dstr` ile tutmuyorsa `resmi_acilis`i None bırak (uydurma açılış yerine ölçülemedi).

#### [ş2] Silahlanma anında sabitlenen limit iç motorda YENİDEN hesaplanıyor — yan tablonun kapattığı iddia edilen ayrışma config değişiminde geri açılıyor

- **Yer:** meridian/broker.py:322, 333-341; meridian/loop.py:781-788, 472-487, 324 · **Sınıf:** yorum-davranış sürüklenmesi / iki-motor ayrışması (dar)

Yan tablonun gerekçesi: 'yeniden hesaplasaydık aynı plan iki farklı limitle iki farklı motorda dolabilirdi' (loop.py:782-784) ve `entry_law` sözlüğü `limit` alanını gerçekten taşıyor. Ama `fill_entry` limiti tablodan ALMIYOR, fill anında yeniden kuruyor: `_law = entry_law()` (broker.py:322) → `_limit = entry_limit_price(trigger, atr, _law)` (334). `entry_law()` her seferinde `config.goal()['execution_v2']`i okur ve `daily_cycle` her turda `config.reload_config()` çağırır (loop.py:324) — yani silahlanma ile dolum arasında goal.yaml'daki `limit_atr_mult`/`limit_pct_cap` değişirse ayna zaten GÖNDERİLMİŞ eski limitle beklerken iç motor yeni tavanı uygular. Yan etki: E2 satırındaki `limit`/`fill_vs_limit_bps` (loop.py:487) uygulanmayan bir tavana göre yazılır.

**Öneri:** fill_entry'ye limiti (veya yasanın sürüm damgasını) argüman olarak geçir; sürüm/limit uyuşmazsa doldurmadan önce alarm yaz.

#### [ş1] Mutabakatın ürettiği E2 yama sayaçlarının hiçbir okuyucusu yok

- **Yer:** meridian/loop.py:1252 vs 1425-1434 · **Sınıf:** ölü mekanizma / okuyucusuz ölçüm (YASA 6 tersi)

`out['entry_slippage'] = _patch_entry_slippage(by_coid, opens, dstr)` ({'eslesen','yazilan','acilis_yok'}) hesaplanıyor, ama döngü sonunda yazılan `broker_reconcile.json` gövdesinde bu anahtar YOK ve çağıran `daily_cycle` dönüşten yalnız `mirror.get('drift')`i kullanıyor (loop.py:1064). Repo genelinde üretim okuyucusu yok: `grep -rn entry_slippage` yalnız tanım, tek çağrı ve fonksiyonu doğrudan çağıran testi buluyor. Özellikle `acilis_yok`, 'kaç dolumun resmî açılışını ölçemedik' paydası — E3 bandının güvenilirliğini anlatan sayı — her turda üretilip atılıyor.

**Öneri:** Üç sayacı broker_reconcile.json'a (ve /api/diagnostics `icra` yüzeyine) ekle ya da hesaplamayı kaldır.

#### [ş1] Hesap okundu ama şeması beklenmedikse ayna yine hayali 100k üzerinden boyutlandırıyor

- **Yer:** meridian/loop.py:819-828 · **Sınıf:** sessiz varsayılan / fail-open beyansızlığı

Koruma yalnız `acct is None` hâlini kapsıyor: `if acct is None and not alpaca.transport()['ok']: ... raise _MirrorUnreachable()`. Bir satır sonra `eq = float(acct['equity']) if acct and 'equity' in acct else START_EQUITY` — yani 200 dönmüş ama 'equity' alanı olmayan/adı değişmiş bir cevapta (sağlayıcı şema değişikliği, kısıtlı anahtar) hiçbir uyarı yazılmadan 100.000$'lık hayali sermayeyle boyutlandırılır. Bu tam olarak aynı bloğun yorumunun kapattığını söylediği sınıf: 'ULAŞILAMADI ≠ 100k. Hesap okunamıyorken START_EQUITY'ye düşmek, hayali bir sermaye üzerinden boyutlandırmak demekti'.

**Öneri:** `acct` var ama 'equity' yoksa da _MirrorUnreachable'a düş (veya en azından alarm yaz); sessiz START_EQUITY dalını kaldır.

### ogrenme-katmani (toplam bulgu: 9)

Öğrenme döngüsünün kablolaması büyük ölçüde kurulmuş (kadans → sprint/eksen-2/dolgu/gölge kitapları hepsi bir üretim kancasında) ama zincir üç ayrı yerde ÖLÇÜM ile HÜKÜM arasında kopuyor: bileşik öneri kuyruğu "measuring"de sonsuza dek asılı kalıyor (hiçbir üretim yolu "measured" yazmıyor), arama/ısınma sondaları resmî aşınma+doğrulama defterine sonda-başına yazıyor (yorumlar "oturum başına bir soru" diyor; ölçüm 7 saatte 204 satır, n_trials 594), ve arka-plan yansıması ile sprint kadansının kendi beyan ettikleri emniyetler kodda yok. Rollback/hüküm tarafı (probgate Bonferroni, düşüş/kuyruk vetoları, DSR/PBO mod-farkındalıklı sert kapı) sağlam ve tek yerde; kopukluk kararda değil kararın BESLENDİĞİ sayaçlarda ve kadans tetiklerinde. Bulguların hepsi kod + canlı state ölçümüyle doğrulandı; MERIDIAN_ENGINEERING_LOG/ROADMAP'te zaten kayıtlı borçlar (Eksen-2 kablolaması, elle-tetik otomasyonu, cf-kolu körlüğü, R1 rotasyonu, ölçek borcu) tekrar bulgulanmadı.

**Sağlam bulunanlar (denetçi özellikle doğruladı):**

- hermes_runtime._restored_baseline + _horizon_progress: yeniden başlatmada tabanın sıfırlanmasıyla oluşan livelock (30 günlük takvim saatinin her restart'ta sıfırlanması) bulunmuş, gerekçesiyle kapatılmış ve panonun geri sayımı kapının OKUDUĞU aynı katı-VE yasasından türetilmiş — 'dürüst durum' disiplini burada gerçekten uygulanmış.
- probgate.p_required_for: tavanlı sahte Bonferroni (min(0.95, ...) → K=16'da donuyordu, K=40'ta aile hatası %87) gerçek α/K düzeltmesiyle değiştirilmiş ve ölçümü yorumda duruyor; P_CEIL'in 'kapının sessizce ölmesini' önleyen sayısal güvenlik sınırı olduğu ayrıca beyan edilmiş.
- hermes_composite._budget_take: `st = st or {}` yeniden-bağlama hatası (boş sözlükte bütçe sayacının hiç artmaması → SINIRSIZ yoklama) ilk koşuda yakalanmış, düzeltilmiş ve REDDİN KENDİSİ de deftere yazılıyor (`_denied`) — 'sessiz sınırsızlık' sınıfının örnek kapanışı.
- shadow_lifecycle: icra yasası kopyalanmamış (PaperBroker/manage_position/_adv/derisk_mult doğrudan çağrılıyor), no-op kol iddiası ölçümle (`divergent_keys`) kanıtlanıyor ve bir kolun arızası kitabı SİLMEDEN damgalanıyor — ölçüm katmanının canlıdan sürüklenmesi yapısal olarak engellenmiş.

**Hafif bulgular:**

#### [ş2] Yeniden başlatmada `bg_reflect_by_regime` geri yüklenmiyor: her restart tüm tarihsel defterle bir arka-plan yansıması tetikliyor ve canlı ufuk tabanını sıfırlıyor

- **Yer:** meridian/hermes_runtime.py:150-155, 257-264, 267-276, 336 · **Sınıf:** durum kaybı / kadans kaçağı

`_state` modül-düzeyi sözlük ve restart'ta `_restored_baseline()` YALNIZ `last_reflect_at`i geri yüklüyor (docstring bu ihtiyacı 'livelock' gerekçesiyle anlatıyor). `bg_reflect_by_regime` yalnız `_persist()` ile diske yazılıyor, HİÇBİR yerde geri okunmuyor — canlı state doğrulaması: hermes_status.json anahtarlarında böyle bir alan yok. Sonuç: her yeni süreçte `baselines.get(r, 0) = 0` → `_horizon_ok(trades, 0, regime=r, ...)` tüm defteri 'yeni kanıt' sayar → docstring'in 'Rejim başına taban: aynı kanıta ikinci kez yansıma yok' güvencesi çalışmaz. Ayrıca `_record` bg turunda da `last_reflect_at = len(trades)` yazıyor (satır 264), yani canlı rejimin 30 günlük takvim birikimi bir bg turuyla sıfırlanıyor — `_restored_baseline`ın kapattığı deliğin ikizi.

**Öneri:** `bg_reflect_by_regime`i açılışta hermes_status.json'dan geri yükleyin (min() korumasıyla) ve bg yansımasının canlı `last_reflect_at` tabanına DOKUNMAMASINI sağlayın (bg için ayrı sayaç).

#### [ş2] Gölge-v2: bir kol no-op'a düştüğü an kitabı (açık pozisyonlar + sermaye eğrisi + n_trades) kalıcı olarak siliniyor

- **Yer:** meridian/shadow_lifecycle.py:511-515, 535-537 (`doc["variants"].pop(vid, None)` + write_json) · karşıt desen: 382-391 · **Sınıf:** veri/kanıt kaybı + iç tutarsızlık

`dropped = noop_arms(eff)` canlı `eff` parametrelerine göre HER TURDA yeniden hesaplanıyor; ardından `for vid in drop_ids: doc['variants'].pop(vid, None)` ve `store.write_json(BOOKS_FILE, doc)`. Aynı dosyadaki arıza yolu tam tersini beyan ediyor: 'KİTAP SİLİNMEZ: kolun açık pozisyonları vardır ve onları düşürmek gerçekleşmemiş bir K/Z'yi defterden yok etmek olurdu.' Senaryo hayali değil: V1 kolu `entry.min_rvol=1.5` (ROADMAP'te ölçülmüş bir aday) — reflect bu değeri ship ettiği gün V1 kontrolle özdeşleşir, kolun aylarca biriktirdiği kitap tek turda yok olur; shadow_trades.jsonl satırları kalır ama eğri/pozisyon/n_trades kopar ve aynı `variant` kimliği altında iki ayrı ömür karışır.

**Öneri:** No-op kolu turdan düşürürken kitabı `pasif: true` + `pasiflesme_tarihi` ile ARŞİVLEYİN (silmeyin); karneyi arşiv kolları için 'bu tarihten sonra ölçüm yok' diye okusun.

#### [ş2] LEARN_HALT kum havuzuna kopyalanıyor: sprint saatlerce Faz A koşup sonucu 'v1 yerel-optimal' diye YANLIŞ raporluyor

- **Yer:** meridian/sprint.py:50-52 (SKIP_COPY) · meridian/reflect.py:702-704 · meridian/sprint_run.py:119-122 · **Sınıf:** dürüst-olmayan durum + boşa CPU

SKIP_COPY = {'bars','sprint','secrets.json','HALT'} — LEARN_HALT yok; yorum HALT için tam bu riski anlatıyor (audit #23: kopyalanan kill-switch sandbox'ı işe yaramaz kılar). `health.learn_halted()` `config.STATE/'LEARN_HALT'` dosyasına bakar ve çocuk süreç MERIDIAN_ROOT=<sbroot> ile koştuğu için SANDBOX kopyasını görür. `reflect.submit` ilk satırda `{'status':'learning_halted'}` döner; sprint_run ise `if res.get('status') != 'shipped'` dalına düşüp `note='hiçbir aday OOS kapısını geçemedi — bu veri diliminde v1 yerel-optimal'` yazar. Yani operatörün öğrenmeyi durdurduğu gecelerde sprint ~500 seanslık Faz A'yı koşar, sonra sebebi YANLIŞ adla kaydeder.

**Öneri:** LEARN_HALT'ı SKIP_COPY'ye ekleyin ya da `should_run`a 'learn_halted → kos=False, sebep=learn_halt' kapısı koyun; ayrıca sprint_run `status=='learning_halted'` dalını ayrı notla raporlasın.

#### [ş2] Dolgu döngüsü ajan kotasını 'bütçe var mı?' yoklaması olarak HARCIYOR

- **Yer:** meridian/hermes.py:2290-2295 · `_agent_budget_take` tanımı: hermes.py:1125-1156 · **Sınıf:** sessiz kaynak sızıntısı

`for day in days[:max_days]: reviews = _review_plans_batch(...); if not reviews: if not _agent_budget_take(0.0): break; continue` — `_agent_budget_take` salt sorgu DEĞİL, MUTASYONdur: başarılıysa `st['minute'].append(now)` ve `st['day'] += 1` yazıp True döner. Yani cevapsız/ayrıştırılamayan her plan-günü için hiçbir LLM çağrısı yapılmadan bir RPD birimi ve bir RPM slotu yanar; N ardışık boş günde N birim gider. Kota, öğrenme katmanının tamamının (canlı öneri, aday incelemesi, nous_eval, dolgu) paylaştığı tek kıt kaynak ve `backfill_budget` tavanı da doğrudan `kalan`dan türüyor — yani sızıntı ertesi gecenin tavanını da küçültüyor.

**Öneri:** Salt-okuma bir `_agent_budget_left()` (ya da `quota_state()['kalan'] > 0`) kontrolü kullanın; `_agent_budget_take` yalnız gerçekten çağrı yapılacağı anda çağrılsın.

#### [ş1] probgate.PairedProbabilisticGate._score çağrılmayan ölü sarmalayıcı

- **Yer:** meridian/probgate.py:194-197 · **Sınıf:** ölü kod

`def _score(self, trades, span_days): return self._score_pair(trades, span_days)[0]` — depo genelinde tek eşleşme tanımın kendisi (`grep -rn '_score(' meridian/probgate.py` → 194); `evaluate` yalnız `_score_pair` çağırıyor (satır 288-289). Zararsız ama 'her mekanizma ya kablolu ya emekli' hedef sözleşmesinin kapsamında ve docstring'i hâlâ kullanımdaymış gibi 'yalnız bootstrap içi kullanım' diyor.

**Öneri:** Silin ya da `_score_pair`in yorumuna taşıyıp emeklilik notu düşün (tek satırlık temizlik, davranış değişmez).

### defter-durum (toplam bulgu: 9)

Defter çekirdeği (store→storage→SQLite) parite-digestli, kilitli ve gerekçe-yoğun bir katman; tip afinitesi ve −0,0 sınıfı gibi zor kusurlar ölçülerek kapatılmış — bu turda o çekirdekte YENİ bir korunum hatası bulamadım. Buna karşılık ARKA UÇ ANAHTARLAMA KAPISI (`storage.active`) "DB dosyası + şema var mı"ya bakıyor, "veri gerçekten taşındı mı"ya bakmıyor; bunun iki somut sonucunu sandbox'ta ÖLÇTÜM: (1) `dbmigrate` hata yolunda geri alma sonrası DB boş ama AKTİF kalıyor ve altı defter birden boş okunuyor (diskteki dosyalar duruyor ama artık okunmuyor), (2) belgelenmiş "acil geri dönüş anahtarı" `MERIDIAN_DB=off` defterleri geri getirmiyor, hepsini varsayılana düşürüyor. İkincil eksen, damga/ofset kimliklerinin çevresindeki sıra ve kanıt bağımlılıkları: `sermaye` resetinin `ledgerstamp`in toplu-yazım kanıtını yan etkiyle bozması, ofset/engel ölçümünün kilit dışında yapılması ve sözleşme tarayıcısının yeni `storage.*` yazım yoluna yapısal körlüğü.

**Sağlam bulunanlar (denetçi özellikle doğruladı):**

- `storage._isaretli_sifir` (storage.py:290-320) — property testinin bulduğu SQLite REAL negatif-sıfır işaret kaybı, hem satır (storage.py:340) hem seri (storage.py:517-518) yazım yolunda AYRI AYRI kapatılmış; gerekçe 'latent, canlıda 0 örnek ama round(-1e-9,4) → -0.0' diye ÖLÇÜMLE yazılmış. Bu sınıf normalde ancak migrasyon parite digesti açıklanamaz biçimde düştüğünde fark edilirdi.
- `sermaye.py` eğriye YENİ NOKTA EKLEMEYİ bilinçli reddediyor (sermaye.py:30-35), çünkü `ledgerstamp.seed_boundary()` tohum sınırını eğrinin son noktasından okuyor — yani reset, kapattığı kusuru (canlı satırların `replay_seed` sanılması) geri açardı. Bu ikinci-derece etkileşimi çoğu tasarım kaçırır; işaret ayrı zarf anahtarına konmuş ve `storage` env_json'da yaşayacağı da doğrulanmış.
- `recompute._code_only` (recompute.py:113-145) — orphan dedektörünün kendi açıklama yorumları/docstring'leri korpusa girip dedektörü körleştiriyordu; AST ile yorum+docstring düşürülüyor. 'Bir bulgunun gerekçesini yazmak bulgunun kendisini yok ediyordu' teşhisi ve `tests/` korpustan çıkarma kararı (recompute.py:76-83), dedektörün kendi ölçütünü ihlal etmemesi adına doğru tarafta duruyor.
- `storage.connect(create=False)` varsayılanı (storage.py:133-153) — yanlış bir okuma yolunun canlı `state/`e boş `meridian.db` bırakmasına karşı yapısal sigorta; ve `storage.meta` `SELECT *` kullanıyor çünkü elle yazılan sütun listesi `source_digest`i bir kez sessizce düşürmüştü. YASA 4 anotasyonları kapsamdaki neredeyse her `except`te var ve çoğu 'burası SESSİZ DEĞİL, çünkü …' diye kanıt zinciri kuruyor.

**Hafif bulgular:**

#### [ş2] `sermaye` reseti, `ledgerstamp`in toplu-yazım kanıtını yan etkiyle bozuyor; `seed_boundary` yanlış NEDEN basıyor

- **Yer:** meridian/ledgerstamp.py:23, 153-156, 182-185 · meridian/sermaye.py:370-382 · **Sınıf:** canlı-vs-replay ayrışma riski · yorum-davranış sürüklenmesi

ledgerstamp.py:23 iddiası: '`equity_curve.json`ı üretimde başka hiçbir yol yazmaz (tek yazar run.py:158)'. Bu artık DOĞRU DEĞİL: sermaye.py:370-382 `with store.file_lock(EQUITY): … store.write_json(EQUITY, eq)` ile aynı dosyayı yazıyor (points'e dokunmadan, zarfa `reset_isaretleri` ekleyerek).
DOSYA ÇAĞINDA SONUÇ: Kanıt-2 (toplu yazım imzası) `trades.jsonl` ↔ `equity_curve.json` mtime farkıdır (ledgerstamp.py:153-156). Reset eğriyi `_atomic_write`/`os.replace` ile yeniden yazdığı için mtime bugüne sıçrar, delta ≫ BULK_WRITE_TOLERANCE_S=5,0 olur ve `toplu=False` çıkar. O dalın ürettiği metin (ledgerstamp.py:183-185) şunu der: 'toplu yazım imzası YOK (mtime farkı Xsn) — defter tohum yazımından SONRA da yazılmış'. Bu cümle YANLIŞTIR: yazılan defter değil EĞRİdir. Üstelik `classify` çelişki-koruması (ledgerstamp.py:235-238, `celiski_toplu_yazim` → `belirsiz`) devreden çıkar ve sınır dışı satır doğrudan `live_paper` damgalanır (240-241) — BT-1'in korumak için var olduğu sayı (gerçek-canlı n) tam buradan kirlenir.
BUGÜNKÜ KAPSAM: A1'de defter SQLite'ta olduğu için `_toplu_yazim_olculebilir()` (ledgerstamp.py:143) zaten False; yani canlıda somut zarar şimdilik YANLIŞ İDDİA ile sınırlı. Dosya-çağındaki her kopya (Mac state'i — burada meridian.db YOK, sandbox'lar, testler, `MERIDIAN_DB=off` hâli) açıktır. Reset TEK YÖNLÜDÜR: bir kez koştuktan sonra dosya çağında bir daha asla 'yuksek' güven üretilemez.

**Öneri:** ledgerstamp docstring'ine ikinci yazarı (sermaye.py, yalnız zarf) beyan et ve Kanıt-2'yi zarf-işaretli eğrilerde geçersiz say (ör. `reset_isaretleri` varsa mtime imzasını 'ölçülemez' dalına düşür) — böylece `neden` metni ölçmediği bir şeyi iddia etmez.

#### [ş2] `sermaye`: engeller ve ofset KİLİT DIŞINDA ölçülüyor, yazım kilit içinde — kaydedilen ofset gerçekten yazılanla ayrışabilir

- **Yer:** meridian/sermaye.py:318-338, 354-366, 387-393 · meridian/recompute.py:213, 222, 233, 245 · **Sınıf:** TOCTOU / kimlik-ofset bütünlüğü

`plan()` kitabı KİLİTSİZ okuyor (sermaye.py:320) ve `durum()` de öyle; `onceki` ve `rapor["ofset"] = round(hedef - float(onceki["cash"]), 2)` (329) bu bayat okumadan türüyor. `uygula()` sonra `kayit`i o BAYAT `onceki`den kuruyor (354-366) ama yazımı TAZE bir kilitli okumayla yapıyor: `pf = store.read_json(PORTFOLIO, {}); yeni = _yeni_kitap(pf, hedef)` (388-389) — ve cash koşulsuz `hedef`e taşınıyor.
İki okuma arasında kitap değişirse (yol açık: `--zorla`, sermaye.py:535-536; ya da bekçinin göremediği ikinci bir yazar — bkz. ayrı bulgu) kayda geçen `ofset` ≠ (hedef − gerçek önceki nakit). Ofset dekoratif değil TAŞIYICI sayıdır: `recompute` üç kimliği de (realized_pnl 222, cash_identity 233, equity_curve_tail 245) onu geçmişten türeyen tarafa ekliyor; yanlış ofset üç kimliği birden kalıcı olarak farkın büyüklüğü kadar kaydırır ve bu KIRMIZI bir daha kendiliğinden yeşile dönmez.
Aynı pencere ENGELİ de deler: `dolu_pozisyon` engeli (sermaye.py:252-259) — 'açık pozisyon varken tabanı taşımak risk_dollars tabanını geriye dönük değiştirir' — kilitsiz `plan()` anındaki pozisyon kümesine bakar; `plan()` ile kilitli yazım arasında açılan bir pozisyon engellenmez.

**Öneri:** `file_lock(PORTFOLIO)` tek kritik bölgesinde oku → engelleri ölç → ofseti hesapla → yaz; `uygula()` bayat `plan()` çıktısını değil kilit altındaki okumayı otorite saysın.

#### [ş2] Canlı-süreç bekçisi yalnız uvicorn'u görüyor; `dbmigrate` ayrıca `store.file_lock`u hiç almıyor

- **Yer:** meridian/barrepair.py:305-313 · meridian/dbmigrate.py:159, 165, 241-246, 261-264 · meridian/ledgerstamp.py:290-293 · meridian/sermaye.py:450-453 · **Sınıf:** eşzamanlılık / sessiz veri kaybı penceresi

Dört araç da aynı ölçümü çağırıyor (doğru karar) ama ölçümün kendisi dar: `pgrep -f "uvicorn meridian.api"` (barrepair.py:309). `python -m meridian.baseline` (versioning.set_row_fields → scoreboard.json), `python -m meridian.sermaye`, `python -m meridian.ledgerstamp` gibi AYRI SÜREÇLER bu bekçiye görünmez — oysa B2/B3 sertleştirmesinin gerekçesi tam olarak 'canlı worker + pano API + sprint AYNI dosyaya yazabiliyor' idi (store.py:126-130).
İkinci ve daha keskin nokta: `dbmigrate` sistemdeki diğer TÜM yazarların paylaştığı `store.file_lock`u hiç almıyor. Karşılaştır: run.py:169 (`with store.file_lock("trades.jsonl"), store.file_lock("equity_curve.json")`), ledgerstamp.py:252, sermaye.py:370/387. `dbmigrate.apply()` yalnız süreç-İÇİ `storage._GUARD` tutuyor (dbmigrate.py:165). Sonuç: `read_source` anlık görüntüsü (159) ile arşiv yeniden adlandırması (241-246) arasında ikinci bir sürecin `trades.jsonl`e eklediği satır, `.migrated` arşiviyle birlikte sessizce düşer — hiçbir sayaç, hiçbir uyarı.

**Öneri:** `dbmigrate` altı varlığın `store.file_lock`unu read_source→COMMIT→rename boyunca tutsun; bekçi `pgrep -f "meridian\."` ile state yazan tüm CLI'ları kapsasın (ve pgrep yoksa zaten muhafazakâr davranıyor — o davranış korunsun).

#### [ş2] Defter sözleşmesinin yazar taraması yeni `storage.*` yazım yoluna yapısal olarak kör

- **Yer:** meridian/ledgers.py:62, 270-271, 347-354, 360-371 · meridian/dbmigrate.py:195-199 · **Sınıf:** denetim mekanizmasının körlüğü (migrasyonun açtığı)

`_WRITE_CALLS = ("write_json", "write_jsonl", "append_jsonl", "merge_dated_jsonl", "update_json", "update_jsonl")` (ledgers.py:270-271) yalnız `store` katmanının adlarını tanıyor; `declared_writers` da yalnız `X.<attr>(ad, …)` çağrılarını eşliyor (347-354). SQLite geçişinden sonra deftere yazmanın İKİNCİ bir yolu var ve `dbmigrate.py:195/197/199` tam onu kullanıyor: `storage.do_replace_rows(c, "trades.jsonl", …)`, `do_write_series`, `do_write_doc`.
Sonuç ölçülebilir: `CONTRACTS["trades.jsonl"].writers = ("loop.py", "run.py", "ledgerstamp.py")` (ledgers.py:62) — `dbmigrate.py` yok, ama `writer_violations()` (360-371) TEMİZ rapor veriyor, çünkü tarayıcı o çağrıyı hiç görmüyor. Sözleşmenin var olma sebebi 'kimsenin haberi olmadan ikinci bir yazar' sınıfıydı (ledgers.py:18-22, 361-363) ve bugün deftere yazan üçüncü bir yol o taramanın DIŞINDA. Ayrıca `validate_row` bu yolda hiç koşmuyor: parite digesti baytları koruyor ama şema sözleşmesini denetlemiyor.
(Bugün somut bir ihlal YOK — dbmigrate satırları birebir taşıyor. Bulgu, dedektörün kapsamı hakkındadır, mevcut bir bozukluk hakkında değil.)

**Öneri:** `_WRITE_CALLS`e `append_row/replace_rows/write_doc/write_series/write_entity/do_*` adlarını ekle ve `dbmigrate.py`yi ilgili sözleşmelerde 'taşıyıcı yazar' olarak beyan et — böylece yarın eklenecek doğrudan bir `storage.*` yazarı sessiz kalmaz.

#### [ş2] `recompute.scoreboard_n_trades` kaynak damgasına kör: ilk canlı-kâğıt kapanışta kalıcı kırmızıya döner

- **Yer:** meridian/recompute.py:256-264 · meridian/run.py:198, 221 · meridian/ledgerstamp.py:53-55 · **Sınıf:** dedektör kurt masalı / canlı-vs-replay ayrışma

Denetim `a = int(versions[current_version]["n_trades"])` ile `b = len(trades)`yi karşılaştırıyor (recompute.py:259-264). `n_trades`i ÜRETİMDE yazan tek yol tohum koşusudur: run.py:198 (`"n_trades": len(res.trades)`) ve ata zincirinde taşınması (run.py:221). Ship yolu (reflect.py:908) n_trades yazmaz; canlı bir kapanış da karneyi güncellemez.
BT-1'den sonra defter BİLEREK karışık popülasyondur (`live_paper` + `replay_seed`, ledgerstamp.py:53-55). Yani A: tohum anındaki sayım, B: tohum+canlı toplamı — iki farklı popülasyon. Bugünkü canlı hâl (95 tohum satırı, karne n=95) yeşil; sermaye ayrıştırması canlı olduğuna göre ilk canlı-kâğıt kapanışta 96≠95 olur ve denetim, yeni bir re-seed ya da ship gelene kadar (ship gelirse satırda `n_trades` olmadığı için denetim SESSİZLEŞİR, düzelmez) kırmızı kalır. Bu tam olarak modülün kendi yasakladığı hâl: recompute.py:288-290 'dedektör … kurt masalı anlatır ve operatör kırmızıyı yok saymayı öğrenir'.

**Öneri:** Karşılaştırmayı `ledgerstamp.split()[REPLAY_SEED]` uzunluğuna (ya da karnenin ölçüldüğü defter revizyonuna) çevir; 'karne tohumu sayar, defter tohum+canlı taşır' ayrımını `detail` metnine yaz.

#### [ş1] `_source_corpus` docstring'i `tests/`i korpusa dahil sanıyor — hemen üstündeki gerekçe tam tersini söylüyor

- **Yer:** meridian/recompute.py:85-89 (glob'lar 85, docstring 89, gerekçe 76-83) · **Sınıf:** yorum-davranış sürüklenmesi (dedektörü körleştirmeye davet)

Kod: `_CORPUS_GLOBS = (("meridian", "*.py"), ("ops", "*"), ("deploy", "*"), ("skills", "*.md"))` — `tests/` YOK. Hemen üstündeki blok (76-83) bunun neden ZORUNLU olduğunu canlı kanıtla anlatıyor: 'bu turda yazılan v122 testi sahte bir orphan kurmak için `server.log` adını string olarak içeriyordu ve tests/ korpusta olduğu sürece o ad ÜRETİMDE de bağışık hâle geliyordu — yani testin varlığı dedektörü kör ediyordu.' Buna rağmen fonksiyonun ilk satırı (89): '"""meridian/ + tests/ + ops/ + deploy/ + skills/ metinlerinin TEK dizgesi'. Docstring'e güvenen bir sonraki düzenleyici `tests/`i 'eksik kalmış' sanıp geri ekleyebilir ve tam olarak belgelenmiş körlüğü yeniden açar.

**Öneri:** Docstring'den `tests/`i çıkar ve tek cümlelik 'tests/ BİLEREK dışarıda (bkz. yukarıdaki gerekçe)' notunu imzaya taşı.

#### [ş1] `ledgerstamp`in kanıt satırı referansları bayat — aynı iki yazım için üç farklı satır numarası veriyor

- **Yer:** meridian/ledgerstamp.py:20-21, 23, 139-140, 151 · gerçek: meridian/run.py:170-171 · **Sınıf:** yorum-davranış sürüklenmesi (kanıt izlenebilirliği)

Modül başlığı 'run.py:157 store.write_jsonl("trades.jsonl", …) / run.py:158 store.write_json("equity_curve.json", …)' diyor (ledgerstamp.py:20-21); satır 23 'tek yazar run.py:158'; `_toplu_yazim_olculebilir` docstring'i ise 'run.py:163 defteri, run.py:164 eğriyi yazar' diyor (139-140). Gerçek kod: run.py:170 (`store.write_jsonl("trades.jsonl", ledgerstamp.stamp_rows(...))`) ve run.py:171 (`store.write_json("equity_curve.json", {...})`). Üç iddianın üçü de yanlış; `sermaye.py:32` aynı yazımı 'run.py:171' diye DOĞRU gösteriyor, yani iki modül aynı olgu hakkında çelişiyor. Bu satır çifti Kanıt-2'nin (toplu yazım imzası) taşıyıcı dayanağı olduğu için bayat işaretçi, iddianın bakışta doğrulanmasını imkânsız kılıyor.

**Öneri:** Satır numaralarını 170/171 ile tazele (ya da numara yerine `run.replay_seed` içindeki `with store.file_lock(...)` bloğuna sembolik atıf yap, böylece bir daha eskimez).

### gozetim (toplam bulgu: 10)

Gözetim alt-sistemi tasarım olarak olgun (yedi desen + makullük + EEMUA bütçesi + fail-closed Faz-6 zinciri) ama üç yapısal delik ölçülebilir biçimde açık: (1) `conservation_report` hâlâ satır-limitli olay penceresi kullanıyor ve K1'in tam onun için yazdığı BROKER_REJECT dalı bugünkü defterde tek bir olayı bile göremiyor — canlıda yanlış "KORUNUM İHLALİ" alarmı mandalda duruyor; (2) `integrity_report` üç dedektörün TABANINI `parity_report` koşmadan ÖNCE ilerletiyor, parity düşerse gerileme alarmsız biçimde yeni tabana emiliyor; (3) `determinism_report` bar dizini okunamadığında `ok=True` dönüyor ve bütün tüketiciler (alarm, mutasyon, pano) yalnız o alana bakıyor. Ayrıca bildirim zincirinin "kanal bağlı ama teslimat başarısız" hâli hiçbir sayaca girmiyor ve `report()["never"]` — bekçinin en yüksek sesli hâli — hiç alarm üretmiyor.

**Sağlam bulunanlar (denetçi özellikle doğruladı):**

- `health.faz6_kilitleri` gerçekten fail-closed ve dürüst: ölçülemeyen kilit KAPALI sayılıyor, `durum` alanı 'kaldı' ile 'hiç ölçülemedi'yi ayırıyor, ve payda (`n_kilit`) listeden TÜRETİLİYOR — altıncı kilit eklendiğinde cümle kendiliğinden düzelir. DSR eşiği ship yolunun okuduğu AYNI fonksiyondan (`validation.dsr_kapi(..., live=True)`) geliyor, ikinci bir kopya yok.
- `watchdog.events_since` + `event_ledger_domination` (watchdog.py:390-407, 530-553) beklenenden iyi: gürültünün DİĞER dedektörleri kapattığını ölçen bir meta-dedektör nadirdir ve çalışıyor — yerel defterde `hotstate_down` 15.865/27.403 = %58 ölçtüm, %40 eşiğinin üstünde, yani satır kırmızı yanardı.
- Monotonluk affı (watchdog.py:1222-1252) meşru küçülme ile sessiz kayıp ikilemine gerçek bir üçüncü yol getirmiş: af TAM (alan, was, now) eşleşmesi ister, bir satır daha kaybolursa bayrak geri gelir, gerekçesiz af `ValueError` fırlatır ve `_amnesty_index` gerekçesiz kaydı af saymaz. `_n_jsonl`'in okunamayan defteri '0 üretti' saymayı reddetmesi de aynı disiplinin iyi bir örneği.

**Hafif bulgular:**

#### [ş2] `report()["never"]` hiç alarm üretmiyor — bekçinin "en yüksek sesli hâli" fiilen tek sessiz hâli

- **Yer:** meridian/watchdog.py:115-128 (`for x in rep["stale"]` — `never` hiç gezilmiyor), :88-89 docstring, :100-101 · **Sınıf:** yorum-davranış sürüklenmesi + bekçi kapsama boşluğu

`report()` docstring'i: "Hiç damgalanmamış mekanizma 'never' listesinde (kurulumdan beri hiç koşmadı — en yüksek sesli hal)". Ama `check_and_alarm` yalnız `rep["stale"]` üzerinde döner ve `store.write_json(ALARMED_FILE, sorted(now_stale))` yazar; `never` ne MECHANISM_STALE üretir, ne bildirim zincirine girer, ne `alarm_budget().duran` sayısına katılır. Dahası :100-101 BOZUK damgayı da `never`e atıyor — yani bir mekanizmanın damgası bozulursa durumu "alarm veren"den "sessiz"e DÜŞER. Yerel anlık görüntü (state/mechanism_beats.json, 2026-07-30 17:50): 17 EXPECTED girdisinden yalnız 9'u damgalı → 8'i `never`; `state/watchdog_alarmed.json` = ["hermes_poll"], yani gerçekten tek alarm gecikmeden. ŞÜPHE: bu anlık görüntü temizlik turunun kadansları A1'de koşmaya başlamadan önceye ait olabilir — canlıda `never` kümesinin bugünkü içeriğini doğrulamadım; yapısal boşluk ise veriden bağımsız, kodda. (ROADMAP ⑧'deki "EXPECTED 9→17" kalemi FARKLI bir sorundu: orada nabız atılıyor ama liste aramıyordu; burada liste arıyor ama `never` kovası alarmsız.)

**Öneri:** `check_and_alarm`e `never` için ayrı jeton (`never:{ad}`) ekle — ilk kez görüldüğünde bir kez MECHANISM_STALE, damgalanınca düşsün.

#### [ş2] `ALARM_HEARTBEAT_STALE` jetonunun dağıtılan süreçte hiç üreticisi yok

- **Yer:** meridian/run.py:303-304 (tek üretici, `worker()` içindeki `while True`), deploy/oracle-a1/meridian.service:43 · **Sınıf:** ölü/kablolanmamış mekanizma + alarm yolu boşluğu

Depo genelinde `obs.alarm(obs.ALARM_HEARTBEAT_STALE, ...)` TEK yerde geçiyor: run.py:304, ve o satır `run.worker()`ın poll döngüsünün içinde. A1 birimi ise `ExecStart=... uvicorn meridian.api:app` (meridian.service:43) — yani üretim süreci api + süreç-içi `scheduler.start()`; `run.worker()` üretimde koşmuyor (serve.sh de run.py'yi yalnız `--dry-run --replay` tohumu için çağırıyor). obs.py:92-95 bu jetonu adıyla "beni uyandır" sınıfının ÜÇÜNDEN biri olarak anıyor ve listede olmamasını geçmiş bir kusur sayıyor; bugün jeton NOTIFY_TOKENS'ta ama ÜRETİCİSİ yok. (Asılı-tick vakasının koruması `meridian-tick-watchdog.timer`dır ve o restart eder — bu jetonu üretmez.)

**Öneri:** Nabız bayatlığı kontrolünü `scheduler.advance_once` başına taşı (zaten `watchdog.check_and_alarm` orada) ya da jetonu beyanlı biçimde emekli et.

#### [ş2] `codelaw.OTHER_TRACK_FILES` bayat muafiyet: bugün sıfır ihlal var ama liste hâlâ alarm çekirdeğini (watchdog.py/analytics.py) testten muaf tutuyor

- **Yer:** meridian/codelaw.py:50-54; tests/test_codelaw_v59.py:120-122 · **Sınıf:** ölü/çağrılmayan mekanizma (bayat muafiyet knob'u)

ÖLÇÜM (koştum, salt-okuma): `codelaw.silent_handlers()` → 0 işaretsiz yakalayıcı, depo genelinde, muaf yedi dosya dâhil. Listenin kendi kuralı satır 52'de yazılı: "Bir dosya bu listeden çıktığında ihlalleri anında teste düşer" — dosyalar temizlendi, liste kalktı mı: hayır. `tests/test_codelaw_v59.py:121-122` hâlâ `if h["file"].rsplit("/",1)[-1] not in codelaw.OTHER_TRACK_FILES` ile eliyor, yani bugün `watchdog.py`ye eklenecek çıplak bir `except Exception: pass` yasanın testini KIRMAZ. Muafiyetin kapsadığı dosyalar tam da bu denetimin kapsamı: watchdog.py, analytics.py, ledgers.py, sieve.py, mutation.py, backtest.py, shadow_model.py.

**Öneri:** `OTHER_TRACK_FILES`ı boşalt (ölçüm sıfır ihlal diyor) ve testteki filtreyi kaldır — aksi hâlde muafiyet, koruduğu yasanın kör noktası.

#### [ş2] `codelaw.report()` `stale_sinks`i ne taşıyor ne `ok`a katıyor; canlı dedektör de `violations`ı elle yeniden hesaplıyor

- **Yer:** meridian/codelaw.py:495-505 (report), :477 (stale_sinks üretimi); meridian/watchdog.py:600-614 · **Sınıf:** YASA 6'nın kendi içinde üretilip tüketilmeyen alan + aynı yasanın iki uygulaması

`artifact_graph` `stale_sinks`i hesaplıyor (:477) ve DECLARED_SINKS metni boyunca kural defalarca yazılı ("tüketici bağlandığı gün bu satırlar KALDIRILMALI — yoksa `stale_sinks` ihlali olarak geri döner", :294). Ama `report()` dönüşünde `stale_sinks` YOK ve `ok = not sil and not graph["violations"] and not UNSCANNED` onu hiç saymıyor — yani "iki yasanın birlikte durumu" iddiası, kabul edilmiş bir ihlal sınıfını dışarıda bırakıyor. Tek zorlayıcı `tests/test_codelaw_v59.py:181`. Ayrıca `watchdog.parity_report:602-603` `[a for a, info in artifacts if info.get("unread") and a not in DECLARED_SINKS]` diye `violations`ı ELDE yeniden türetiyor, oysa `_ag["violations"]` hazır (:476) — aynı yasanın ikinci uygulaması, bu deponun kendi kara listesindeki desen. (Bugün ölçüm: `stale_sinks == []`, `violations == []` — sorun mekanizmanın kendisi, verinin değil.)

**Öneri:** `report()`a `stale_sinks` alanını ekle ve `ok`a kat; watchdog'daki orphan hesabını `_ag["violations"]`e indirge.

#### [ş2] `meridian-barsarchive` servisinin ne OnFailure bildirimi ne de bekçi nabzı var

- **Yer:** deploy/oracle-a1/meridian-barsarchive.service:39-40 (Restart=always, OnFailure YOK) vs deploy/oracle-a1/meridian.service:18; meridian/barsarchive.py (hiç `watchdog.beat` yok); meridian/watchdog.py:22-64 (EXPECTED'de girdi yok) · **Sınıf:** bekçi kapsama boşluğu (izlenmeyen üretim mekanizması)

`meridian.service` OnFailure=meridian-fail-notify.service taşıyor (ve o birim 2026-07-30'da test-ateşlendi); `meridian-barsarchive.service` yalnız `Restart=always` + `RestartSec=30` taşıyor, OnFailure yok. `grep -n "watchdog\|beat(" meridian/barsarchive.py` → boş; `watchdog.EXPECTED`in 17 girdisinde intraday arşivleyici yok. Sonuç: arşivleyici çöküp restart döngüsüne girse ya da kalıcı düşse hiçbir alarm jetonu, hiçbir MECHANISM_STALE satırı ve hiçbir bildirim üretilmez — ROADMAP:222'deki "iç dead-man = 17 bekçi + tick-watchdog + fail-notify" cümlesi bu servisi kapsamıyor.

**Öneri:** Birime `OnFailure=meridian-fail-notify.service` ekle ve arşivleyici turunda `watchdog.beat("barsarchive")` at + EXPECTED'e seans-bağımlı pencereyle gir.

#### [ş2] `autonomy_ladder` okunamayan olay defterinde "temiz sicil" veriyor — yorumun uyardığı şeyi kodun kendisi yapıyor

- **Yer:** meridian/analytics.py:150-160 (except dalı) ve :180-186 (`crit("Zero unexplained breaker trips", breaker_trips == 0, ...)`) · **Sınıf:** sessiz fail-open / yorum-davranış sürüklenmesi

except dalı: `_o.warn("ladder_breaker_count_unavailable", ..., detail="devre kesici sicili OKUNAMADI — 0 sanılmasın")` ardından `breaker_trips = 0`. Sonra ölçüt `breaker_trips == 0` ile kurulduğu için `met=True` olur ve `auto_met = sum(1 for c in l0_l1 if c["met"] and not c["manual"])` sayacına GİRER; `detail` metni "olay defteri OKUNAMADI" der ama `met` alanı — panonun ilerleme rozetini süren alan — geçti der. Aynı bloğun yorumu tehlikeyi adıyla koymuş: "okunamayan olay defteri temiz sicil DEĞİLDİR ve merdiveni haksız yere yükseltebilir". Kapsam sınırı (şiddeti 3 değil 2 yapan): tek tüketici api.py:792 → pano; `guard.py` bu fonksiyonu okumuyor, yani gerçek para kapısını açmıyor, operatörü yanıltıyor.

**Öneri:** Ölçütü üç-değerli yap: ölçülemediğinde `met=False` (fail-closed) ver, `manual`/`olculemedi` bayrağıyla ayır ki 'kaldı' ile 'bakılamadı' karışmasın.

### api-scheduler (toplam bulgu: 10)

Kadans haritası tek bir gerçek tetikte toplanıyor: canlı A1 `uvicorn + MERIDIAN_AUTOSTART_CYCLE=1` ile koştuğu için üretimdeki TEK kadans `scheduler.advance_once` (300 sn poll); seans-sonrası her şey (onarım/sip, öğrenme, Y4, haftalık üçlü, arming/selfreview/orphan/nous/revision, sprint, crosscheck) `if fresh:` bloğunun içinde asılı ve `fresh` de tek bir değerden — `_last_closed_session()` — türüyor. Rota yetkisi sağlam: 68 rotanın yazma yüzeyinin tamamı `_auth`lu, kimlik istisnaları `KIMLIK_UCLARI` tek kaynağında gerekçeli duruyor; ama iki güvenlik/otorite kapısı (genel-arayüz parola reddi, "elle tik sprint başlatmaz") üretim koşullarında hiç ateşlenmeyen girdilere bağlı. Config tekilliğinde goal/bounds/strategy hattı disiplinli (derin kopya + atomik yazım + reload), buna karşılık kadans sabitleri ve durum damgaları iki yerde yaşıyor: `refetch_max` api.py'de literal 8, `_DURABLE` haftalık damgaların yarısını kapsamıyor.

**Sağlam bulunanlar (denetçi özellikle doğruladı):**

- config.py:38-68 — `goal()`/`bounds()` lru_cache üstüne DERİN KOPYA + `cache_clear` geçirgenliği: "değişmez sözleşme yalnız DOSYA için geçerliydi, bellekteki kopya serbestti" kusuru doğru katmanda kapatılmış ve `reload_config` gerçekten kablolu (loop.py:324, mutation.py:76/81) — yani uzun ömürlü süreçte sözleşmenin donması sessizce geri gelemez.
- api.py:99-133 — numpy/NaN sigortasının ROTA katmanına konması, `response_class`ın neden GEÇ kalacağının yazılı gerekçesi, ve `store.sanitize`ın yeniden tanımlanmak yerine paylaşılması (diskteki gerçek ile telden geçen gerçek tek fonksiyondan). Üstüne `app.router.route_class` atamasının ilk `@app.get`ten ÖNCE olması gerektiği notu — sigortanın yarım takılma modu bile öngörülmüş.
- api.py:593-610 — yetkisiz uçların istisna listesi (`KIMLIK_UCLARI`) ÜRETİM kodunda, girdi başına gerekçeyle; iki ayrı test dosyası onu kopyalamak yerine OKUYOR. Bir güvenlik istisna listesini tek kaynakta tutmak ve büyütmeyi denetimde görünür kılmak beklenenden iyi bir duruş.
- scheduler.py:198-222 — `_declare_terminal_skip` alarm alan adlarını ve `kind`ini bilinçli olarak DONDURUYOR çünkü watchdog parite dedektörü o imzayı okuyor; "adı kaydırmak dedektörü sessizce kör bırakırdı" dersi koda çivilenmiş.

**Hafif bulgular:**

#### [ş2] _DURABLE kadans damgalarının yarısını kapsıyor: haftalık LLM/bildirim/walk maliyetleri her yeniden başlatmada yeniden yanıyor

- **Yer:** meridian/scheduler.py:43-60 (vs 717, 723, 769, 780, 799, 826, 890) · **Sınıf:** durum tekilliği · bütçe sızıntısı

`_DURABLE` yorumu gerekçeyi açıkça yazıyor: 'süreç başına sıfırlanan bir seans damgası, her yeniden başlatmada kadansı BAŞTAN koşturur ... bir gecede üç kez yeniden başlatılan bir pano, gecelik bütçeyi üç kez harcardı' — ve `learn_session`/`y4_session`/`validation_week`i listeye bu yüzden alıyor. Ama listede OLMAYANLAR: `earnings_week`, `earnings_attempts` (723: 'haftada en çok 5 deneme' → gerçekte süreç başına 5, ve `earnings.refresh` ~15 ağ isteği + FMP yedeği), `arming_week` (771: `arming.evaluate(bars,index)` walk-forward), `selfreview_week` (782: `selfreview.weekly()` bildirim gönderir), `orphan_sweep_week`, `nous_eval_week` (832: `nous_eval.haftalik_degerlendirme()` LLM çağrısı), `revision_week`. `_rehydrate` yalnız `_DURABLE` anahtarlarını geri yükler (satır 92), oysa `_persist` hepsini diske YAZIYOR — yani bilgi diskte duruyor, okunmuyor. `refetch_attempts` için `_rehydrate`ı doğuran kusurun (80-88) birebir aynısı.

**Öneri:** Yedi damgayı `_DURABLE`a ekle; bilerek uçucu bırakılan varsa anahtar başına gerekçesini aynı bloğa yaz (asimetri gerekçesiz kalamaz).

#### [ş2] "Elle tik ASLA sprint başlatmaz" kapısı üretimde tutmuyor — yalnız daemon ÖLÜYKEN, yani testlerde çalışıyor

- **Yer:** meridian/scheduler.py:956-960 (kapı gerekçesi 943-955; çağıran api.py:2247-2256) · **Sınıf:** yorum-davranış sürüklenmesi · yanlış yerde ölçülen otorite

`_mesgul = ("bar_kovalamasi" if _state.get("refetch_chase") else None if (_thread and _thread.is_alive()) else "elle_tik")`. Ölçülen şey 'daemon ipliği ölü mü', 'çağıran kim' değil. A1'de `MERIDIAN_AUTOSTART_CYCLE=1` olduğu için `_thread` DAİMA canlıdır → panodaki 'bir tur ilerlet' düğmesi (`POST /api/scheduler/advance` → `scheduler.advance_once()`) `mesgul=None` geçirir → `sprint.should_run` yalnız gece penceresine bakar (sprint.py:353 `if mesgul: ... 'mesgul:'`) ve 4 işçilik `meridian.sprint_run` alt süreci 4 OCPU'luk kutuda başlayabilir. Kapının kendi gerekçesi (951-955) ve ROADMAP:719 tersini iddia ediyor: 'gerekçesi test değil DOĞRULUKTUR: operatör ... 4 çekirdek yiyen bir antrenman başlatmayı İSTEMEMİŞTİR'. Regresyon çivisi de yalnız kapının çalıştığı hâli sabitliyor: tests/test_ogrenme_otomasyonu_v136.py:482 `assert sch._thread is None or not sch._thread.is_alive()`.

**Öneri:** `advance_once(*, elle: bool = False)` imzası aç ve `api_scheduler_advance` `elle=True` geçsin — çağıran kimliği çıkarsanmak yerine taşınsın; test de daemon canlıyken elle tiki sınasın.

#### [ş2] Öğrenme/Y4 kadansının "sonraki poll yeniden dener" vaadi yanlış: arıza bir SEANS boyu sürer

- **Yer:** meridian/scheduler.py:852-873 (yorumlar) vs 631-635, 643 · **Sınıf:** yorum-davranış sürüklenmesi · yeniden denemeyen yeniden-deneme

Yorum (853-855): 'Damga İLERLEMEZ (iç adımlar kendi korumalarında), yani bir sonraki poll yeniden dener' — Y4 için de aynısı (868-869). Ama her iki blok da `if fresh:` (643) içinde. Bar geldiği poll'de satır 667-668 `_state.update(last_refetch_session=last_closed, ..., refetch_chase=None, ...)` çalışır; bir sonraki poll'de `_dense = last_closed != last_refetch_session` → False, `_chasing = bool(refetch_chase) and ...` → False, `_sparse` → False, dolayısıyla `fresh=False` ve blok bir daha ~24 saat boyunca HİÇ girilmez. Yani `_learning_cadence` KENDİSİ patlarsa (antrenman+Eksen-2+dolgu üçü birden) o seans için bir daha denenmez; kadansın 279-280'deki 'bar varışına bağlı DEĞİLDİR' tasarım notu tam da bu yüzden gövdede karşılanmıyor.

**Öneri:** Seans damgalı iki kadansı (`learn_session`, `y4_session`) `if fresh:` bloğunun DIŞINA, halt kapısından sonraki gövdeye taşı — tasarım notları zaten bar varışından bağımsız olduklarını söylüyor; taşınmıyorsa yeniden-deneme cümlesini sil.

#### [ş2] /api/diagnostics tek noktadan komple kararıyor: 40+ korumasız alt çağrı tek return sözlüğünde

- **Yer:** meridian/api.py:1842-2154 · **Sınıf:** hata-yolu sağlamlığı

Tek `return {...}` içinde korumasız çağrılar: 1842 `an.edge_verdict(), an.result_verdict(), an.validation_trio()` (blok-bootstrap + DSR), 1910 `earnings.blackout_radar(list(REPLAY_UNIVERSE), today)`, 1919 `earnings.snapshot_stats(...)`, 1998 `health.faz6_kilitleri(...)`, 2063 `an.profit_waterfall()`, 2067 `oos_erosion.report()`, 2105 `integrity_registry.coverage_report()`, 2151 `ledgers.report()`, 2154 `sieve.report()`. Bunlardan HERHANGİ biri patlarsa uç 500 döner ve Operasyon sayfasının TAMAMI kararır — `sessiz_hat` (2081), `watchdog` (2077) ve `alarm_butcesi` (2084) dahil, yani bir şey bozukken operatörün bakacağı satırlar da onunla gider. Aynı dosya doğru deseni bir fonksiyon yukarıda zaten uyguluyor: `_saglayicilar` (1507-1548) her sağlayıcıyı ayrı `try/except` ile sarıp `{'ad': ..., 'ok': None, 'olculemedi': type(e).__name__}` döndürüyor, gerekçesi de yazılı ('boş bir kart sağlayıcı bozuk değil, bu süreçte çağrı yapılmadı demektir').

**Öneri:** Üst düzey blokları `_saglayicilar` deseniyle sar (`{'olculemedi': '<TipAdı>'}` düşsün) — tek bir alt ölçüm arızası tüm gözlemlenebilirlik yüzeyini düşürmesin.

#### [ş2] pipeline.refetch_max: 8 — scheduler.DENSE_ATTEMPTS'in api.py'deki sessiz kopyası

- **Yer:** meridian/api.py:2109 · **Sınıf:** config kaynak tekilliği ihlali

`"refetch_attempts": sched.get("refetch_attempts", 0), "refetch_max": 8,` — sabitin sahibi `scheduler.py:75 DENSE_ATTEMPTS = 8` ve zamanlayıcının kendi yorumu (39-42) panonun bu alana bağlı olduğunu yazıyor ('panonun 8'lik halkası doğru okumaya devam eder', 'dış tüketiciler ... bu iki adın ANLAMINA bağlı'). Tüketici tarafında da ikinci bir literal var (web/app.js:3429 `const mx = pl.refetch_max || 8;`, 3437 '8 adımlı sabır sayacı'). `DENSE_ATTEMPTS` değişirse hem uç hem pano sessizce eski paydayı çizmeye devam eder — bu deponun 'aynı yasanın iki uygulaması, biri güncellenmiş' hata deseni.

**Öneri:** `from . import scheduler` ile `"refetch_max": scheduler.DENSE_ATTEMPTS` servis et (aynı istekte zaten `scheduler_status.json` okunuyor); app.js'teki literal yedek de kaldırılsın.

#### [ş2] /halt panik sayfası sırrı hâlâ URL'e koyuyor — _auth'un 2026-07-28'de emekli ettiği desen

- **Yer:** meridian/api.py:2343-2359 (kural: api.py:213-219) · **Sınıf:** yorum-davranış sürüklenmesi · sır hijyeni

`mobile_halt` docstring'i: 'reads the token from ?token= so it works over the tunnel'; gövde: `const t=new URLSearchParams(location.search).get('token');`. Oysa `_auth` docstring'i (217-219) aynı deseni ADIYLA emekli etmiş: '`?token=` QUERY PARAMETRESİ KALDIRILDI (2026-07-28): URL'ler sunucu loglarına, tarayıcı geçmişine ve `Referer` başlığına düşer' ve sunucu tarafı testle çivili (tests/test_authority_boundaries_v77.py:1011 '`?token=` sorgu yolu GERİ AÇILMIŞ'). Yasanın yalnız sunucu yarısı güncellenmiş: panik sayfasının BELGELENMİŞ kullanımı hâlâ `https://…/halt?token=<sır>`, yani token tünel/vekil erişim loglarına ve tarayıcı geçmişine düşmeye devam ediyor. Sayfanın `fetch(p,{method:'POST',headers:H})` çağrısı zaten aynı-origin olduğu için oturum çerezini kendiliğinden gönderiyor — sorgu yolu işlevsel olarak da gereksiz.

**Öneri:** Sayfadan `?token=` okumasını ve docstring'in o cümlesini kaldır (çerez yolu yeterli); token gerçekten gerekiyorsa betik yolu `x-meridian-token` başlığıdır.

#### [ş1] /api/public/summary kimliksiz ve her anonim istekte tüm defteri baştan tarıyor

- **Yer:** meridian/api.py:332-376 + 379-408 · **Sınıf:** kaynak/maliyet sınırı yok

Uç bilinçli olarak `_auth`suz (docstring: 'kimlik doğrulaması YOK, bilerek') ve gövdesi her çağrıda `store.read_jsonl("hypotheses.jsonl")`, `store.read_jsonl("trades.jsonl")`, `skills_registry.json` okuyup `_setup_regime_matrix(trades)` ile tüm kurulum×rejim matrisini yeniden kuruyor. `_NOCACHE` bile YOK (statik uçların aksine hiçbir `Cache-Control` başlığı set edilmiyor) ve hız sınırı da yok — `auth.locked_out` yalnız `/api/login`de. Bu, uygulamanın tek anonim HESAP yüzeyi ve maliyeti defter büyüklüğüyle doğrusal büyüyor. Sızıntı yok (sermaye/pozisyon/sembol dışarıda), sorun yalnız sınırsız iş.

**Öneri:** `trades.jsonl` mtime'ına anahtarlı basit bir memo ya da kısa `Cache-Control: public, max-age=60` ekle — tanıtım sayfasının tazelik ihtiyacı saniye ölçeğinde değil.

### olcum-sasisi (toplam bulgu: 7)

Kapsamdaki dört dosya baştan sona okundu (backtest.py 798, shadowlaw.py 619, component_ic.py 829, olcum_araclari.py 203 satır) ve bulgular canlı yol (loop.py/broker.py/reflect.py/probgate.py), canlı defterler ve research çıktılarıyla çapraz doğrulandı; MERIDIAN_ENGINEERING_LOG.md ve ROADMAP.md'de kayıtlı borçlar (hayalet-round-2 kirliliği, measurement_bars'ın walk-forward'a bağlanmaması, öğrenme rehineliği, E4 %1 limit tavanı okuması) yeniden bulgulanmadı. En ağır iki kalem canlı-vs-replay ayrışmasıdır: replay giriş limitini ATR bacağı olmadan kuruyor (ölçüldü: barların ~%41'inde canlı limit kesin olarak daha sıkı — backtest dolumları iyimser) ve günlük kayıp devre kesicisi iki motorda farklı pencere ölçüyor (canlıda fiilen yalnız gecelik boşluk kesicisi). Üçüncü ağır kalem ölçüm semantiğidir: `mtm_dd_veto` mutlak M2M düşüşünü göreli bir marja (DD_VETO_MARGIN) kıyasladığı için ölçülen dört kolun dördünde de `ihlal=True` çıkıyor ve EDG-2026-003 kartının kill ölçütü bu yüzden ayırt edici değil.

**Sağlam bulunanlar (denetçi özellikle doğruladı):**

- `olcum_araclari.temiz_taban` beklenenden sağlam bir yaprak: takvim tarihi ile bar indeksi aynı seride görülürse ValueError ATIYOR (olcum_araclari.py:170-173) — sessizce toplayıp 'hiçbir yerde yanlış görünmeyen' bir pencere aritmetiği üretmiyor; ölçülebilir satır yoksa `kirlilik_orani` None (0.0 değil), günü çözülemeyen satır `n_cozulemeyen` + `uyari` metnine düşüyor ve saf-yaprak olduğu için obs kanalı olmadığı da yazılı. Testleri (tests/test_wpm_borclari_v146.py:206-271) bu üç kuralı da çiviliyor.
- Confirm-sızıntısı kapatması gerçekten TEK YASA ile yapılmış: fold'lar (backtest.py:756-760) ve kuyruk vetosu (769) aynı `_embargoed_start` + `_in_segment` üyelik yasasıyla Search dilimine daraltılmış, tam-OOS karşılıkları ayrı ve etiketli tutulmuş; yani 'aramanın gördüğü popülasyon' ile 'vetonun gördüğü popülasyon' yapı gereği ayrışamıyor. Aynı disiplin `_embargoed_start`in başarısızlık yolunda da sürüyor: ambargo uygulanamazsa sessizce seg_start'a düşmüyor, `embargo_disabled` uyarısıyla 'OOS skoru SIZINTILI olabilir' diyor (backtest.py:608-613).
- `shadowlaw.MEASURED_V3` bir sabit gibi değil ölçüm kaydı gibi işletilmiş: `variance_drift` üç yazılı değişmezi (marj çevriminin yuvarlaması, düşüş vetosunun kendi gürültüsünün dışında kalması, PARA payının tek terim olması) canlı defterde yeniden ölçüyor, sabitleri GÜNCELLEMİYOR ve gerçekten bir kadansa bağlı (meridian/scheduler.py:492-499). Ölçüm var-bekçi yok deseninin bu dosyada kapatılmış olması denetim beklentisinin üstünde.

**Hafif bulgular:**

#### [ş2] Replay'de 'silahlı → dolu' hunisinin hiçbir basamağı motorun içinde muhasebe edilmiyor (209/446 plan sessizce düşüyor)

- **Yer:** meridian/backtest.py:196-202 ve 210 · **Sınıf:** YASA 4 (sessiz yutma) / gözlemlenebilirlik boşluğu

`for plan in armed:` bloğunda dolum yalnız `t in per and d in per[t].index and not breaker_tripped and size_mult > 0 and len(broker.positions) < eff_max_open and t not in broker.positions` şartıyla denenir; şart tutmazsa plan hiçbir sayaç, olay veya alan bırakmadan `armed = []` (backtest.py:210) ile silinir. Ayrıca `fill_entry` `reject_out` almadığı için `entry_missed_limit` / `max_chase` / `entry_gap_veto` retleri de kaydedilmiyor. Canlı motor aynı yolda her satırı yazıyor (`_entry_exec_write` + `obs.warn(_reason, ...)`, loop.py:478-500). Kanıt niceliği: karne ölçümü bu basamakları ancak DIŞARIDAN enstrümante ederek çıkarabildi — SİLAHLI 446 → fill denemesi 237 → DOLU 201 (research/olcumler/karne_olcum/RAPOR.md:145-148), yani 209 silahlı plan hiç denenmeden, 36 deneme de sebepsiz düşmüş. 'canlıda neden 0 işlem' sorusunun replay karşılığı motorun kendi çıktısından cevaplanamıyor.

**Öneri:** `BacktestResult`e küçük bir `fill_log` (plan_id, karar, sebep) ekle — `reject_out` sözlüğünü `fill_entry`e geçirip red sebebini, deneme dahi edilmeyen planlarda da (bar_yok/breaker/slot/size_mult) sebebi yaz.

#### [ş2] `oos_folds_full` ve `oos_tail_risk_full` üretiliyor ama repoda TEK okuyucusu yok; `tail_risk` docstring'i 'walk_forward başına bir kez' diyor, iki kez çağrılıyor

- **Yer:** meridian/backtest.py:762-763, 769-770, 786-787 (docstring: meridian/score.py:192-193) · **Sınıf:** YASA 6 (okuyucusuz yazım) + yorum-davranış sürüklenmesi

Depo geneli grep (`.venv` ve `__pycache__` hariç): `oos_folds_full|oos_tail_risk_full` → TEK eşleşme üretim satırının kendisi (backtest.py:787); ne `reflect`, ne `oos_pipeline`, ne pano/api, ne tests, ne research. `reflect.record_candidate` alanları beyaz listeyle yazıyor (reflect.py:483-516) ve hypotheses.jsonl `backtest` bloğunda da yok (canlı defterden okundu: 19 kayıt, anahtarlar arasında `*_full` yok). Ayrıca `score.tail_risk` docstring'i maliyeti şöyle savunuyor: 'Maliyet çağrı başına ~5 ms ve tail_risk walk_forward başına BİR KEZ çağrılır' (score.py:193) — `walk_forward` bugün onu iki kez çağırıyor (backtest.py:769 ve 770), yani her walk-forward'da 2×20.000 replikasyonun yarısı kimsenin okumadığı bir alan için harcanıyor.

**Öneri:** Ya iki alanı da bir okuyucuya bağla (ör. kapı kaydına teşhis alanı olarak) ya da kaldır; `score.tail_risk` docstring'indeki 'bir kez' cümlesini gerçekle hizala.

#### [ş1] `holding_day_r_curve`: 'atlanan satır KAYBOLMAZ' beyanı tutmuyor — ön filtreyle düşenler hiçbir sayaca girmiyor, n=0 dalında `n_atlanan` alanı hiç yok

- **Yer:** meridian/backtest.py:411-416, 421-426, 452-453 · **Sınıf:** YASA 4 (sessiz yutma) / yorum-davranış sürüklenmesi

`rows = [t for t in (trades or []) if t.get('bars_held') is not None and t.get('r_multiple') is not None]` (411-412) — bu adımda düşen satırlar HİÇBİR YERDE sayılmıyor. `n = len(rows)` filtreden SONRAKİ sayı; dönüşteki `'n_atlanan': n - n_used` (452) yalnız `int()/float()` dönüşümünde patlayan satırları kapsıyor. Oysa 421-426'daki sessiz-yutma yorumu şunu vaat ediyor: 'atlananların sayısı dönen sözlüğün `n_atlanan` alanında raporlanır, yani "kaç satır ölçülemedi" sorusu kayıttan cevaplanabilir kalır'. Ek olarak n==0 erken dönüşü (414-415) `n_atlanan` anahtarını hiç içermiyor, yani 'girdi boştu' ile 'her satır elendi' hâlleri çıktıdan ayırt edilemiyor. Bugünkü canlı defterde etkisi yok (95/95 satırda bars_held ve r_multiple dolu), ama gölge/cf defterleri aynı fonksiyona verilirse fark sessizce kaybolur.

**Öneri:** Ön filtreyi de say (`n_giris = len(trades or [])`, `n_atlanan = n_giris - n_used`) ve n=0 dalına aynı alanları ekle.

#### [ş1] `_in_segment` docstring'i 'hâlâ açık işlem HİÇBİR dilime girmez' diyor; kod açık işlemi dilime SOKUYOR

- **Yer:** meridian/backtest.py:530-538 · **Sınıf:** yorum-davranış sürüklenmesi + test kapsama boşluğu

Docstring: 'A still-open trade near the boundary is graded in NO segment — a purge, not a leak.' Kod: `return bool(lo <= op < seg_end and (not cl or cl <= seg_end))` — `cl` boşsa (`ts_close` yok = pozisyon açık) koşul TRUE olur ve işlem dilime GİRER, yani docstring'in reddettiği davranışın tam tersi. Bugün canlı defterde etkisiz (state/trades.jsonl: 95/95 satırda `ts_close` dolu; replay zaten son barda `eod_markout`/`delisted_markout` ile her şeyi kapatıyor, backtest.py:364-381), ama `segment_score` canlı defteri de puanlıyor (`baseline.would_have_replay`, baseline.py:190) — defter bir gün açık pozisyon satırı taşırsa dilim sınırındaki bir işlem sessizce sızar. tests/test_oos_pipeline_audit_v36.py ve test_gate_statistics_v74.py `_in_segment`i çiviliyor ama `ts_close` YOK vakasını hiç sınamıyor.

**Öneri:** Ya kodu docstring'e uydur (`cl` yoksa False dön) ya da docstring'i gerçek davranışa çevir; hangi olursa olsun 'ts_close yok' vakası için bir çivi testi ekle.

