# secrets — NEVER commit, NEVER copy to the VM (Hard Rule 5)
.env
.env.*
!.env.example

# mutable state — canlı veri versiyonlanmaz (yedek zinciri: VM-tar + Mac-pull; git DEĞİL)
# NOT (2026-08-01 düzeltmesi): `state/` dizin-dışlaması negasyonları ÖLDÜRÜYORDU (git, dışlanan
# dizinin içine hiç bakmaz) — `state/*` desenine geçildi ki goal/bounds istisnaları GERÇEKTEN işlesin.
state/*
!state/goal.yaml
!state/bounds.yaml
backups/

# oturum artıkları (scratchpad repo dışında yaşar; sızıntı olursa da girmesin)
scratchpad/
.impeccable/

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/
dist/
build/

# ölçüm araçlarının ÇALIŞMA dizinleri (WP-H, 2026-07-31) — üretilmiş artefakt, kaynak değil.
# .hypothesis/ : karşı-örnek veritabanı + shrink önbelleği. Makineye özgüdür ve commit'lenirse
#                bir makinede bulunan karşı-örnek diğerinde "geçmiş gibi" görünür.
# mutants/     : mutmut'un kaynak ağacının MUTASYONA UĞRAMIŞ kopyası (yüzlerce dosya, saniyede
#                yeniden üretilir). Commit'lenirse depoda ikinci, bozuk bir meridian paketi durur.
.hypothesis/
mutants/

# ölçüm ham çıktıları (WP-U/N4 vd, 2026-08-09) — özet `sonuc.json` + `olcum*.py` KAYNAK olarak commit'lenir;
# ham seans dökümü / betik state / stderr YENİDEN ÜRETİLEBİLİR (olcum*.py deterministik), versiyonlanmaz.
research/olcumler/*/seanslar.json
research/olcumler/*/*.stderr.log
research/olcumler/*/state/

# os
.DS_Store

# Claude oturum artıkları (worktree/ayarlar — yerel)
.claude/

# mutasyon ham logları — üretilmiş artefakt (rapor .md versiyonlanır, ham log versiyonlanmaz)
docs/mutasyon/.*.ham.log

# Claude harness skill artefaktları (repo dışı araç-katmanı; 2026-08-06)
.agents/
skills-lock.json
.codex/
.github/agents/
.github/hooks/
.github/skills/
research/olcumler/*/seanslar_*.json
research/olcumler/*/kosum_*.log
research/olcumler/*/state_*/
research/olcumler/*/sandbox/

# SQLite okuma yan-ürünleri (2026-08-22): ajanlar donmuş DB kopyalarını okuyunca doğar,
# kanıt değildir ve her okumada yeniden doğar — dagit temiz-ağaç kapısını gereksiz kirletiyordu
*.db-shm
*.db-wal

# ölçüm ajanlarının yerel önbellekleri (yeniden üretilebilir; 140MB bars.pkl vakası 2026-08-24)
research/olcumler/**/_cache/
research/olcumler/**/__pycache__/

# geçici tasarım comp'ları (depoya girmez)
scratch-panov2/

# SATOSHI — LİSANS BELİRSİZLİĞİ NEDENİYLE VERSİYONLANMAZ (2026-08-24).
# ITF Free Font License v2.0 §02 dağıtım-kanalı listesinde "repository" NİTELEMESİZ geçiyor;
# §01 ise "internal sharing does not constitute redistribution" diyor ve özel/kamu ayrımını
# commit bağlamında HİÇ yapmıyor. Git geçmişi KALICIDIR — belirsiz bir izni geri alınamaz bir
# yere yazmak, kazancı sıfır olan bir risktir (panonun en büyük başlığı 28px; Dub'ın kendi
# kuralı Satoshi'yi 36px altında YASAKLIYOR, yani ekranda sıfır karakter çizerdi).
# Ölçüm ve lisans metni depoda KALIR; yalnız İKİLİ dosya dışarıda.
research/olcumler/yazi_tipi_2026-08-24/satoshi/fonts/
research/olcumler/yazi_tipi_2026-08-24/satoshi/woff2/
